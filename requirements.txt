pygame-ce>=2.5.0
pygame_gui==0.6.14  # PROJ-411 Task 3.1: pinned — game/ui/pygame_gui_patch.py depends on the chained-str.join cache-key bug in UIAppearanceTheme.build_all_combined_ids. Bump only when that patch is removed.
scipy>=1.15.0
PyYAML>=6.0
requests>=2.31.0  # PROJ-296: LLM service HTTP client
Pillow>=10.0.0
