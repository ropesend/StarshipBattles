!EXECUTE_PROTOCOL: Debugging\protocols\02a_batch_fix.md

1. LOAD the attached protocol file protocols/02a_batch_fix.md.
2. READ Debugging/debug_plan.md to identify all [Pending] and [In-Progress] bugs.
3. CRITICAL: For each bug, complete Phase 0 (Architectural Context) before attempting
   any fix. Check git history, active projects, and architecture docs. NEVER revert
   recently refactored code — fix forward or escalate to [Needs Clarification].
4. BEGIN BATCH LOOP:
   * Select highest priority bug.
   * LOAD ticket file (e.g., Debugging/active_bugs/BUG-XX.md).
   * EXECUTE full cycle including Phase 2.5 integrity check:
     Phase 0 (Context) -> Phase 1 (Red) -> Phase 2 (Green) -> Phase 2.5 (Integrity Check) -> Phase 3 (Doc) -> Phase 4 (Status).
   * If Phase 2.5 detects reversion of recent refactors, set [Needs Clarification] and move to next bug.
   * DO NOT wait for input - proceed to next bug.
5. EXIT when context >= 80% OR no Pending bugs remain.

AUTONOMOUS MODE: Do not stop between bugs. Only stop for context limit or empty queue.
