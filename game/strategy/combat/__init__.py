"""Strategy layer combat package.

Introduced by PROJ-269 Phase 1. Home of the strategy spec compiler and
(in Phase 2) the `PostBattleHook` implementation that writes outcomes
back to `ShipInstance.components`.
"""
