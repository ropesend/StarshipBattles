"""Internal slice implementation for `StrategySessionFacade` (PROJ-309 sub-phase 3.7).

These modules are NOT part of the public facade contract — callers continue
to interact with `game.strategy.facade.strategy_session_facade.StrategySessionFacade`.
The slice classes here exist only to keep the composer file under the
500-LOC ceiling.
"""
