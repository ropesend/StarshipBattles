!EXECUTE_PROTOCOL: Features\protocols\02b_deep_dive.md

TARGET: FEAT-XX

This feature has turned out to be more complex than expected. Initiate deep analysis.

1. LOAD the protocol file `protocols/02b_deep_dive.md`.
2. ADOPT the 'Lead Feature Analyst' persona.
3. READ the ticket `Features/active_features/FEAT-XX.md` and review ALL previous implementation attempts in the Work Log.
4. UPDATE status in `feature_plan.md` to `[Deep Investigation]`.

5. EXECUTE Phase 1 - Agent Swarm:
   - Launch 4 Explore agents IN PARALLEL
   - Agent 1: Analyze architecture impact
   - Agent 2: Map dependencies of affected systems
   - Agent 3: Find similar patterns already implemented
   - Agent 4: Assess scope (feature vs project)
   - Document findings in `## Analysis Report`

6. EXECUTE Phase 2 - User Interview:
   - Use AskUserQuestion to gather requirements context
   - Document answers in `## Requirements Context`

7. EXECUTE Phase 3 - Complexity Assessment:
   - Estimate scope (files, LOC, new abstractions needed)
   - Rate complexity: Simple / Moderate / Complex / Project-Scale
   - Document in `## Complexity Assessment`

8. EXECUTE Phase 4 - Implementation Strategy:
   - If implementable: Write detailed plan
   - If project-scale: Draft project proposal
   - Document in `## Implementation Strategy`

9. EXECUTE Phase 5 - Resolution:
   - If implementable: Implement with TDD, set [Awaiting Confirmation]
   - If project-scale: Generate proposal, set [Needs Project]

CONSTRAINT: Do NOT mark as [Completed]. Do NOT move to archived_features/.
