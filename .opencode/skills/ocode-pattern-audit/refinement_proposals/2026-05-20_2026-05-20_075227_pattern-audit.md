# Refinement Proposals — pattern audit `2026-05-20_075227_pattern-audit`

> Source skill: `ocode-pattern-audit` | Generated by Protocol 18 (create_from_pattern_audit) Phase H | 2026-05-20
> Created project: PROJ-470

## Summary

The independent third-pass re-verification produced **0 REJECTED** findings out of 18 candidates (15 VERIFIED, 3 UNCERTAIN→Defer, 3 OUT_OF_SCOPE). The audit's own `findings/verification.md` had already disputed/downgraded its weakest claims (MAJ-H1, MAJ-H4, MAJ-H5) before this pass, so the candidate set was pre-filtered — the clean third pass is explained, not luck. No user-flagged misses.

## Proposed refinement (one)

**`raw/protocol_registry.json` is incomplete — it omits same-module TypeGuard functions.**

The audit's `raw/protocol_registry.json` did not list `is_fleet`, `is_planet`, `is_storm`, or `is_star_system`, which exist in `game/core/protocols/strategy_entities.py:415-445`. This nearly caused the Pattern #2 (TG-001..004) findings to be mis-verdicted as `UNCERTAIN` ("no TypeGuard exists → fix would broaden scope") when in fact the TypeGuards already exist and the fixes are drop-in replacements (`VERIFIED MINOR`).

**Recommendation for the audit tooling:** the protocol/TypeGuard scanner that builds `protocol_registry.json` should capture module-level `def is_*(...) -> TypeGuard[...]` functions in `game/core/protocols/*.py`, not only `class ... (Protocol)` declarations. Otherwise downstream Protocol-bypass verification (Protocol 18 Phase C `protocol_bypass` checklist step 4) systematically over-classifies isinstance findings as UNCERTAIN.

## No other refinements

REJECTED set is empty and there were no user-flagged misses, so no false-positive patterns to feed back beyond the registry-completeness item above.
