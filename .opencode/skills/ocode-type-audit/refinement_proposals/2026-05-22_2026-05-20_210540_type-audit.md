---
source_skill: ocode-type-audit
audit_dir: Reviews/results/2026-05-20_210540_type-audit/
generated_by: claude-proj-from-type-audit
generated_at: 2026-05-22
created_projects: [PROJ-481, PROJ-482, PROJ-483]
verified_count: ~138
rejected_count: 0
user_flagged_misses_count: 0
status: pending_review
---

# Refinement Proposal — type audit

Generated after `claude-proj-from-type-audit` processed `Reviews/results/2026-05-20_210540_type-audit/` and created PROJ-481 (UI per-finding), PROJ-482 (Strategy per-finding), and PROJ-483 (Foundation + strict quick wins).

This proposal is **suggestions only**. The user decides which items, if any, to merge into `.opencode/skills/ocode-type-audit/SKILL.md` or its underlying `Tools/type_audit/` scripts.

## 1. False-Positive Patterns

Zero per-finding REJECTIONS in this run — the audit's own verifier reported 0/5 CRITICAL and 0/5 MAJOR false positives, and Claude's third-pass skeptical re-verification also surfaced no genuine FPs across ~138 items. This is a strong result and there are no FP filters to propose.

> ⚠️ One soft caution: the protocol notes that "zero rejections is suspicious not reassuring." On this run the audit's per-finding sourcing was unusually rigorous (exhaustive file reads + cited file:line + verified line numbers). The lack of REJECTED items reflects audit quality, not verifier laxity. Track this metric across runs — if it stays at zero on every future run, sample manually.

## 2. Missing Checks

No items the user explicitly flagged as missed during bundling. However, the third-pass verifier surfaced two systematic gaps worth noting:

### MC-1: Cross-shard consistency check on identical patterns

- **What was missed:** Two identical `# type: ignore[assignment]` patterns (`defeat_dialog.py:83` and `turn_failed_dialog.py:99`) received opposite verdicts from different shards. Shard 01 flagged defeat_dialog as UNJUSTIFIED/removable; Shard 04 flagged turn_failed_dialog as JUSTIFIED. Same code, same context (DismissableModalDialog test-bypass path), same correct fix (declare `Optional[UIButton]` in constructor). The audit's own verification.md cross-shard consistency check noticed this and surfaced it — Claude's verifier had to re-confirm. The audit already has this cross-shard analysis layer; the issue is that final report.md didn't propagate the verifier's correction into the per-shard severity classifications.
- **Examples (user-flagged via verifier):**
  - `game/ui/screens/defeat_dialog.py:83` — `# type: ignore[assignment]` rated MAJOR in Shard 01
  - `game/ui/screens/turn_failed_dialog.py:99` — identical pattern rated JUSTIFIED in Shard 04
- **Why the audit missed it:** Verification.md correctly identified the inconsistency but the recommendation didn't ripple into the consolidated report.md's severity tables.
- **Proposed addition:**
  - **Phase 1 deterministic:** when consolidating shard reports, detect identical `# type: ignore[<code>]` strings on lines with structurally-equivalent context (same parent class, same method name) and force a consistent rating across shards. Could be a post-merge AST pass over the shard JSON outputs.
  - **Phase 2 prompt:** add a `### Cross-Shard Reconciliation` step to `SKILL.md` that explicitly tells the consolidator: "After the verifier runs, if it identified severity inconsistencies, update the affected per-shard rows in `report.md` to match the verifier's recommended consistent rating."
- **Estimated false-positive rate:** LOW (verifier already detects these; this is about wiring its output back into the report)
- **Recommendation:** ADD_TO_PROMPT

### MC-2: Line-drift detection between scan and report

- **What was missed:** Two findings had line numbers that drifted between the scan and the report being read days later:
  - TYP-01-049 `fleet_report_window.process_event` reported at line 248; current line 277
  - TYP-01-050 `_get_role_filter_options` reported at line 388; current line 396
- **Why the audit missed it:** The line numbers were correct at scan time. The audit doesn't include a guard against the source moving between scan and consumption. For type findings this is usually harmless (the function still exists), but for missing-return-type findings it forces consumers to grep.
- **Proposed addition:**
  - **Phase 1 deterministic:** include a short `symbol_fingerprint` field (e.g. function signature first 80 chars) in each finding row in `raw/*.json`. Consumer tools can then grep for the fingerprint and self-correct the line number if it has drifted.
  - **Phase 2 prompt:** none needed — this is purely a tool fix.
- **Estimated false-positive rate:** LOW
- **Recommendation:** ADD_TO_TOOL

## 3. Severity Calibration

### SC-1: The `_get_*_mutator()` private-helper pattern

- **Audit assigned:** mixed — 4 findings rated MINOR (shard 01 + shard 04), 5 findings rated MAJOR (shard 02 + shard 03)
- **Verifier reclassified:** consistently rated MAJOR (all 9 are concrete narrowable returns; severity should reflect that the mutator surface is the engine→consumer contract)
- **Audit's own cross-shard analysis recommended MINOR**, but the verifier disagrees: every one of these helpers narrows to a concrete `IPlanetMutator` / `IEmpireMutator` / `IShipInstanceMutator` and is consumed by engine sub-modules. The narrowing is high-leverage despite the `_` prefix because every consumer benefits.
- **Pattern:** Audit equates `_` prefix with low severity; verifier evaluates by consumer breadth instead.
- **Proposed change:** Update the severity guide in the agent template:
  ```
  CURRENT (paraphrased): "Private (`_`-prefix) helpers with concrete returns: MINOR."
  PROPOSED: "Private (`_`-prefix) helpers with concrete returns: MINOR by default, but elevate to MAJOR when (a) the helper returns a Protocol-typed value, AND (b) the helper is consumed across more than one module within the layer. Consumer-breadth check trumps the `_` prefix."
  ```
- **Recommendation:** ADD_TO_PROMPT

## 4. Phase 1 Tool Bugs

### TB-1: mypy `--strict` error-count estimator is significantly off for heavy layers

- **Symptom:** The audit's `report.md` §4 estimated strict-mode error counts that diverged from reality:

  | Layer | Audit estimate | Actual (verifier, 2026-05-22) | Ratio |
  |-------|---------------|------------------------------|-------|
  | services | 0 | 1 | (env-only) |
  | assets | 0 | 15 | ∞ (audit said zero) |
  | research | 0 | 0 | accurate |
  | engine | ~5 | 14 | 2.8× |
  | ai | ~54 | 60 | 1.1× |
  | core | ~85 | 116 | 1.4× |
  | simulation | ~126 | 622 | 4.9× |
  | strategy | ~375 | 1070 | 2.85× |
  | ui | ~452 | 2571 | 5.7× |

  research/ai/core are within tolerance. assets/sim/strategy/ui are substantially understated. The audit produces a heatmap-style estimate from counts of `-> Any` / `:Any` / missing-returns, but the actual strict-mode error categories include `attr-defined`, `no-untyped-def` (on internal funcs), `type-arg` (missing `list[T]` parameterizations), `has-type` (forward-ref cycles) — none of which the deterministic scanner enumerates.
- **Reproduction:**
  ```
  python -m mypy --strict --follow-imports=silent game/assets/   # 15 (audit: 0)
  python -m mypy --strict --follow-imports=silent game/simulation/  # 622 (audit: ~126)
  python -m mypy --strict --follow-imports=silent game/strategy/  # 1070 (audit: ~375)
  python -m mypy --strict --follow-imports=silent game/ui/        # 2571 (audit: ~452)
  ```
- **Likely fix location:** `Tools/type_audit/type_audit.py` (or wherever the heatmap density score is computed). Either (a) actually run `mypy --strict --follow-imports=silent` per layer instead of estimating, or (b) widen the heuristic to include the categories above.
- **Recommendation:** ADD_TO_TOOL

### TB-2: Shard-02 `_button_handlers` "false positive" call needs re-check

- **Symptom:** Shard 02 marked `gravity_target_editor.py:164` and `water_target_editor.py:173` `_button_handlers` as "False positive (already annotated)". Shard 03's missing-returns verifier disagreed and said they ARE missing annotations.
- **Reproduction:** open both files at the cited lines and confirm whether `def _button_handlers(...)` has a return annotation.
- **Likely fix location:** the shard-02 agent's verification step (or its source manifest). The shard may have read a stale view of the file, or conflated `_button_handlers` with a different method on the same class.
- **Recommendation:** DISCUSS (small but indicates the per-shard verification can occasionally misread state)

## 5. UNCERTAIN Items Worth Skill Attention

| Finding ID | File:Line | Category | Why uncertain | Suggestion |
|------------|-----------|----------|---------------|------------|
| TYP-04-MR-008 | `game/ui/screens/workshop_viewmodel.py:129` `_with_ship` | missing return | template-method pattern: returns whatever `on_success`/`on_failure` returns | Add audit guidance: template-method patterns deserve `TypeVar` annotation, not `Any`. Mention this in the severity guide. |
| (cluster) | `game/core/protocols/strategy_entities.py` 12 properties | narrowable Protocol Any | shards rated INFO/duck-typing, cross-layer report rated TIER 2 high-leverage | The audit's per-shard reviewers and the cross-layer report disagree about whether Protocol narrowing via TYPE_CHECKING string annotations is in scope. Pick one stance: either (a) shards should also recommend narrowing where TYPE_CHECKING avoids the cycle, or (b) the cross-layer report should respect shard verdicts. Currently the inconsistency forces the consumer to relitigate. |
| `core/formula_evaluator.py:81` | `_eval_node` recursive AST | narrowing complexity | recursive types are hard to narrow; audit marked INCONCLUSIVE | Add audit guidance: explicitly mark recursive AST evaluators as INFO-justified rather than INCONCLUSIVE-leave-to-consumer. Saves a verification round-trip. |

## 6. Disposition Notes (free-form)

- The audit's per-shard reviewers are unusually rigorous on cited evidence (line numbers, return-path traces). The "0 false positives" prior held up under third-pass scrutiny for the per-finding items; the calibration gaps are in strict-mode error estimation, not per-finding accuracy.
- The cross-layer report and the per-shard reports occasionally take opposite stances on the same code (Protocol narrowing INFO vs TIER-2 recommendation). Consider whether the cross-layer pass should be authoritative or advisory, and document the policy.
- Consider adding a `verification.md` row count to `report.md` Executive Summary so the consumer can immediately see how much skeptical filtering happened versus how much survives.

---

**Next step (for the user):**

1. Read this proposal end-to-end.
2. For each `ADD_TO_PROMPT` item: edit the relevant section in `.opencode/skills/ocode-type-audit/SKILL.md`.
3. For each `ADD_TO_TOOL` item: edit the corresponding script under `Tools/type_audit/`.
4. For `DISCUSS` items: raise with the team or in a tracking ticket.
5. Once merged or rejected, update the frontmatter `status:` to `merged`, `rejected`, or `partial` — or just delete the proposal file.
