---
source_skill: ocode-error-audit
audit_dir: Reviews/results/2026-05-07_220225_error-audit/
generated_by: claude-proj-from-error-audit
generated_at: 2026-05-08
created_projects: [PROJ-381]
verified_count: 26
rejected_count: 1
user_flagged_misses_count: 0
status: pending_review
---

# Refinement Proposal — error audit

Generated after `claude-proj-from-error-audit` processed `Reviews/results/2026-05-07_220225_error-audit/` and created PROJ-381.

This proposal is **suggestions only**. The user decides which items, if any, to merge into `.opencode/skills/ocode-error-audit/SKILL.md` or its underlying `Tools/error_audit/` scripts.

## 1. False-Positive Patterns

Only one finding was rejected by independent verification — too small a sample to identify a *systematic* false-positive pattern. The single rejection is recorded for traceability and submitted as a DISCUSS item rather than a hard filter rule, since auto-suppressing it could hide legitimate same-line-comment-missing cases elsewhere.

### FP-1: Block-comment immediately preceding `except` line

- **Examples (rejected findings):**
  - `ERR-03-005` — `game/ui/screens/transfer_dialog.py:383` — rejected because: lines 373-377 contain a multi-line block comment explaining the catastrophic-dispatch-failure rationale, the dialog-kill, and the re-raise. The verifier judged this materially equivalent to the canonical same-line `# Intentional broad catch:` comment for human readers, even though it fails the strict scanner format.
- **Proposed filter:**
  - **Phase 1 deterministic** (preferred when possible): when scanning for "broad except missing comment," extend the comment-detection window upward by N lines (suggest N=8) and look for either the canonical `# Intentional broad catch:` prefix OR a contiguous comment block (≥2 lines of `#`-prefixed text) explaining the catch. The current scanner appears to look only at the same line and the immediately preceding line.
  - **Phase 2 prompt guidance**: in the agent's "What NOT to Report" section, add: "When a multi-line comment block immediately precedes the `except` line and explains the catch's rationale, treat that as compliant even if the canonical same-line form is absent. Note the format-mismatch as MINOR/style rather than MAJOR/missing-justification."
- **Risk of filtering:** could mask cases where a comment block above an `except` describes something else (e.g., the *previous* statement, not the `except`). The N≤8-lines window keeps the false-suppression rate low, but the prompt guidance is the safer of the two changes.
- **Recommendation:** DISCUSS — single example is insufficient evidence for a deterministic rule change; the prompt-guidance variant is lower risk.

## 2. Missing Checks

User did not flag any audit-missed issues during interactive bundling. The audit's coverage is judged comprehensive for the categories it advertises.

(no entries)

## 3. Severity Calibration

The audit's severity assignments held up well under independent verification:

- All 14 MAJOR items survived as MAJOR (no downgrades).
- All 12 MINOR items survived as MINOR (no upgrades or downgrades).
- The single CRITICAL item (B-5) was independently re-confirmed CRITICAL with the full 6-link call chain traced.

No systematic over- or under-classification across 3+ findings detected. Skipping this section.

## 4. Phase 1 Tool Bugs

The deterministic scanner inputs (`raw/broad_except_sites.json`, `raw/json_bypass_sites.json`, `raw/bare_except_sites.json`, `raw/raise_generic_sites.json`, `raw/print_debug_sites.json`) accurately matched what verification found in source. No tool bugs surfaced.

One observation that may suggest a minor tool refinement:

### TB-1: Cross-shard duplicate findings on the same `file:line`

- **Symptom:** `ERR-02-003` (Shard 02, MINOR severity) and `ERR-04-002` (Shard 04, MAJOR severity) both flag `game/strategy/engine/turn_state_snapshot.py:56`. Same site, two different severity assignments. The bridge had to manually deduplicate and pick the more conservative classification.
- **Reproduction:** Look at `findings/error_review_02.md` and `findings/error_review_04.md` for ID matches on `(file, line)` tuples.
- **Likely fix location:** post-shard merge step in `Tools/error_audit/` (or the agent template that assembles per-shard reports). Either: (a) make shard assignment exclusive on file/line so the same site never appears in two shards, OR (b) add a deduplication pass after shard reports are aggregated.
- **Recommendation:** ADD_TO_TOOL — small win, but eliminates the bridge's manual reconciliation step and removes a class of severity-disagreement noise.

## 5. UNCERTAIN Items Worth Skill Attention

| Finding ID | File:Line | Category | Why uncertain | Suggestion |
|------------|-----------|----------|---------------|------------|
| ERR-04-007 | `game/strategy/data/star_generation_config.py:192` | broad_except (over-broad tuple) | The catch tuple `(ImportError, FileNotFoundError, OSError, KeyError, TypeError, ValueError)` includes `ValueError` and `KeyError` — these often indicate data-integrity bugs that could be silently masked. But the function is an `lru_cache`-decorated singleton factory with explicit defaults fallback, which is a documented defensive pattern. | The audit could distinguish "broad-tuple in factory/cache code with defaults fallback" (defensible) from "broad-tuple in pure load code that surfaces config to callers" (likely buggy). User accepted the narrower form here, but the audit's recommendation didn't acknowledge the singleton-factory context. Adding a prompt-level note about this pattern would reduce future debate. |

## 6. Disposition Notes

The audit was high-quality overall — verification rate of 26/27 actionable items confirmed (96%) and only 1 rejection across 28 findings is exactly the kind of signal that justifies the third-pass verification existing as a low-friction sanity check rather than a heavyweight gate. The audit's internal verifier (`findings/verification.md`) sampled 5 of the 16 MAJOR findings and found 0 false positives — that sampling rate produced an estimate that matched the bridge's full-population result.

**Next step (for the user):**

1. Read this proposal end-to-end.
2. For each `ADD_TO_PROMPT` item: edit the relevant section in `.opencode/skills/ocode-error-audit/SKILL.md`. (None proposed this run.)
3. For each `ADD_TO_TOOL` item: edit the corresponding script under `Tools/error_audit/`. (TB-1 is a small post-shard dedup pass.)
4. For `DISCUSS` items: raise with the team or in a tracking ticket. (FP-1 same-line-comment-window question; ERR-04-007 singleton-factory disambiguation.)
5. Once merged or rejected, update the frontmatter `status:` to `merged`, `rejected`, or `partial` — or just delete the proposal file.
