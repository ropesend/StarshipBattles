New Game Setup
Need to be able to select number of stars

Race Setup - should use larger window

RAce setup --> racial points should bew displayed in the Environment windows

Game Setup + RAce Setup should follow the same themes as the strategy layer

All reference to race should be species instead

Aptitudes shoud be 1 to 100, base 50, and it should be linear to 50, but exponentially expensive beyond 50

When the homeworld type is selected, all environmental preferences should be set to a typical value for that type of planet.

In the Design Workshop placing a component in multiple layers causes problems,  I had a Life Support component in the Inner layer, then added one to the outer layer, and then it looks like this: C:\Dev\Starship Battles\output\screenshots\screenshot_20260207_150507_593326_mouse_focus.png  The component in the outer layer shows, but the inner layer component disapears, and there is a blank space.

In the Design Workshop, int the Component modifyers Panel, all applicable modifiers should always be selected, and shown, all non applicable modifyers should not be shown.  Here is a visual of the panel when a Life Support module is selected, the Hardened component should be selected, but just default to 1.00 it should not be possible to unselect it, and it does not start out selected.,  please look into all modifyerss, as there are a few that seem to do this: C:\Dev\Starship Battles\output\screenshots\screenshot_20260207_150924_614024_mouse_focus.png

In the Design Workshop when playing the actual game, I should not be able to select different themes, currently I can select different themes, but they don't actually impact the image of the ships shown.