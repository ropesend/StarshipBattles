# PROJ-306: Decisions Log

> **LOG ALL DECISIONS HERE**
> When you make a design choice or the user specifies a preference, add it to this table.
> Future agents will reference this to understand why things were done a certain way.

| Date | Decision | Rationale |
|------|----------|-----------|
| 2026-04-26 | Project initialized | Closes PROJ-274 / PROJ-211 by eliminating the two surviving global-lookup fallbacks in the Simulation layer |
| 2026-04-26 | Both call sites in scope; `protocols.py:38` TYPE_CHECKING import is OUT OF SCOPE | The TYPE_CHECKING import is a documented unavoidable trade-off (acknowledged in the original code review). Touching it would create new problems without solving any |
| 2026-04-26 | `ApplicationContext`-based migration is preferred when caller surface is large | If many callers exist, requiring them all to pass `ship_builder`/`registry_provider` is high churn. Pulling from a context (analogous to `get_default_ship_materializer`) keeps caller surface small while still eliminating the global-lookup pattern from Simulation. Phase 1/2 surveys will inform the choice per call site |
| 2026-04-26 | Line-90 comment in `registry_loader.py` is KEPT after the fix | "Pass registry_provider explicitly (no fallback)" is the correct documentation once the fallback is actually gone. Becomes truthful, useful for future maintainers |
| 2026-04-27 | **Pattern: hybrid — ship_builder remains Optional, registry_provider becomes required when ship_builder is None** | Inventory found 3 production callers in Category B (omit `ship_builder`) and 3 production callers that import the private `_default_ship_builder_from_context` directly. Forcing all 6 to pass `ship_builder` is high churn AND removes a useful ergonomic. Instead: add a new `registry_provider: Optional[IRegistryProvider]` kwarg to `run_battle` / `BattleController.start_from_spec`. When `ship_builder is None`, the `registry_provider` MUST be provided — the engine refuses to global-lookup from Simulation. Non-Simulation production callers (`simulation_adapter`, `app.py`, `test_execution_service`) call `get_default_registry_provider()` themselves and pass it in. This eradicates the Simulation-layer global lookup while keeping the public `ship_builder=None` ergonomic intact for tests that don't need it. |
| 2026-04-27 | **Rename `_default_ship_builder_from_context` → `build_context_ship_builder`** and make it public, accepting `registry_provider: IRegistryProvider` as a REQUIRED parameter | The 3 combat_lab callers that currently `from game.simulation.battle_runner import _default_ship_builder_from_context` need a stable public successor. The new public name documents the intent ("build a ship_builder backed by the context materializer") and the required `registry_provider` parameter eliminates the in-function global lookup |
| 2026-04-27 | **`registry_loader.reload_registries_from_directory`: Pattern A — `registry_provider` becomes a required keyword parameter** | This function is called ONLY from tests (verified via grep). Zero production callers. Required-parameter migration is trivial: each test file already has `fresh_registry` / `mock_registry_manager` fixtures and can construct/pass a provider in one line |
