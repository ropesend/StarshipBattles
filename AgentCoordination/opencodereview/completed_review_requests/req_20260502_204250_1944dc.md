# Review Request: Repo tidy-up plan review
**Request ID:** req_20260502_204250_1944dc
**Review Type:** plan
**Created:** 2026-05-02T20:42:50Z
**Requester:** claude-code

## Scope

Plan file under review: `C:\Users\rossr\.claude\plans\perform-a-review-of-wild-lampson.md`

The plan proposes a tidy-up of `c:/Developer/StarshipBattles`. It is grouped into four tiers:

- Tier 1: delete root-level junk — `help.txt`, `help_utf8.txt`, `providers_help.txt`, `providers_list.txt`, `providers_login_help.txt`, `new 1.txt`, `script_test_icons.py`, `test_scroll.py`, `test_scroll2.py`, and the empty `ui/` dir
- Tier 2: gitignore `coverage.json`, `git rm --cached coverage.json`, delete file
- Tier 3: `git mv "Temp Review Docs" Reviews/results/2026-04-18_skeptic_proj-283-290`
- Tier 4: archive `Reviews/results/` folders dated 2026-01-23 through 2026-02-01 into `Reviews/results/_archive_2026_Q1/`; consolidate root-level `tests/repro_*.py` (`repro_facade_colonies.py`, `repro_load_cargo_bug.py`, `repro_warp_bug.py`) into `tests/repro_issues/`; move 6 docs from `AgentCoordination/opencodereview/_archive/` into `_marked_for_deletion_2026-05-29/AgentCoordination/_opencodereview_archive/`

Explicitly out of scope (do NOT review these as omissions):
- `assets/` move (referenced by game code, deliberate skip)
- `pytest.ini` → `pyproject.toml` merge (deliberate skip)
- `_marked_for_deletion_2026-05-29/` (intentional 30-day staging, deletion date 2026-05-29)
- `docs/_ignore/` (user scratch space per CLAUDE.md, off-limits)

Today is 2026-05-02.

## Instructions

The user wants a fresh-eyes second opinion on this plan. Focus the review on these four questions:

1. **Load-bearing items mistakenly listed for deletion or move.** For each path in Tiers 1–4, verify by reading the repo:
   - `grep` the codebase for the filename or path
   - Check if it is referenced in CI, scripts, hooks, `.claude/settings.json`, or test discovery
   - For `test_scroll.py` / `test_scroll2.py` — confirm pytest is not collecting them as real tests (they begin with `test_` so this matters); the plan claims they are scratch
   - For `script_test_icons.py` — confirm no `Tools/` or skill references it
   - For `coverage.json` — confirm nothing reads it (some CI badges/tools do)
   - For `ui/` (root) — confirm truly empty and no reference to that path
   - For the `tests/repro_*.py` move — confirm no test or import path references those root-level paths
   - For the `_archive/` opencodereview docs — confirm no live doc/skill links them

2. **Risk of breaking imports, tests, tooling, or hooks.** Specifically:
   - Does `pytest --collect-only` include any of the to-be-deleted `test_scroll*.py` files? If so, deleting them changes the collected test set
   - Are any `.claude/settings.json` hooks referencing any of these paths?
   - Are any of the `providers_*.txt` / `help*.txt` files referenced from documentation that would break links?
   - Is the `Reviews/results/` archive cutoff date (2026-02-01) reasonable, or are any of those folders linked from `reviews_index.md` or other live docs in a way that would break?

3. **Reviews/results cutoff criteria.** The plan picks "on or before 2026-02-01" as the archive cutoff. Is that well-chosen? Consider:
   - Activity boundary — when did the review cadence shift in 2026?
   - Whether the cutoff bisects an active project's review trail
   - Whether age alone is the right criterion vs status (completed/abandoned)

4. **Anything missing.** What would a fresh reviewer flag that the plan does not? Examples to consider:
   - Other obvious junk at the root that the plan missed
   - Stale subdirs in `Tools/`, `scripts/`, `tests/` that were not surveyed
   - Files in `Projects/active_projects/` or `Tracking/` that should be archived
   - Generated artifacts that should be gitignored (e.g., `.test_durations.json`, log files, build outputs)
   - Anything in `AgentCoordination/` outside of `_archive/` that is stale

Severity scheme: CRIT (will break something), MAJ (item is load-bearing or cutoff is wrong), MIN (nice-to-have additions or refinements). Per finding, give the file/path, the concrete risk, and the suggested fix.

Please be skeptical — the plan was written quickly and the reviewer's job is to find what was missed, not to validate.

## Context

The plan is in plan mode awaiting approval. The user explicitly requested a delegate review before executing. They have already chosen "All tiers (1–4)" with one commit per tier.

## Expected Deliverable

A findings report with severities (CRIT/MAJ/MIN), each citing the path, the issue, and a concrete recommendation. End with a one-line overall verdict: APPROVE / APPROVE WITH CHANGES / REJECT.


**PickedUp:** 2026-05-02T20:42:52Z

## Results

**Status:** failed
**Completed:** 2026-05-02T21:12:53Z
**FailureReason:** timed out
