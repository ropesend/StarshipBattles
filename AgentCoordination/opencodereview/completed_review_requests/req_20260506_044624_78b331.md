# Review Request: PROJ-375 audit-shrink consolidation — verify duplications were collapsed cleanly with no semantic drift
**Request ID:** req_20260506_044624_78b331
**Review Type:** code
**Created:** 2026-05-06T04:46:24Z
**Requester:** claude-code

## Scope

Three commits on `feat/03c-phase-aware-execution`:
- `2d2cc3566` — Phase 1: delete dead method `_find_shield_component_id`
- `a71b35944` — Phase 2: 6 strategy-layer duplication clusters consolidated (DUP-X-01, DUP-X-02+06, DUP-X-05, DUP-X-07/Cluster 11, Cluster 5, Cluster 29+30)
- `c0f58bb0b` — Phase 3: 3 UI-layer duplication clusters (DUP-X-03, DUP-X-04, Cluster 6)

Files changed (Phase 2 + Phase 3):
- `game/strategy/services/component_inspector.py` — new `iter_facility_ability_entries` generator (DUP-X-02+06)
- `game/strategy/engine/handlers/base.py` — new `_resolve_player_planet` (DUP-X-01)
- `game/strategy/engine/planet_command_handlers.py` — 7 handlers refactored + 4 SetXTarget handlers merged via `_apply_planet_environmental_target` (Cluster 5)
- `game/strategy/engine/superweapon_command_handlers.py` — 4 mission handlers routed through `_emit_validated_order` (DUP-X-07/Cluster 11)
- `game/strategy/services/race_description_llm_controller.py` — bio/socio mirrored attrs replaced with `_fields: dict[str, _FieldState]` (DUP-X-05); 6 public properties preserved as shims
- `game/strategy/engine/harvesting_engine.py` — `_get_ability_info` / `_get_ability_data_from_registry` extracted (Cluster 29+30)
- `game/strategy/engine/planet_action_engine.py` — migrated 4 ability-extraction sites to `iter_facility_ability_entries`
- `game/ui/screens/workshop_event_router.py` — `_apply_resolver_dropdown` + `_apply_confirmation_dropdown` extracted (DUP-X-03)
- `game/ui/screens/planet_list_window.py`, `star_list_window.py`, and the underlying `DataListWindowMixin` — `_run_update_template(slider_keys)` extracted (DUP-X-04)
- `game/ui/screens/builder/structure_list_items.py` — `_rebuild_modifier_icons_for_item` extracted (Cluster 6)

Project docs: `Projects/active_projects/PROJ-375/plan.md`, `design.md`, `decisions.md`, `findings/verification_report.md` (the source-of-truth for the 10 in-scope items).

## Instructions

PROJ-375 consolidates duplications previously identified by an audit + independent verifier. The audit explicitly tagged 10 items as verified-safe; this project landed all 10. **Do not re-litigate whether the duplications were worth consolidating** — that decision is final. Focus instead on:

1. **Semantic equivalence.** For each consolidation, was the merged path semantically identical to every original site, or did the consolidation introduce subtle behavior changes? Walk through each cluster and check:
   - DUP-X-02+06 (`iter_facility_ability_entries`): are the 5 engines + 4 sites in `planet_action_engine.py` consuming the generator with the same iteration order / list-vs-dict normalization as their original loops?
   - DUP-X-01 (`_resolve_player_planet`): does the new helper produce the same return shape / error path as the 7 handlers' original inline code?
   - Cluster 5 (`_apply_planet_environmental_target`): the 3 SetXTarget handlers + 1 (atmosphere?) merged — do they all need exactly the same validation / event emission, or did one have an asymmetric step?
   - DUP-X-07/Cluster 11 (`_emit_validated_order`): the 4 superweapon mission handlers — are their pre-emission checks identical, and does `_emit_validated_order` cover all of them?
   - DUP-X-05 (`RaceDescriptionLLMController._fields` dict): the 6 public properties are preserved as shims — do all 6 read the dict in the same way the originals read mirrored fields? Are there setter paths that need to write through?
   - Cluster 29+30 (`_get_ability_info` / `_get_ability_data_from_registry`): the 3 wrapper functions (`get_harvester_info`, `_get_storage_info`, `_get_staging_info`) — do they preserve the original return shape including any defaults or null handling?
   - DUP-X-03 (`_apply_resolver_dropdown` + `_apply_confirmation_dropdown`): the 5 dropdown handlers — does the parameterised resolver correctly handle both options-list lookup AND registry-loop iteration, and is the per-handler dialog sizing/warning text faithful?
   - DUP-X-04 (`_run_update_template`): both `update()` methods are now one-liners — verify the 4-step routine (scrollbar → slider sync → header sort/swap → preset dropdown) is invoked in the right order with the right slider keys for each window.
   - Cluster 6 (`_rebuild_modifier_icons_for_item`): the two methods on `IndividualComponentItem` / `LayerComponentItem` are now thin wrappers — do they pass the right `self` state to the module-level helper?

2. **Test coverage of consolidated paths.** Phase 2 added `test_component_inspector.py` (9 cases for `iter_facility_ability_entries`). Phase 3 rewired 3 column-swap tests to call the mixin directly. Are these test changes sufficient, or were the original duplication sites covered by tests that no longer exercise the consolidated helpers? Specifically: are there tests that monkey-patched the old method (e.g. `_find_shield_component_id`) or asserted on the old call shape?

3. **Public surface preservation.** DUP-X-05 specifically preserves 6 `@property` shims for backward compatibility. Verify zero call sites broke. Other consolidations should not have changed public surfaces — flag any that did.

4. **No new bandaids introduced.** Per CLAUDE.md, consolidations must produce real shared helpers, not lossy abstractions. Are any of the new helpers awkwardly parameterised (god-function-y) such that they're harder to maintain than the original duplication?

5. **Files that PROJ-370 will also touch.** PROJ-375 modifies `planet_command_handlers.py`, `harvesting_engine.py`, `handlers/base.py`. PROJ-370 (data-layer mutator protocols) is queued next. Do the new shared helpers introduced here make PROJ-370's mutator routing harder, easier, or unchanged?

## Output format

Markdown report grouped by severity (CRIT / MAJ / MIN / INFO). Each finding cites file:line, describes the issue, suggests remediation. The user will run a verifier subagent against your report before acting, so be precise. If a cluster's consolidation is sound, say so explicitly (one INFO line per cluster is fine).

## Context

Wave A project 2 of 3. PROJ-374 (GridLayer cache) just closed with a constructive review (0 CRIT / 2 MAJ / 4 MIN / 6 INFO). PROJ-375 is structurally riskier than PROJ-374 — it modifies 11 production files across the strategy and UI layers, vs. PROJ-374's narrow cache addition. The verification_report at `Projects/active_projects/PROJ-375/findings/verification_report.md` already pre-vetted that the duplications are real; the question is whether the *consolidations* are correct.

## Expected Deliverable

Findings report with per-cluster verdict, severity-tagged. Concise + precise > exhaustive.


**PickedUp:** 2026-05-06T04:46:25Z

## Results

**Status:** completed
**Completed:** 2026-05-06T04:57:35Z
**Report:** Reviews/results/2026-05-06_044625_code_proj-375-audit-shrink-consolidation-verify-duplica_req-req_20260506_044624_78b331/report.md
**Findings:** 12 total (C: 0, M: 1, m: 3, I: 8)
