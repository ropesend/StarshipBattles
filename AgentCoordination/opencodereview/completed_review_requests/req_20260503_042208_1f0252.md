# Review Request: PROJ-319 audit-shrink cleanup — independent review of 30-item implementation
**Request ID:** req_20260503_042208_1f0252
**Review Type:** code
**Created:** 2026-05-03T04:22:08Z
**Requester:** claude-code

## Scope

PROJ-319 implements 30 verified items from the 2026-05-02 audit-shrink review (`Reviews/results/2026-05-02_184210_audit_shrink/`). Project files: `Projects/active_projects/PROJ-319/plan.md`, `decisions.md`, `manifest.md`, `findings/verification_report.md`, `phase_1_checklist.md`, `phase_2_checklist.md`, `phase_4_checklist.md`. The branch diff vs. `main` is the source of truth for what changed.

**New production modules (all under `game/`):**
- `game/strategy/services/race_resolver.py`
- `game/ui/widgets/column_toggle_section.py`
- `game/ui/widgets/range_slider_builder.py`
- `game/ai/spatial_behaviors/_formation_utils.py`
- `game/ui/screens/list_data_source_base.py`
- `game/ui/screens/planet_target_editor_base.py`
- `game/ui/screens/data_list_window_mixin.py`

**New helpers / mixins / methods added in existing files:**
- `RaceConfigResolverMixin` in `game/ui/screens/species_selector_mixin.py`
- `_with_ship` on `WorkshopViewModel` in `game/ui/screens/workshop_viewmodel.py`
- `_emit_validated_order` on `BaseCommandHandler` in `game/strategy/engine/handlers/base.py`
- `_validate_star_targeted_superweapon` in `game/strategy/validation/superweapon_validator.py`
- `_open_planet_target_editor` in `game/ui/screens/strategy_event_router.py`
- `_handle_superweapon_click` in `game/ui/screens/strategy_click_dispatcher.py`
- `_load_json_or_empty` + `_apply_intrinsic_abilities` in `game/strategy/data/galaxy_system_generator.py`

**Phase 1 — 14 dead-code deletions (see `phase_1_checklist.md`):** `GameState.FORMATION` (constants.py:29), `_ccm_mod` import (context.py:116), `naming_data_path` param (galaxy.py:624), `age_ratio` param (stars.py:303), `MASS_MOON` import (planet_gen.py:23), `import warnings` (design_metadata.py:13), `get_shield_info` import (planet_action_engine.py:25), `FleetType` TYPE_CHECKING import (fleet_dto.py:11), unreachable `return 1` (action_time_resolver.py:115), `sig_digits` param (modifier_impact_grid.py:273), `ConfirmationDialog` import (test_lab/screen.py:32), `ShipIOType` TYPE_CHECKING import (ship_io_adapter.py:19), `STAR_FALLBACK` import (galaxy_test/system_mode.py:17), redundant `y_offset = 0` (build_queue_selector.py:97).

**Phase 2 — 2 dead-function deletions:** `_extract_weapon_summaries` (battle_runner.py:647-671) and `_planet_has_shield_facility` (strategy_detail_fmt.py:316-347).

**Phase 4 — 14 duplication consolidations (DUP-X-01..14):** see `phase_4_checklist.md` for per-task audit IDs and target classes/helpers.

**Test files modified to track refactor:**
- `tests/integration/strategy/test_planet_physics.py` (re-import of `MASS_MOON`)
- `tests/unit/ui/screens/test_event_log_sidebar.py`
- `tests/unit/ui/screens/test_fleet_report_sidebar.py`
- `tests/unit/ui/screens/test_fleet_report_window_multi_select.py`
- `tests/unit/ui/screens/test_planet_list_components.py`

Final sharded test run on the implementer's machine: 16374 passed, 0 failed, 3 skipped (16377 collected).

**OUT OF SCOPE:** the source audit-shrink report itself (`Reviews/results/2026-05-02_184210_audit_shrink/`) — that is the input. Review the implementation only.

## Instructions

## Goal

Act as a strict, skeptical second-pass reviewer. The implementer (Claude) has self-attested that all 30 items pass the full sharded test suite. The verifier missed at least one re-export case during planning (`MASS_MOON` from `planet_gen.py` was reachable as a re-export by `tests/integration/strategy/test_planet_physics.py`); be alert for similar misses.

## What to verify

### 1. Phase 1 deletions — confirm dead reachability

For every Phase 1 symbol (the 14 in scope), grep the entire repo for **all of**:

- The bare symbol name in `game/`, `tests/`, `docs/`, `data/`, `Tools/`.
- The symbol as a quoted string (e.g. `"FORMATION"`, `"sig_digits"`) — string-keyed dispatch.
- `from <original_module> import <symbol>` across `tests/` and `game/` — **re-export hubs**. The implementer caught one such case (`MASS_MOON`) only because a test broke. Audit every other Phase 1 symbol the same way and report any missed re-exports as findings.
- For TYPE_CHECKING imports (`FleetType`, `ShipIOType`): grep the file the import lives in for the symbol used as a quoted string annotation (e.g. `"FleetType"` inside the file).
- For removed parameters (`naming_data_path`, `age_ratio`, `sig_digits`): confirm zero callers pass them positionally OR by keyword anywhere in `tests/` or `game/`.

### 2. Phase 2 deletions — confirm supersession

- `_extract_weapon_summaries` was claimed superseded by `WeaponSummaryAggregator` instantiated at `battle_runner.py:402` and consumed via `weapon_aggregator.snapshot(engine)` at `battle_runner.py:436`. Verify the snapshot output is the same shape (same per-component fields: `component_id`, `component_name`, `shots_fired`, `shots_hit`) the deleted function produced.
- `_planet_has_shield_facility` was claimed superseded by `_planet_has_ability_facility` (same file, line 419). Confirm the new code path is invoked for the same conditions — specifically that the loop at lines 289-299 of `strategy_detail_fmt.py` iterates `_ALL_TOGGLEABLE` (which includes `'PlanetaryShield'`) and calls `_planet_has_ability_facility(planet, ability_key)` for each.

### 3. Phase 4 extractions — line-by-line behavior preservation

For each task below, confirm the new helper / base / mixin produces **byte-identical** behavior to the deleted/inlined original. Anywhere it doesn't, file a finding.

- **DUP-X-01 (race_resolver):** Compare `game/strategy/services/race_resolver.py::resolve_race_config` against the previously-duplicated `_get_race_config` body in `happiness_engine.py` (was lines 130-159) and `population_engine.py` (was lines 164-193). Confirm:
  - Resolution order: race_registry → empire fallback → None.
  - Returns `None` when `empire.race_config is None`.
  - Returns `None` when `race_config.race_id != race_id` (does NOT silently return the wrong race).
  - Engine wrappers correctly thread `self._race_registry` to the helper.

- **DUP-X-02 (superweapon click dispatcher):** `_handle_superweapon_click` uses `getattr(self.scene._superweapons, designation_method)` — confirm this is functionally equivalent to the prior explicit `self.scene._superweapons.handle_<name>_designation(...)` calls. Confirm the 5 thin wrapper methods pass the correct designation method names. Pay special attention to right-click (button==3) cancel behavior preservation.

- **DUP-X-02 (_emit_validated_order):** The deleted handlers used `logger.info(f"GameSession: Issued <ORDER> order for Fleet {fleet.id}")` (f-string). The new helper uses `logger.info("GameSession: Issued %s order for Fleet %s", log_label, fleet.id)` (lazy %-formatting). Confirm:
  - The log message text rendered to consumers (e.g. log file, log capture tests) is identical.
  - No test or debug consumer is parsing log records by exact-string match.
  - The behavior on `result.is_valid == False` is unchanged: no order added, no log line emitted, original error result returned.

- **DUP-X-04 (RaceConfigResolverMixin):** Open all four target editors (`atmosphere_target_editor.py`, `gravity_target_editor.py`, `water_target_editor.py`, `radiation_shield_editor.py`). For each, confirm:
  - The mixin appears BEFORE the parent base class in the MRO (so `_get_active_race_config` actually resolves to the mixin, not silently shadowed).
  - The previous local `_get_active_race_config` method is fully deleted.
  - The instance attributes the mixin reads (`_species_dropdown`, `_default_race_id`, `race_config`) are still set by the editor's `__init__`.

- **DUP-X-05 (PlanetTargetEditor base):** Confirm the new base in `planet_target_editor_base.py`:
  - Inherits `RaceConfigResolverMixin, StrategyModalWindow` in the right order.
  - The four editors now inherit only from `PlanetTargetEditor`.
  - The `_button_handlers()` overrides on each editor return the same set of (button → method) pairs the original `process_event` was dispatching to — no missing buttons, no extra ones.
  - The base's `process_event` correctly returns True for handled events and falls through to `super().process_event()` for everything else (matching the original `handled = super().process_event(event)` pattern).
  - The close-callback wiring (UI_WINDOW_CLOSE → on_close_callback) is preserved including the `event.ui_element is self` guard.

- **DUP-X-06 (_open_planet_target_editor helper):** Confirm:
  - Passes `window_manager=self.ui.window_manager` into the editor (Pattern #31 modal-tracking — drop-out is a real bug class).
  - The default `rect_size=(400, 300)` matches what gravity/water/radiation_shield used originally.
  - The `target_kwarg` parameter is correctly threaded into the command class as `**{target_kwarg: target_value}`.

- **DUP-X-07 (range_slider_builder):** Confirm `build_range_slider_row` returns the dict shape the callers' `ui_filters[key]` expects (`{'min', 'max', 'min_txt', 'max_txt', 'limits'}`).

- **DUP-X-08 (column_toggle_section):** Confirm `build_column_toggle_section` returns `(new_y, buttons_dict)` and the sidebars merge `buttons_dict` into `self.column_buttons` correctly.

- **DUP-X-09 (_validate_star_targeted_superweapon):** Confirm the helper preserves both error messages exactly (`"System has no stars to destroy."` for stellerate, `"System must have stars to create a Dyson Sphere."` for dyson sphere). The implementer added a `no_stars_message` parameter — verify both wrappers pass the right one.

- **DUP-X-10 (_with_ship on WorkshopViewModel):** Confirm:
  - Three return-type variants are correctly handled (bool for add_component / add_component_instance, Optional[Component] for remove_component, bool for move_component).
  - The `on_success` lambda pattern returns the right value: `True` for the bool variants, `result.removed_component` for remove_component.
  - The guard semantics match: when `_require_ship` returns False, the helper returns `on_failure` without calling the service or notifying.

- **DUP-X-11 (_load_json_or_empty):** Confirm:
  - Each loader's previous behavior is preserved: planet_types extracts `data.get('planet_types', {})`, star_types extracts `data.get('star_types', {})`, system_archetypes returns the whole JSON (`dict_key=None`).
  - The module-level cache variables (`_PLANET_TYPES_CACHE`, `_STAR_TYPES_CACHE`, `_SYSTEM_ARCHETYPES_CACHE`) are still populated and reused on subsequent calls.

- **DUP-X-12 (_apply_intrinsic_abilities):** Confirm:
  - The lambda type-key extractors (`lambda p: p.planet_type.name`, `lambda s: s.star_type.name`) return the same values the original code's `entity.planet_type.name` / `entity.star_type.name` returned.
  - Idempotency check is preserved (`if entity.intrinsic_abilities: continue`).
  - The default `random.Random()` fallback when `rng is None` is preserved.

- **DUP-X-13 (compute_circular_position):** Confirm the math is byte-identical:
  - `total = max(int(total), 1)` matches the original `total = max(len(group_ships), 1)` at the call site (escort.py and screen.py).
  - `angle = (2 * math.pi * slot_index) / total` and `cos`/`sin` order preserved.
  - The anchor source distinction (escort: `anchor_ship.position`, screen: `kwargs['anchor_position']`) is preserved at the call site.
  - The function lives at `game/ai/spatial_behaviors/_formation_utils.py` (with leading underscore in the path); confirm any old `_compute_circular_position` references aren't dangling.

- **DUP-X-14 (ListDataSource base):** Confirm:
  - `PlanetDataSource` and `StarDataSource` correctly inherit and override only `_render_icon`.
  - The legacy property aliases (`_planets` on PlanetDataSource, `_stars` on StarDataSource) return the right list (`self._rows`).
  - The `get_planet_at_index` / `get_star_at_index` aliases delegate to `self._entity_at(row_index)`.
  - The base's `update_data(rows)` is compatible with the previous `update_data(planets)` / `update_data(stars)` signatures from the caller side.

### 4. Project hygiene

- Confirm `Projects/active_projects/PROJ-319/phase_1_checklist.md`, `phase_2_checklist.md`, and `phase_4_checklist.md` have zero unchecked `[ ]` boxes.
- Confirm `Projects/active_projects/PROJ-319/manifest.md` lists every file modified in this branch. **Specifically, check whether** `game/strategy/engine/superweapon_command_handlers.py` (Task 4.12) and the four test files patched for Tasks 4.3 and 4.4 (`test_event_log_sidebar.py`, `test_fleet_report_sidebar.py`, `test_fleet_report_window_multi_select.py`, `test_planet_list_components.py`) appear in the manifest. If not, file findings.
- Confirm `decisions.md` captures the four notable decisions: zero-rejection rate, MASS_MOON re-export bug, LLM flaky timing test, BuildContext protocol pre-existing bug. If any decision is missing, file a finding.
- Confirm `findings/verification_report.md` Round 3 documents the re-export gap discovered during execution.

### 5. Pre-existing-bug accounting

The implementer claimed two failing tests are pre-existing on `main`, NOT caused by PROJ-319:

- `tests/unit/strategy/data/test_build_context.py::test_fleet_satisfies_build_context_protocol`: Verify by `git log --oneline tests/unit/strategy/data/test_build_context.py` and `git log --oneline game/strategy/data/fleet.py`, looking for the PROJ-210 commit that removed Fleet pass-through methods. Confirm the test still expected the old Fleet attributes after that commit. Run the test on a clean checkout of `main` (without PROJ-319 changes) — does it fail there?
- `tests/unit/services/llm/test_background.py::test_elapsed_seconds_is_monotonic_then_frozen`: Run it 5 times in a row on a clean tree (PROJ-319 stashed). If you see it fail at least once, the flake claim is corroborated.

Report BOTH the verification result AND the underlying root cause for each (e.g. "Confirmed pre-existing — PROJ-210 commit `<sha>` removed the pass-through; test was never updated").

## Style / hygiene checks

While reviewing, also flag:

- Layer violations (UI importing strategy internals, simulation importing UI, etc.).
- 500-LOC ceiling violations introduced by the consolidation work.
- Any new `except Exception` without an intentional-reason comment (per project conventions).
- Any `from __future__ import annotations` mismatch with TYPE_CHECKING usage.
- Inconsistent naming between the audit's recommended target name (e.g. `_compute_circular_position`) and the implementation name (`compute_circular_position` — no leading underscore).

## Severity tags

- **CRITICAL** — A reachable callsite was deleted (real regression that would break the game), a behavior changed, or test-suite green is misleading.
- **HIGH** — A genuinely broken extraction (wrong helper signature, wrong base-class MRO, wrong default value) that hasn't surfaced in tests but will at runtime.
- **MEDIUM** — Style / hygiene / documentation issue, potential layering concern, missing manifest/decision-log entry.
- **LOW** — Suggestion for improvement, nice-to-have, naming nit.

Use file:line for every finding. Suggest a concrete fix (paste the exact line(s) you'd write).

## Context

PROJ-319 was created from the 2026-05-02 audit-shrink review by `/claude-proj-from-audit-shrink`. The skill ran two verification rounds before implementation: Round 1 returned 30/30 VERIFIED, Round 2 (skeptical) raised 6 challenges that were all rejected on review. During execution, a third issue surfaced (the MASS_MOON re-export). All this is documented in `Projects/active_projects/PROJ-319/findings/verification_report.md`.

The project's design.md explicitly noted the zero-rejection rate as suspicious and recommended per-task pytest runs as a safety net during execution. The implementation followed that pattern.

The implementer is asking for an INDEPENDENT third opinion to confirm the work is sound before archive. Treat this as a high-stakes review — the code in scope is in `game/` (not `_marked_for_deletion_*` or `Refactoring/`) and will run in production.

## Expected Deliverable

Markdown report at `Reviews/results/<date>_<slug>/report.md` with:

1. Per-task verdict (PASS / FAIL / UNCERTAIN) for each of the 30 items, with one-line evidence.
2. Findings list with severity tags (CRITICAL / HIGH / MEDIUM / LOW), file:line, and a concrete suggested fix.
3. Pre-existing-bug verification results (with git-blame evidence for `test_build_context.py` and timing histogram for the LLM flake).
4. A summary section: total findings count by severity, and a final recommendation (READY-TO-ARCHIVE / NEEDS-FIXES / NEEDS-DEEPER-REVIEW).


**PickedUp:** 2026-05-03T04:22:08Z

## Results

**Status:** completed
**Completed:** 2026-05-03T04:41:27Z
**Report:** Reviews/results/2026-05-03_042208_code_proj-319-audit-shrink-cleanup-independent-review-o_req-req_20260503_042208_1f0252/report.md
**Findings:** 13 total (C: 0, M: 3, m: 10, I: 0)
