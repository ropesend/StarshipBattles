# Review Request: PROJ-378: galaxy cleanup test pattern update (post-PROJ-372 facade-delegate)
**Request ID:** req_20260507_042609_bb3683
**Review Type:** code
**Created:** 2026-05-07T04:26:09Z
**Requester:** claude-code

## Scope

Commits in scope on branch feat/03c-phase-aware-execution:
- 9667eaa5a PROJ-378 phase 1: shared make_galaxy_stub fixture + migrate test_galaxy_cleanup.py
- 2611ce6e1 PROJ-378 phase 2: sweep test_empire + test_fleet_registration_lifecycle to make_galaxy_stub

Files:
- tests/fixtures/galaxy_fixtures.py (NEW, canonical impl)
- tests/unit/strategy/data/conftest.py (NEW, thin pytest fixture bridge)
- tests/unit/strategy/data/test_galaxy_cleanup.py (modified — 3 fixtures migrated)
- tests/integration/strategy/test_empire.py (modified — 5 sites migrated)
- tests/integration/strategy/test_fleet_registration_lifecycle.py (modified — 1 inline factory)

Project docs (read-only reference):
- Projects/active_projects/PROJ-378/plan.md
- Projects/active_projects/PROJ-378/design.md
- Projects/active_projects/PROJ-378/decisions.md

## Instructions

Review the PROJ-378 implementation against its design and the post-PROJ-372 facade. Focus areas:

1. **Fixture correctness** — does `make_galaxy_stub()` in `tests/fixtures/galaxy_fixtures.py` correctly construct a post-PROJ-372 Galaxy facade? Check that `_state`, `_registry`, `_spatial` are wired with the right constructor signatures.

2. **Migration correctness** — verify each migrated fixture/call site preserves the original test semantics. In particular: does the `test_fleet_id_persists_across_save` round-trip via `galaxy2._state.next_fleet_id = saved_counter` actually preserve behaviour vs. the old `galaxy2._next_fleet_id = saved_counter` (route through `_ensure_state` setter)?

3. **Hidden coupling** — the `MagicMock` planet in `TestGalaxyGetAllFleetsInSystem` had to grow `planet.radius_hexes = 0` because post-PROJ-372 `_spatial.get_all_fleets_in_system` reads it. Are there other latent fields the MagicMock will return as a `MagicMock` object that could pass numeric comparisons silently in future test additions?

4. **Layering / convention adherence** — fixtures live at `tests/fixtures/galaxy_fixtures.py` (canonical impl) + a thin `tests/unit/strategy/data/conftest.py` bridge. Is this consistent with the `tests.fixtures.*` convention documented in `tests/fixtures/README.md`? Any unintended cross-tree import issues?

5. **Completeness sweep** — confirm that no `Galaxy.__new__(Galaxy)` or `patch.object(Galaxy, '__init__')` call sites remain in `tests/` outside the canonical implementation in `tests/fixtures/galaxy_fixtures.py`.

6. **Plan vs. implementation drift** — are there any deviations from `Projects/active_projects/PROJ-378/plan.md` / `design.md` / `decisions.md` that should be logged to `decisions.md` but weren't?

7. **Pre-existing failure** — sharded suite reports 1 pre-existing failure: `test_pathfinder_attached_after_init` references `galaxy._intercept` which `Galaxy.__init__` never sets. Confirm it's unrelated to PROJ-378 (PROJ-378 only touched test files; cannot affect production-side galaxy._intercept).

## Context

PROJ-378 is the test-pattern follow-up to PROJ-372 (Galaxy/Planet/Star facade-delegate decomposition). 15 setup-errors in test_galaxy_cleanup.py used the legacy `Galaxy.__new__(Galaxy)` pattern; the very first line `galaxy.radius = 100` invoked the new property setter on a galaxy whose `_state` was never assigned. This project introduces a shared `make_galaxy_stub()` factory and migrates all 3 legacy-pattern test files. Plan, design, decisions all live in Projects/active_projects/PROJ-378/. Joint Codex+Claude review (r004) already approved the plan; this is the implementation review.

## Expected Deliverable

Standard OpenCode review report with CRIT/MAJ/MIN/INFO classification.


**PickedUp:** 2026-05-07T04:26:09Z

## Results

**Status:** completed
**Completed:** 2026-05-07T04:33:14Z
**Report:** Reviews/results/2026-05-07_042609_code_proj-378-galaxy-cleanup-test-pattern-update-post-p_req-req_20260507_042609_bb3683/report.md
**Findings:** 5 total (C: 0, M: 0, m: 2, I: 3)
