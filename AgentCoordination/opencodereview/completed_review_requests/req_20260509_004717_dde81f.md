# Review Request: PROJ-384 — Deprecated *_static methods deletion (167 LOC removed)
**Request ID:** req_20260509_004717_dde81f
**Review Type:** code
**Created:** 2026-05-09T00:47:17Z
**Requester:** claude-code

## Scope

## Scope

Project directory:
- `Projects/active_projects/PROJ-384/`

Single commit: `6398bb1da`.

Production:
- `game/simulation/components/ability_manager.py` (341 → 285 LOC, -56)
- `game/simulation/components/modifier_manager.py` (330 → 219 LOC, -111)

Tests:
- `tests/unit/simulation/components/test_ability_manager.py` (3 test methods migrated to instance API)
- `tests/unit/simulation/components/test_modifier_manager.py` (comment re-attribution only)

Reference:
- `Reviews/results/2026-05-07_220621_legacy-audit/` — source audit verified 0 prod callers, 3 test callers, 1 self-internal reference
- `Projects/active_projects/PROJ-384/findings/verification_report.md`

## Instructions

## Instructions

**Quick verification of a quick-win deletion.** This was a zero-callsite deletion claim; the audit and Sonnet's third pass both verified zero production callers. The agent removed 167 LOC. Verify:

1. **Zero remaining callers anywhere in the repo.** Grep across `game/`, `tests/`, `combat_lab/`, `Tools/` for each of the 12 deleted method names (`get_abilities_static`, `get_ability_static`, `has_ability_static`, `has_pdc_ability_static`, `get_ui_rows_static`, `instantiate_abilities_static`, `add_modifier_static`, `remove_modifier_static`, `get_modifier_static`, `get_all_effects_static`, `get_stat_summary_static`, `remove_modifier_inplace`). If ANY remaining callers exist (production or test), this is CRITICAL — the deletion broke an unmigrated path.

2. **Test migration in `test_ability_manager.py`.** Three test methods were migrated from static to instance API. Verify:
   - The migrated tests still test the same behavior they originally tested (no semantic drift).
   - The tests use the canonical instance access path.
   - Any setup needed for the instance (constructor args, fixture wiring) is correct.

3. **The `remove_modifier_inplace` deletion is special.** It had 1 internal caller inside `add_modifier_static` itself. Both were deleted together. Verify:
   - The internal caller was actually `add_modifier_static` (not some other path the agent missed).
   - No other code expected `remove_modifier_inplace` as a public API.

4. **TYPE_CHECKING import cleanup.** Agent reports removing the now-unused `GameRegistries` TYPE_CHECKING import from `modifier_manager.py`. Verify it's actually unused after the deletion (i.e., not still referenced by a remaining type annotation in the file).

5. **Sibling project PROJ-380 cross-impact.** The agent noted that PROJ-380 Phase 2 was scoped to delete the same `ModifierManager.*_static` methods and is now obsolete. If you see PROJ-380's plan still claiming this work, flag it.

6. **Audit accuracy.** PROJ-380 originally claimed 'preserve `remove_modifier_inplace` if still used internally' (DUP-X-05 was scope-reduced for that reason). PROJ-384 went further and deleted it. Verify PROJ-384 is correct and PROJ-380's preservation note was outdated by the time we got here.

## Severity guidance

- CRITICAL: any remaining caller of any of the 12 deleted methods. This is a regression.
- MAJOR: test migration that drifts from original test intent; semantic loss.
- MINOR: comment cleanup gaps; missed unused imports.
- INFO: PROJ-380 plan staleness for orchestrator follow-up.

## Context

## Context

Fourth of 11 sequential PROJ runs (Stage 2 lead-off, the largest single-PR deletion in the audit). PROJ-381 + PROJ-382 + PROJ-393 all merged ahead of this commit. PROJ-380 (next in queue) was originally scoped to delete the same ModifierManager statics; PROJ-384 supersedes that scope.

The PROJ-384 agent encountered a pre-existing UU merge state from a prior `git stash pop` conflict and stopped without committing. The orchestrator examined the conflicts, confirmed they were either trivial status-line diffs or stale work superseded by HEAD/PROJ-382, and discarded the merge state via `git checkout HEAD -- <files>`. PROJ-384 commit landed cleanly after.

63 focused tests (`pytest tests/ -k 'ability_manager or modifier_manager'`) pass post-deletion.

## Expected Deliverable

Standard review report under `Reviews/results/<date>_code_proj-384-_req-req_<id>/`. Severity-ranked findings tied to file:line. If any of the 12 deleted method names is still referenced anywhere in the repo, label it CRITICAL.


**PickedUp:** 2026-05-09T00:47:18Z

## Results

**Status:** completed
**Completed:** 2026-05-09T00:52:43Z
**Report:** Reviews/results/2026-05-09_004718_code_proj-384-deprecated-static-methods-deletion-167-lo_req-req_20260509_004717_dde81f/report.md
**Findings:** 1 total (C: 0, M: 0, m: 0, I: 1)
