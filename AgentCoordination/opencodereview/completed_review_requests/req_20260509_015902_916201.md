# Review Request: PROJ-380 — Audit-shrink cleanup (3 phases, 11 verified items, 2 audit-claim narrowings)
**Request ID:** req_20260509_015902_916201
**Review Type:** code
**Created:** 2026-05-09T01:59:02Z
**Requester:** claude-code

## Scope

## Scope

Project directory:
- `Projects/active_projects/PROJ-380/` — plan, 3 phase checklists, manifest, design, findings (verification_report has Already-Done + Audit-Narrowing notes)

12 commits on `feat/03c-phase-aware-execution` (post-merge `b00012fcb` + post-PROJ-386 `00e4abac6`).

**Phase 1 — DCV-01 Dead imports (1 commit):**
- `game/ai/controller.py` — removed dead `IControllableShip` TYPE_CHECKING import + updated stale `'IControllableShip'` string annotation to `'ShipControllableAdapter'`.

**Phase 2 — superseded by PROJ-384 (closeout-only commit, no code changes).** PROJ-384 commit `6398bb1da` already deleted all 6 ModifierManager `*_static` methods (-111 LOC). PROJ-380's scope-reduction (preserve `remove_modifier_inplace`) was overtaken by PROJ-384's full audit confirming zero callers.

**Phase 3 — Duplication consolidation (9 tasks, multiple commits):**
- DUP-X-02 (3.1): new shared `game/services/provider_factory.py`; `game/services/llm/factory.py` and `game/ui/services/image/factory.py` delegate.
- DUP-X-06 (3.2): `_distribute_cargo_to_fleet` extracted in `game/strategy/data/fleet_consumable_aggregator.py`.
- DUP-X-08 (3.3): `Camera.hex_at_screen` convenience method added; replaces 5 `pixel_to_hex` sites in `strategy_superweapons.py`, `strategy_click_dispatcher.py`, `strategy_fleet_ops.py`, `strategy_colonization.py`, `strategy_input_handler.py`. 5 test files updated to mock `camera.hex_at_screen` instead of module-level `pixel_to_hex`.
- DUP-X-09 (3.4): `_get_cell_detail` helper extracted in `event_log_data_source.py`.
- DUP-X-10 (3.5): `_format_result_error` extracted in `strategy_fleet_ops.py` (3 result-handling sites).
- DUP-X-01 (3.6): `MissionCommandHandler` template (a `BaseCommandHandler` subclass) consolidates 5 mission handlers in `superweapon_command_handlers.py`.
- DUP-X-07 (3.7) **NARROWED**: `_cancel_input_mode` helper for the right-click cancel block only in `strategy_click_dispatcher.py`. The wider audit claim (left+right click base for 9 handlers) was rejected — left-click bodies diverge too much (move/intercept vs colonize vs cargo vs warp vs superweapon).
- DUP-X-12 (3.8) **NARROWED**: `_iter_ability_sources` extracted in `ability_iterator.py` for **3** providers (not the audit's claimed 7 — only 3 share the right shape).
- DUP-X-11 (3.9): base serialization in `battle_end_conditions.py` for 9 condition subclasses.

**Test changes:** 5 test files for the 3.3 `pixel_to_hex` → `camera.hex_at_screen` migration. No tests deleted — only mocks rewritten.

Reference:
- `Reviews/results/2026-05-07_220215_audit_shrink/` — source audit
- `Projects/active_projects/PROJ-380/findings/verification_report.md`

## Instructions

## Instructions

**This is a 9-task consolidation refactor — review for two failure modes: (a) introducing more abstraction than was removed, (b) papering over real differences with a template that doesn't fit.**

1. **DUP-X-02 ProviderFactory consolidation.** New shared `game/services/provider_factory.py`. Verify:
   - Both consumers (`llm/factory.py`, `image/factory.py`) genuinely delegate, not just rename their internal class.
   - The shared base captures the actual common behavior, not just type hints.
   - The shared module doesn't introduce a new layer violation (e.g., `services/provider_factory.py` shouldn't import from `ui/`).

2. **DUP-X-08 `Camera.hex_at_screen`.** 5 callers migrated. Verify:
   - The new method has the same coordinate semantics as the inline `pixel_to_hex(camera_x, ...)` it replaces.
   - No caller passes camera state that the new method can't reconstruct (e.g., zoom, pan offset).
   - The 5 test files mock `camera.hex_at_screen` instead of `pixel_to_hex` — verify they cover the same input/output assertions.

3. **DUP-X-01 `MissionCommandHandler` template.** Consolidates 5 mission handlers. The agent claims it doesn't conflict with PROJ-383's command_handlers shim deletion (which targets a sibling `command_handlers.py`, not `superweapon_command_handlers.py`). Verify:
   - The template is a `BaseCommandHandler` subclass, not a new shim or wrapper.
   - All 5 mission handlers actually share the consolidated shape (no parameter shoehorning).
   - The template's hooks/abstract methods are minimal, not over-engineered.

4. **DUP-X-07 narrowing claim.** Agent narrowed from 9 handlers to right-click only, claiming left-click bodies diverge too much. Independently verify:
   - List the 9 click-handler entry points in `strategy_click_dispatcher.py`.
   - For each, summarize what they do.
   - Is the agent's narrowing call correct? (If 3-4 left-click handlers DO share enough, the narrowing was over-conservative — that's MAJOR.)

5. **DUP-X-12 narrowing claim.** Agent narrowed from 7 ability providers to 3, claiming the audit was wrong. Verify:
   - List the ability-source providers in `game/strategy/services/ability_sources/`.
   - For each, summarize the iteration shape.
   - Did the agent correctly identify only 3 share the right shape, or were 4-5 plausibly consolidatable?

6. **DUP-X-11 base serialization for 9 BattleEndCondition subclasses.** Verify:
   - The base really captures common to_dict/from_dict logic without losing subclass-specific fields.
   - All 9 subclasses inherit cleanly (no broken signatures).
   - Round-trip tests still pass for each subclass.

7. **CLAUDE.md Rule 3 compliance.** None of these consolidations should introduce a shim. Specifically: `_distribute_cargo_to_fleet`, `_get_cell_detail`, `_format_result_error`, `_iter_ability_sources` — these are private helpers, but verify they don't backfill behavior the audit didn't explicitly call for (e.g., a 'fallback path' in the helper that's actually a re-export-shim in disguise).

8. **Phase 2 superseded marker.** PROJ-380 Phase 2 was marked Complete (superseded) without code changes. Verify: `grep -n '_static' game/simulation/components/modifier_manager.py` returns 0 hits. If 1 remains, PROJ-384 didn't actually finish and PROJ-380's marker is wrong.

## Severity guidance

- CRITICAL: a consolidation breaks subclass behavior; new shim introduced; PROJ-384's deletion didn't actually finish (Phase 2 marker is wrong).
- MAJOR: narrowing call (3.7 or 3.8) was over-conservative — leftover dup-pairs that should have been consolidated; ProviderFactory base captures only types not behavior; Camera.hex_at_screen has different semantics than inline.
- MINOR: test mocks subtly different from original; private helper has surprise behavior; comment polish.
- INFO: future-cleanup follow-ups outside scope.

## Context

## Context

Sixth of 11 sequential PROJ runs. Stage 2 third project. PROJ-380 was the largest cleanup of the bunch: 11 verified items + Phase 2 obsoletion handling + 9 Phase 3 consolidations. The agent landed all phases without deferral but **narrowed two audit claims on merit** (DUP-X-07 from 9 handlers to right-click only; DUP-X-12 from 7 providers to 3).

The two narrowings are the highest-value review targets — if the agent narrowed too aggressively, you'll see leftover dup-pairs. If the audit's broader claims were genuinely wrong, the narrowings are the right call and the audit's classifier needs sharpening (refinement-feedback channel).

Pre-existing test failure noted: `tests/integration/strategy/test_save_round_trip_phase4.py::test_pathfinder_attached_after_init` (`Galaxy._intercept` missing) — unrelated, present on `main`.

## Expected Deliverable

Standard review report. Severity-ranked findings tied to file:line. Special attention to (a) the two narrowing decisions (DUP-X-07, DUP-X-12) — were they correct?, (b) the new `Camera.hex_at_screen` semantics, and (c) the `MissionCommandHandler` template's fitness across 5 mission handlers.


**PickedUp:** 2026-05-09T01:59:04Z

## Results

**Status:** completed
**Completed:** 2026-05-09T02:10:51Z
**Report:** Reviews/results/2026-05-09_015904_code_proj-380-audit-shrink-cleanup-3-phases-11-verified_req-req_20260509_015902_916201/report.md
**Findings:** 43 total (C: 0, M: 5, m: 8, I: 30)
