# Review Request: PROJ-393 — Test-injection legacy fallbacks + comment cleanups (3 phases, 3 deferrals)
**Request ID:** req_20260509_002246_bca19e
**Review Type:** code
**Created:** 2026-05-09T00:22:46Z
**Requester:** claude-code

## Scope

## Scope

Project directory:
- `Projects/active_projects/PROJ-393/` — plan, 3 phase checklists, manifest, design, findings (verification_report has Deferred During Implementation section)

5 commits on `feat/03c-phase-aware-execution` (a23948b9d → 9321b0692).

**Phase 1 — comment-only deletions (4 items, 1 commit):**
- `game/simulation/combat/formation.py` (LEG-03-002 — legacy snap comment)
- `game/strategy/combat/spec_compiler.py` (LEG-03-003 — legacy `EnvironmentalEffects` comment)
- `game/strategy/systems/save_game_service.py` (LEG-02-005 — historical `# legacy` comment)
- `game/context.py` (LEG-02-017 — stale `# PROJ-258` docstring tag)

**Phase 2 — test-injection legacy fallbacks (5 items, 2 commits):**
- `game/run_loop.py`, `game/ui/research/research_scene.py`, `game/ui/screens/galaxy_test/screen.py` (LEG-02-002 — IScene event handling)
- `game/strategy/validation/planet_order_validator.py` (LEG-03-004 + LEG-03-005 — activate + deactivate `ability_name` fallbacks deleted)
- `game/ui/panels/build_queue_drag_handler.py` (LEG-03-006 — test-fallback when callback None)
- `game/ui/screens/empire_build_queue_window.py` (LEG-03-007 — test-fallback when facade None) — **note: PROJ-382 had already made facade required; PROJ-393 deleted dead branch and wired test fixture's fake-facade**

**Phase 3 — backward-compat fields + misc (4 fully done, 1 partial, 2 deferred):**
- `game/strategy/engine/planet_action_engine.py` (LEG-02-003 — `'PlanetaryShield'` hardcoded fallback deleted)
- `game/strategy/engine/commands/__init__.py` (LEG-02-004 — **PARTIAL: only the misleading 'Kept for backward compat' tag removed**; full `fleet_id` field deletion DEFERRED — no `entity_id`/`entity_type` field exists yet, ~20 callers use `fleet_id`)
- `game/ui/screens/build_queue_helpers.py`, `game/ui/screens/strategy_ui.py` (LEG-02-013 — module-level `ResourceCatalog.from_json` → lazy init)
- `game/ui/renderer/sprites.py` (LEG-03-024 — `_LEGACY_PATTERN` deleted; asset scan confirmed zero matches)
- `game/strategy/engine/order_handlers/transfer_branches.py` (LEG-04-004 — `populations[0]` Legacy/Default fallback)

**DEFERRED in `findings/verification_report.md`:**
- LEG-02-006 (`view=None` branch): `PlanetSelectionWindow` lacks facade access; threading separate scope
- LEG-03-023 (BattleScreen Combat Lab vars): audit misread `# NOQA: legacy-retained` — vars are actively used by run_loop, test_lab/screen, battle_screen itself
- LEG-02-004 (`fleet_id` field full deletion): needs sibling project to design `entity_id`/`entity_type`

PLUS 4 pre-existing pytest-collection import-mismatch errors flagged as orthogonal to PROJ-393.

## Instructions

## Instructions

**Primary focus areas:**

1. **Phase 2 test-fallback deletions — was the test-side audit thorough?** Each Phase 2 task required confirming every test injects the canonical dep BEFORE deleting the production fallback. Verify:
   - For LEG-03-006 (`build_queue_drag_handler`): grep `tests/` for `BuildQueueDragHandler(` instantiations and confirm every one passes `_on_remove_from_queue=...`. Are there any tests that still trigger the deleted fallback path (now an unreachable branch or a missing-arg error)?
   - For LEG-03-007 (`empire_build_queue_window`): the agent claims PROJ-382 already made facade required. Confirm via grep — and verify the 'wired the test fixture's fake-facade' is a clean fixture vs an inline mock that bypasses the facade contract.
   - For LEG-03-004/005 (`planet_order_validator` activate/deactivate fallbacks): confirm no caller passes `component_key=None` anywhere in the repo.

2. **Phase 3 Task 3.2 (`fleet_id` field) — was the partial fix the right call?** The agent removed only the misleading 'Kept for backward compat' tag and deferred the full deletion because no `entity_id`/`entity_type` exists yet. Verify:
   - Is the partial fix actually a no-op cosmetic change, or does it introduce confusion (a field that USED to be backward-compat now appears to be the real field)?
   - Should the partial be reverted in favor of leaving the tag until the sibling project is ready?
   - The deferral logic claims '~20 callers use `fleet_id`' — spot-check that count.

3. **Phase 3 Task 3.5 deferral (BattleScreen Combat Lab vars) — was the audit really wrong?** The agent claims the audit misread `# NOQA: legacy-retained` and the vars are 'actively used by run_loop, test_lab/screen, battle_screen itself.' Verify:
   - Are those usages legitimate runtime dependencies, or are they scaffolding from an old test mode that should also be removed?
   - The original PROJ-270 closeout docs claimed these vars were 'kept for back-compat with older visual test scenarios' — has that need actually expired?
   - This is a flag for the legacy-audit refinement-feedback channel.

4. **Phase 3 Task 3.6 asset scan — methodology check.** The agent claims 'zero matches under `assets/` for filenames whose basename starts with `Comp_NNN.ext`.' Confirm:
   - The grep pattern actually matched the regex `_LEGACY_PATTERN = re.compile(r'Comp_(\d+)\.\w+$')`.
   - The scan covered ALL asset directories (`assets/`, `data/assets/`, etc.) and not just `assets/`.

5. **`game/run_loop.py` IScene migration (LEG-02-002).** The two scenes (`RESEARCH_TREE`, `GALAXY_TEST`) were migrated to `IScene.handle_event`. Verify:
   - Both scenes now actually implement the handler interface (not just delete the legacy branch).
   - The previously-existing `handle_input` paths produce equivalent behavior under the new event dispatch.
   - No event types are silently dropped.

6. **CLAUDE.md Rule 3 compliance.** None of these deferrals should be a 'compatibility shim' in disguise. Verify:
   - The partial Task 3.2 (tag removed but field kept) — is keeping the field justified by the lack of a replacement, or is it a Rule 3 violation that the verification report rationalized?
   - The Task 3.5 deferral — is the audit's misread genuine, or is it a Rule 3 violation justified post-hoc?

7. **The 4 pre-existing pytest-collection errors.** Spot-check 1-2 to confirm they really are orthogonal — i.e., they fail on `main` for the same reason. Don't take the agent's word.

## Severity guidance

- CRITICAL: deferrals that are actually Rule 3 violations rationalized post-hoc; partial Task 3.2 introduces production confusion; missed test caller now broken.
- MAJOR: IScene migration drops events; asset scan methodology gap; Phase 2 test-side audit incomplete (some tests still need the deleted fallback).
- MINOR: comment cleanups missed in spirit (e.g., other stale tags nearby that should also have gone); pre-existing-error mis-attribution.
- INFO: improvements for the legacy-audit source skill's classification heuristics (e.g., NOQA comment misread).

## Context

## Context

Third of 11 sequential PROJ runs. PROJ-381 review in flight (`req_20260508_230317_779973`). PROJ-382 review in flight (`req_20260508_235748_8c0ea0`). PROJ-393 closes Stage 1 (the 3 hub projects with the most cross-project conflicts).

**Cross-project notes from the implementing agent:**
- LEG-03-007 (Phase 2 Task 2.5) was already partially handled by PROJ-382 Phase 1 (which made `facade` required on `EmpireBuildQueueWindow`). PROJ-393 deleted the now-dead fallback branch and wired the test fixture.
- The `# PROJ-258` cleanup in `context.py` (Phase 1) had two cousins flagged by strict grep — agent retained those as legitimate architectural references and documented in `findings/verification_report.md`.
- 3 of 16 items in this catch-all bundle ended up deferred. That's a meaningful deferral rate for what was billed as a quick-win cleanup project. Understanding why is part of this review's value.

## Expected Deliverable

Standard review report under `Reviews/results/<date>_code_proj-393-test-injection-fallbacks-_req-req_<id>/`. Severity-ranked findings tied to file:line. Special attention to (1) whether Phase 2 deletions broke any test path, (2) whether Task 3.2's partial fix is a Rule 3 violation in disguise, and (3) whether the 3 deferrals are honest scope acknowledgments or rationalized retreats.


**PickedUp:** 2026-05-09T00:22:47Z

## Results

**Status:** completed
**Completed:** 2026-05-09T00:41:27Z
**Report:** Reviews/results/2026-05-09_002247_code_proj-393-test-injection-legacy-fallbacks-comment-c_req-req_20260509_002246_bca19e/report.md
**Findings:** 19 total (C: 1, M: 6, m: 8, I: 4)
