# Review Request: PROJ-391 — Three small consolidations (harvester info, iter_components, FormationSpec serialization)
**Request ID:** req_20260509_024407_344a83
**Review Type:** code
**Created:** 2026-05-09T02:44:07Z
**Requester:** claude-code

## Scope

## Scope

Project directory: `Projects/active_projects/PROJ-391/`

4 commits on `feat/03c-phase-aware-execution`.

**Task 1.1 — `_get_harvester_info` → `get_harvester_info`:**
- `game/strategy/services/planet_economy_projector.py` — local `_get_harvester_info` deleted; `compute_planet_production` migrated to `iter_components` + canonical `get_harvester_info`. Cross-system secondary site (manual layer iteration at lines 220-231) migrated under same task.
- `tests/unit/ui/panels/test_planet_report_panel.py`, `tests/unit/ui/screens/test_strategy_detail_formatter.py` — test mocks updated.

**Task 1.2 — `_iter_components` → `iter_components`:**
- `game/ui/screens/battle_setup/spec_compiler.py` — local `_iter_components` deleted; `isinstance(component_data, dict)` guard added at call site (canonical yields strings + dicts; legacy yielded dicts only).
- `tests/unit/ui/screens/battle_setup/test_spec_compiler.py`, `tests/unit/simulation/combat/test_ability_stat_registry.py` — test/comment updates.

**Task 1.3 — `_formation_to_dict/from_dict` → `FormationSpec` (Pattern 17):**
- `game/simulation/combat/formation.py` — `FormationSpec.to_dict(self) -> dict` and `@classmethod from_dict(cls, data: dict) -> FormationSpec` added per Pattern 17 (Serializable Protocol).
- `game/strategy/data/task_force.py` — duplicate helpers DELETED, callers migrated to `FormationSpec.to_dict/from_dict`.
- `game/simulation/replay/replay_serialization.py` — duplicate helpers DELETED, callers migrated. Replay-layer fallback for non-FormationSpec inputs (which produced a synthetic LINE_ASTERN/spacing=0 placeholder) was DROPPED — replay wrapper now stores `None` for that vestigial slot.
- `tests/unit/simulation/replay/test_serialization.py` — 1 test rewritten.

**Pattern 17 reconciliation:** task_force.py used `[float(p.x), float(p.y)]`; replay_serialization.py used `_vec_to_list(p)` (which is `[float(v.x), float(v.y)]`). Byte-identical output. Canonical `FormationSpec.to_dict()` adopts the explicit-list form so the type stays self-contained.

Reference:
- `Reviews/results/2026-05-07_220621_legacy-audit/` (cross-system pair findings)
- `docs/02_PATTERNS.md` Pattern #17 Serializable Protocol

## Instructions

## Instructions

**Three small consolidations — verify each is semantically equivalent and that the deleted helpers don't have hidden callers.**

1. **Final grep verification (per-symbol).**
   - `git grep '_get_harvester_info' game/ tests/` — should be ZERO production hits. Acceptable: test-local aliases (`from ... import get_harvester_info as _get_harvester_info`).
   - `git grep '_iter_components' game/ tests/` — same. Acceptable: unrelated test-local helper in `test_dual_scope_validation.py` (loads JSON, different purpose); `test_iter_components_*` test method names.
   - `git grep '_formation_to_dict' game/ tests/`, `git grep '_formation_from_dict' game/ tests/` — should be ZERO hits anywhere.
   - If any production reference remains, that's CRITICAL.

2. **`get_harvester_info` return-type widening.** Canonical returns `dict | list | None`; legacy returned `Optional[dict]`. Agent says caller normalizes to a list of entries. Verify:
   - The migrated `compute_planet_production` correctly handles all 3 return cases (dict, list, None).
   - Production data shape: is `ResourceHarvester` always a single dict in practice, making the list-handling defensive code dead? If yes, that's a MINOR — defensive code that never executes.

3. **`iter_components` semantic change.** Canonical handles list + dict layer formats AND yields strings as well as dicts. Legacy `_iter_components` only yielded dicts from list-format. The migration adds an `isinstance(component_data, dict)` guard at the call site. Verify:
   - Strings are correctly skipped (or handled, if appropriate).
   - The cross-system secondary site at `planet_economy_projector.py:220-231` was also migrated.

4. **`FormationSpec.to_dict/from_dict` semantic preservation.** The two original implementations diverged in subtle ways:
   - `task_force.py` direction: `[float(p.x), float(p.y)]`
   - `replay_serialization.py` direction: `_vec_to_list(p)` which equals `[float(v.x), float(v.y)]`
   Agent claims byte-identical. Verify by reading both pre-migration impls (from git history at parent commit) and confirming.

5. **Replay wrapper's vestigial slot.** Agent dropped the synthetic LINE_ASTERN/spacing=0 placeholder in `replay_serialization.py` for non-FormationSpec inputs. Verify:
   - No replay caller actually relies on that placeholder.
   - The new `None` slot doesn't break replay deserialization.

6. **Pattern 17 conformance.** `FormationSpec.to_dict(self) -> dict` and `@classmethod from_dict(cls, data: dict) -> FormationSpec`. Verify:
   - Method signatures match Pattern 17 exactly.
   - Round-trip works: `FormationSpec.from_dict(spec.to_dict()) == spec`.
   - The `FormationResolver.DEFAULT_SPACING` lazy reference inside `from_dict` (resolved at call time, not import time) — verify no circular-import surprise.

7. **CLAUDE.md Rule 3 compliance.** No replacement shim. The 4 deleted helpers are gone, full stop.

## Severity guidance

- CRITICAL: any production reference to deleted helpers; semantic break in `compute_planet_production` from return-type change; replay deserialization broken by the dropped vestigial slot.
- MAJOR: defensive list-handling that never executes (dead code); replay caller relies on dropped placeholder; Pattern 17 signature drift.
- MINOR: `iter_components` strings handling subtly different; comment polish.
- INFO: cleanup follow-up.

## Context

## Context

Tenth of 11 sequential PROJ runs. Stage 3 third project.

The agent ran a partial sharded suite at the end and got 19733 passed / 3 pre-existing failures + 2 pre-existing errors. The 3 failures (testcoverage_audit, scalene_profiling_workflow, save_round_trip_phase4) and 2 collection errors are all pre-existing — confirms my Stage 2 closeout fixes (transfer_order siblings + colonization_facade) actually worked.

Last remaining Stage 3 project is PROJ-392 (misc orphan wrappers).

## Expected Deliverable

Standard review report. Three small consolidations — should be a quick review. Headline question: was the FormationSpec Pattern 17 migration semantic-preserving?


**PickedUp:** 2026-05-09T02:44:08Z

## Results

**Status:** completed
**Completed:** 2026-05-09T02:51:44Z
**Report:** Reviews/results/2026-05-09_024408_code_proj-391-three-small-consolidations-harvester-info_req-req_20260509_024407_344a83/report.md
**Findings:** 1 total (C: 0, M: 0, m: 1, I: 0)
