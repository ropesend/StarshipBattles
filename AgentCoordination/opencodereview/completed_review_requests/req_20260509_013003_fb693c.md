# Review Request: PROJ-386 — Save-format migration eradication (4 LEG findings, banned by CLAUDE.md Rule 3)
**Request ID:** req_20260509_013003_fb693c
**Review Type:** code
**Created:** 2026-05-09T01:30:03Z
**Requester:** claude-code

## Scope

## Scope

Project directory:
- `Projects/active_projects/PROJ-386/`

Single commit: `00e4abac6` (sits on top of merge `b00012fcb`).

Production (4 files):
- `game/strategy/data/component_activation_state.py` — LEG-03-017: deleted `{'active': bool}` legacy-format branch in `from_dict` (lines 144-149). Now requires the canonical `phase` field.
- `game/strategy/data/ship_instance_serializer.py` — LEG-03-018: deleted silent-ignore for legacy `component_damage` + graceful-degrade for missing `components` (lines 100-102, 127-138). Made `to_dict` always emit `components` so round-trip is symmetric.
- `game/ui/screens/battle_setup/controller.py` — LEG-03-008: deleted `_complex_toggles` legacy migration in `_load_from_path` (lines 548-568).
- `game/ui/screens/battle_setup_state.py` — LEG-04-005: deleted `side_0`/`side_1` legacy emit + read (lines 257-300). Now only N-side `sides` list format.

Tests:
- DELETED 5 test classes/methods that targeted only the removed legacy paths:
  - `TestSaveLoadLegacyMigration` in `tests/unit/ui/screens/battle_setup/test_controller.py`
  - `test_backward_compat_active_*`, `test_backward_compat_empty_dict` in `tests/unit/strategy/data/test_component_activation_state.py`
  - `test_backward_compat_active_*`, `test_backward_compat_old_format` in `tests/unit/strategy/data/test_facility_activation.py`
  - `test_from_dict_ignores_legacy_component_damage_key` in `tests/unit/strategy/ship_instance/test_ship_instance_serializer.py`
  - `test_ship_instance_legacy_save_without_components_defaults_empty` in `tests/unit/strategy/fleets/test_ship_instance_components.py`
- REWROTE 14 test fixtures across 11 test files to feed new-format data (added `'components': {}` where serializers now require it). Files: `tests/unit/strategy/ship_instance/test_serialization.py`, `test_registries_di.py`, `test_validation.py`, `test_ship_instance_serializer.py`; `tests/unit/strategy/data/test_design_role.py`; `tests/unit/strategy/test_ship_serial_numbering.py`; `tests/unit/strategy/fleet/test_serialization.py`, `test_fleet_validation.py`; `tests/unit/strategy/test_fleet_capability_calculator_di.py`; `tests/integration/strategy/production/test_fleet_save_load.py`
- Renamed `test_role_absent_from_old_saves` → `test_role_absent_from_save_dict` (no longer about 'old' saves).

Reference:
- `Reviews/results/2026-05-07_220621_legacy-audit/`
- `Projects/active_projects/PROJ-386/findings/verification_report.md`
- CLAUDE.md Rule 3: 'No save-file migration. Old saves are disposable.'

## Instructions

## Instructions

**Validation focus: did 'eradicate' actually eradicate, or did it leave hidden fallback paths?**

1. **Grep verification of complete deletion.** For each deleted symbol/branch, confirm zero remaining references in production:
   - `_complex_toggles` should appear 0 times in `game/` (was lines 548-568 in `controller.py`).
   - `'active'` as a key in component-activation old-format paths — search for any remaining `data.get('active', ...)` or `if 'active' in data` in `component_activation_state.py` and any code path that constructs `ComponentActivationState`.
   - `component_damage` — should appear 0 times in `ship_instance_serializer.py`'s `from_dict` body (and ideally 0 times in production overall, except in deleted-test references).
   - `side_0` / `side_1` as save-format keys — should appear 0 times in `battle_setup_state.py`. (They may legitimately appear as in-memory property names — that's outside scope per the agent's note.)
   - If any of these still exist in production, that's CRITICAL.

2. **`to_dict` round-trip symmetry.** PROJ-386 made `ship_instance_serializer.to_dict` always emit the `components` key, claiming this is necessary for round-trip with the new `from_dict` contract. Verify:
   - The `to_dict` change really emits the key for both empty and non-empty cases.
   - No caller depends on the omit-when-empty behavior.
   - The change doesn't break any test asserting the old shape (those should have been migrated; spot-check 2 of the 14 rewritten tests).

3. **Test deletions: were they really only-legacy tests, or did they cover real behavior?** The 5 deleted test methods/classes claimed to test 'only the legacy fallback.' For each:
   - Read the deleted test (from git history, commit `00e4abac6`'s parent `b00012fcb`).
   - Confirm the test's assertions only made sense under the deprecated contract.
   - If a deleted test was actually exercising real behavior incidentally relying on the legacy fallback, it should have been REWRITTEN, not deleted. That's a coverage regression.

4. **Test rewrites: are they semantic-preserving?** The 14 rewritten test fixtures added `'components': {}` to feed the new contract. Verify:
   - The fixture changes don't accidentally test a different code path than the original.
   - The original test names still describe what's tested (e.g., `test_role_absent_from_save_dict` rename is justified).

5. **CLAUDE.md Rule 3 strict adherence.** No `if 'version' in data` gates. No `# DEPRECATED, will remove later` comments. No fallback-to-default for missing keys when the key is now required. The agent claimed the deserializers raise on legacy format — verify this is actually the failure mode (not silent return-None or graceful-skip).

6. **Cross-impact with the merged PROJ-388 work.** The parallel branch landed PROJ-388 (ModifierLogic class deletion) in commit `37d79879d`. PROJ-386 doesn't touch the modifier-logic files directly, but cross-check that PROJ-386's component_activation/serialization changes don't create surprise interactions with the now-absent `ModifierLogic` consumers.

7. **The `to_dict` symmetry claim deserves scrutiny.** Original was `if ship.components: dict[...] = ...` — emit-on-non-empty. Now it always emits. The agent claims this is 'symmetric with new from_dict.' But `from_dict` still accepts empty dict gracefully (just yields empty components). Is the always-emit change actually required for symmetry, or is it a nice-to-have that introduces a tiny semantic shift the audit didn't ask for?

## Severity guidance

- CRITICAL: any of the 4 deleted symbols/branches still has a production reference; deserializer silently accepts legacy format somewhere; Rule 3 violation rationalized post-hoc.
- MAJOR: deleted test was actually covering real behavior (coverage regression); rewritten test exercises a different code path than original.
- MINOR: `to_dict` always-emit change introduces semantic shift the audit didn't ask for; comment polish.
- INFO: opportunities for follow-up.

## Context

## Context

Fifth of 11 sequential PROJ runs. Stage 2 second project. Sits on top of merge `b00012fcb` which brought in parallel work — PROJ-385 (formula_evaluator aliases), PROJ-387 (Galaxy backward-compat forwarders), PROJ-388 (ModifierLogic class deletion) — all reviewed and approved on the parallel branch. PROJ-394 was also created on the parallel branch (orchestrator hasn't inspected it yet).

The merge had 1 conflict on `fleet_navigation_service.py` resolved by orchestrator: the auto-merge inlined `_resolve_warp_exit` (regressing PROJ-382's delegation pattern) and only applied PROJ-387's `galaxy.state.global_hex_warp_points` fix at the old location. Orchestrator restored PROJ-382's delegation in `fleet_navigation_service.py` and applied PROJ-387's fix at the new location in `fleet_warp_resolution.py:65`.

PROJ-386 itself is policy-non-negotiable per CLAUDE.md Rule 3 — no fallback, no version-gate. The agent reported all 4 LEG findings closed cleanly with 93 focused tests passing.

## Expected Deliverable

Standard review report under `Reviews/results/<date>_code_proj-386-_req-req_<id>/`. Severity-ranked findings tied to file:line. Pay particular attention to (1) Rule 3 strict adherence — any rationalized fallback is CRITICAL, (2) whether deleted tests had been covering real behavior, and (3) the `to_dict` always-emit semantic shift.


**PickedUp:** 2026-05-09T01:30:04Z

## Results

**Status:** completed
**Completed:** 2026-05-09T01:39:03Z
**Report:** Reviews/results/2026-05-09_013004_code_proj-386-save-format-migration-eradication-4-leg-f_req-req_20260509_013003_fb693c/report.md
**Findings:** 6 total (C: 0, M: 0, m: 2, I: 4)
