# Review Request: PROJ-385 — formula_evaluator alias removal verification
**Request ID:** req_20260508_225305_4c9ad3
**Review Type:** code
**Created:** 2026-05-08T22:53:05Z
**Requester:** claude-code

## Scope

Changes on branch `feat/03c-phase-aware-execution` for PROJ-385:

- `game/core/formula_evaluator.py` (deleted backward-compat aliases at lines 407-413; minor docstring update around line 207)
- `tests/unit/simulation/test_formula_exceptions.py`
- `tests/unit/systems/test_formula_overflow_underflow.py`
- `tests/unit/systems/test_formula_system.py`

Project docs: `Projects/active_projects/PROJ-385/plan.md`, `phase_1_checklist.md`, `manifest.md`.

## Instructions

PROJ-385 removed three module-level backward-compat aliases (`evaluate_math_formula`, `safe_evaluate_math_formula`, `validate_formula`) from `game/core/formula_evaluator.py` and migrated ~23 test import sites / ~118 invocations across 3 test files to call `FormulaEvaluator.*` directly.

Verify:
1. **Completeness of removal** — `grep -rn -E "(evaluate_math_formula|safe_evaluate_math_formula|validate_formula)\b" .` should return only legitimate hits: (a) a different class `ModifierEffectEvaluator.validate_formula` method (NOT the deleted alias), (b) historical/archived docs, (c) PROJ-385's own tracking files. Confirm no live caller still uses the removed names.
2. **Migration correctness** — sample several call sites in each migrated test file. Confirm semantics are preserved: `evaluate_math_formula(x)` ↔ `FormulaEvaluator.evaluate(x)`, `safe_evaluate_math_formula(x)` ↔ `FormulaEvaluator.safe_evaluate(x)`, `validate_formula(x)` ↔ `FormulaEvaluator.validate(x)`. Watch for argument-order subtleties.
3. **Test integrity** — were any tests weakened, skipped, or had assertions softened during migration? Diff the call patterns; the only change should be the API surface.
4. **Compat-shim hygiene** — was anything new introduced as a compat wrapper, helper, or fallback? The change should be a clean deletion + caller migration with NO new shims (Rule 3).
5. **Scope discipline** — did the change touch anything outside the alias removal + test migration (no production behavior changes, no unrelated refactors)?
6. **`game/core/formula_evaluator.py` size and structure** — file should be cleaner; flag any dead code now exposed by the removal.
7. **Pre-existing failures** — sharded suite shows 3 failures (`test_scalene_workflow_files_are_documented`, `test_skill_does_not_claim_coverage_json_is_supported`, `test_pathfinder_attached_after_init`). Briefly confirm these are unrelated to PROJ-385 (none reference the formula module). If they ARE related, that's a critical finding.

Deliver findings with severity (CRIT / MAJ / MIN / INFO) and exact file:line references.

## Context

PROJ-385 is one of three sibling legacy-removal projects (385, 387, 388) being executed in series tonight. All three target backward-compat shims flagged by audit `Reviews/results/2026-05-07_220621_legacy-audit/`. Repo rule (CLAUDE.md Rule 3): no compat shims, no fallback systems — when a system is replaced, remove the old path and migrate all callers.

## Expected Deliverable

Standard review report under `Reviews/results/<date>_proj385_<...>/` with findings sectioned by severity and a recommendation: APPROVE / REQUEST_CHANGES.


**PickedUp:** 2026-05-08T22:53:05Z

## Results

**Status:** completed
**Completed:** 2026-05-08T22:58:06Z
**Report:** Reviews/results/2026-05-08_225305_code_proj-385-formula-evaluator-alias-removal-verificat_req-req_20260508_225305_4c9ad3/report.md
**Findings:** 7 total (C: 0, M: 0, m: 0, I: 7)
