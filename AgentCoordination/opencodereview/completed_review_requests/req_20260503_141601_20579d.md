# Review Request: Phase-aware project execution with cumulative delegated reviews (PROJ-XX phase DAG)
**Request ID:** req_20260503_141601_20579d
**Review Type:** plan
**Created:** 2026-05-03T14:16:01Z
**Requester:** claude-code

## Scope

## Scope

**Primary plan under review:**
- `AgentCoordination/Scratchpad/plans/2026-05-03_phase_review_dag_plan.md`

**Files the plan modifies or extends (read for context, not under review themselves):**
- `Projects/protocols/03a_continue_working.md` — current sequential single-branch execution protocol; the plan adds 03c as an envelope around it.
- `Projects/protocols/03b_parallel_projects.md` — sibling protocol for inter-project parallelism; the plan's 03c document must mirror its structural conventions.
- `.claude/skills/claude-delegate-review/SKILL.md` — the review skill the plan relies on; confirm the plan uses it correctly (cumulative scope, no follow-ups, payload path conventions).
- `.claude/skills/claude-proj-start/SKILL.md` — gets the new `Depends on:` template field and `findings_ledger.md` stub.
- `.claude/skills/claude-proj-continue/SKILL.md` — gets project-branch creation, `phase_complete.py` integration, lookback hook.
- `.claude/skills/claude-proj-parallel/SKILL.md` — should cross-reference 03c.
- `.claude/skills/claude-proj-audit/SKILL.md` — its scope shrinks once cumulative reviews exist.
- `Projects/scripts/validate_phase.py` — wrapped (not replaced) by the new `phase_complete.py`.
- `Tools/agent_coordination/create_review_request.py` — used as-is by the new flow.
- `Tools/agent_coordination/review_daemon.py` — used as-is.
- `Projects/active_projects/PROJ-300/` (representative example layout — phase_*_checklist.md files, manifest.md, etc.) — the plan's metadata changes apply to NEW projects, not these.
- `CLAUDE.md` and `AGENTS.md` — convention/Scratchpad rules the plan must comply with.
- `docs/03_CONVENTIONS.md` — branch naming convention extension (`proj/PROJ-XX`, `proj/PROJ-XX/phase-N`).

**Out of scope:**
- Implementation code — none has been written yet. This is a plan review only.
- Existing 17 active projects under `Projects/active_projects/` — explicitly NOT migrating; they continue under legacy 03a.

## Instructions

## Instructions

This is a **plan review**, not a code review. The plan proposes a new project-execution layer that adds phase dependency tracking, cumulative OpenCode reviews at phase boundaries, intra-project worktree parallelism, and a project-branch lifecycle. No implementation has started.

Please evaluate:

### Correctness
1. Are the eligibility rules in §F (single-parent depth-1 buffer; multi-parent wait-for-all-verified) consistent across the document and free of edge-case holes? Walk through specific DAG shapes — chains, fan-outs, fan-ins, diamonds — and check whether the rules give a sensible answer in each.
2. Does the cumulative-review approach (§E) actually replace the need for follow-up reviews, or are there scenarios where a finding could persist without ever being explicitly verified as resolved?
3. The Findings Ledger (§Pushback #2) is the mechanism for `acknowledged-deferred` findings. Is its design sufficient to prevent reviewer noise without becoming a vehicle for hiding real issues?
4. The scrap-and-restart escape hatch (§G + §Pushback #4): are the safety preconditions sufficient? Specifically, the cascade rule: pause descendant worktrees by default, scrap with `--cascade`. Anything missing?

### Compatibility with existing systems
5. The plan keeps planning artifacts on `main` and execution work on `proj/PROJ-XX` (§Pushback #3). Does this hybrid create any awkward states — e.g., two different versions of `plan.md` (the on-main initial version and the on-branch updated version)? Suggest fixes if so.
6. Protocol 03b runs multiple projects in parallel, each in its own worktree off main. The plan claims 03c nests cleanly inside an 03b worktree. Is this actually true, or does the project-branch + phase-worktree model conflict with how 03b uses worktrees?
7. Does the plan respect `CLAUDE.md`'s scratchpad rule (`AgentCoordination/Scratchpad/`)? The plan mentions `.agent_reports/proj-phase-session/{PROJ-ID}` for session state; is that the right location, or should worker-context files live under Scratchpad?

### Documentation conformance
8. The plan promises 03c will mirror 03b's structural conventions (header, Role, Configuration table, Goal, Critical Constraints, Prerequisites, Coordinator State, Execution Procedure with numbered steps). Sketch in the actual proposed 03c and verify it lines up.

### Streamlining vs. complexity
9. The plan adds 7 new scripts under `Projects/scripts/`. Is this the right granularity, or could some be merged (e.g., `spawn_phase_worker.py` and `phase_dag.py`)? Conversely, is anything missing — e.g., a single "resume the project" entry-point that picks up where the previous session left off?
10. Does the plan introduce more cognitive load than the reliability gains justify? Be honest. The user has stated a strong preference for keeping process changes proportional to value.

### Risks I may have missed
11. Identify edge cases, failure modes, or operational surprises the plan does not address. Specifically called out for scrutiny:
    - Concurrent-write races on the Findings Ledger if two phase agents complete simultaneously
    - Recovering when the review daemon crashes mid-cumulative-review
    - What happens when `phase_complete.py` succeeds at commit but fails at review-dispatch (partial state)
    - A project where every phase has 1 task — does the overhead of phase-worktrees-and-reviews exceed the value?
    - Permissions/path-handling on Windows specifically (the user is on Windows; some scripts may need Windows-friendly subprocess calls)

### What you do NOT need to evaluate
- Implementation specifics (no code exists yet).
- Backward compatibility with the 17 active projects (explicitly not in scope per the plan).
- The `claude-delegate-review` skill itself — that is fixed infrastructure, not under review.

### Output format

Use the standard delegate-review report format. Categorize findings as CRIT / MAJ / MIN / NIT. For each finding, include: which section of the plan it applies to, the concrete concern, and a suggested resolution. End with a short overall recommendation (proceed / proceed-with-revisions / rethink).

## Context

## Context

The user (Ross) is the sole human developer on Starship Battles, a tactical Pygame combat + strategic galaxy-map game. He is augmenting an existing project-execution system with mid-project quality gates and limited intra-project parallelism. Today's flow has zero review steps between project start and project completion (only a post-hoc audit). The user's goal is to catch bugs earlier without slowing throughput.

This plan emerged from a long planning conversation in which the user explicitly:
- Pushed back on the original execution-only scope and asked for the planning phase to capture phase dependencies.
- Specified that one branch per project with phase worktrees inside is preferred over per-phase merges to main.
- Specified the cumulative-review model (replacing follow-up reviews after remediation).
- Asked for parallelism on independent phases, with multi-parent phases pausing until all parents are verified.
- Specified the depth-1 buffer rule for single-parent chains (a phase may run 1 ahead of an unverified ancestor, no further).
- Acknowledged the scrap-and-restart escape hatch for cases where rebasing a descendant onto a remediated parent would be too costly.
- Confirmed all 17 currently active projects are completed and awaiting verification, so no migration is needed.
- Explicitly invited pushback on his ideas; the plan's §Pushback section captures the agent's concerns.

The user is also independently requesting a plan review from Codex; the agent will integrate findings from both reviews before any implementation begins.

Key reference paths:
- Existing protocols: `Projects/protocols/03a_continue_working.md`, `Projects/protocols/03b_parallel_projects.md`
- Existing skills: `.claude/skills/claude-proj-*/SKILL.md`, `.claude/skills/claude-delegate-review/SKILL.md`
- Daemon + helper: `Tools/agent_coordination/review_daemon.py`, `Tools/agent_coordination/create_review_request.py`
- Scratchpad rule: `AgentCoordination/SCRATCHPAD.md` and `CLAUDE.md`
- Project conventions: `AGENTS.md`, `docs/03_CONVENTIONS.md`

## Expected Deliverable

A standard delegate-review report (CRIT / MAJ / MIN / NIT findings) with concrete suggested resolutions for each, a verification matrix is not required since this is a plan review, and a short overall recommendation: proceed / proceed-with-revisions / rethink. If you recommend revisions, prioritize them so the user can decide which are blockers vs. nice-to-haves before implementation begins.


**PickedUp:** 2026-05-03T14:16:02Z

## Results

**Status:** completed
**Completed:** 2026-05-03T14:22:34Z
**Report:** Reviews/results/2026-05-03_141602_plan_phase-aware-project-execution-with-cumulative-dele_req-req_20260503_141601_20579d/report.md
**Findings:** 15 total (C: 0, M: 6, m: 5, I: 4)
