# Review Request: PROJ-330 + PROJ-329A Phase 2 — independent code review (Wave 1 batch 3b, production refactors)
**Request ID:** req_20260504_222600_cac643
**Review Type:** code
**Created:** 2026-05-04T22:26:00Z
**Requester:** claude-code

## Scope

Two production-refactor projects (resubmitted as smaller batch after v2 timed out).

**PROJ-330 — strategy_screen.py LOC decomposition (production refactor + 62 new helper tests):**
- `game/ui/screens/strategy_screen.py` (708 → 458 LOC, under 500 ceiling per docs/03_CONVENTIONS.md §2.4)
- `game/ui/screens/strategy_screen_lifecycle.py` (NEW, 148 LOC) — menu/save/load/quit
- `game/ui/screens/strategy_screen_order_editing.py` (NEW, 91 LOC) — EDIT_MOVE / EDIT_TRANSFER
- `game/ui/screens/strategy_screen_assets.py` (NEW, 88 LOC)
- `game/ui/screens/strategy_screen_selection.py` (NEW, 99 LOC)
- `tests/unit/ui/screens/test_strategy_screen.py` (62 existing tests preserved; 5 patch-target lines updated to point at new helper modules)
- 4 new helper test files (~62 tests across lifecycle/order_editing/assets/selection)
- This is **NOT a testability retrofit** — Compositional Construction seam from PROJ-327 Phase 4 (`StrategyScreenComposition` Protocol) was preserved unchanged. Pure size decomposition per `docs/03_CONVENTIONS.md` §2.4.

**PROJ-329A Phase 2 — UIWindow fast-win retrofits (production refactor + 31 new tests):**
- `game/ui/screens/food_allocation_editor.py` (360 → 394, +34 LOC; +2 bypass invariants)
- `game/ui/screens/fleet_selection_window.py` (123 → 152, +29 LOC; +11 new tests)
- `game/ui/screens/planet_selection_window.py` (189 → 232, +43 LOC; +18 new tests)
- 3 new fixtures in `tests/fixtures/{food_allocation_editor,fleet_selection_window,planet_selection_window}_ui_builder.py`
- Project plan: `Projects/active_projects/PROJ-329A/{plan.md, decisions.md, phase_2_checklist.md}`
- Reference recipe: `Projects/active_projects/PROJ-328/phase_1_checklist.md` Tasks A.2/A.3/A.4
- Pattern: `docs/02_PATTERNS.md` §33 (two-stage UIWindow construction)

## Instructions

## What to look for

This batch is 2 production-refactor projects. Behavioral parity is the primary concern.

### PROJ-330 — strategy_screen.py LOC decomposition

1. **Behavioral parity** — for 3 picked methods (event handler, lifecycle hook, save/load), trace through both the original and the refactored screen.delegating-to-helper code. Verify identical observable behavior.
2. **No semantic change** — pure code movement; helpers take screen as parameter; no new abstractions.
3. **LOC ceiling** — confirm `strategy_screen.py` is now 458 LOC (< 500), each helper module < 500.
4. **Test parity** — `test_strategy_screen.py` has 62 tests preserved. Check the 5 updated patch targets are correct (e.g., patches that referenced `strategy_screen.is_fleet` now point at `strategy_screen_selection.is_fleet`).
5. **No new MVVM seam** — agent claimed nothing was added beyond PROJ-327 Phase 4's `StrategyScreenComposition`. Verify by looking for any new Protocol classes / factories in the helper modules.

### PROJ-329A Phase 2 — UIWindow fast-win retrofits

1. **Pattern §33 conformance** — for each of the 3 classes, verify cheap state + delegates BEFORE bypass guard, explicit `ui_builder` parameter, default builder in production path, Null/Mock fixture pair.
2. **Behavioral parity** — does the refactored `__init__` produce equivalent state for production callers? (Mock `ui_builder` should NOT be called in production; default builder should be.)
3. **Bypass safety** — under `bypass_init`, can the test fixtures construct a usable instance? Key check: does kill() work when widgets weren't built?
4. **TDD-first quality** — for FleetSelectionWindow + PlanetSelectionWindow which had NO existing tests: are the 11 + 18 new characterization tests meaningful (not vacuous)?

### Cross-project

5. **Concurrent-commit contamination** — multiple agents committed to `feat/03c-phase-aware-execution` in parallel. Several reports noted that one agent's files got bundled into another agent's commit. Verify (via `git log --stat`) that no test files are missing or duplicated.

## What we do NOT want

- Style nits.
- General praise paragraphs.
- Recommendations beyond test/production scope.

## Deliverable

Markdown findings report with `CRITICAL`/`MAJOR`/`MINOR`/`OBSERVATION` severity tags. Cite file + line. Under 2500 words.

## Context

Resubmission split from req_215406_0d7958 which timed out. This half covers production refactors. The other half (PROJ-337/340 test coverage) was resubmitted separately as req_w1b3a.

## Expected Deliverable

Markdown findings report.


**PickedUp:** 2026-05-04T22:26:01Z

## Results

**Status:** completed
**Completed:** 2026-05-04T22:34:48Z
**Report:** Reviews/results/2026-05-04_222601_code_proj-330-proj-329a-phase-2-independent-code-review_req-req_20260504_222600_cac643/report.md
**Findings:** 11 total (C: 2, M: 1, m: 1, I: 7)
