# Review Request: PROJ-390 — log_event module-level shim retirement (single commit; PROJ-382 had already done 90% of the work)
**Request ID:** req_20260509_023243_7fd457
**Review Type:** code
**Created:** 2026-05-09T02:32:43Z
**Requester:** claude-code

## Scope

## Scope

Project directory:
- `Projects/active_projects/PROJ-390/` (verification_report has Already-Done by PROJ-382 section + Surprises notes)

Single commit: `b2ffda5c5`.

**Changes:**
- `game/core/event_logging.py` — module-level `log_event()`, `set_event_handler()`, `get_event_handler()` + `_event_handler` global DELETED. Module docstring rewritten to record the retirement (PROJ-390).
- `game/core/__init__.py` — re-exports stripped, `__all__` updated.
- `game/simulation/entities/projectile.py` — `_default_event_logger` rewritten as no-op (was the only remaining live caller of the shim).
- `conftest.py` — cleanup hook for the deleted global removed.
- `tests/unit/core/event_logging/test_event_logging.py` — DELETED (only tested the deprecated shim).
- `docs/02_PATTERNS.md` §10 — "Module-level `log_event()` is a compatibility shim" sentence removed; replaced with explicit retirement note.

**Already-done by PROJ-382 (logged in `findings/verification_report.md`):**
- `game/strategy/data/empire.py`, `fleet.py` — EventBus injection (PROJ-382 Phase 2)
- `game/strategy/engine/conflict_resolution_engine.py`, `superweapon_order_processor.py`, `order_handlers/base.py`, `production_spawner.py`, `production_engine.py`, `planet_energy_engine.py`, `planet_action_engine.py` — all use injected `event_bus.log_event(...)` already

**Architectural decision:** EventBus is session-scoped (PROJ-252 design) and intentionally NOT added to `ApplicationContext` (process-scoped) — putting it on the context would re-create the isolation problem this shim retirement was meant to fix. `GameSession` constructs the bus and threads it via constructor injection.

Reference:
- `Reviews/results/2026-05-07_220621_legacy-audit/`
- `docs/02_PATTERNS.md` §10

## Instructions

## Instructions

**This was supposed to be the largest Stage 3 project but turned out tiny because PROJ-382 had already done most of the work. Verify the retirement is total and the architectural decision (EventBus stays session-scoped, NOT on ApplicationContext) is correct.**

1. **Final grep verification.** Confirm zero remaining references:
   - `git grep -E 'from game\.core\.event_logging import (log_event|set_event_handler|get_event_handler)' game/ tests/ combat_lab/ Tools/` — should return ZERO hits.
   - `git grep '_event_handler' game/ tests/ combat_lab/ Tools/` — should return ZERO hits in production code (only acceptable in deleted-file historical context, which is gone).
   - `git grep 'event_logging\.log_event' game/ tests/` — same.
   - If any production grep returns hits, that's CRITICAL.

2. **The 'already-done by PROJ-382' claim.** Agent says PROJ-382 Phase 2 already migrated 9-10 of the audit's claimed sites. Independently verify by:
   - Reading 2-3 of the named files (`empire.py`, `fleet.py`, `conflict_resolution_engine.py`).
   - Confirming each uses `self._event_bus.log_event(...)` (constructor-injected) rather than the module-level shim.
   - If any still use the shim, the agent's already-done claim is wrong and that's a CRITICAL miss.

3. **Architectural decision: EventBus session-scoped, not on ApplicationContext.** Agent intentionally did NOT add EventBus to `ApplicationContext`. Verify this is the right call:
   - Is EventBus genuinely session-scoped? (Look at `GameSession` constructor for the EventBus instantiation site.)
   - Would adding it to ApplicationContext break session isolation? (e.g., would two `GameSession`s in the same process share the same bus?)
   - The agent claims PROJ-252 mandates session-scope. Is that correctly reflected in PROJ-252's archived design docs?

4. **`projectile.py::_default_event_logger` rewrite.** Was the only remaining caller. Agent rewrote it as a no-op. Verify:
   - Is the no-op the right semantics? (No event gets logged from this path now. Is that acceptable, or did the original log convey real information?)
   - Are there callers that rely on the side effect? (e.g., a test that asserts on the log call)

5. **`conftest.py` cleanup hook removal.** The deleted global `_event_handler` had a cleanup hook in `conftest.py`. Verify:
   - The hook is gone (no dangling reference to a deleted symbol).
   - No remaining test depends on the hook for state isolation.

6. **Test file deletion.** `tests/unit/core/event_logging/test_event_logging.py` was deleted entirely. Agent claims it 'only tested the deprecated shim.' Verify:
   - From git history (parent commit `f8a396655`), read the deleted test file.
   - Confirm every test in it was about the module-level shim, not about EventBus or related still-living code.
   - If any test was actually about real behavior, that's a coverage regression.

7. **`docs/02_PATTERNS.md` §10 update.** Verify:
   - The 'compat shim' sentence is gone.
   - The replacement text is accurate (constructor injection is the only supported pattern).
   - No stale references to the removed module-level functions remain in §10.

## Severity guidance

- CRITICAL: any production reference to deleted symbols; PROJ-382 'already done' claim is wrong (some file still uses the shim); EventBus accidentally process-scoped; `_default_event_logger` no-op breaks a real caller.
- MAJOR: `conftest.py` cleanup gap; deleted test was actually testing real behavior; docs/02_PATTERNS.md §10 still has stale reference.
- MINOR: comment polish; doc-formatting drift.
- INFO: cleanup follow-up.

## Context

## Context

Ninth of 11 sequential PROJ runs. Stage 3 second project.

**This is a remarkable cross-project finding:** the audit estimated 12 callers; the agent found 1 actual remaining caller because PROJ-382's Phase 2 (Pattern #10 EventBus injection in Empire/Fleet/projectile + cascade through engine layer) had already migrated 9-10 callers as a side-effect. The orchestrator's stage-ordering (PROJ-382 in Stage 1 before PROJ-390 in Stage 3) made this consolidation natural.

PROJ-394 was created during the merge and not yet inspected. Not relevant to PROJ-390.

## Expected Deliverable

Standard review report. The headline questions: (1) Is the retirement TOTAL? (2) Is EventBus genuinely session-scoped per PROJ-252, justifying the decision NOT to put it on ApplicationContext?


**PickedUp:** 2026-05-09T02:32:44Z

## Results

**Status:** completed
**Completed:** 2026-05-09T02:39:21Z
**Report:** Reviews/results/2026-05-09_023244_code_proj-390-log-event-module-level-shim-retirement-si_req-req_20260509_023243_7fd457/report.md
**Findings:** 7 total (C: 0, M: 0, m: 0, I: 7)
