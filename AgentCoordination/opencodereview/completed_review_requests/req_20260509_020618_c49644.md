# Review Request: PROJ-383 — command_handlers.py shim eradication (82 LOC removed; 30 imports migrated)
**Request ID:** req_20260509_020618_c49644
**Review Type:** code
**Created:** 2026-05-09T02:06:18Z
**Requester:** claude-code

## Scope

## Scope

Project directory:
- `Projects/active_projects/PROJ-383/`

3 commits on `feat/03c-phase-aware-execution`:
- `db061fe90` — production migrations (Tasks 1.1 + 1.3)
- `e171d3573` — test migrations (Task 1.4)
- `f37514b78` — closeout (Task 1.5 + state updates, file deletion)

**Production (5 sites — audit estimated 6, one was already done by PROJ-382 phase 3):**
- `game/strategy/engine/planet_command_handlers.py` (4 lazy imports at lines 55, 123, 145, 181 → migrated to `game.strategy.engine.handlers.base`)
- `game/strategy/engine/game_session.py` (1 import at line 67 → migrated to `game.strategy.engine.handlers`)
- `game/strategy/engine/superweapon_command_handlers.py` (already done by PROJ-382 phase 3 commit `73eb2a635` — logged as Already-Done in `findings/verification_report.md`)

**Tests (25 sites across 10 files — matches audit):**
- `tests/unit/strategy/test_command_handlers.py`
- `tests/unit/strategy/engine/test_command_handlers_public_api.py` (retargeted to canonical package)
- `tests/unit/strategy/engine/test_base_command_handler.py`
- `tests/unit/strategy/engine/test_command_ownership.py`
- `tests/unit/strategy/engine/test_superweapon_command_handlers.py`
- `tests/unit/strategy/engine/test_colonize_mission_handler.py`
- `tests/unit/strategy/engine/test_build_order_command_handler.py`
- `tests/integration/colonization/test_explicit_orders.py`
- `tests/integration/strategy/test_warp_orders.py`
- `tests/integration/strategy/test_fleet_join_redirect.py`

**File deletion:**
- `game/strategy/engine/command_handlers.py` (82 LOC) — confirmed via `git rm`, commit `f37514b78` shows `delete mode 100644`.

**Doc updates:**
- `game/strategy/engine/handlers/__init__.py` (docstring)
- `game/strategy/engine/handlers/base.py` (docstring)
- `Projects/active_projects/PROJ-383/plan.md`, `phase_1_checklist.md`, `findings/verification_report.md`

Reference:
- `Reviews/results/2026-05-07_220621_legacy-audit/`

## Instructions

## Instructions

**Verify the deletion is total — no hidden references anywhere in the repo.**

1. **Final grep verification (most important).** Independently run:
   - `grep -rn 'from game.strategy.engine.command_handlers' game/ tests/ combat_lab/ Tools/` — should return ZERO hits in production paths. Hits in `Projects/`, `Reviews/`, `archived_projects/`, etc. (documentation references) are intentional.
   - `grep -rn 'import command_handlers' game/ tests/ combat_lab/ Tools/` — should return zero (no module-form imports either).
   - `grep -rn 'command_handlers\.' game/ tests/ combat_lab/ Tools/` — should return zero (no attribute access).
   - If ANY production grep returns hits, that's CRITICAL.

2. **The 'Already-done' Task 1.2 claim.** Agent says `superweapon_command_handlers.py:15` was migrated by PROJ-382 phase 3 commit `73eb2a635`. Verify by:
   - Reading the current `superweapon_command_handlers.py` line 15 — should import from `handlers.base` directly, not from `command_handlers`.
   - `git show 73eb2a635 -- game/strategy/engine/superweapon_command_handlers.py` to confirm PROJ-382 made that change.

3. **Production migration correctness.**
   - For `planet_command_handlers.py`: the 4 lazy imports were migrated from a top-level import to lazy form. Were they preserved as lazy (function-local) per the original pattern, or accidentally hoisted to top-level? Lazy was the original pattern to avoid circular imports — verify the migration kept that.
   - For `game_session.py:67`: migrated to `game.strategy.engine.handlers` (not `handlers.base`). Verify `create_default_registry` is exported from the package `__init__.py`, not just `handlers.base`.

4. **Test migration correctness.** 25 test imports across 10 files. Spot-check 3 files:
   - Did the migrated import path still resolve to the same symbol? (E.g., did some tests import `BaseCommandHandler` from the shim and accidentally get migrated to import from a different module that exports a different `BaseCommandHandler`?)
   - Did any test rely on the shim's specific re-export shape (e.g., `command_handlers.foo` as a module attribute)?

5. **CLAUDE.md Rule 3 compliance.** The shim's docstring explicitly said 'this shim is **transitional**' — Rule 3's deletion mandate is the headline outcome. Verify:
   - No replacement shim was introduced (e.g., a new module that re-exports from `handlers/` for some 'compat' reason).
   - The `handlers/__init__.py` and `handlers/base.py` docstring updates don't accidentally turn the canonical package itself into a re-export shim.

6. **Cross-check with PROJ-380.** PROJ-380 phase 3.6 (`MissionCommandHandler` template) lives in `superweapon_command_handlers.py`. PROJ-383 didn't touch that file (already-done). Verify the PROJ-380 work and PROJ-383's 'already done' don't interact in surprising ways.

7. **Smoke-test the delete.** The shim file is gone. Confirm `python -c 'import game.strategy.engine.handlers'` loads cleanly without error (should — handlers/ is the canonical package).

## Severity guidance

- CRITICAL: any production grep hit on the deleted module path; replacement shim introduced; lazy imports accidentally hoisted to top-level breaking circular-import contract.
- MAJOR: test migration imported wrong symbol; `game_session.py` `create_default_registry` migration broke if it's not exported from package init; PROJ-382 'already done' claim is wrong (line 15 still imports from old path).
- MINOR: doc-string drift; comment polish; missed opportunity to clean up adjacent code.
- INFO: future cleanup outside scope.

## Context

## Context

Seventh of 11 sequential PROJ runs. Stage 2 closeout. Sits on top of PROJ-380's 12 commits, PROJ-386's commit, and the merge bringing in PROJ-385/387/388.

This was a deliberate Stage 2 placement — PROJ-380's MissionCommandHandler template (phase 3.6) and PROJ-382's superweapon_command_handlers.py import re-route had to land first. PROJ-383 closes the loop by deleting the shim itself.

The call-site count came in at 30 actual (5 prod + 25 test) versus the audit's estimated 31 (6 prod + 25 test) — the missing prod site was Task 1.2 already done by PROJ-382. This is signal that the audit's call-site counting heuristic is good but the sequencing of dependent projects matters.

Pre-existing test failures noted: `test_order_processor_transfer.py` species-id fallback removal failures (PROJ-393 — already known, fixed in `88a2342ef`). The agent re-confirmed via stash round-trip that they're independent of PROJ-383.

## Expected Deliverable

Standard review report. The headline question: is the shim TOTALLY GONE, or are there hidden references the agent's grep missed?


**PickedUp:** 2026-05-09T02:06:19Z

## Results

**Status:** completed
**Completed:** 2026-05-09T02:12:51Z
**Report:** Reviews/results/2026-05-09_020619_code_proj-383-command-handlers-py-shim-eradication-82-l_req-req_20260509_020618_c49644/report.md
**Findings:** 3 total (C: 0, M: 0, m: 1, I: 2)
