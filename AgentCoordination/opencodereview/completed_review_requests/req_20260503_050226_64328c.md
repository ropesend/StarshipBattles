# Review Request: PROJ-319 Phase 5+6 remediation review (follow-up)
**Request ID:** req_20260503_050226_64328c
**Review Type:** code
**Parent:** req_20260503_042208_1f0252
**Created:** 2026-05-03T05:02:26Z
**Requester:** claude-code

## Scope

Follow-up to req_20260503_042208_1f0252. Verify the Phase 5 + Phase 6 remediation actually resolved the prior findings, and flag any regressions introduced by the fixes.

**Phase 5 (hygiene/manifest/records) — files modified:**
- `Projects/active_projects/PROJ-319/manifest.md` (full rewrite)
- `game/ai/spatial_behaviors/_formation_utils.py` (added `from __future__ import annotations`)
- `game/ui/widgets/range_slider_builder.py` (added `from __future__ import annotations`)
- `game/ui/widgets/column_toggle_section.py` (added `from __future__ import annotations`)
- `game/ui/screens/list_data_source_base.py` (added `from __future__ import annotations`)
- `game/ui/screens/planet_target_editor_base.py` (added `from __future__ import annotations`)
- `game/ui/screens/data_list_window_mixin.py` (added `from __future__ import annotations`)
- `Projects/active_projects/PROJ-319/decisions.md` (added review-verdict + scope rows during prep, before Phase 5)
- `Projects/active_projects/PROJ-319/plan.md` (Quick Status + Current State updates)
- `Projects/active_projects/PROJ-319/phase_5_checklist.md` (NEW)
- `Projects/active_projects/PROJ-319/phase_6_checklist.md` (NEW)

**Phase 6 (sort-key extraction) — files modified:**
- `game/ui/screens/list_filter_utils.py` (NEW — hosts `make_attr_sort_key(col)` factory)
- `game/ui/screens/planet_list_filters.py` (replaced inline `def sort_key(p)` at lines ~221-234 with `make_attr_sort_key(col)` call; added import)
- `game/ui/screens/star_list_filters.py` (same shape: replaced inline `def sort_key(s)` at lines ~134-147; added import)
- `Projects/active_projects/PROJ-319/phase_4_checklist.md` (annotated Task 4.14 sub-bullet to record deferral and Phase 6 resolution)

**Final test status:** `python Tools/test_sharded/test_sharded.py` — 16374 passed, 0 failed, 3 skipped on the verifying re-run. (One initial run showed 2 transient failures that passed on isolation/re-run; same documented intermittent test-ordering flake captured in `decisions.md`.)

## Instructions

## Goal

Verify that each finding from the parent review (`req_20260503_042208_1f0252`) the implementer agreed to remediate is now actually fixed. Flag any regression introduced by the fixes.

The implementer's self-verification of the parent review found the parent under-counted H1 (10 actually missing, not 9) and M1 (6 files actually missing `__future__`, not 3). Please confirm the remediation acted on the FULL count, not just the parent's quoted numbers.

## Findings to verify

### H1 (manifest gaps) — must be RESOLVED

Parent finding: 9 missing manifest entries plus the implementer's self-found 10th (`column_toggle_section.py`) and the line-30 inaccurate claim about `strategy_superweapons.py`.

- [ ] Confirm `Projects/active_projects/PROJ-319/manifest.md` now lists ALL files in `git diff HEAD~1 HEAD --name-only | grep -E '^(game|tests)/'`. Cross-check by computing the set difference both ways. There should be 0 omissions and 0 phantom entries (manifest rows that don't correspond to a real diff).
- [ ] Confirm the `strategy_superweapons.py` row is now accurate — it should describe the file as UNCHANGED with a note that `_resolve_superweapon_target` extraction was deferred. Cross-check `git diff HEAD~1 HEAD -- game/ui/screens/strategy_superweapons.py` returns no output.
- [ ] Confirm `list_filter_utils.py` is present in the manifest as a Phase 6 NEW file.

### H3 (sort-key duplication) — must be RESOLVED

Parent finding: `planet_list_filters.py:221` and `star_list_filters.py:134` had byte-identical `sort_key` inner functions; the manifest claim that this was absorbed by Task 4.14 was wrong.

- [ ] Confirm `grep -n 'def sort_key' game/ui/screens/planet_list_filters.py game/ui/screens/star_list_filters.py` returns ZERO hits.
- [ ] Confirm `game/ui/screens/list_filter_utils.py` exists and exports `make_attr_sort_key(col)` returning a callable.
- [ ] Confirm the sort-key factory's behavior matches the original byte-for-byte: `func` extraction returns `col['func'](entity)`; `attr` extraction walks dotted path and returns the final value (or `""` on missing); plain `""` fallback when neither key is present.
- [ ] Confirm both filter files import `make_attr_sort_key` and use it at the previous `else:` fallback branch.
- [ ] Run `pytest tests/unit/ui/screens/test_planet_list_filters.py tests/unit/ui/screens/test_star_list_filters.py` and confirm the sort-related tests still pass.
- [ ] **REGRESSION CHECK:** the audit's M5 claim is that the original behavior would also be valid for `func` extraction with non-typed return. Confirm the new helper preserves whatever the `func` lambda returns (no `str()` cast that would break numeric sort).

### M1 (__future__ imports) — must be RESOLVED

Parent finding: 3 (under-counted) / actually 6 of 7 new modules missing `from __future__ import annotations`.

- [ ] Confirm `from __future__ import annotations` is present at the top of all 7 new modules:
  - `game/strategy/services/race_resolver.py` (already had it before remediation)
  - `game/ai/spatial_behaviors/_formation_utils.py`
  - `game/ui/widgets/range_slider_builder.py`
  - `game/ui/widgets/column_toggle_section.py`
  - `game/ui/screens/list_data_source_base.py`
  - `game/ui/screens/planet_target_editor_base.py`
  - `game/ui/screens/data_list_window_mixin.py`
- [ ] Confirm `list_filter_utils.py` (the Phase 6 NEW file) also has it.
- [ ] Verify each import sits AFTER the module docstring and BEFORE the first regular import (PEP 563 / `from __future__` placement convention).

### M2 (manifest naming) — must be RESOLVED

Parent finding: `manifest.md:53` said `_compute_circular_position` (with leading underscore); actual exported name is `compute_circular_position`.

- [ ] Search the rewritten manifest for `_compute_circular_position` (with underscore). Should return zero hits.
- [ ] Confirm the manifest correctly references `compute_circular_position` (no underscore) and notes the deliberate deviation from the audit's recommendation.

## Regression checks

- [ ] Run `python Tools/test_sharded/test_sharded.py` once. Two failure signatures may legitimately appear from previously-documented intermittent flakes: (a) `tests/unit/services/llm/test_background.py::test_elapsed_seconds_is_monotonic_then_frozen` (Windows timing flake), and (b) `tests/integration/strategy/test_mutual_join_rendezvous.py::*` family (intermittent test-ordering flake). If you see only those, it's not a regression. If you see ANY other failure, flag it.
- [ ] **Re-export check** for the new `list_filter_utils.py`: grep `from game.ui.screens.list_filter_utils import` across `tests/` to ensure no test relies on a re-export hub that wasn't updated. (The MASS_MOON re-export bug surfaced this exact failure mode during execution.)
- [ ] **Import-cycle check:** the new `list_filter_utils.py` lives in `game/ui/screens/`; confirm it doesn't import from any module that itself imports from `planet_list_filters.py` or `star_list_filters.py`.
- [ ] **Behavior preservation:** spot-check 2-3 sort scenarios in `planet_list_filters.py` and `star_list_filters.py` to confirm the sort order is identical before and after Phase 6's extraction. Specifically, sort by `gravity` (numeric attr), `temperature` (numeric attr), and a column with a custom `func` extractor.

## Severity tags

Use the same scheme as the parent review:
- **CRITICAL** — A finding from the parent that should be RESOLVED is still present, OR Phase 5+6 introduced a real regression.
- **HIGH** — A documentation gap or partial fix that needs another round.
- **MEDIUM** — Style / hygiene issue introduced by the remediation.
- **LOW** — Suggestion for improvement.

For each parent finding, return one of: RESOLVED / PARTIAL / NOT-RESOLVED / REGRESSED.

## Final verdict

Report one of: APPROVE (all parent findings RESOLVED, 0 regressions) / NEEDS-MINOR-FIX (1-2 small items to clean up) / NEEDS-REWORK (something material is still broken).

## Context

Parent review request `req_20260503_042208_1f0252` returned 0 CRITICAL / 3 HIGH / 5 MEDIUM / 5 LOW with verdict READY-TO-ARCHIVE.

The implementer agreed to remediate H1 (manifest gaps), H3 (sort-key duplication), M1 (`__future__` imports), and M2 (manifest naming). The remaining findings (H2 LOC ceiling, L1 dead `get_shield_info`, L2 monkey-patch pattern, M3 `==`→`is`, M4 `_with_ship` placement, L3 / L4 / L5) were explicitly accepted as out-of-scope or stylistic and are documented as deferred in `decisions.md`.

This follow-up should NOT re-litigate the deferred items — focus only on whether the four targeted remediations are clean and whether they introduced any new problem.

## Expected Deliverable

Markdown report at `Reviews/results/<date>_<slug>/report.md` with:

1. Per-parent-finding verdict (RESOLVED / PARTIAL / NOT-RESOLVED / REGRESSED) with one-line evidence.
2. Regression findings list (severity-tagged).
3. Final recommendation: APPROVE / NEEDS-MINOR-FIX / NEEDS-REWORK.


**PickedUp:** 2026-05-03T05:02:27Z

## Results

**Status:** completed
**Completed:** 2026-05-03T05:11:44Z
**Report:** Reviews/results/2026-05-03_050227_code_proj-319-phase-5-6-remediation-review-follow-up_req-req_20260503_050226_64328c/report.md
**Findings:** 0 total (C: 0, M: 0, m: 0, I: 0)
