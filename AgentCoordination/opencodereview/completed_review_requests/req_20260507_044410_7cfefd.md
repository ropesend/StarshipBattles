# Review Request: PROJ-377: PROJ-372 Phase 5 leftovers — golden-save fixture + pathfinding shim partial sweep + AST guard
**Request ID:** req_20260507_044410_7cfefd
**Review Type:** code
**Created:** 2026-05-07T04:44:10Z
**Requester:** claude-code

## Scope

Commits in scope on branch feat/03c-phase-aware-execution:
- 3b8370f7a PROJ-377 phase 1: golden-save JSON fixtures + round-trip identity tests
- 83a31662f PROJ-377 phase 2: migrate 5 pathfinding-shim Class A sites (later partially reverted in phase 3)
- 9cb543f4c PROJ-377 phase 3: AST shim-scope guard + docstring + revert site #3

Files in scope:
- tests/fixtures/saves/_capture_baseline.py (NEW, capture script)
- tests/fixtures/saves/galaxy_proj372_baseline.json (NEW, ~99 KB)
- tests/fixtures/saves/galaxy_proj372_populated.json (NEW, ~252 KB)
- tests/integration/strategy/test_save_round_trip.py (modified — 2 new round-trip tests)
- game/ui/screens/strategy_screen.py (modified — 3 method bodies)
- game/ui/screens/strategy_colonization.py (modified — 1 method body)
- game/strategy/engine/superweapon_order_processor.py (Phase 2 migrated, Phase 3 reverted — net unchanged from baseline aside from new docstring? No — file is back to original)
- tests/unit/strategy/data/test_pathfinding_shim_scope.py (NEW, AST guard)
- game/strategy/data/pathfinding.py (modified — docstring rewrite only)
- Projects/active_projects/PROJ-377/{plan,decisions,phase_*_checklist}.md (modified)
- Projects/active_projects/PROJ-372/decisions.md (cross-link row appended)

## Instructions

Review PROJ-377 against design.md. Focus areas:

1. **Golden-fixture correctness** — does `_capture_baseline.py` produce a fixture that round-trips identically through `Galaxy.from_dict → to_dict`? Are the `_normalize_image_fields` placeholders semantically safe (do they survive both directions of the round trip)? Confirm the fixture's structural coverage actually exercises the Planet 47-field surface (atmosphere, deposits, stockpile, intrinsic_abilities, populations, etc).

2. **Capture-script idempotence trade-off** — design.md OQ-related decision row 2026-05-07 in decisions.md says capture is best-effort deterministic and CI's contract is round-trip identity not byte-equality. Is that trade-off correctly documented in `_capture_baseline.py` docstring? Anything else that could be deterministically seeded without invasive changes?

3. **Phase 2 migrations** — sites #10, #11, #12 (`strategy_screen.py`) and #14 (`strategy_colonization.py`): verify direct `galaxy._pathfinder.X(...)` calls preserve semantics. In particular: for `_get_system_at_hex(hex_c)`, did the previous shim `get_system_at_hex(galaxy, hex, radius=50)` default to radius=50 while the migrated `galaxy._pathfinder.get_system_at_hex(hex_c)` may use a different default? Check the service signature.

4. **Site #3 revert correctness** — Phase 2 migrated `superweapon_order_processor.py` then Phase 3 reverted it after discovering test-patch coupling. Confirm the revert restored the file to its pre-PROJ-377 state byte-for-byte. Check `git show 9cb543f4c -- game/strategy/engine/superweapon_order_processor.py` — does it show the revert as a clean restoration?

5. **AST guard scope** — `test_pathfinding_shim_scope.py`'s `EXPECTED_SHIM_FUNCTIONS` set: are all 10 names (8 free funcs + 2 helpers) actually in `pathfinding.py`? Are any extra functions in pathfinding.py that the guard misses? Run a quick parse against the file.

6. **Plan-vs-implementation drift** — is the final migration count (4 of 14) accurately reflected in plan.md, decisions.md, the docstring, and PROJ-372's cross-link?

7. **Test-patch transparency invariant** — for the 8 deferred Class B sites, did anything else change that could break their patches? In particular, the `get_system_at_hex` import in `superweapon_order_processor.py` (line 31) is back — confirm via grep.

8. **PROJ-372 cross-link consistency** — check `Projects/active_projects/PROJ-372/decisions.md` last row mentions the migration count + the deferred-site set + the AST guard test path correctly.

## Context

PROJ-377 closes PROJ-372 review MAJ-001 (golden-save fixture) and MIN-002 (pathfinding shim sweep) — partial. The shim is now a permanent test-patch transparency surface; the AST guard pins its function set. Phase 3 surfaced and reverted a Phase 2 over-migration where the original planning grep missed `patch('module.local_import_name')` patterns alongside `patch('shim_path.X')` patterns. Sharded suite: 19057 passed / 1 pre-existing failure (`test_pathfinder_attached_after_init`, unrelated PROJ-372-side bug).

## Expected Deliverable

Standard OpenCode review report with CRIT/MAJ/MIN/INFO classification.


**PickedUp:** 2026-05-07T04:44:12Z

## Results

**Status:** completed
**Completed:** 2026-05-07T04:51:46Z
**Report:** Reviews/results/2026-05-07_044412_code_proj-377-proj-372-phase-5-leftovers-golden-save-fi_req-req_20260507_044410_7cfefd/report.md
**Findings:** 16 total (C: 0, M: 1, m: 3, I: 12)
