# Review Request: PROJ-376: BuildQueueScreen lifecycle refactor (3 phases)
**Request ID:** req_20260508_003618_4ba7dc
**Review Type:** code
**Created:** 2026-05-08T00:36:18Z
**Requester:** claude-code

## Scope

Commits in scope on branch feat/03c-phase-aware-execution:
- 56bbe4c54 PROJ-376 phase 1: BuildQueueScreen lifecycle seam
- a93330bb9 PROJ-376 phase 2: BuildQueueScreen instance reuse + is_visible() gates
- 4ef34e87b PROJ-376 phase 3: editorial close-out + PROJ-373 review hygiene

Files in scope:
- game/ui/screens/build_queue_screen.py (Phase 1 split __init__ + lifecycle methods; Phase 2 _request_close + close-button repoint)
- game/ui/panels/build_queue_drag_handler.py (Phase 1 reset_state)
- game/ui/screens/strategy_build_queue_manager.py (Phase 2 _open_build_queue helper + lazy-construct + reuse + close-callback no-null)
- game/ui/screens/strategy_event_router.py (Phase 2 is_visible gate at :58)
- game/ui/screens/strategy_input_handler.py (Phase 2 is_visible gate at :55)
- game/ui/screens/strategy_screen.py (Phase 2 is_visible gate at :246)
- game/ui/panels/build_queue_controller.py (Phase 3 MIN-001 + MIN-002)
- game/ui/components/table/virtual_table.py (Phase 3 MIN-003 docstring)
- tests/unit/ui/screens/test_build_queue_screen_lifecycle.py (NEW; ~13 lifecycle tests)
- tests/unit/ui/screens/test_strategy_build_queue_manager.py (Phase 2 tests rewritten + new tests)
- tests/unit/ui/screens/test_sub_window_hotkeys.py (Phase 2 _close→_request_close)
- tests/integration/ui/build_queue_screen/test_basics.py (Phase 2 _close→_request_close)
- tests/unit/ui/components/table/test_virtual_table.py (Phase 3 MIN-005 column-config test)
- Projects/active_projects/PROJ-373/plan.md (Phase 3 MAJ-003 + MAJ-004 backfill)
- Projects/active_projects/PROJ-376/{plan,decisions,phase_*_checklist}.md (status updates)

## Instructions

Review PROJ-376 against design.md + decisions.md. Focus areas:

1. **Lifecycle correctness** — `BuildQueueScreen.__init__` shell + `open_for_yard()` produces identical post-init state vs eager `__init__(initial_yard=...)`. Spot-check: do all 12+ yard-specific attributes (build_context, hex_coord, queue_sources, active_queue_source, selected_queue_indices, controller.build_context, controller.hex_coord, controller.active_queue_source, controller.selected_category, controller.selected_role, planet_selection_window) get set the same way in both paths? Cite test_open_for_yard_initial_yard_kwarg_matches_post_open_state and verify it actually compares all 12.

2. **Instance-reuse contract** — `StrategyBuildQueueManager._open_build_queue` constructs once, reuses thereafter. Verify: (a) no construction sites bypass this helper; (b) the design_library / design_loader / portrait_loader rebinding in `_open_build_queue` is correct (decisions.md row 2026-05-07 documents this); (c) drag_handler.design_library is rebound on every open via open_for_yard's existing path; (d) the close-callback no longer nulls `self._screen.build_queue_screen`.

3. **`is_visible()` contract** — three sites migrated (event_router :58, input_handler :55, strategy_screen :246). Verify: (a) each site previously was `is not None`; (b) post-migration it's `is not None and is_visible()` (defense-in-depth); (c) input_handler gate prevents events from reaching a hidden screen; (d) draw gate prevents drawing a hidden screen; (e) event-router modal-block check correctly preserves the original 'is the build queue currently the focused overlay' semantics.

4. **`_request_close()` contract (locked decision OQ-4)** — `hide()` does NOT invoke `on_close`; only `_request_close()` does, in the sequence `self.hide(); self.on_close()`. Verify: (a) close-button handler routes through `_request_close()`; (b) Esc handler routes through `_request_close()`; (c) `hide()` itself only kills planet_selection_window + sets panel.visible=False; (d) old `_close()` is removed (no longer reachable); (e) the regression test `test_close_method_is_removed` pins this.

5. **`_rebuild_panels` correctness** — for planet ↔ fleet transition, panels are killed and rebuilt; collaborators (renderer, controller, drag_handler) are reconstructed (decisions.md row 8). Verify the test `test_open_for_yard_planet_to_fleet_rebuilds_panels` actually proves the panel object identity changed; and `test_open_for_yard_planet_to_planet_does_not_rebuild_panels` proves it didn't change for same-context-type.

6. **Plan-vs-implementation drift** — any deviations not logged in decisions.md? In particular: (a) the pygame_gui UIPanel API correction (set_visible vs hide/show) — logged 2026-05-07; (b) inline dependency rebinding vs `_rebind_dependencies()` method — logged 2026-05-07; (c) `_close()` removed entirely vs alias — logged 2026-05-07.

7. **PROJ-373 review hygiene** — verify Phase 3's MIN-001/002/003/005 fixes are accurate vs the original review report at `Reviews/results/2026-05-06_051908_code_proj-373-build-queue-latency-phases-1-3-4-cache-co_req-req_20260506_051906_ddfd29/report.md`. Backfill of PROJ-373 plan.md MAJ-003 + MAJ-004 is consistent.

8. **Tests** — 13+ new lifecycle tests + ~5 manager-test rewrites + 2 new manager tests + 3 Phase-2 lifecycle tests + 1 column-config test. Spot-check at least 3 for actual coverage of the claim (not just function calls).

9. **LOC ceiling** — `build_queue_screen.py` was 659 LOC pre-PROJ-376; what's it now? CLAUDE.md says soft target 500. Phase 1 used `_construct_collaborators` extraction to avoid duplicating 30+ lines. Is the file growth acceptable given the lifecycle methods added?

10. **Manual smoke + re-profile** — Task 3.1 is user-side. Confirm decisions.md / plan.md document this clearly.

11. **Pre-existing failure** — sharded shows 1 pre-existing failure (`test_pathfinder_attached_after_init` references `galaxy._intercept` deleted in PROJ-372). Confirm unrelated to PROJ-376.

## Context

PROJ-376 closes PROJ-373 Phase 2 (deferred); UI lifecycle refactor enabling instance reuse for build-queue panels. Manager constructs BuildQueueScreen once per session, reuses via open_for_yard(). hide() does not kill panels; _request_close() is the new close path. Sharded suite: 19077 passed / 1 pre-existing failure unchanged. User-side re-profile (Tools/profile_game/profile_game.py) is the remaining acceptance gate — code-side closeout is what this review covers.

## Expected Deliverable

Standard OpenCode review report with CRIT/MAJ/MIN/INFO classification.


**PickedUp:** 2026-05-08T00:36:19Z

## Results

**Status:** completed
**Completed:** 2026-05-08T00:43:31Z
**Report:** Reviews/results/2026-05-08_003619_code_proj-376-buildqueuescreen-lifecycle-refactor-3-pha_req-req_20260508_003618_4ba7dc/report.md
**Findings:** 6 total (C: 0, M: 1, m: 3, I: 2)
