# Review Request: Repo tidy-up plan review (retry)
**Request ID:** req_20260503_004958_cbe243
**Review Type:** plan
**Created:** 2026-05-03T00:49:58Z
**Requester:** claude-code

## Scope

Plan file under review: `C:\Users\rossr\.claude\plans\perform-a-review-of-wild-lampson.md`

The plan proposes a tidy-up of `c:/Developer/StarshipBattles`. It is grouped into four tiers:

- Tier 1: delete root-level junk — `help.txt`, `help_utf8.txt`, `providers_help.txt`, `providers_list.txt`, `providers_login_help.txt`, `new 1.txt`, `script_test_icons.py`, `test_scroll.py`, `test_scroll2.py`, and the empty `ui/` dir
- Tier 2: gitignore `coverage.json`, `git rm --cached coverage.json`, delete file
- Tier 3: `git mv "Temp Review Docs" Reviews/results/2026-04-18_skeptic_proj-283-290`
- Tier 4: archive `Reviews/results/` folders dated **on or before 2026-03-31** into `Reviews/results/_archive_2026_Q1/` (~55 folders); consolidate root-level `tests/repro_*.py` (`repro_facade_colonies.py`, `repro_load_cargo_bug.py`, `repro_warp_bug.py`) into `tests/repro_issues/`; move 6 docs from `AgentCoordination/opencodereview/_archive/` into `_marked_for_deletion_2026-05-29/AgentCoordination/_opencodereview_archive/`

Explicitly out of scope (do NOT review these as omissions):
- `assets/` move (referenced by game code, deliberate skip)
- `pytest.ini` → `pyproject.toml` merge (deliberate skip)
- `_marked_for_deletion_2026-05-29/` (intentional 30-day staging, deletion date 2026-05-29)
- `docs/_ignore/` (user scratch space per CLAUDE.md, off-limits)
- `tests/archive/`, `tests/refactor/`, `tests/strategy/` (deliberately deferred — touching tests/ subdir layout is more than a tidy)

Today is 2026-05-02. Note: a prior submission of this review (req_20260502_204250_1944dc) timed out at 30 min. This is a retry — please scope your effort to the time budget rather than exhaustive verification.

## Instructions

**Time budget: aim for under 15 minutes of work.** A previous attempt timed out at 30min — be efficient.

You do NOT need to re-verify everything; the requester already verified the following:
- pytest does NOT collect root `test_scroll*.py` (testpaths is `tests/`)
- `.claude/settings.json` hooks reference only `Tools/check_context/` and `Tools/agent_coordination/`
- Grep for any Tier 1–4 path returned zero hits in code/docs
- `tests/repro_issues/` exists with dozens of `test_bug_*.py`
- The 6 `_archive/` opencodereview docs (DELEGATION_v2*, codex_*) have no live links

Focus on these four questions ONLY — answer briefly, do not exhaustively verify:

1. **Does the revised Tier 4 cutoff (2026-03-31) bisect any active project's review trail?** Quickly scan `Reviews/reviews_index.md` and any `Projects/active_projects/PROJ-*/` subdirs to check whether any active project links to a result folder that would now be in `_archive_2026_Q1/`. If yes, suggest either keeping that specific folder live or updating the link as part of Tier 4.

2. **Did the plan miss any obvious junk at the repo root or in `Tools/`/`scripts/`?** `ls c:/Developer/StarshipBattles/` and `ls Tools/` once each. If you spot something obviously stale (e.g. a `*_old.py`, `_backup/`, `tmp_*`), flag it. Skip if nothing jumps out.

3. **Should anything in `AgentCoordination/` outside of `_archive/` also be retired?** A 30-second scan of `AgentCoordination/`'s top level is enough.

4. **One overall risk that the plan's author may have missed.** Trust your judgment.

Severity: CRIT (will break something) / MAJ (item is load-bearing or cutoff is wrong) / MIN (nice addition). Per finding: path, concrete risk, suggested fix.

If you find nothing in a question, write 'no findings' and move on. Do not pad. End with a one-line verdict: APPROVE / APPROVE WITH CHANGES / REJECT.

## Context

Retry of req_20260502_204250_1944dc which timed out. The user has confirmed they want all four tiers, one commit per tier. A delegate-review pass is the last gate before execution.

## Expected Deliverable

A short findings report (~300–600 words) with severities, ending in a one-line verdict.


**PickedUp:** 2026-05-03T00:50:00Z

## Results

**Status:** failed
**Completed:** 2026-05-03T00:50:48Z
**FailureReason:** cannot reach C:\Users\rossr\.claude\plans\perform-a-review-of-wild-lampson.md: it resolves outside the project root (cwd=C:\Developer\StarshipBattles). The OpenCode worker can only review files inside the repository. Ask the requester to copy the file into the repo and resubmit.
