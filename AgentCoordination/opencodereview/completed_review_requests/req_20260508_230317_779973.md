# Review Request: PROJ-381 — Error handling cleanup (strategy/ui/assets/sim) — 27 audit items + 1 CRITICAL UI boundary
**Request ID:** req_20260508_230317_779973
**Review Type:** code
**Created:** 2026-05-08T23:03:17Z
**Requester:** claude-code

## Scope

## Scope

Project directory:
- `Projects/active_projects/PROJ-381/` — plan, 3 phase checklists, manifest, design, findings (verification_report, source_audit, bundling_decisions)

Production files changed (most recent 6 commits on `feat/03c-phase-aware-execution`):
- `game/core/exceptions.py` — added `ImageUnexpectedError`, `SessionInitializationError`, `TurnFailedError`, `BattleResolutionError`
- `game/core/error_codes.py` — added `OWNERSHIP_MISMATCH` (V005)
- `game/ui/screens/strategy_game_state_manager.py` — **CRITICAL B-5** UI error boundary; modal dialog on `EnginePhaseError`
- `game/strategy/engine/turn_engine.py` — broad-catch comments; narrowed snapshot catch; B-2 context enrichment (turn_number/save_path)
- `game/strategy/engine/turn_state_snapshot.py` — comment + `save_json` migration
- `game/strategy/engine/conflict_resolution_engine.py` — B-7 ERROR-log + hex/empire context
- `game/strategy/engine/commands/registry.py` — `ValueError` → `ValidationException`
- `game/strategy/engine/handlers/base.py` — `ValueError` → `ValidationException` (3 sites)
- `game/strategy/engine/game_session.py` — B-11 init boundary + null-object recovery
- `game/strategy/facade/strategy_session_facade.py` — B-4 `EnginePhaseError` → `TurnFailedError`
- `game/strategy/adapters/simulation_adapter.py` — B-6 `SimulationException` wrap with battle context
- `game/strategy/services/design_validator.py` — ERR-03-004 surface sim-validator failure
- `game/strategy/formulas/colony_output.py`, `data/ship_instance.py`, `config/economy_config.py`, `data/galaxy_system_generator.py`, `data/galaxy_warp_generator.py`, `data/star_generation_config.py` — broad-catch comments / JSON bypass / narrowed tuple
- `game/assets/asset_manager.py` — narrowed star image catch
- `game/ui/services/image/background.py` — `ImageUnexpectedError` wrap + `_done_event`/`wait()` parity
- `game/ui/services/tkinter_utils.py` — comment normalization (4 sites)
- `game/ui/screens/battle_setup/controller.py` — broad-catch comment
- `docs/05_ERROR_HANDLING.md` — `ImageUnexpectedError` contract description

New test files (3) + augmented (8):
- New: `tests/integration/ui/test_strategy_turn_error_boundary.py`
- New: `tests/unit/ui/services/image/test_background.py`
- New: `tests/unit/strategy/engine/test_conflict_resolution_modifier_logging.py`
- Augmented: `tests/unit/strategy/services/test_design_validator.py`, `tests/unit/strategy/test_game_session.py`, `tests/unit/strategy/data/test_star_generation_config.py`, `tests/unit/strategy/turn_engine/test_turn_engine_snapshot_integration.py`, `tests/unit/strategy/adapters/test_simulation_adapter.py`, `tests/unit/strategy/turn_engine/test_turn_state_snapshot.py`, `tests/unit/strategy/engine/test_base_command_handler.py`, `tests/unit/strategy/test_command_handlers.py`

Reference docs:
- `docs/05_ERROR_HANDLING.md` — error-handling conventions
- `docs/02_PATTERNS.md` — esp. Pattern #10 (EventBus) since this PROJ touches event-log paths in `conflict_resolution_engine.py`
- `Reviews/results/2026-05-07_220225_error-audit/` — source audit (28 unique findings)

## Instructions

## Instructions

**Primary focus areas:**

1. **B-5 UI error boundary (`strategy_game_state_manager.py`).** Verify the new `except EnginePhaseError` boundary in `process_full_turn()` produces a modal dialog without breaking the existing turn flow. Specifically:
   - Does the dialog block correctly per Pattern #31 (StrategyModalWindow), or is it a one-shot pygame dialog that bypasses modal tracking?
   - Does the post-error state leave the turn engine in a recoverable state (snapshot rollback already done in turn_engine, but does the UI know that)?
   - Does the regression test in `tests/integration/ui/test_strategy_turn_error_boundary.py` exercise the actual failure path or stub too much?

2. **New exceptions in `game/core/exceptions.py`.** `ImageUnexpectedError`, `SessionInitializationError`, `TurnFailedError`, `BattleResolutionError`. Are they:
   - Consistent with the layered hierarchy in `docs/05_ERROR_HANDLING.md`?
   - Inheriting from the right base (`SimulationException` / `PersistenceException` / etc.)?
   - Carrying the canonical context fields (turn_number, save_path, battle_id, etc.) per the docs §74?
   - Are any of them never actually raised in the new code (dead exception classes)?

3. **Were any of the 27 audit findings papered over rather than root-caused?** Each finding should have a real fix, not a band-aid:
   - `ValueError` → `ValidationException` migrations (ERR-01-002, ERR-01-003) — does the new `ValidationException` carry enough context, or did the migration just rename without adding error_code/payload?
   - JSON bypass migrations (ERR-02-003/004/005, ERR-04-003/004) — do they use the canonical `save_json`/`load_json` API with proper exception chaining?
   - Broad-except comment normalizations (ERR-04-006, etc.) — are the comments substantive (`# Intentional broad catch: <reason>`) or pro-forma boilerplate?
   - B-11 GameSession init recovery — does the null-object actually represent a usable degraded state, or is it just a way to swallow the error?
   - B-7 modifier-collection log promotion — does the ERROR log actually surface to the right channel (EventBus + log handler) per Pattern #10?

4. **Tests checking implementation rather than behavior.** Look for:
   - Tests that mock the exception class itself rather than triggering the failing path
   - Tests that assert on exact log strings (brittle) vs error_code/exception type (robust)
   - Tests that bypass the new error boundary and test the underlying `EnginePhaseError` raise rather than the dialog flow
   - Any test that's too narrow — e.g., asserting only one of N possible failure modes is handled

5. **Knock-on effects in 12 broad-catch sites.** The audit identified 12 broad-except sites needing comment normalization or narrowing. For each one, verify:
   - The comment now follows the canonical `# Intentional broad catch: <specific reason>` form
   - Narrowed catches don't accidentally break legitimate paths (i.e., test coverage exercises both the now-narrow type and any wider type that would previously have been swallowed)

6. **The 89 pre-existing failures the agent noted.** The implementer ran a phase-3 broad sweep and found 89 unrelated failures (`test_galaxy_entity_registry.py`, `test_storm.py`, `test_planet_classification_logic.py`). They `git stash`-baselined to confirm pre-existing. Spot-check 2-3 of these to confirm the baseline-check actually proves they were pre-existing — i.e., they fail on `main` too, not because of an indirect side effect of PROJ-381 changes (e.g., a new exception type reaching code that catches the old type).

7. **CLAUDE.md Rule 3 compliance.** No save-migration code, no compatibility shims, no fallback systems. The added exceptions and the B-11 null-object recovery are the highest-risk for accidental shim-shape. Confirm B-11's recovery is genuinely a 'fail loudly with degraded state' rather than 'silently mask a broken init.'

## Severity guidance

- CRITICAL: papering over Rule 3 violations; B-5 dialog leaves turn engine in inconsistent state; new exceptions never raised; tests pass without exercising real failure paths.
- MAJOR: missing context fields on new exceptions; non-canonical broad-catch comments; null-object swallows real bugs; layer violations introduced.
- MINOR: doc drift in `docs/05_ERROR_HANDLING.md`; test brittleness; slightly-off comment text.
- INFO: opportunities for follow-up cleanup outside this PROJ's scope.

## Context

## Context

**This is the first of 11 sequential PROJ-NNN runs being orchestrated by Claude.** The 11 projects (PROJ-380, 381, 382, 383, 384, 386, 389, 390, 391, 392, 393) form one connected component in the file-conflict graph and are running serially. PROJ-381 is the lead-off because it contains the only CRITICAL bug fix (B-5 UI error boundary).

The project skeleton (plan, phase checklists, manifest, design, findings) was created from `2026-05-07_220225_error-audit` via `/claude-proj-from-error-audit`. A Sonnet third-pass independently verified all 28 audit findings: 26 confirmed, 1 user-included from UNCERTAIN (ERR-04-007), 1 rejected as false positive (ERR-03-005), 2 out-of-scope (B-8, LLM-2). All 27 in-scope items are now claimed complete by the implementer.

**No findings were deferred during implementation** per the agent's report — all 27 closed in 6 commits. This is a strong claim; OpenCode's review should validate that completion is genuine and not pro-forma.

The 6 commits sit on top of `feat/03c-phase-aware-execution` and have not yet been merged to main. The next 10 projects in the sequence will be committing on top of these.

## Expected Deliverable

Standard review report under `Reviews/results/<date>_code_proj-381-error-handling-cleanup-_req-req_<id>/`. Severity-ranked findings tied to specific file:line. If a finding is a Rule 3 violation, label it explicitly. If the B-5 boundary has a hole that breaks the turn-engine recovery contract, label it CRITICAL — that's the one finding that would force a remediation project before subsequent projects can proceed.


**PickedUp:** 2026-05-08T23:03:18Z

## Results

**Status:** completed
**Completed:** 2026-05-08T23:25:02Z
**Report:** Reviews/results/2026-05-08_230318_code_proj-381-error-handling-cleanup-strategy-ui-assets_req-req_20260508_230317_779973/report.md
**Findings:** 42 total (C: 3, M: 14, m: 18, I: 7)
