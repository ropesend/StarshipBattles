# Review Request: PROJ-392 — Misc orphan wrappers + zero-callsite placeholders (12 + 1 UNCERTAIN + 2 INFO, 4 commits)
**Request ID:** req_20260509_030941_2fd173
**Review Type:** code
**Created:** 2026-05-09T03:09:41Z
**Requester:** claude-code

## Scope

## Scope

Project directory: `Projects/active_projects/PROJ-392/` (verification_report has Implementation-Time Corrections section)

4 commits on `feat/03c-phase-aware-execution`: `21ab2bdc0`, `51b216bf9`, `19d929385`, `bd0d150ee`.

**Phase 1 — Zero-callsite quick deletions (1 commit):**
- `game/simulation/entities/ship_stats.py` — `_priority_sort_key` deleted (LEG-01-001)
- `game/ui/screens/race_setup/screen.py` — `name_input = None` placeholder deleted (LEG-02-007)
- `game/ui/panels/battle_panels.py` — `expanded_ships` alias deleted (LEG-03-025); 14 test reads of `panel.expanded_ships` migrated to `panel._expanded_ids`

**Phase 2 — 9 inline-and-delete + small migrations (2 batched commits + closeout):**
- LEG-01-006: 3 strategy_renderer image-load wrappers inlined
- LEG-01-007: 2 quickstart_builder dir wrappers inlined; 4 test patch targets retargeted to `Paths.get_starter_*_dir` directly
- LEG-01-009: `find_path_deep_space` static inlined; 4 caller migrations including a 4th not in the audit (`tests/integration/strategy/test_save_round_trip_phase4.py:42` `galaxy._pathfinder.find_path_deep_space` replaced with `find_hybrid_path`)
- LEG-01-010: `priority_sort_key` wrapper deleted (vacuous after Phase 1 deleted the only prod caller)
- LEG-02-015 (UNCERTAIN-included): `Game._menu_scene` private property renamed to public `menu_scene`
- LEG-03-010 (INFO-included): `get_asset_manager` 1-line alias deleted (find-and-replace migration)
- LEG-03-014: `_get_sector_text` instance wrapper inlined at `empire_build_queue_window.py:589`
- LEG-03-016 (INFO-included): `_get_total_crew_requirement` renamed to public `get_total_crew_requirement`; `get_crew_required` wrapper deleted; production dispatch-registry KEY `'get_crew_required'` retained (referenced by name in `data/stats_layout.json` and `data/stats_sections.json`)
- LEG-04-006: 2 static wrappers in `new_game_setup_screen.py:701-720` deleted; 9 test callers in `test_new_game_setup.py` + 2 patches in `test_new_game_setup_extended.py` migrated; `controller._screen.validate_save_name` indirection simplified

**Files changed (rough):**
- Production (~12 files): `game/{simulation/entities/{ship_stats.py, stat_contributors/{command,registry}.py}, ui/screens/{race_setup/screen.py, strategy_renderer.py, empire_build_queue_window.py, builder/{stat_getters,stats_config}.py, new_game_setup_screen.py, new_game_setup_controller.py, strategy_screen_assets.py}, ui/panels/battle_panels.py, strategy/{quickstart_builder.py, services/galaxy_pathfinding_service.py, data/pathfinding.py}, app.py, assets/asset_manager.py}`, `docs/04_SERVICES.md`
- Tests (~13 files updated for migrated patch targets and renamed APIs)

Reference:
- `Reviews/results/2026-05-07_220621_legacy-audit/`
- `Projects/active_projects/PROJ-392/findings/verification_report.md` (especially Implementation-Time Corrections)

## Instructions

## Instructions

**Quick verification of a 12-task catch-all cleanup. The 7 audit-claim corrections in verification_report.md are the most interesting territory.**

1. **Final grep for each of the 12 deleted/renamed symbols.** Confirm zero remaining production references:
   - `_priority_sort_key`, `name_input` (placeholder context), `expanded_ships` (as `panel.expanded_ships` reads), 3 `_load_*_image` wrappers, 2 `get_quickstart_*_dir` wrappers, `find_path_deep_space`, `priority_sort_key` (in command.py, not the dispatch registry key), `_menu_scene` (private form), `get_asset_manager` (no `_default` prefix), `_get_sector_text` (instance method form), `get_crew_required` (function form, NOT the dispatch-registry KEY which had to remain), `validate_save_name`/`generate_default_save_name` (as `NewGameSetupScreen.<method>` static-form callers).

2. **The dispatch-registry-key retention (LEG-03-016).** Agent kept the dispatch-registry KEY `'get_crew_required'` because it's referenced by name in two JSON data files. Verify:
   - `grep -E '"get_crew_required"' data/stats_layout.json data/stats_sections.json` returns hits.
   - The renamed function is registered under the legacy key, so the data file references still resolve.
   - This is NOT a Rule 3 violation — the data-file string is a configuration value, not a code shim.

3. **Audit-vs-actual call-site corrections.** Agent flagged 7 audit underestimates:
   - LEG-03-025 'expanded_ships': audit said 0 readers, found 14.
   - LEG-02-007 'name_input': 2 test fixture references audit missed.
   - LEG-01-007: 4 mock.patch targets needed.
   - LEG-01-009: 4th caller in `test_save_round_trip_phase4.py:42`.
   - LEG-04-006: actual count was 9 + 2 (audit said 2).
   For each, spot-check the agent's migration:
   - Is the migration semantically correct (i.e., the new code does what the old code did)?
   - Did anything get dropped? (e.g., a test assertion that was actually about the wrapper indirection)

4. **`_menu_scene` → `menu_scene` rename.** Verify:
   - Every `screen._menu_scene` and `Game._menu_scene` reference renamed to public form.
   - The setter still works (or was removed if unused).
   - The PROJ-381 `Game.running` legacy duplicate-flag work didn't accidentally introduce another `_menu_scene` reference.

5. **`get_asset_manager` → `get_default_asset_manager` migration.** ALL callers should now use the canonical form. Verify by grep.

6. **`_get_total_crew_requirement` rename.** The underscore-prefixed helper was renamed to public. Verify:
   - All callers (production + tests) now use `get_total_crew_requirement` (no underscore).
   - The dispatch registry points at the new public name.

7. **`new_game_setup_screen` static-wrapper deletion + controller indirection cleanup.** Agent noted simplifying `controller._screen.validate_save_name` indirection that 'existed solely to support pre-PROJ-328 test patches.' Verify:
   - The simplification is correct (no production code relied on the indirection).
   - The migrated tests use the new direct-call pattern.

8. **CLAUDE.md Rule 3 compliance.** No replacement shim anywhere. The dispatch-registry-key retention is configuration, not a shim — confirm.

## Severity guidance

- CRITICAL: any deleted symbol still referenced; dispatch-registry KEY retention turns out to be a Rule 3 violation; static-wrapper deletion broke production code path.
- MAJOR: audit-correction migration drops semantics; renamed helper has dangling references; `_menu_scene` setter usage broken.
- MINOR: comment polish; doc-formatting drift.
- INFO: cleanup follow-up.

## Context

## Context

Last of 11 sequential PROJ runs. Stage 3 closeout. Catch-all bundle for misc legacy items.

The agent flagged 7 audit underestimates — all handled in-stride during implementation. This is a useful signal for the legacy-audit refinement-feedback channel: the audit's call-site counting heuristic systematically undercounts when test files use `mock.patch` against the production module rather than directly importing the symbol.

Final sharded suite from the agent: 19729/19740 (5 pre-existing failures + 2 pre-existing collection errors, all confirmed unrelated by stash-and-rerun).

## Expected Deliverable

Standard review report. Headline questions: (1) Are all 12 symbols GONE? (2) Is the dispatch-registry-KEY retention legitimate config-vs-shim? (3) Did the audit-underestimate corrections preserve semantics?

**Status:** in-progress

**PickedUp:** 2026-05-09T03:09:42Z
