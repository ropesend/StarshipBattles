# BUG-115: New Game Setup — Cancel button does not work

## Description
On the new-game setup flow, the Cancel button does not respond when
clicked. The button is visible and looks interactive, but pressing it
has no effect — the screen remains open and no cancel action is
performed.

Reported during QA Session 20260426_083959 at 08:43:54 while the user
was on the Race Setup screen.

### Suspected scope
Two Cancel buttons exist in the new-game flow and the report could
apply to either (or both):

- **Race Setup screen** — [race_setup_screen.py:629-634](../../../game/ui/screens/race_setup_screen.py#L629-L634)
  (`btn_cancel`), handler at [race_setup_screen.py:904-908](../../../game/ui/screens/race_setup_screen.py#L904-L908)
- **New Game Setup screen** — [new_game_setup_screen.py:191-196](../../../game/ui/screens/new_game_setup_screen.py#L191-L196)
  (`btn_cancel`), handler at [new_game_setup_screen.py:525-529](../../../game/ui/screens/new_game_setup_screen.py#L525-L529)

Both handlers call `on_cancel_callback()` then `self.kill()`, which
matches the working Cancel pattern at
[save_selection_window.py:348-352](../../../game/ui/save_selection_window.py#L348-L352).
Wiring looks correct on paper.

### Initial investigation hints
- An Explore-agent investigation suggested the most likely cause is
  in `NewGameSetupScreen.process_event()` around
  [new_game_setup_screen.py:357-358](../../../game/ui/screens/new_game_setup_screen.py#L357-L358),
  which may early-return while the race-setup modal is active and
  swallow the cancel click before the button check runs.
- No log entry was recorded at the timestamp of the click, which is
  consistent with the click event never reaching the intended handler.

### Reproduction
1. Launch the game.
2. Start a new game and proceed to the Race Setup screen.
3. Click the Cancel button.
4. **Expected:** screen closes, returns to previous screen.
5. **Actual:** nothing happens.

(Worth confirming whether the Cancel button on the parent New Game
Setup screen has the same issue.)

## Priority
Medium

## Status
Awaiting Confirmation

## Work Log
- 2026-04-26: Created from QA Session 20260426_083959.
- 2026-04-27: Investigation + fix (investigator-bug-115).
  - **Root cause:** the title-bar `[X]` close on `RaceSetupScreen` only
    called `self.kill()` (default `UIWindow` behaviour) and never
    invoked `on_cancel_callback`. The parent
    `NewGameSetupScreen._on_race_dialog_cancelled` therefore never ran,
    so `self.active_race_modal` stayed truthy after the modal was
    destroyed. The early-return guard at
    `new_game_setup_screen.py:354-358` (`if self.active_race_modal is
    not None: return super().process_event(event)`) then silently
    swallowed every subsequent `UI_BUTTON_PRESSED` for the parent's
    Cancel / Start / Load Species / Setup Species buttons.
  - **Fix A** (`game/ui/screens/race_setup/screen.py`): override
    `on_close_window_button_pressed` to delegate to
    `self._controller.on_cancel()`. That controller method already
    invokes `on_cancel_callback()` and then `kill()` — single
    canonical cancel path for both [X] and `btn_cancel`.
  - **Fix B** (`game/ui/screens/new_game_setup_screen.py`): drop the
    `active_race_modal` early-return guard. pygame_gui z-orders child
    windows above the parent and dispatches events to them first, so
    the modal already has first crack at events; the guard added no
    safety beyond what pygame_gui already provides and made the parent
    fragile to any path that left `active_race_modal` stale.
  - **Tests added:**
    - `tests/unit/ui/screens/test_race_setup_screen.py::TestBug115CloseButtonInvokesCancel`
      (2 tests) — `[X]`-close fires `on_cancel_callback` + delegates
      through `controller.on_cancel`.
    - `tests/unit/ui/screens/test_new_game_setup_extended.py::TestBug115CancelAfterModalLeak`
      (2 tests) — parent `btn_cancel` and `btn_start` still dispatch
      when `active_race_modal` is a stale (killed) reference.
  - **Test results:** 4/4 new BUG-115 tests pass. Targeted
    `tests/unit/ui/screens/` 2131/2131 pass. Broader UI scope
    `tests/unit/ui/ + tests/integration/ui/` 3733/3733 pass. Full
    sharded suite reports 10 unrelated failures, all in
    `tests/unit/workshop/test_move_component.py` — these are caused
    by the parallel BUG-116 WIP changes to `data/vehiclelayers.json`
    in the same working tree, not by BUG-115.
  - **Out of scope / follow-up:** `RaceBrowserDialog` (created at
    `new_game_setup_screen.py:421-427`, also tracked via
    `active_race_modal`) likely has the same latent bug — its
    `[X]`-close probably bypasses `on_cancel_callback` too. Recommend
    a follow-up ticket to audit and apply the same `Fix A` pattern
    there. Out of scope for this PR per coordinator instruction.
  - **Branch:** main.
