# BUG-101: Fleet Info Panel Shows Raw Enum Names for MOVE_TO_FLEET and JOIN_FLEET Orders

## Description
The lower-right fleet info panel on the strategy map displays orders like "MOVE_TO_FLEET" and "JOIN_FLEET" as raw enum names, without showing the target fleet ID. The Orders dialog window correctly formats these as "INTERCEPT Fleet 10000" and "JOIN Fleet 10000".

### Screenshots

Orders dialog correctly showing target fleet IDs:
[![Orders dialog showing INTERCEPT Fleet 10000 and JOIN Fleet 10000](../../Tools/qa_observer/session_data/20260323_143412/images/bug_capture_144835.png)](../../Tools/qa_observer/session_data/20260323_143412/images/bug_capture_144835.png)

Fleet info panel showing raw enum names without targets:
[![Fleet info panel showing MOVE_TO_FLEET and JOIN_FLEET without target IDs](../../Tools/qa_observer/session_data/20260323_143412/images/bug_capture_144854.png)](../../Tools/qa_observer/session_data/20260323_143412/images/bug_capture_144854.png)

### Root Cause
`strategy_detail_fmt.py:_format_orders()` (lines ~278-328) doesn't handle `MOVE_TO_FLEET` and `JOIN_FLEET` order types. They fall through to the generic `else` clause at line ~326 which renders `order.type.name`.

The correct formatting logic already exists in `fleet_orders_window.py:_get_order_description()` (lines ~185-220), which uses `is_fleet(order.target)` to extract the target fleet ID and formats as "INTERCEPT Fleet {id}" / "JOIN Fleet {id}".

### Fix Reference
Add MOVE_TO_FLEET and JOIN_FLEET cases to `_format_orders()` in `strategy_detail_fmt.py`, matching the pattern from `fleet_orders_window.py`:
```python
elif order.type == OrderType.MOVE_TO_FLEET:
    f_id = order.target.id if is_fleet(order.target) else "?"
    text += f" {i+1}. Intercept Fleet {f_id}<br>"
elif order.type == OrderType.JOIN_FLEET:
    f_id = order.target.id if is_fleet(order.target) else "?"
    text += f" {i+1}. Join Fleet {f_id}<br>"
```

## Priority
Low

## Status
Awaiting Confirmation

## Investigation Report

### Code Path Trace
```
StrategyScreen (user selects fleet)
  → StrategyDetailFormatter._format_fleet()
    → format_fleet_info() [strategy_detail_fmt.py:331-366]
      → _format_orders() [strategy_detail_fmt.py:278-328] ← BUG HERE (else clause at line 326)
```

### Dependency Map
**Callers of `_format_orders()`:**
- `format_fleet_info()` in `strategy_detail_fmt.py:364`
- `tests/unit/ui/screens/test_strategy_detail_fmt.py` (direct test calls)
- `tests/repro_warp_bug.py` (debug script)

**Working reference:** `fleet_orders_window.py:_get_order_description()` (lines 185-220) handles ALL order types correctly.

### Similar Patterns Found
`fleet_orders_window.py` uses `is_fleet()` from `game.core.protocols` (Protocol + TypeGuard pattern per `docs/02_PATTERNS.md`) to safely extract target fleet ID. Format: "INTERCEPT Fleet {id}" / "JOIN Fleet {id}".

### Coverage Gap (WIDER THAN REPORTED)
`_format_orders()` is missing explicit handlers for **7 order types**, not just 2:

| Order Type | In `fleet_orders_window`? | In `_format_orders()`? |
|---|---|---|
| MOVE_TO_FLEET | Yes ("INTERCEPT Fleet {id}") | **MISSING** |
| JOIN_FLEET | Yes ("JOIN Fleet {id}") | **MISSING** |
| LOAD_POPULATION | Yes ("load cargo") | **MISSING** |
| UNLOAD_POPULATION | Yes ("drop cargo") | **MISSING** |
| IMPLODE_PLANET | Yes | **MISSING** |
| STELLERATE_STAR | Yes | **MISSING** |
| CREATE_DYSON_SPHERE | Yes | **MISSING** |

### Git History Analysis
- **2026-01-14:** MOVE_TO_FLEET and JOIN_FLEET added to OrderType enum
- **2026-02-25:** `_format_orders()` last updated (PROJ-193 Phase 4) — added OPEN_WARP_POINT/CLOSE_WARP_POINT but overlooked MOVE_TO_FLEET/JOIN_FLEET which already existed
- **Conclusion:** Simple omission during PROJ-193, not a regression

### Documentation Discrepancies
**Code vs docs mismatches:** None — code matches docs. `docs/systems/orders_system.md` correctly documents both order types and their targets.
**Docs last updated:** 2026-02-20 (orders_system.md)
**Code last updated:** 2026-02-25 (strategy_detail_fmt.py)

**Docstring issue:** `_format_orders()` docstring (line 282) claims it "Handles MOVE, COLONIZE, BUILD, TRANSFER, and generic order types" but actually also handles WARP, OPEN_WARP_POINT, CLOSE_WARP_POINT — and still misses 7 other types.

## User Context

**Scope Decision:** User confirmed fixing all 7 missing order types (not just the 2 reported).
**Process Decision:** Skipped diagnostic logging — root cause was already confirmed.

## Hypothesis Log

### Hypothesis 1: Simple omission during PROJ-193 - CONFIRMED
**Theory:** MOVE_TO_FLEET/JOIN_FLEET were in the enum before `_format_orders()` was last updated in PROJ-193, but were overlooked when adding WARP/OPEN_WARP_POINT/CLOSE_WARP_POINT handlers.
**Evidence For:** Git history confirms MOVE_TO_FLEET/JOIN_FLEET added 2026-01-14, `_format_orders()` last updated 2026-02-25 — over a month later.
**Evidence Against:** None.
**Test:** Added 9 new tests covering all 7 missing order types. All 9 failed before fix, all 9 pass after.
**Result:** CONFIRMED — simple omission, no deeper issue.

## Work Log
- 2026-03-23: Created from QA Session 20260323_143412. Root cause identified during triage.
- 2026-03-24: Deep Investigation (Protocol 02b). Agent swarm confirmed root cause: simple omission in `_format_orders()`. Discovered wider gap — 7 order types missing, not just 2.
- 2026-03-24: TDD fix applied. Added 9 new tests + explicit handlers for all 7 missing order types in `_format_orders()`. Updated docstring. No docs changes needed (code now matches documented patterns).
