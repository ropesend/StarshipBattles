# BUG-119: Plasma Storm sector effects not displayed (storm hex coordinate-frame mismatch)

## Description
A Plasma Storm declares two `scope: "sector"` abilities in `data/storms.json`
(`ShieldModifier` 0.7x and `EnvironmentalDamage` 0.5/turn plasma). The storm's
hover tooltip says *"Effects appear in the Sector Effects panel."* When the
user clicks on a hex occupied by the storm, the Sector panel shows the storm
reference and "Local Radiation Analysis" — but **no Sector Effects entries**
are listed.

The same problem affects every storm type (Ion Storm, Plasma Storm, Magnetic
Storm, Radiation Storm, Gravimetric Storm) since they all share the same
sector-scope ability registration path.

Reproduced in QA Session 20260427_151244 at 15:23–15:24:

[![Galaxy view: 3-hex Plasma Storm cluster](../../../tools/qa_observer/session_data/20260427_151244/images/bug_capture_152330.png)](../../../tools/qa_observer/session_data/20260427_151244/images/bug_capture_152330.png)

[![Sector panel: storm reference shown but no Sector Effects rows](../../../tools/qa_observer/session_data/20260427_151244/images/bug_capture_152340.png)](../../../tools/qa_observer/session_data/20260427_151244/images/bug_capture_152340.png)

[![Plasma Storm description: "Effects appear in the Sector Effects panel"](../../../tools/qa_observer/session_data/20260427_151244/images/bug_capture_152447.png)](../../../tools/qa_observer/session_data/20260427_151244/images/bug_capture_152447.png)

(System view's red-giant thermal damage system effect is correct and unrelated:
[![System view](../../../tools/qa_observer/session_data/20260427_151244/images/bug_capture_152347.png)](../../../tools/qa_observer/session_data/20260427_151244/images/bug_capture_152347.png))

## Root Cause — Coordinate-frame mismatch
- `Storm.location` and `Storm.occupied_hexes` are **local** system coordinates
  (`game/strategy/data/storm.py:50-58`: "Returns all hexes occupied by this
  storm in local system coordinates.").
- `StormAbilitySource.affects_hex(hex_coord)` does a raw
  `hex_coord in self.storm.occupied_hexes`
  (`game/strategy/services/ability_sources/storm.py:40-49`).
- The Sector panel passes the **global** hex from the strategy map view
  (sector header in screenshot reads `[1969, 817]` — well outside the
  ~101-diameter local system frame).
- Result: every `affects_hex` call returns False; the storm never yields
  abilities; the collector returns empty; the UI never renders the Sector
  Effects group.

The matching planet path translates correctly via `_planet_global_hex` in
`game/strategy/services/ability_iterator.py:162-175` — the storm provider has
no equivalent translation.

## Suggested Fix
Add a global→local (or local→global) translation in `_storm_provider` /
`StormAbilitySource.affects_hex`, mirroring `_planet_global_hex`. Easiest:
have `_storm_provider` translate `hex_coord` from global to system-local
before constructing/querying adapters, or compute the storm's global
occupied hexes (`system_loc + storm.location + offset`) and compare in global
frame.

Verify the fix with the existing storm types in `data/storms.json`. The
Sector Effects panel should show `ShieldModifier x0.7` and
`EnvironmentalDamage 0.5 plasma/turn` (or formatted equivalents) on each of
the storm's hexes.

## Priority
High

## Status
Awaiting Confirmation

## Work Log
- 2026-04-27: Created from QA Session 20260427_151244.
- 2026-04-27: Investigation confirmed triage diagnosis exactly. `StormAbilitySource.affects_hex` did a raw `hex_coord in self.storm.occupied_hexes` while sibling adapters (Planet/Star/WarpPoint) all translate `system.global_location + entity.location` first. Bug was masked in tests by every fixture using `global_location=HexCoord(0, 0)`.
- 2026-04-27: Fix applied. `StormAbilitySource` now accepts an optional `system: Any = None` and translates `sys_loc + storm.location + offset` inside `affects_hex` (mirrors `PlanetIntrinsicAbilitySource`). `_storm_provider` in `ability_iterator.py` passes `system=system`. Legacy local-frame fallback retained for unparented mock fixtures (`_StormWithAbilities`, `_MixedKindStorm`, `_StormWithBadScope`) that supply `occupied_hexes` directly.
- 2026-04-27: Three masking tests in `test_storm.py` / `test_ability_iterator.py` used `HexCoord(0, 0)` origin and silently masked the bug — updated to non-zero origin to make local-vs-global meaningful. Two new red-first regression tests added: one in `test_ability_iterator.py` exercising the iterator path with non-zero `global_location`, one in `test_system_effects_collector.py` (`TestBug119StormGlobalCoordinateFrame`) exercising the full collector→iterator→adapter pipeline end-to-end. Added one-line docstring note ("Operates in the GLOBAL galaxy-map frame; local entity coordinates are translated via `system.global_location`") to `affects_hex` on `StormAbilitySource`, `PlanetIntrinsicAbilitySource`, `StarAbilitySource`, `WarpPointAbilitySource`.
- 2026-04-27: Targeted tests 117/117 pass (strategy services + ability_sources scope). Full sharded suite 15700/15700 pass, 0 failures.
