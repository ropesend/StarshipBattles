# BUG-120: Planet "Yard" resource row shows yard capacity instead of actual queue consumption

## Description
On the Planet detail view (resource summary table), the **Yard** row reports
the same magnitude across every resource (Metals / Organics / Vapors /
Radioactives / Exotics — all `-22,106.5/turn` in the captured save), even
though the planet's Build Queue contains a single item (`bs_battleship_gc`)
that consumes only Metals 30,000, Radioactives 14,185, Exotics 18,788 per
turn (and nothing in Organics or Vapors).

The two views disagree on actual per-resource consumption:

| Resource | Planet "Yard" row | Build queue per-turn | Reality |
|---|---|---|---|
| Metals | -22,106.5 | 30,000 | 30,000 (single item) |
| Organics | -22,106.5 | — (0) | 0 |
| Vapors | -22,106.5 | — (0) | 0 |
| Radioactives | -22,106.5 | 14,185 | 14,185 |
| Exotics | -22,106.5 | 18,788 | 18,788 |

Reproduced in QA Session 20260427_151244 at 15:49:

[![Planet detail — Yard row showing uniform -22,106.5 across all resources](../../../tools/qa_observer/session_data/20260427_151244/images/bug_capture_154924.png)](../../../tools/qa_observer/session_data/20260427_151244/images/bug_capture_154924.png)

[![Build Queue — single battleship item with per-resource per-turn cost](../../../tools/qa_observer/session_data/20260427_151244/images/bug_capture_154950.png)](../../../tools/qa_observer/session_data/20260427_151244/images/bug_capture_154950.png)

## Root Cause — Two different formulas
- `_project_yard_drain()` in
  `game/strategy/services/planet_economy_projector.py:131-156` sums each
  yard's `build_rate` (the yard's maximum capacity per resource — uniform
  22,106.5 across all 5 resources at this yard's size/level). It never
  inspects what is queued.
- `get_resource_rate_text()` in
  `game/ui/screens/empire_build_queue_formatter.py:147-167` reads
  `cost_per_tick` from the first queued item and multiplies by 100
  ticks/turn. Real per-item consumption.

The Planet view is therefore showing **yard capacity**, not **actual draw**.

## Fix
Rewrite `_project_yard_drain()` so it iterates the queued items on each yard
source and sums their `cost_per_tick * 100` per resource (the same source of
truth used by the build queue panel). Result:
- Empty queue → no Yard line draws (or zero across the board).
- Queue with items → Yard row equals the per-resource sum of currently-firing
  items, matching what the Build Queue panel reports.

If both views need a "yard capacity" alongside "actual draw", split into two
rows (e.g., "Yard Capacity" vs "Yard Drain") rather than overloading one
ambiguous row.

## Priority
High

## Status
Awaiting Confirmation

## Work Log
- 2026-04-27: Created from QA Session 20260427_151244.
- 2026-04-27: Investigation by investigator-bug-120 confirmed the triage diagnosis. Root cause is the inner loop in `_project_yard_drain` summing `BuildQueueSource.build_rate.items()` — that dict is the yard's per-resource **capacity** (uniform 30000/turn for `space_shipyard` from `data/production_rates.json`, scaled by ~0.737 habitability = the QA-observed -22106.5). The canonical "actual draw" calculation already exists at `game/strategy/engine/construction_forecast.py::forecast_queue_turn_spend` and was already in production use by `EmpireEconomyCalculator._aggregate_construction_expenses` (Treasury, line 287). Fix: plumb the same helper into the projector. Recommended against splitting into "Yard Capacity" + "Yard Drain" rows — no consumer of capacity numbers exists in the source tree.
- 2026-04-27: Implementation TDD-Red phase. Added 3 new tests under `TestYardDrainMatchesQueueItemCosts` (drain-zero-for-non-cost-resources, cost-bound-clamp, carry-over-to-next-item). Updated 2 existing tests in `TestYardDrain` (`test_single_queued_complex_drains_per_turn_spend`, `test_multi_queue_aggregates_drains`) and 2 fixture stanzas in `TestNetIs...` + `TestHabitabilityScales...` so queue items declare `total_cost`. Confirmed 4 of 5 affected tests fail under the buggy code (the carry-over test happens to coincidentally pass against capacity-summing logic but pins post-fix carry-over behaviour as a regression guard).
- 2026-04-27: Implementation TDD-Green phase. Rewrote `_project_yard_drain` (`game/strategy/services/planet_economy_projector.py:131-178`) to delegate to `forecast_queue_turn_spend` per-source. Habitability is applied to each source's `build_rate` BEFORE the forecast walk, mirroring `ProductionEngine._process_queue_tick_dynamic` line 291-296. Resources the queued items don't cost are filtered out with `if amount > 0` guard so the result dict stays sparse. ~25 LOC change, no signature changes, no new module.
- 2026-04-27: Tests after fix:
  - `tests/unit/strategy/services/test_planet_economy_projector.py` — 11 / 11 passed (was 8 / 8 baseline; 3 new BUG-120 regression tests).
  - `tests/integration/strategy/test_projector_drain_matches_engine.py` — 3 / 3 passed; module docstring's "ghost projections, not bugs" caveat removed (the projector and engine now agree on every resource, not just `item_cost_resources`).
  - `tests/unit/strategy/ + tests/integration/strategy/` — 3521 passed, 1 skipped.
  - `tests/unit/ui/` — 3540 passed (consumers `planet_report_panel`, `planet_list_window`, `build_queue_panel_factory`, `strategy_detail_formatter` all stable).
  - Full sharded suite (`Tools/test_sharded/test_sharded.py`) — 15706 tests, 15705 passed, 1 pre-existing unrelated test-isolation flake (`test_collect_movements_respects_speed`, fleet movement engine — passes in isolation; no file overlap with BUG-120's touched files).
- 2026-04-27: Files modified:
  - `game/strategy/services/planet_economy_projector.py` — `_project_yard_drain` body rewritten.
  - `tests/unit/strategy/services/test_planet_economy_projector.py` — 3 new tests, 4 existing tests' fixtures updated.
  - `tests/integration/strategy/test_projector_drain_matches_engine.py` — module docstring updated.
  - `docs/04_SERVICES.md` — `PlanetEconomyProjector._project_yard_drain` description rewritten; `Last verified` bumped.
  - `docs/systems/strategy_layer.md` — equivalence-contract paragraph extended; BUG-120 callout added; `Last verified` bumped.
  - `docs/systems/production_system.md` — `forecast_queue_turn_spend` cross-linked into the Queue Discovery / Rate Resolution table; `Last verified` bumped.
- 2026-04-27: Smoke-test guidance for the user — load the QA save (or any save with a planet whose shipyard has a queued battleship), open the Planet detail. The "Yard" row should now show non-uniform per-resource numbers that match the build queue panel's per-turn rate column (Metals 30,000, Radioactives 14,185, Exotics 18,788, Organics 0, Vapors 0). Empty queues should produce a blank "Yard" row across the board.
