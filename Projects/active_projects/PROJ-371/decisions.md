# PROJ-371: Decisions Log

> **LOG ALL DECISIONS HERE**
> When you make a design choice or the user specifies a preference, add it to this table.
> Future agents will reference this to understand why things were done a certain way.

| Date | Decision | Rationale |
|------|----------|-----------|
| 2026-05-05 | Project initialized | Strategy-layer review (`AgentCoordination/Scratchpad/reviews/strategy_layer_tech_debt_2026-05-05.md:73-91`) flagged `specs.py` (661 LOC, 35 specs) as the #4 extensibility target. PROJ-273 + PROJ-278 are the pattern sources. PROJ-363 already landed the consolidated tuple; PROJ-371 takes the next step (self-registering registry). |
| 2026-05-05 | Three-phase plan: introduce alongside → migrate consumers → docs+regression | Two phases (introduce+migrate, then docs) is tight; three phases give a clean SHA-pinned review boundary between "new infrastructure exists" and "old infrastructure deleted." Phase 1 ships a bit-identity contract; Phase 2 deletes; Phase 3 documents + smoke-tests. |
| 2026-05-05 | `CommandRegistry` is a class with `__slots__`, NOT a module-level dict | PROJ-273 used a module-level dict for `ABILITY_STAT_REGISTRY`. PROJ-371 has more behaviour (helper methods like `movement_order_types()`, `order_to_ability_map()`, `specs_by_facade_helper()`). A class is the right shape. |
| 2026-05-05 | Global singleton `command_registry` (lower-case) — NOT `COMMAND_REGISTRY` (upper-case) | PROJ-273 used UPPER for an immutable dict; PROJ-278 used lower for a stateful registry. `command_registry` is mutable (mods add commands at import time), so lower-case is correct. |
| 2026-05-05 | Registration via `@command_spec(**fields)` decorator on the handler class | Most idiomatic; colocates metadata with handler. The decorator wraps the class, instantiates `CommandSpec(handler_class=cls, **fields)`, and calls `command_registry.register(spec)`. Returns the class unchanged. Alternative D in design.md (separate manifest module) was rejected. |
| 2026-05-05 | Programmatic `command_registry.register(spec)` is also public | Phase 3's third-party smoke test needs to register a `FakeModCommand` at test setup time, where decorator syntax doesn't fit. Both surfaces are public. Alternative B in design.md (decorator-only) was rejected. |
| 2026-05-05 | Conflict policy is a boolean `replace=True` flag, NOT the full `RegistrationConflictPolicy` enum from PROJ-278 | A Command DTO can have only one handler — there's no APPEND semantics. Boolean is sufficient. Future mod work could expand to enum if needed. |
| 2026-05-05 | `seed_default_commands()` is idempotent (boolean guard); called by `create_default_registry()` if registry is empty | Mirrors PROJ-367's `_seed_builtin_contributors()` decision. Avoids "imported the registry but didn't seed" failure mode. |
| 2026-05-05 | `reset_command_registry()` (test helper) clears AND re-seeds defaults | Same idiom as `reset_stat_contributor_registry` from PROJ-367 (decision 2026-05-05 — clears and re-seeds, idempotent). Conftest will use this helper. |
| 2026-05-05 | `CommandSpec` dataclass moves from `specs.py` to `registry.py` at Phase 1 Task 1.2 | Co-locating the dataclass with the registry that owns it. Phase 1 keeps a re-export shim in `specs.py` for the duration of Phase 1 — both paths work. Phase 2 deletes `specs.py`. |
| 2026-05-05 | `data/order_types.py` frozensets stay as module-level constants — NOT migrated to instance methods on the registry | `data/order_types.py` is a leaf in the import graph; importing `commands.registry` from there creates a cycle (commands import OrderType from order_types). Pinned by contract test (Phase 2-updated to read `command_registry.X()`). |
| 2026-05-05 | Local `MOVEMENT_ORDER_TYPES` frozenset at `action_time_resolver.py:48` is **deleted** in Phase 2 | Redundant + silently drifted (lacks `WARP`, while `data/order_types.py:58-62` has it). One-line free-find as part of the migration. |
| 2026-05-05 | `strategy_session_facade.py:186-300` (31 hand-written `dispatch_*` forwarders) collapses to `__getattr__` in Phase 2 | Missed by PROJ-363 Phase 4 (which only collapsed the slice-level forwarders). Mirrors `command_dispatch_slice.py:72`. ~150 LOC removed. |
| 2026-05-05 | Save/replay determinism is N/A — commands are NOT persisted | Verified: only `Order` objects are persisted (`game/strategy/data/order_types.py:108-181`). Registry insertion order matters only for human-readable logging and test reproducibility, not save correctness. The "registry must be deterministic" concern from the project description does not apply here. |
| 2026-05-05 | AST regression test forbids `COMMAND_SPECS = (CommandSpec(...), ...)` module-level tuple literals in `game/` | Mechanical guard against tuple-literal reintroduction. Walks `game/`, parses each `.py`, matches `ast.Assign` with tuple-literal RHS containing `Call(func=Name('CommandSpec'))` elements. Allowed in `tests/`. |
| 2026-05-05 | `tests/fixtures/command_registry.py` provides `register_test_command` / context-manager snapshot helpers; production code never unregisters | Production code never unregisters. Tests need clean isolation; the helper snapshots the registry, lets the test register/dispatch, then restores the snapshot. |
| 2026-05-05 | Cross-link to PROJ-368 declared but no shared infrastructure | PROJ-368 (OrderProcessor decomposition) introduces `OrderHandlerRegistry` keyed by `OrderType`. PROJ-371's `CommandRegistry` is keyed by Command DTO class. Different layer; no file overlap. Either project can land first. |
| 2026-05-05 | Cross-link to PROJ-364 preserved | PROJ-364's `superweapon_registry.SUPERWEAPONS` is asserted against `COMMAND_SPECS` at `tests/unit/strategy/services/test_superweapon_registry_contract.py:149`. Phase 2 updates that test to read `command_registry.all()` — same assertion, new source. |
| 2026-05-05 | `CommandHandlerRegistry` (existing class at `engine/handlers/base.py:362`) is **NOT** renamed | Different concept (runtime dispatch table vs metadata registry). Renaming would churn 24 files for no gain. Document the distinction in the new `registry.py` docstring. |
| 2026-05-05 | Open questions remaining for user | See design.md § "Open questions for the user" — 6 questions on decorator vs explicit, conflict-policy granularity, naming, unregister support, action_time_resolver dead-code scope, phase count. The plan reflects the architect's recommendation on each; the user may overrule. |
| 2026-05-06 | Codex+Claude joint review: applied r004 edits — see `AgentCoordination/Scratchpad/Discussion/20260506T024652Z_strategy-tech-debt-plans-review/outcome.md`. | E371-1..3: `@command_spec` is METADATA-ONLY sugar — attaches `__command_spec_kwargs__`, returns class unchanged, does NOT register at import time. Each handler module exposes `def register(registry)` calling `registry.register(CommandSpec(handler_class=H, **H.__command_spec_kwargs__))`. `seed_default_commands(registry)` imports modules then calls each module's `register()`. `reset_command_registry()` = clear + seed (no `_SEEDED` flag, no `importlib.reload`). New Phase 1 Task 1.2.5 pins the "import does not mutate registry" invariant. Avoids the duplicate-registration foot-gun where decorator + seed would double-register after reset. |
