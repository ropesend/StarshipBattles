# PROJ-494 Codex Audit Verification (2026-05-23)

Consult leaf: `AgentCoordination/Scratchpad/Consult/20260523T144314Z_audit-PROJ-494/`
Codex response: `AgentCoordination/Scratchpad/Consult/20260523T144314Z_audit-PROJ-494/response.md`

| id | verdict | evidence | action |
|----|---------|----------|--------|
| F1: T3.14 battle-over + input-handler sub-clusters skipped | VERIFIED + IN-SCOPE (partial) | `tests/unit/ui/test_battle_screen_simulation.py:175-222` shows 4 winner-determination tests (3 are clean `(kill_setup, expected_winner)` parametrize candidates; 1 needs extra ship). `tests/unit/ui/test_battle_screen_simulation.py:289-369` shows 5 event-handler tests with heterogeneous setup. Task wording in `Projects/archived_projects/PROJ-480/phase_3_checklist.md:262-268` says "3 clusters". Implementer did 1 cluster and noted the other 2 needed "deeper refactor" in the task body but still marked `[x]`. | Phase 5: parametrize the 3 winner tests, document why input-handler cluster stays distinct, mark T3.14 sub-status accurately. |
| F2: T3.15 margin cluster skipped | VERIFIED + IN-SCOPE (partial) | `tests/unit/research/test_research_renderer.py:149-184` shows 4 directional margin tests with symmetric `(axis_side, edge_pos, margin, expected)` shape — clean parametrize. The remaining 3 (all_corners, zero_margin, large_margin) are heterogeneous and reasonably stay distinct. PROJ-480 task at `phase_3_checklist.md:271-276` named both clusters. Implementer marked `[x]` after doing only visibility cluster. | Phase 5: parametrize the 4 directional margin tests on `(pos_outside, pos_far_outside, margin, expected_outside, expected_far)`. |
| F3: T4.6 workshop assertion too loose | VERIFIED + IN-SCOPE | `tests/unit/ui/screens/test_workshop_event_router_select_component.py:99-100` asserts `mass > 50` and `mass < 150`. PROJ-480 task wording at `Projects/archived_projects/PROJ-480/phase_4_checklist.md:54` explicitly required `mass / expected_base in [0.9, 1.1]` — i.e., a relative-ratio band against the formula's expected base, not a fixed upper bound. The current `< 150` lets a 30%+ calculation error slip through. | Phase 5: replace `mass < 150` with `0.9 <= mass / (50 * math.sqrt(2.0)) <= 1.1` (the relative-ratio formula from task), preserving the lower bound. Keep `mass > 50` as a separate sanity assert. |
| F4: orphan helpers | REJECTED | Codex confirmed `tests/fixtures/ship_mocks.py` is imported by `tests/unit/ui/screens/test_fleet_report_filters.py:11-13` and the new `tests/integration/ui/conftest.py` additions are consumed by `tests/integration/ui/test_build_queue_formatting.py:15-17`. Codex's own commentary about sibling-doubles (e.g., `tests/integration/ui/build_queue_screen/conftest.py`) is out-of-scope cleanup, not a PROJ-494 deviation. | None (Codex itself did not raise this as a formal finding). |
| F5: false-stale drops | REJECTED | Codex confirmed Phase 0 had no false-stale drops. `Projects/active_projects/PROJ-494/phase_0_checklist.md:17-30` shows the only Phase-0-era drop (T1.3) is still correct — `_make_fleet`/`_make_galaxy`/`_mapper` are already module-level in `tests/unit/ui/screens/test_fleet_menu_items.py:53-99`. | None. |
| F6: T3.3 / T3.10 reclassification | CONFIRMED CORRECT | Codex agrees the 3 system-selection-window contracts (confirm-with-selection, cancel-without-callback, no-selection-no-op) are too heterogeneous to parametrize. Same for the 2 instance_label tests with different setup. | None — implementer's call stands. |
| Risk: test_list_windows centered-window contract | NOT A FORMAL FINDING | Codex flagged but did not elevate. Task wording at `Projects/archived_projects/PROJ-480/phase_4_checklist.md:71-75` explicitly allowed shape-based assertions (`topleft[0] > 0`). | None — keep as-is. The test name vs. assertion drift is mild and not contract-violating. |

## Decision

3 verified in-scope findings → spawn Phase 5 remediation. Estimated work: ~5 minutes LLM time, well under the 15-minute escalation threshold.
