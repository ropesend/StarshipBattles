# PROJ-455: Decisions Log

> **LOG ALL DECISIONS HERE**
> When you make a design choice or the user specifies a preference, add it to this table.
> Future agents will reference this to understand why things were done a certain way.

| Date | Decision | Rationale |
|------|----------|-----------|
| 2026-05-18 | Project initialized | Starting point for Planet-FMS engine-mediated behavioral coverage (DI-001 ActionExecutionEngine half) |
| 2026-05-19 | Phase 1 Task 1.5 "synthetic RED via resolver-injection removal" is accepted as the RED step for the smoke test, with the explicit understanding that this is a weaker form of TDD than fail-first writing of an unimplemented behavior. | Codex audit feedback (Bucket B, 2026-05-19) noted that "labels a synthetic 'remove the resolver injection and watch it fail' check as RED; that is weaker than true fail-first TDD." For a test-only coverage project where the production behavior already exists and the work is establishing *coverage* (not driving new behavior), strict fail-first TDD is awkward — there is no missing behavior to fail against. The synthetic RED via resolver-removal proves the test is non-trivial (it observes a state change that depends on the injected resolver); the alternative RED via mutating `_build_lay_mines_scenario` (Task 1.5 also documents this) provides a second independent RED check. Both are intentional and adequate for this coverage-establishing project; no checklist change required. |
| 2026-05-19 | Phase 2 in-progress branch coverage uses a dedicated parametrised test (Option A); Option B (extend Task 2.1 with `(order_type, expected_completion)` tuples) rejected. | Codex pre-execution consult + claude subagent independent review (2026-05-19) found that Phase 2 only closed the completion branch of `_process_planet_action_tick` (`action_execution_engine.py:278-289`); the in-progress branch (`:290-297`, where `action_completed=False` is returned because `execution_progress < action_time`) is acknowledged in `findings/PROJ-455_findings.md:41-43` as uncovered but never exercised. Option A (Task 2.4: dedicated `test_process_planet_action_tick_in_progress_branch` parametrised across the same 5 order types, using `_FixedActionTimeResolver(3)` so one tick increments progress to 1 without triggering completion) was chosen over Option B (parametrise Task 2.1 across `(order_type, expected_completion)` tuples) because Task 2.1's fixture is tightly built around a single `_FixedActionTimeResolver(1)` and a single `_assert_post_dispatch_state` helper that asserts handler-level post-conditions, which only fire on completion. Bifurcating that helper for both branches would be more disruption than adding the dedicated in-progress test. Result: both branches of `_process_planet_action_tick` are covered for all 5 FMS order types. |
| 2026-05-19 | Phase 1 `test_lay_mines_e2e_smoke` retained alongside the Phase 2 parametrised `test_process_planet_action_tick_end_to_end[OrderType.LAY_MINES]` (Task 2.3 decision). | The smoke test is a fast-path debug aid — when a future regression hits, the LAY_MINES smoke is faster to isolate than re-running the full parametrised suite. The marginal cost (one extra test on the LAY_MINES path; <50ms) is negligible. |
| 2026-05-19 | `_StubPlanet.staging_yard` made into a `@property` backed by `_staging_yard` (diverges from the precedent stub in `test_fms_planet_lay_mines.py`). | `PlanetStagingYardIssuerAdapter.pop_carried` reads from `self._planet.staging_yard` (issuer_adapter.py:323) but writes via `self._planet._staging_yard = remaining` (issuer_adapter.py:335). The precedent's `_StubPlanet` keeps both as separate plain attributes, which works only because the precedent's assertions never observe post-pop yard state. PROJ-455's `_assert_post_dispatch_state` helper DOES observe the post-pop yard state for the LAY_MINES / LAUNCH_FIGHTERS / LAUNCH_SATELLITES cases, so the stub must honour the adapter's write contract. Making `staging_yard` a property backed by `_staging_yard` mirrors the real `Planet` shape and unblocks the three launch/lay parametrised cases without changing the precedent. |
| 2026-05-19 | `_assert_post_dispatch_state` adopted the looser RECOVER_* assertion shape suggested by Phase 2 Notes ("groups MAY be removed by handler policy"). | The actual handler behaviour leaves the empty `FighterWing`/`SatelliteConstellation` in `empire.deployed_groups` (verified by 5 green parametrised cases). The assertion accepts either policy (empty group remaining OR group removed) by iterating `empire.deployed_groups` and only requiring `len(group.ships) == 0` on whatever groups are still present, plus a recovered ship in the staging yard. |
