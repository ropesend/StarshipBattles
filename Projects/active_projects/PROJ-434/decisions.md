# PROJ-434: Decisions Log

> **LOG ALL DECISIONS HERE**
> When you make a design choice or the user specifies a preference, add it to this table.
> Future agents will reference this to understand why things were done a certain way.

| Date | Decision | Rationale |
|------|----------|-----------|
| 2026-05-17 | Project initialized | Spun out of PROJ-427's Phase 6 partial close. Source: [`../PROJ-427/decisions.md`](../PROJ-427/decisions.md) 2026-05-17 "Phase 6 partial" + "Project halted at Phase 0 entry; scope-vs-budget blocker" entries. |
| 2026-05-17 | Land as a separate project (not a PROJ-427 phase) | Two prior phase-6 subagents independently deferred the same scope citing 7 production files + ~30 test files + API gaps on `DesignCatalog`/`DesignRepository` + the QA-Obs-3 cache contract through `FacadeSessionState`. PROJ-427 achieved its TD-05 core intent (production off disk, replay-store instance-owned, sharded suite green 20996/20996); the remaining deletion is mechanical cleanup that can ship independently. Keeping PROJ-427's scope closed avoids reopening a five-phase-complete project for cleanup work and lets PROJ-428..PROJ-431 proceed unblocked. |
| 2026-05-17 | Adopt 03c-phase-aware-execution protocol | Matches PROJ-427's protocol and the project-system default. Lets `validate_phase.py` / `phase_complete.py` enforce intra-project gates. |
| 2026-05-17 | Three-phase shape: 0 = API extension, 1 = `BuildQueueScreen` family, 2 = remaining screens + deletion gate | Phase 0 is the gating prerequisite — every deferred caller needs at least one of the missing API methods, so the additive work must land first. Phase 1 isolates the `BuildQueueScreen` + 3 panel collaborators because they share `DesignLibrary` state and must migrate atomically. Phase 2 absorbs the three independent UI screen entry points + the ~25 remaining test files + the deletion gate + the `DesignLoadResult` shim removal. Splitting Phase 2 finer was considered but rejected — once Phase 0's API is in place and Phase 1 proves the migration pattern, the remaining screens are independent of each other and can land in one phase. |
| 2026-05-17 | Phase 0 stays additive — no `DesignLibrary` deletion until Phase 2 | Mirrors PROJ-427's Phase 1/2 additive discipline. Phase 0 adds the missing methods on `DesignCatalog` / `DesignRepository` and wires QA-Obs-3 through `FacadeSessionState` without removing or stubbing the existing `DesignLibrary` UI flow. The tree stays green at Phase 0 close even though no callers have migrated yet. |
| 2026-05-17 | Rich `save_design(ship, name, built_designs)` lands on `DesignRepository`, not `DesignCatalog` | PROJ-427's 2026-05-17 decision split the responsibility: the low-level `save_design_data(design_id, data)` is on `DesignRepository`. The rich variant welds metadata embedding + overwrite-protection + on-disk write, all of which are repository responsibilities (disk-bound). The associated per-turn cache invalidation is a `DesignCatalog` responsibility, triggered by the workshop save handler calling `catalog.invalidate(design_id)` after the repository write returns. Keeping the rich save on the repository preserves the catalog's "no filesystem access" contract from PROJ-427's design. |
| 2026-05-17 | No behavior change beyond API parity | Scope is mechanical migration only. Any refactor of the workshop save flow, the build-queue UI ergonomics, or the design selector search semantics is out of scope. If a deferred Phase 6 caller has latent bugs, log them as findings and address them in a separate project. |
