# PROJ-468: Decisions Log

> **LOG ALL DECISIONS HERE**
> When you make a design choice or the user specifies a preference, add it to this table.
> Future agents will reference this to understand why things were done a certain way.

| Date | Decision | Rationale |
|------|----------|-----------|
| 2026-05-20 | Project initialized | Starting point for Docs cleanup — reference: systems + guides docs (2026-05-20) |
| 2026-05-20 | Bundled findings from `2026-05-20_073330_docs-audit` by doc-file-cluster locality across systems and guides docs (autonomous decision; Codex consult advised the doc-tier split) | Bundling driven by doc-file-cluster rather than severity to maximize editing continuity (one doc swept end-to-end per task); full bundling discussion in findings/bundling_decisions.md |
| 2026-05-20 | `docs/04_SERVICES.md` component_inspector content error placed in THIS bundle (not foundation) despite being a `docs/0N_*` doc | The fix is part of the component_inspector/effect_ability_metadata family that spans 6 systems/guides docs; co-locating it keeps the coordinated find-and-replace in one project for editing continuity |
| 2026-05-20 | Codex consult (planning, `AgentCoordination/Scratchpad/Consult/20260521T032207Z_docs-audit-bundling/response.md`) confirmed the doc-tier split and warned to build checklist wording from the re-verified item list, not report.md prose. Final decision (Claude owns): accepted. | Codex advises, initiator owns the call; checklists were authored from independently verified file:line evidence. |
| 2026-05-20 | EXECUTION re-verification (post-PROJ-467): All Phase 1 CRITICAL items still hold against live repo — `component_inspector.py` + `effect_ability_metadata.py` both absent; `component_abilities.py`/`component_layers.py`/`ability_metadata.py` present; docs still teach dead paths. Phase 2 dead refs all confirmed present (planetary.py file refs, data/spectrum.py, planet_context_menu.py, test_effect_ability_metadata.py, test_component_inspector.py, test_damage.py). superweapon_order_processor.py + game_initializer.py both exist with zero doc mentions. | Each finding re-grepped against current files before editing. |
| 2026-05-20 | DROPPED Task 2.5 (DEAD-RES-ui): `docs/systems/research_system.md:24` is NOT self-contradictory. It already reads "Do not recreate the old `game/research/ui/` path. It was moved to `game/ui/research/`..." — a correct historical-corrective warning, exactly the outcome the task asked for. Every other `research/ui` mention in the doc (lines 5,16,21,170,197) uses the correct live `game/ui/research/` path. No edit needed. | Finding no longer holds; the doc is already accurate. Dropping per step 2. |
| 2026-05-20 | Symbol-accuracy note: `ability_metadata.py` exposes the `AbilityMetadata` dataclass + module-level functions (`get_ability_metadata`, etc.); "AbilityMetadataRegistry" is the module's conceptual name (docstring), not a class. Doc edits reference `ability_metadata.py` and `AbilityMetadata` accurately, avoiding a fictitious `AbilityMetadataRegistry` class. | Prevent introducing a new dead symbol while fixing old ones. |
| 2026-05-20 | CODEX AUDIT (advisory, one round): partner_invoke reported error_kind=missing-response, but Codex DID produce a substantive audit — the harness only failed consult/v1 frontmatter validation and moved the file to `audit.md.invalid-output-20260521T054152Z.txt`. No retry needed. Codex (a) found 5 residual dead refs in 2 in-scope files; (b)/(d) confirmed NO introduced replacement path/symbol is wrong; (c) the 5 residuals. | Codex's content was complete and useful despite the frontmatter validation quirk. |
| 2026-05-20 | All 5 Codex findings VERIFIED against live code/docs → added Phase 3. `docs/guides/adding_abilities.md:511` (`planetary.py` ability-category entry) + `:582` (`EFFECT_ABILITY_METADATA` troubleshooting step); `docs/systems/strategy_layer.md:706` (`EFFECT_ABILITY_METADATA` shim claim, contradicting corrected text), `:746` (`planetary.py`), `:752` (`SYSTEM_EFFECT_ABILITIES`). `SYSTEM_EFFECT_ABILITIES` absent from game/ (grep empty); `EFFECT_ABILITY_METADATA` survives only as a code comment in ability_metadata.py. Same component_inspector/effect_ability_metadata/planetary-package fix family the project owns — missed by the original audit's line list, not new scope. Codex REJECTED count for (b)+(d): 0 (no wrong replacement). | Findings are real and in-scope; fixed in Phase 3 (validate_phase PASSED). |
