# PROJ-410: Decisions Log

> **LOG ALL DECISIONS HERE**
> When you make a design choice or the user specifies a preference, add it to this table.
> Future agents will reference this to understand why things were done a certain way.

| Date | Decision | Rationale |
|------|----------|-----------|
| 2026-05-10 | Project initialized | Created from `Projects/Triage/build_queue_caching_overhaul.md` (QA session `20260510_060431`). |
| 2026-05-10 | Project title: "Build Queue Widget Cache Invalidation" | Captures the targeted-invalidation work distinctly from the underlying perf landings (PROJ-373 phase 3 + PROJ-376 phase 2). User-confirmed. |
| 2026-05-10 | **Yard-selector missing-on-second-player symptom is in scope** | User answer to Phase A Q1. Phase B Yard-Selector Investigator found it's a *separate, smaller* container-visibility bug, not the cache-contamination bug. Folding the fix in is cleaner than splitting because the regression test covers both bugs at the same turn-boundary scenario. |
| 2026-05-10 | **Invalidation strategy: B+C combined** | User answer to Phase A Q2. B = renderer-level hook (`BuildQueueRenderer.refresh_queue_display()` calls `virtual_table.invalidate_widget_caches()`); C = screen-level hook (`BuildQueueScreen.open_for_yard()` resets controller / drag-handler state). Avoids the D-strategy's perf regression and avoids the complexity of A's data-source generation counter. |
| 2026-05-10 | **Turn-boundary mechanism: switched from A (facade event) to B (manager polling)** | Original user answer was A. Phase B swarm consensus (Architecture Analyst + Pattern Scout + API Reviewer) found that `StrategySessionFacade` has no callback/subscribe surface today — only one-shot `progress_callback` on `process_turn`. Manager polling `facade.get_active_empire()` on each `_open_build_queue()` is more consistent with the existing facade read-only accessor pattern (Pattern #5) and adds zero new event infrastructure. User confirmed switch to B after swarm review. |
| 2026-05-10 | Add explicit save/load hook to scope | Phase B Risk Assessor found that loading a saved game mid-session leaves the cached `BuildQueueScreen` with stale `Empire`/`Planet` refs. Phase 4 Task 4.3 wires `StrategyScreen.session` setter to call `on_active_player_changed()`. |
| 2026-05-10 | Data-identity dirty flag is **ephemeral** (cleared after one `update_visible_rows()` re-render) | Phase B Risk Assessor flagged that a permanent dirty flag would re-render every visible row every frame after invalidation — ~10–20% FPS drop. Ephemeral semantics: invalidate sets the flag; the next `update_visible_rows()` re-renders all visible rows, then clears the flag. The early-return guard returns to normal behavior on subsequent frames. |
| 2026-05-10 | **Defer** generation counter on `BuildQueueQueueDataSource` | Performance Analyst measured ~1–2 ms per redundant B-hook call when queue contents were unchanged. Below the threshold to justify scope expansion in this project. Note as a possible follow-up if the cumulative cost ever shows in profiling. |
| 2026-05-10 | Strict TDD: all 7+ failing tests written in Phase 1 before any production change | Per `AGENTS.md` Rule 1 and `CLAUDE.md`. Phase 1 must end with all new tests red and `TestRowPoolReuseGuard` / `TestSecondClickReuse` still green. |
| 2026-05-10 | `VirtualTable.invalidate_widget_caches()` must NOT call any widget's `.kill()` | Hard constraint from `TestRowPoolReuseGuard` (PROJ-373 phase 3). Asserts widget `.kill()` call counts. Invalidation must only null cache attributes (`_last_text` / `_last_img` / `_last_color`) and set the dirty flag. |
| 2026-05-10 | Pre-existing `BuildQueueDragHandler.reset_state()` `selected_design` claim — verify in Phase 1 | Triage and one Phase B agent (Test Impact Analyst) say `selected_design` is NOT cleared. Yard-Selector Investigator says it IS cleared. Resolved by reading source directly in Phase 1 Task 1.1 before deciding whether to land the fix in Phase 3 Task 3.3. |
| 2026-05-10 | Test baseline established: 19828 tests; 19824 passed; 4 skipped; 0 failures | `python Tools/test_sharded/test_sharded.py` wall time 116s. Baseline recorded under `AgentCoordination/generated/test_baseline/`. |
| 2026-05-10 | **A-hook also rebinds cached `BuildQueueScreen` domain context (`facade`/`galaxy`/`empire`), not just invalidates** | Codex review (arc01-002) found `BuildQueueScreen.__init__` stores these once at lines 95, 113-114; manager only rebinds design_library/loader/portrait on reuse. Without an empire rebind, `on_active_player_changed()` would detect the player change but `collect_build_queues_at_hex()` (`build_queue_source.py:412-416`) would still filter as the previous empire. This is the actual root cause of the missing-yard-selector + cross-player-merged-display symptoms. Rebind happens in the manager before `open_for_yard()`. |
| 2026-05-10 | **Active-empire accessor: poll `self._screen.current_empire.id` (existing property)** | Codex review confirmed `facade.get_active_empire()` does not exist. Two options: (a) add a `get_active_empire_id() -> int \| None` to `StrategySessionFacade`, or (b) poll the existing `StrategyScreen.current_empire` property at `strategy_screen.py:192`. Chose (b) for minimum churn — manager already has `self._screen` as `StrategyScreen`. Preserves the facade-only command-dispatch contract; we're reading a property the manager already has, not bypassing the facade. Optional facade addition deferred. |
| 2026-05-10 | **Save/load: production load creates a fresh `StrategyScreen`; the `session` setter is test-only** | Codex review cited `screen_router.py:324-344` (production `_on_load_game()` constructs `StrategyScreen(session=game_session, ...)` and replaces the scene) and the `StrategyScreen.session` setter docstring (test-only). Phase 1 Task 1.8 reframed to assert the new screen has `build_queue_screen is None` after load — the production guarantee is "no cached screen survives scene replacement," not "session-setter rebinds correctly." Phase 4 Task 4.3 dropped (defaults to drop unless arc01-004 says otherwise). |
| 2026-05-10 | **`BuildQueueDragHandler.reset_state()` already clears `selected_design`** | Codex review confirmed against source: `build_queue_drag_handler.py:101` sets `self.selected_design = None`. Phase 1 Task 1.1 becomes a *locking regression* (don't let future refactors drop the line); Phase 3 Task 3.3 dropped. Resolves the discrepancy noted in the original plan. |
| 2026-05-10 | **Button-handler refactor not needed** | Codex review confirmed against source: `check_action_button_press()` already reads `row.get("row_index", -1)` at click time (`virtual_table.py:516-524`), and `_rebuild_row_pool()` does not capture index in a closure. The existing test at `test_virtual_table.py:725-764` locks this behavior. Phase 2 Task 2.4 simplified from [Complex] (closure refactor) to [Simple] (verify `row["row_index"]` updates on dirty refresh + add cross-yard regression). |
| 2026-05-10 | **Zero-source yard switch added to scope** | Codex review point 4: when yard A has sources and yard B has none, `BuildQueueScreen.open_for_yard()` skips the `controller.set_active_queue()` call (only fires when source is non-None at lines 317-324), leaving `controller.selected_queue_sources` populated with yard A's refs. New Phase 1 Task 1.9 (failing test) and new Phase 3 Task 3.6 (explicit reset). |
| 2026-05-10 | **`force_update()` already triggers BQ refresh; new dirty flag is defense-in-depth** | Codex review noted `BuildQueueRenderer.refresh_queue_display()` already calls `force_update()` at `build_queue_renderer.py:161-164`, which resets scroll/count sentinels in `force_update()` (`virtual_table.py:555-558`). The actual BQ fix relies on the cache-nulling path of `invalidate_widget_caches()`, not on the dirty-flag-gated guard. Keep the flag — it covers generic VirtualTable consumers and expresses data-identity state cleanly — but document that BQ specifically gets its trigger from `force_update()`. |
| 2026-05-10 | **Strategy screen slot for build queue is `_build_queue`, not `_build_queue_manager`** | Codex review: `strategy_screen.py:151` shows `self._build_queue = comp.make_build_queue_manager(self)`. Manifest and Phase 4 checklist references corrected. |
| 2026-05-11 | **Phase 3 Task 3.4 (selector container visibility) confirmed UNNECESSARY** | After Phases 2+3+4 landed, Task 1.7 went GREEN on its own. Codex's prediction in arc01-002 was correct: the missing-yard-selector symptom was a pure consequence of the cached `BuildQueueScreen.empire` ref filtering as the prior empire (`build_queue_source.py:412-416`), not a container-visibility regression. The Phase 4 manager-rebind alone fixes it. No production change needed for Task 3.4. |
| 2026-05-11 | **Project complete** | All 5 phases delivered. Production change in 4 files: `virtual_table.py` (invalidation surface), `build_queue_renderer.py` (B-hook), `build_queue_screen.py` (zero-source reset + on_active_player_changed), `strategy_build_queue_manager.py` (polling + cached-screen rebind). Tests added in 5 files (17 PROJ-410 tests, all GREEN). Pattern #11 in `docs/02_PATTERNS.md` extended with cross-context invalidation guidance. Final test sweep: 5415 pass + 2 skipped, 0 failures across `tests/unit/ui/`, `tests/integration/ui/build_queue_screen/`, `tests/static_guards/`. Full sharded suite running in background as final verification. |
