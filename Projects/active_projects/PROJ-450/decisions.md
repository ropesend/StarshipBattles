# PROJ-450: Decisions Log

> **LOG ALL DECISIONS HERE**
> When you make a design choice or the user specifies a preference, add it to this table.
> Future agents will reference this to understand why things were done a certain way.

| Date | Decision | Rationale |
|------|----------|-----------|
| 2026-05-18 | Project initialized | Starting point for Typed staging-yard substrate completion |
| 2026-05-19 | Plan + manifest + 6 phase checklists populated from Codex r4 redesign job 2 + Stage 3 preflight findings | Codex r4 redesign at `AgentCoordination/Scratchpad/Consult/20260519T004841Z_stages-1-2-audit-and-redesign/response.md` re-bundled the substrate widening + UI reader migration + integration test cluster + validator/write-service tightening into PROJ-450 as a single job-oriented project. Stage 3 preflight at `Projects/archived_projects/PROJ-444to447_coordinator/stage_3_joint_a_preflight_findings.md` provided the 310-occurrence / 49-file audit + 3 hard blockers + Path A trade-off analysis. PROJ-450 phases the work: re-verify audit (Phase 0) → Path A engine cleanup (Phase 1) → substrate widening with one-phase projection bridge (Phase 2) → UI + DTO + validator + write-service migration + bridge deletion (Phase 3) → integration test cluster migration (Phase 4) → static guard tightening (Phase 5). |
| 2026-05-19 | Hard dependency on PROJ-449 Phase 3 | PROJ-449 Phase 3 deletes the Planet @property cluster and the legacy-kwarg wrapper. PROJ-450 Phase 1 cannot widen `add_to_staging_yard` cleanly while a @property for `staging_yard` still exists with a setter. Phase 0 of PROJ-450 verifies the PROJ-449 Phase 3 commit landed before Phase 1 starts. |
| 2026-05-19 | Phase 2 temporary dict-projection bridge property is deliberate | CLAUDE.md "no compat shims" rule interpreted as forbidding ENDURING shims. The Phase-2 → Phase-3 migration window keeps a `staging_yard` projection property alive for exactly one phase to avoid a silent UI reader regression (preflight BLOCKER #1). Phase 3 deletes it in the same commit that migrates the UI reader. |
| 2026-05-19 | Save format stays dict-shaped | Per CLAUDE.md "saves are disposable" rule, OLD saves can be dropped; the CANONICAL on-disk save format stays the same. `_normalize_to_typed` converts dict → typed on load; `planet_to_dict` serializes typed → dict on save via per-entry `.to_dict()`. Phase 2 Task 2.1's `test_save_format_remains_dict_shape` pins this invariant. |
| 2026-05-19 | Integration test cluster ownership confirmed | Stage 3 preflight §2.2 noted that `tests/integration/test_fms_*` was "unowned" in the PROJ-444..447 partition. Codex r4 redesign explicitly assigns this cluster to PROJ-450 as Phase 4 work. No more ambiguity. |
| 2026-05-19 | Codex audit fixes applied (Bucket A audit, `response.md`) | Codex's audit at `AgentCoordination/Scratchpad/Consult/20260519T014918Z_audit-bucket-A-substrate-triad/response.md` found 5 issues. (1) BLOCKER #1: Phase 3's "delete the Phase-2 bridge" plan would silently break UI/facade/DTO readers (`getattr(planet, "staging_yard", None)` returns None, not AttributeError, when the property is absent — silent regression). Fixed via Option A below. (2) manifest.md omitted `game/core/protocols/strategy_mutators.py` even though Phase 3 Task 3.5 tightens `IPlanetMutator` there — added to manifest. (3) PROJ-449/450 had two incompatible gate mechanisms; resolved to "direct code inspection" only (see entry below). (4) Mandatory archive pre-read violated self-containment; Path A rationale + BLOCKER #1 reasoning are now inlined into `design.md` and the archive is downgraded to OPTIONAL background. (5) Phase 2 bridge silent-mutation risk on `planet.staging_yard.clear()` at `test_fms_a_e2e.py:305` — fixed with Task 2.0 pre-migration of the in-place caller before Phase 2's read-only projection lands. |
| 2026-05-19 | BLOCKER #1 = Option A: permanent typed read-only `Planet.staging_yard` property | After PROJ-449 Phase 3 deletes the original @property accessor, the substrate would have no public name. Phase 2's bridge was originally going to be deleted in Phase 3, leaving UI / facade / DTO readers seeing `None` via `getattr(planet, "staging_yard", None)` — a silent regression. Option A: replace the Phase-2 dict-projection bridge with a permanent typed read-only property `Planet.staging_yard -> Tuple[CarriedVehicle \| DropPod, ...]`. Mutations route through `add_to_staging_yard` / `pop_staging_yard_typed` / `remove_from_staging_yard`. No setter; no list-mutation. Cheaper than Option B (rewriting every reader to access `_staging_yard` directly before bridge deletion) and matches the PROJ-436 read-protocol typed-narrowing pattern. |
| 2026-05-19 | Cross-project gate from PROJ-449 = direct code inspection (no SHA signal) | Codex audit flagged two incompatible gate mechanisms: (a) PROJ-449 Phase 3 Task 3.7 wrote PROJ-450's plan.md with a commit-SHA signal, (b) PROJ-450 Phase 0 Task 0.1 inspects current code directly. Resolved: (a) removed from PROJ-449. PROJ-450 Phase 0 already inspects `planet.py` directly for the absence of `_planet_init_with_legacy_kwargs` and the @property cluster — that is the canonical signal. Direct inspection is robust to sequencing errors and avoids the manifest-incompleteness paradox (cross-project write would have required PROJ-450/plan.md in PROJ-449's manifest). |
| 2026-05-19 | Phase 2 Task 2.0 pre-migrates `test_fms_a_e2e.py:305` `.clear()` caller | Phase 2's dict-projection bridge returns a fresh list of dicts; in-place `.clear()` would silently no-op against `_staging_yard`. Migrate the one known in-place-mutation caller BEFORE Phase 2's bridge lands — the bridge then has correct read-only-projection semantics from the moment it lands. Phase 4 covers `.append`/`.extend` migrations on the OLD substrate, before Phase 2 widens; those are not affected by the silent-mutation risk. |
