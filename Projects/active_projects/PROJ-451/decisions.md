# PROJ-451: Decisions Log

> **LOG ALL DECISIONS HERE**
> When you make a design choice or the user specifies a preference, add it to this table.
> Future agents will reference this to understand why things were done a certain way.

| Date | Decision | Rationale |
|------|----------|-----------|
| 2026-05-18 | Project initialized | Starting point for Production resource-consumption semantics (DI-006 + DI-007 engine half) |
| 2026-05-19 | Plan + manifest + 4 phase checklists populated from Codex r4 redesign job 3 | Codex r4 redesign at `AgentCoordination/Scratchpad/Consult/20260519T004841Z_stages-1-2-audit-and-redesign/response.md` re-bundled the engine-side residue of DI-006 (UX gap), DI-007 (bool-return handling), and F-B-019 (Protocol contract complement) into PROJ-451. Sequencing: RED reproduction (Phase 1) → GREEN shortage emit (Phase 2) → option a/b decision + DI-007 closure (Phase 3) → implementer ratchet (Phase 4). Independent of PROJ-449 / PROJ-450; parallel-safe. |
| 2026-05-19 | Phase 3 decision deferred to implementer (default to option b) | Phase 3 is the decision gate between (a) defensive bool-capture + tick_capacity skip and (b) strict Protocol contract hard-assert. Codex r4 recommends (b) as cheaper; CLAUDE.md "Capability validation is hard, not soft" supports (b) on principle. The default is (b) unless an implementer can articulate why (a) is needed. The chosen option is recorded in this decisions.md when Phase 3 executes. |
| 2026-05-19 | DI-006 data half pre-closed (PROJ-444 Phase 2) — **SUPERSEDED 2026-05-19** | PROJ-444 Phase 2 (2026-05-18) closed the data-layer half of DI-006: `Fleet.has_cargo_resources` now does `int(round(amount))` symmetric to `consume_cargo_resource`. Both sides agree on what gets charged. PROJ-451 closes the engine-side UX gap (no RESOURCE_SHORTAGE emitted when affordability passes but consume actually charges 0). Cross-reference: `fleet.py:245-269` for the data-layer half (read-only verify in PROJ-451). **NOTE: Codex r5 review (group1-preexecution-review/response.md, 2026-05-19) found this claim WRONG — see next row.** |
| 2026-05-19 | DI-006 data half PARTIALLY closed; consume-side gate still raw (codex r5) | Codex r5 verified that `consume_cargo_resource` at `fleet.py:285` still uses `if total < amount:` (raw float) while `has_cargo_resources` at `:267` uses `int(round(amount))`. The two methods disagree: e.g. cargo=0, amount=0.5 → has returns True (0 < 0 is False), consume returns False (0 < 0.5 is True). PROJ-444 Phase 2 only addressed half of Option B; the consume-side gate was missed. PROJ-451 Phase 2 Task 2.0 now rounds the consume-side gate to close the data-half symmetry properly. After Task 2.0, both methods agree on what gets charged for rounded-to-zero amounts. |
| 2026-05-19 | Parallel-safe independence from PROJ-449 and PROJ-450 confirmed | The engine-side residue is entirely contained in `production_engine.py` and the engine-test directories. No shared file surface with wrapper-retirement (PROJ-449) or staging-yard substrate (PROJ-450). PROJ-451 can be sequenced first, last, or interleaved freely. |
| 2026-05-19 | Codex audit fixes applied (Bucket A audit, `response.md`) | Codex's audit at `AgentCoordination/Scratchpad/Consult/20260519T014918Z_audit-bucket-A-substrate-triad/response.md` found 2 minor issues: (1) Phase 1 framing said it "reproduces the DI-007 bool-return seam" but Task 1.2 actually only exercises the DI-006 zero-consume / no-diff path (mocking `production_consume_resource.return_value = True`). DI-007 proper (false-return contract-breach path) is deferred to Phase 3 — Phase 1 docstring and Task 1.2 docstring now scope this explicitly. (2) Phase 1 mixed "both new tests FAIL" with "commit them as xfail so sharded stays green" without explicit sequencing. Task 1.3 now requires a targeted-pytest raw RED run WITHOUT xfail markers FIRST (proving the tests genuinely fail), then xfail markers are added, then committed — the standard pattern for committing failing tests on `main` while keeping the sharded suite green. |
