# PROJ-397: Decisions Log

> **LOG ALL DECISIONS HERE**
> When you make a design choice or the user specifies a preference, add it to this table.
> Future agents will reference this to understand why things were done a certain way.

| Date | Decision | Rationale |
|------|----------|-----------|
| 2026-05-08 | Project initialized | Starting point for PROJ-393 remediation — review CRITICAL + MAJOR + 3 deferred items (Combat Lab dead vars reclaim, fleet_id field deletion, view=None branch) |
| 2026-05-08 | **Phase 3 Task 3.1: Path B simplified** — keep `fleet_id` on `ClearOrdersCommand`/`DeleteOrderCommand`/`ReorderOrderCommand`; delete the `entity_type: str = "fleet"` dead field on all three. | Sibling commands `ClearPlanetOrdersCommand`/`DeletePlanetOrderCommand` already handle the planet path explicitly (in `orders_window_ctrl.py:53-69`). The fleet-side commands strictly handle fleets — no handler reads `cmd.entity_type`. Renaming `fleet_id` → `entity_id` would needlessly parallel `ClearPlanetOrdersCommand.planet_id`. Cleanup focuses on deleting the dead `entity_type` field (the actual review finding F-04) rather than churning identifier names. Net: 3 dead fields removed, zero handler changes, zero call-site changes (all callers use `fleet_id=` keyword which already matches). |
| 2026-05-08 | **Phase 3 Task 3.2: thread `facade` through `PlanetSelectionWindow`** | The legacy `view=None` branch in `format_planet_info` only fires for owned planets in `PlanetSelectionWindow` (uncolonized planets short-circuit earlier on `owner_id is None`). The window has 2 production callers: `selection_prompts.prompt_planet` (uncolonized — branch unused) and `build_queue_screen._prompt_target_planet` (colonized — branch active). Both callers have facade access. Solution: add `facade: Any = None` kwarg to `PlanetSelectionWindow.__init__`, pass it to the inner `PlanetReportPanel` per planet via `view=facade.get_colony_demographic_view(planet.id) if facade else None`. Then delete the `view=None` legacy branch in `strategy_detail_fmt.py`. |
| 2026-05-08 | **F-02/F-03 (fleet_id docstring): rewrite, do NOT restore the old "Kept for backward compat" tag.** | Per the resolved Path-B direction, `fleet_id` is the canonical name (no parallel `entity_id` exists or is planned). The docstring should accurately describe that, and the fabricated "~20 call sites" count should be removed — actual count is 3 (1 per command). |
