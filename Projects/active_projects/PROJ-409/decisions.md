# PROJ-409: Decisions Log

> **LOG ALL DECISIONS HERE**
> When you make a design choice or the user specifies a preference, add it to this table.
> Future agents will reference this to understand why things were done a certain way.

| Date | Decision | Rationale |
|------|----------|-----------|
| 2026-05-09 | Project initialized | Starting point for Tier 5: closeout MAJ-013 EventBus shim + MAJ-014 raw EnginePhaseError catch |
| 2026-05-09 | **MAJ-013 closure mode: ratified — already actively closed by PROJ-390.** | Investigation: the original PROJ-381 review (`Reviews/results/2026-05-08_230318_code_proj-381-error-handling-cleanup-strategy-ui-assets_req-req_20260508_230317_779973/findings/07_architecture_rules_report.md:103`) identified MAJ-013 as the module-level `log_event()` / `set_event_handler()` / `get_event_handler()` "compatibility shim" at `game/core/event_logging.py:57-88`. **PROJ-390 actively retired that shim** (see `Projects/active_projects/PROJ-390/plan.md:21` and the docstring at `game/core/event_logging.py:30-32` — "PROJ-390 retired the module-level `log_event` / `set_event_handler` / `get_event_handler` compatibility shim — only the EventBus class remains"). Verified by grep: only `EventBus` is imported across `game/core/__init__.py`, `game/simulation/combat/attack_contract.py`, `game/simulation/combat/weapon_firing_system.py`, and `game/strategy/engine/game_session.py`; the deleted module-level functions have zero call sites. The `Pattern #10` references that remain in the codebase (`game/simulation/entities/projectile.py`, `game/strategy/data/empire.py`, `game/strategy/data/fleet.py`, `game/ui/screens/builder/event_bus.py`) are PROJ-382 Phase 2 *constructor-injection* breadcrumbs — the canonical Pattern #10 implementation, not the retired shim. The PROJ-395 reviewer flagged the file because it was in scope but did not pick up that PROJ-390 had already closed the finding. No code change needed; this entry ratifies the existing closure. |
| 2026-05-09 | **MAJ-014 closure mode: actively deleted.** | Removed the defensive raw `except EnginePhaseError` branch in `game/ui/screens/strategy_game_state_manager.py:149-158` and the import at line 19. PROJ-381 Phase 3's facade conversion (`StrategySessionFacade.process_turn` wraps `EnginePhaseError` → `TurnFailedError`) is now unit-tested directly by PROJ-408 C-02 (`tests/unit/strategy/facade/test_strategy_session_facade.py::TestProcessTurnErrorConversion`), making the secondary UI catch dead code. Per CLAUDE.md Rule 4 (no fallbacks for scenarios that can't happen). Two new unit tests in `tests/unit/ui/screens/test_strategy_game_state_manager.py::TestProcessFullTurnErrorBoundary` pin the contract: canonical `TurnFailedError` path opens the dialog + clears overlay + skips autosave; raw `EnginePhaseError` propagates uncaught. Integration tests in `tests/integration/ui/test_strategy_turn_error_boundary.py` updated to inject `TurnFailedError` (matching production after facade conversion); the real-engine wiring test now threads the genuine `EnginePhaseError` through the same conversion. Commit: `c0ff79f92`. |
