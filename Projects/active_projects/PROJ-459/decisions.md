# PROJ-459: Decisions Log

> **LOG ALL DECISIONS HERE**
> When you make a design choice or the user specifies a preference, add it to this table.
> Future agents will reference this to understand why things were done a certain way.

| Date | Decision | Rationale |
|------|----------|-----------|
| 2026-05-18 | Project initialized | Starting point for Strategy data LOC extractions (fleet_serde + planet_gen split) |
| 2026-05-19 | Project scope set per Codex r4 redesign Job 11 | Codex r4 (`AgentCoordination/Scratchpad/Consult/20260519T004841Z_stages-1-2-audit-and-redesign/response.md`) closed the layer-bucket PROJ-444..447 attempt that cycled, and respun the work as 12 job-oriented projects. PROJ-459 is Job 11: strategy data LOC extractions. Carries F-A-008 (fleet_serde extraction) + F-A-009 (planet_gen split) for closure; carries F-A-007 (ship_instance.py LOC) as a measurement-decision only — Codex explicit: "F-A-007 should not be smuggled in as a side quest". |
| 2026-05-19 | Phase 0 is mandatory | Re-measurement must precede Phase 1 because PROJ-449 (wrapper retirement) and PROJ-451 (production semantics) both touch the fleet.py / ship_instance.py surface area being extracted. Without re-measurement, scope is unverified. Phase 0 is a read-only audit phase. |
| 2026-05-19 | Phase 3 is measurement-only, not extraction | Per Codex r4, the ship_instance.py LOC ceiling is a hard "next-touch" project boundary. If PROJ-449 brings it under 500: close F-A-007 here. If still over: spin out a fresh project. Either way, PROJ-459 does not attempt the split. |
| 2026-05-19 | Planet_gen.py Phase 2 may end in a documented deferral | If no clean split axis exists, document the structural reason and a concrete next-touch criterion. Per Codex r4: "don't force a bad cut." Deferral with rationale closes F-A-009 just as legitimately as splitting does. |
| 2026-05-19 | Dependencies are phase-scoped (per audit feedback Bucket D response.md) | PROJ-451 is a HARD predecessor for Phase 1 (and all later phases) because it changes the production-resource surface immediately adjacent to the fleet.py serde extraction line (fleet.py:245-315). PROJ-449 is a HARD predecessor for Phase 3 ONLY because it drives the ship_instance.py LOC delta; Phases 1-2 can proceed before PROJ-449 lands. Phase 0's dependency gate is updated accordingly. |
| 2026-05-19 | Phase 1 framing is characterization-first, not RED-then-GREEN strict TDD (per audit feedback) | The extraction introduces no new behavior, so the standard RED-then-GREEN cycle does not apply. Per CLAUDE.md's allowance for pure-refactor work, the discipline is characterization-first: write the round-trip test, confirm it passes against current code (captures the current dict shape), freeze the dict, then refactor — any drift post-extraction is a real failure. This re-framing replaces the original "Strict TDD discipline" wording, which was inaccurate for pure-refactor work. |
| 2026-05-19 (deferred to Phase 1 execution) | `fleet_from_dict_kwargs` returns only `Fleet.__init__` kwargs; ship/order hydration runs outside the helper | Verified against `game/strategy/data/fleet.py:44-68`: `Fleet.__init__` accepts only `fleet_id`, `owner_id`, `location`, `speed`, `component_registry`, `display_name`. `self.ships`, `self._task_forces`, `self.fleet_policy`, `self.orders`, `self.path` are initialized internally and would TypeError if passed as kwargs. The helper therefore CANNOT return a "ready-to-go kwargs including the ships list" shape. Per-ship deserialization (`ShipInstance.from_dict(ship_data, registries=registries)`) lives in a sibling helper (`_deserialize_fleet_ships`) called from `Fleet.from_dict` AFTER `Fleet(**fleet_from_dict_kwargs(...))` returns. Final shape captured at Phase 1 execution time; this row pre-stages the decision per audit feedback. |
| 2026-05-19 (Phase 1 execution) | Final fleet_serde public surface: `fleet_to_dict`, `fleet_from_dict_kwargs`, `_deserialize_fleet_ships`, `_deserialize_fleet_orders` | Confirmed at execution. `fleet_from_dict_kwargs(data, registries=None)` returns ONLY the six `__init__` kwargs. `_deserialize_fleet_ships(ship_data_list, registries, fleet_id=None)` wraps the strict-deserialization loop (PROJ-251 error-wrapping behavior preserved verbatim). `_deserialize_fleet_orders(orders_data, fleet_id)` is a thin module-level wrapper over the long-standing `OrderSerializer.deserialize_orders` (PROJ-210) so `Fleet.from_dict` has a single serde surface to import. TaskForce and CombatPolicy hydration remained inline in `Fleet.from_dict` (each is a single line + a conditional); promoting them into standalone helpers would be ceremony. |
| 2026-05-19 (Phase 1 execution) | `Fleet.resolve_order_references` stays a method on `Fleet`, not moved into `fleet_serde.py` | Mirrors planet_serde precedent (which has no `resolve_*` equivalent — Planet's orders don't reference cross-entity object IDs). The method already delegates to `OrderSerializer.resolve_order_references(self, galaxy, empires)` and mutates `self.orders`. It runs late (post-load, after galaxy is constructed), not as part of deserialization. Moving it into `fleet_serde.py` would relocate a 2-line shim without conceptual benefit. |
| 2026-05-19 (Phase 1 execution) | Phase 1 LOC outcome: fleet.py 693 → 632 (−61); fleet_serde.py 168 | Smaller drop than the ~140-LOC target because the post-construction hydration in `Fleet.from_dict` (task_forces, fleet_policy, path, construction_queue) had to remain on the Fleet side per the `__init__` constraint. The serde module is comparable in size to `planet_serde.py` (168 vs 219). fleet.py is still above the 500-LOC ceiling (632), but the next mechanically-extractable surface (order-queue management at ~120 LOC) is the natural next-touch target — not bundled here because it would broaden Phase 1 beyond F-A-008. |
| 2026-05-19 (Phase 2 execution) | Phase 2 split landed: `planet_gen_surface.py` carries 3 module-level functions | Extracted `_generate_surface_flags`, `_determine_type`, `_generate_resources` from `PlanetGenerator` into a new sibling module `game/strategy/data/planet_gen_surface.py` (236 LOC). All three were `self`-method declarations only — they didn't actually reference `self`. The move to module-level functions is a pure mechanical change; seeded `random` module state is preserved (same global RNG). Public surface: `generate_surface_flags`, `determine_planet_type`, `generate_resources`. The `_get_planetary_ids` cached helper moved with `_generate_resources` (its only caller). `planet_gen.py` 610 → 427 LOC (−183, under the 500 ceiling). Closes F-A-009. Test sweep: 27 call-site migrations across 2 test files (`test_planet_gen.py`, `test_planet_classification_logic.py`); one downstream import fix in `tests/integration/strategy/test_planet_physics.py` re-pointed leaky re-exports (`MASS_EARTH`, `MASS_JUPITER`, `generate_atmosphere`) at their canonical sources. Sharded 23397/23397. |
