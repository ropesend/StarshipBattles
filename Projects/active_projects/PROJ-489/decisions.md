# PROJ-489: Decisions Log

> **LOG ALL DECISIONS HERE**
> When you make a design choice or the user specifies a preference, add it to this table.
> Future agents will reference this to understand why things were done a certain way.

| Date | Decision | Rationale |
|------|----------|-----------|
| 2026-05-22 | Project initialized | Starting point for Legacy removal — ModifierService consolidation (2026-05-20) |
| 2026-05-22 | Bundled findings from `2026-05-20_210635_legacy-audit/` by removal cluster `modifier_service_canon` per user direction | Bundling driven by removal cluster (one project per system being eradicated) rather than severity to maximize deletion-PR coherence; full bundling discussion in findings/bundling_decisions.md |
| 2026-05-22 | Task 1.1: kept `ModifierService` as canonical with no production change required | Behavioral diff between `_has_arc_set_effect` (generic) and `ModifierLogicService` hardcoded `turret_mount` is a strict superset — canonical handles ALL modifiers with an `arc_set` effect (including novel weapon abilities) while the UI variant only handled the literal `turret_mount` id and a hardcoded list of weapon-ability names. Existing tests (`test_modifier_service.TestArcSetEffectDetection`, `test_turret_mount_finds_firing_arc_in_novel_weapon_ability`) already lock in the generic behavior. Delegation upgrades `ModifierLogicService`/UI from narrow to generic; no UI test fixtures depended on the narrow behavior. |
| 2026-05-22 | Task 1.1: `hardened_mount`/`efficiency_mount` initial-value 1.0 special case is now the only authoritative source via delegation | `ModifierService.get_initial_value` returns 1.0 for `hardened_mount` and `efficiency_mount`; the UI's `ModifierLogicService.get_initial_value` previously hit `default_val` instead. Real registry JSON sets `default: 1.0` for both, so the practical behavior matches today, but delegation removes the silent drift hazard. |
| 2026-05-22 | Task 1.2: `ModifierManager.add_modifier` constructs `ModifierService` lazily from `self._component._registries.modifiers` (cached on the manager) and delegates to `is_modifier_allowed` | Caching on the manager avoids per-call construction. The fix adds the previously-missing `allow_abilities` check; verifier noted no production modifier definition relied on this gap, and the suite confirms no test fixtures depended on the buggy permissive behavior. |
| 2026-05-22 | Task 1.4/1.5: `ModifierLogicService` constructor now takes a `ModifierService` directly rather than a registry provider | Cleanest delegation surface; eliminates the `ComponentService` round-trip that the old facade performed for restriction checks. UI callers (`workshop_screen.py`) construct one `ModifierService` from `context.registries.modifiers` and pass it to `ModifierLogicService(modifier_service=…)`. `calculate_snap_value` remains a static UI-only helper. |
