# PROJ-367: Decisions Log

> **LOG ALL DECISIONS HERE**
> When you make a design choice or the user specifies a preference, add it to this table.
> Future agents will reference this to understand why things were done a certain way.
>
> **This file reflects the post-Codex-consensus state** (r002 + r003 + r004 corrections merged from `AgentCoordination/Scratchpad/Discussion/20260505T150915Z_proj-367-plan-review/`).

| Date | Decision | Rationale |
|------|----------|-----------|
| 2026-05-05 | Project initialized as PROJ-367 (PROJ-366 already taken by sink-wiring follow-up) | Continuation work from PROJ-360 audit; deferred findings EXT-07 + EXT-11 + EXT-13 are coupled and best handled as one project. |
| 2026-05-05 | Three-phase plan: typed abilities → registry-as-pipeline → typed accumulator | Each phase delivers independent value but they reinforce each other. Phase 1 alone closes EXT-07 at the API level; Phase 2 alone collapses the runtime two-tier model; Phase 3 alone closes EXT-13. Splitting further ships incomplete value. |
| 2026-05-05 | Replacement is implicit at registration (default policy `REPLACE_WARN`) — no separate suppression frozenset | The current `BUILTIN_HANDLED_ABILITIES` mechanism encodes "registering replaces built-in" as a side channel. Make it first-class via `RegistrationConflictPolicy.REPLACE_WARN` default. |
| 2026-05-05 | Built-ins as registry entries (Phase 2), not as a separate "domain registry" | Three extension mechanisms is worse than two. Goal is one universal mechanism. Alternative B in design.md (rejected). |
| 2026-05-05 | `StatAccumulator` is a dataclass with 10 scalars + 4 named map fields = 14 total — NOT a TypedDict | Runtime `AttributeError` for misspelled scalar/map field names is the explicit goal of EXT-13. TypedDict is only mypy-time. Alternative A (rejected). |
| 2026-05-05 | Dynamic resource keys live inside `resource_storage` / `resource_generation` maps, NOT as flat dataclass fields | Resource types come from data files at runtime; promoting them statically would contradict the data-driven invariant at `ship_stats.py:285-295`. Alternative E (rejected). |
| 2026-05-05 | Contributor signature stays `(ship, comp, accumulator)` — `ship` parameter retained | Command/launch contributors legitimately set ship-level stats (`max_targets`, `fighter_capacity`) that aren't pure aggregations. Splitting "aggregator" from "ship-setter" is deferred. Alternative D in design.md. |
| 2026-05-05 | Golden snapshot at `test_ship_stats_golden.py` is the bit-identity safety net through all 3 phases | The PROJ-360 remediation built it specifically for this kind of refactor. No `--update-snapshot` runs across the project, except Phase 1 Task 1.9 which adds new fixtures; existing 7 designs untouched. |
| 2026-05-05 | Modder contributors default to `phase_order=99` (after all built-ins at 10..50) | Mirrors today's behavior where `apply_registered_contributors` runs after the built-in domain calls. Risk R2 in design.md. |
| 2026-05-05 | Replacement entries inherit `phase_order=99` (modder default) unless explicitly overridden | Mirrors today's "skip-built-in-then-modder-runs-last" semantics from `is_builtin_suppressed_for`. Codex C5. |
| 2026-05-05 | Carrier + multiplex-equipped designs added to golden fixtures during Phase 1 | EXT-07 closure requires that the new typed classes are actually exercised. Also incidentally closes PROJ-360 review FIND-001 / FIND-005 (golden coverage gaps). |
| 2026-05-05 | `PodStorageAbility.capacity_mass: float` only — NO `pod_class` | Verified `data/components.json:2396-2397` and `ship_stats.py:315-319`; `docs/systems/ability_reference.md:768-786` confirms single scalar. The `capacity + pod_class` shape was a hallucination in the original scaffold. Codex C1. |
| 2026-05-05 | `VehicleLaunchAbility` extended with `max_launch_mass: float`; STAT_BINDINGS unchanged | Existing typed class at `markers.py:9-52` parses `capacity` / `cycle_time` / `fighter_class` but the hangar contributor at `launch.py:49` reads `max_launch_mass` from the raw dict. Phase 1 must extend the class before consumer migration. `max_launch_mass` is additive, not modifier-scaled. Codex C2. |
| 2026-05-05 | `VehicleStorageAbility(Ability)` with `capacity: int`, no STAT_BINDINGS | Production data is scalar (`data/components.json:1241`); current code adds it directly to `ship.fighter_capacity`. Tied to `CAPACITY_MULT` was rejected — storage is additive. Phase 1 Task 1.3 tests both scalar and dict shapes. Codex C3. |
| 2026-05-05 | `weapons.py` is FULLY out-of-scope for Phase 2 | Phase 5 helper, post-physics. `aggregate_targeting_scores` / `apply_armor_and_repair_scores` / `init_armor_pool` run after Phase 4 physics; folding them requires accumulator state to survive the physics boundary, which is a real architectural concern not in scope. Future project required. Codex C4 + D1. |
| 2026-05-05 | Phase 5 helpers (`aggregate_targeting_scores`, `apply_armor_and_repair_scores`, `init_armor_pool`) untouched | Same reasoning as the `weapons.py` decision above. Codex D1. |
| 2026-05-05 | `_phase_endurance` does not exist; `calculate_combat_endurance` reads `ship.resources` | The original scaffold referenced a non-existent method. Phase 3's only endurance-related requirement is that `_apply_aggregated_stats` round-trips `accumulator.resource_storage` / `resource_generation` into `ship.resources`. Codex C7. |
| 2026-05-05 | Registration returns a `RegistrationHandle`; unregister takes a handle | Without a handle, `unregister_stat_contributor("X")` is ambiguous under APPEND. Cannot tell "remove the entry I just added" from "remove all my appends" or "remove the default." Codex D3. |
| 2026-05-05 | APPEND entries are individually addressable; defaults are NOT directly unregisterable | Defaults are managed via the seed/reset cycle. Attempting to unregister a default by handle raises `CannotUnregisterDefaultError`. Codex D3. |
| 2026-05-05 | Backward-compat shim `unregister_stat_contributor_by_name` exists during Task 2.7a only; deleted at Phase 2 close | Migration path for tests that currently call by name. Emits `DeprecationWarning` while it exists. Codex D3. |
| 2026-05-05 | `reset_stat_contributor_registry()` clears AND re-seeds defaults (idempotent) | Root `conftest.py` calls reset before/after every test; after Phase 2 lands, that helper restores the default-seeded registry rather than leaving it empty. Codex C8. |
| 2026-05-05 | `unregister_stat_contributor(handle)` after a `REPLACE_*` restores the default | Predictable test-cleanup semantics. Codex C8. |
| 2026-05-05 | Phase 2 default-seed enumeration is finalized at Task 2.4 implementation time and pinned by a registry-defaults test; NOT pre-pinned in the project plan | `BUILTIN_HANDLED_ABILITIES` has 8 names today; `Armor` / `VehicleStorage` / shield-energy-cost split decisions are deferred to implementation. Pinning a count forces those decisions before the implementer reads the code in context. Codex E1. |
| 2026-05-05 | `Armor` / `VehicleStorage` / shield-energy-cost entry split decisions are made at Phase 2 implementation time, not during planning | No Phase 1 dependency. Codex E1. |
| 2026-05-05 | Plan reviewed by Codex via inter-agent discussion; reached consensus after 13 material corrections | Discussion at `AgentCoordination/Scratchpad/Discussion/20260505T150915Z_proj-367-plan-review/` (9 messages, r002 + r003 + r004 plan revisions). Codex caught hallucinations on PodStorage shape, function names, Phase 5 boundary, accumulator counts, and the seed-count claim. |
