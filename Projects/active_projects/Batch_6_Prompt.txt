BATCH 6 ORCHESTRATOR — Cluster F (3 projects, serial) — Modifier blast radius + snapshot harness

You are the orchestrator for Batch 6 of a 3-batch parallel execution (Batches 4/5/6 run CONCURRENTLY on separate machines, each on its own branch). The other two batches:
  - Batch 4 = PROJ-491, PROJ-492, PROJ-493 (PROJ-479 follow-through — CAT-6 test rewrites + HLP sweeps + SuperweaponValidator DI seam)
  - Batch 5 = PROJ-494, PROJ-495, PROJ-496 (PROJ-480 follow-through — test polish CAT-8/9/10/11/12)

These three batches were scaffolded on 2026-05-23 by four parallel planning subagents working with Codex consults. The 9 new projects (PROJ-491..499) were partitioned across the batches by file-overlap analysis. Batch 6's scope is two independent but related threads:
- The PROJ-489 ModifierService consolidation surfaced a real `allow_abilities` enforcement gap; Codex's static scan showed 105-168 theoretical mismatches per restricted modifier with 0 currently-broken shipped designs. PROJ-497 makes the user-facing data-intent decisions; PROJ-498 hardens the engineering (reason-bearing API + save-restore logging + matrix test).
- PROJ-499 fixes a regression-snapshot harness gap (compare_snapshots ignores extra keys) that allowed schema drift; bulk re-shoots ~58 stale baselines.

> **Cross-batch file friction (READ THIS):** Batch 6 is largely DISJOINT from Batches 4 and 5 — Batches 4/5 touch tests/unit/strategy/ + tests/unit/ui/ + tests/conftest.py; Batch 6 touches data/, game/simulation/services/modifier_service.py, and tests/regression/modifier_ability_snapshots/. No known shared files. The one overlap risk is if a Batch 4/5 test happens to import from `game.simulation.services.modifier_service` and PROJ-498 changes that signature; Batch 4/5 are test-only so this is unlikely but worth watching.

> **Hard sequence inside this batch:** PROJ-498 hard-depends on PROJ-497 (data decisions land first so the matrix test in PROJ-498 encodes the final intended data surface, not today's accidental surface). PROJ-499 is independent and could in principle run first, but ordering it after the modifier work avoids potential snapshot-collision risk (PROJ-497 conditionally re-shoots some snapshots if user approves data edits).

> **PROJ-497 requires USER DECISIONS in its Phase 1.** This batch CANNOT run unattended through PROJ-497 without those decisions. The orchestrator must either (a) collect the decisions up-front via AskUserQuestion before spawning the PROJ-497 implementer subagent, or (b) accept that the PROJ-497 implementer will surface the decisions and pause. See the per-project notes below.

Your 3 projects MUST run in this exact serial order. Do NOT parallelize within the batch — every project's workflow must complete before the next begins.

================================================================
PROJECT ORDER (run one at a time, in this sequence)
================================================================

1. PROJ-497 — Data-intent decisions (3 phases, 12 tasks; USER INPUT REQUIRED at Phase 1 for: efficient_engines disposition, mini_capital_missile type+ability-payload decision, facing/turret_mount seeker allowance)
2. PROJ-498 — Engineering hardening (4 phases, 16 tasks; reason-bearing check_allowance() API + save-restore rejection logging + data-driven matrix test + docs)
3. PROJ-499 — Regression-snapshot harness symmetric key comparison + baseline cleanup (6 phases: census, comparator fix, bulk re-shoot ~58 baselines, spot-check, docs, negative-guard test)

Why this order (do not change without re-running conflict analysis):
- 497 first: blocking for 498. Without the data decisions, 498's matrix test would encode the wrong intended surface. Also: PROJ-497 may conditionally re-shoot snapshots if data edits land — better to do that before PROJ-499's bulk re-shoot.
- 498 second: needs the post-PROJ-497 data shape stable. Adds reason-bearing API in production + new tests; layered on top of 497's data.
- 499 last: bulk snapshot re-shoot operates on the post-497 / post-498 data surface, so the new baselines reflect the converged state, not an intermediate snapshot.

================================================================
PER-PROJECT WORKFLOW (apply this loop to each project, in order)
================================================================

For project PROJ-NNN, execute the following steps in sequence:

STEP A — IMPLEMENT

  PROJ-497 special handling (USER DECISIONS):
  Before spawning the implementer subagent, use AskUserQuestion to collect the 3 (or 4) decisions PROJ-497's plan.md identifies as user-gated:
    1. efficient_engines disposition — delete / redesign / keep-inert
    2. mini_capital_missile — keep BeamWeaponAbility / retype to SeekerWeaponAbility (and whether to edit `type` field, ability payload, or both)
    3. facing/turret_mount seeker allowance — remove / keep-with-documented-intent / defer
    4. (optional, PROJ-498) any "intentionally rejected despite passing rules" override pairs (default: none)

  Use AskUserQuestion with one question per decision (max 4 questions). Record answers in PROJ-497/decisions.md before proceeding.

  Then spawn the implementer subagent with prompt:
    "Run /claude-proj-continue PROJ-497. The user has decided: <enumerate decisions>. Apply these decisions in Phase 1, then proceed through Phase 2 (TDD data edits) and Phase 3 (docs/decisions/handoff). Mark each phase Complete in plan.md and the phase checklist. Do not commit. Report files modified and any conditional snapshot re-shoots performed."

  PROJ-498 / PROJ-499 standard prompt:
    "Run /claude-proj-continue PROJ-NNN. Implement every phase listed in plan.md per the protocol at Projects/protocols/. Mark each phase Complete in plan.md and the phase checklist when its checkboxes are all checked. Do not commit. When all phases are done, report back with files modified, behavioral changes, and targeted test results."

  Wait for the subagent's reply. Do NOT spawn the next project's subagent until this step completes.

  Special notes per project:
  - PROJ-497 Phase 2 Task 2.2 (mini_capital_missile): if the user approved a type-only edit but NOT the ability-payload edit, this is a no-op for the allowance check. The implementer should explicitly halt and surface that before applying a misleading edit.
  - PROJ-498 Phase 1 must NOT break the 3 existing `is_modifier_allowed()` callers (modifier_manager.py:124, component_service.py:104, modifier_logic.py:54). The new `check_allowance()` is additive; `is_modifier_allowed()` stays as a bool convenience. Include a bool-semantics regression-guard test (per Subagent C's audit fix to PROJ-498 Phase 1).
  - PROJ-499 Phase 3 deletes all 65 baselines and lets `fail_missing_baseline()` regenerate them. Run pytest TWICE: first run writes baselines (and fails loud); second run passes against newly-written baselines. If the second run's diff goes beyond additive default-value keys, STOP and investigate — that indicates real behavioral drift, not just schema growth.

STEP B — CODEX AUDIT (mid-project review)
  Use the Skill tool to invoke:
    Skill: claude-consult
    args: --with codex --mode mid-project-review --slug audit-PROJ-NNN --question "Audit the just-completed PROJ-NNN. The implementer reported the file set <files>. The project's plan is at Projects/active_projects/PROJ-NNN/plan.md, decisions at decisions.md, manifest at manifest.md. Read the git diff against main for these files and report: (1) any deviation from the project's stated Goals or Scope in plan.md, (2) any introduced regression, layer-violation, or convention violation per docs/01-03, (3) any obvious missed callers / orphan symbols / stale comments left behind, (4) for PROJ-497 only: any data edit that silently re-broadens a previously-rejected pair the user did not approve; (5) for PROJ-498 only: any check_allowance() reason that over-promises (semantics drift beyond the locked enum UNKNOWN_MODIFIER_ID/TYPE_NOT_ALLOWED/TYPE_DENIED/ABILITY_NOT_ALLOWED/ALLOWED); (6) for PROJ-499 only: any baseline that picked up real behavioral drift (not just schema growth) without being flagged. Be specific (file:line). Do NOT propose new features."

  The consult will write a response.md you can read.

STEP C — VERIFY CODEX FINDINGS
  For EACH finding in the consult response.md `## Findings` section:
    1. Open the cited file:line and confirm the finding is real (the code matches the description).
    2. Decide one of:
       - VERIFIED + IN-SCOPE: real issue, belongs in this project's scope per plan.md.
       - VERIFIED + OUT-OF-SCOPE: real issue but outside this project's stated Scope — log via /claude-di-log with link back to PROJ-NNN, then move on.
       - REJECTED: false positive (contrary evidence in the cited file, or audit-self-retraction pattern). Record rationale.

  Write your verification table to Projects/active_projects/PROJ-NNN/findings/audit_verification.md with rows: id | verdict | evidence | action.

  Special caution: DI-2026-05-23-005 (`deny_abilities` not enforced by live ModifierService) is logged and OUT-OF-SCOPE for both PROJ-497 and PROJ-498. If Codex re-surfaces it during the audit, mark as KNOWN/REJECTED (already logged).

STEP D — REMEDIATE (if verified in-scope findings exist)
  If any finding is VERIFIED + IN-SCOPE:
    1. Determine the next phase number N (= current max phase + 1).
    2. Create Projects/active_projects/PROJ-NNN/phase_N_checklist.md using the PHASE_TEMPLATE format from Projects/scripts/create_project.py:126-158. Title it "Phase N: Audit remediation (Codex consult <date>)". Add one task per verified finding.
    3. Update Projects/active_projects/PROJ-NNN/plan.md Quick Status table — add a new row for Phase N linking to the new checklist.
    4. Update Current State to point to Phase N.
    5. Spawn ANOTHER subagent (same skill: /claude-proj-continue PROJ-NNN) to execute the new phase.
    6. Wait for completion.
    7. Do NOT re-audit. One round only.

  Estimate first: if the remediation work would take more than ~15 minutes of LLM time, STOP and surface the situation to the user with a summary of findings. Do not unilaterally expand scope.

STEP E — VERIFY DONE
  Confirm all phases (incl. any added Phase N) are marked Complete in plan.md and their checklists. Then move to the next project in the order list above.

================================================================
PARALLEL-BATCH AWARENESS
================================================================

Batches 4 and 5 are editing the working tree concurrently on their own branches.

**Batch 6 ↔ Batch 5 shared files (1 known per Codex audit 2026-05-23)**:

- LOW-severity (textually disjoint regions per Codex spot-check):
  - tests/unit/simulation/entities/test_ship_serialization.py (PROJ-498 adds rejection-log assertion near line :591-598; PROJ-495 T3.28 parametrizes roundtrip tests at :328-415 and :767-879)

**Batch 6 ↔ Batch 4**: no known shared files.

Batch 6 operates on:
- data/modifiers.json
- data/components.json
- game/simulation/services/modifier_service.py
- game/simulation/components/modifier_manager.py (read-only check in PROJ-498)
- game/ui/services/component_service.py (read-only check in PROJ-498)
- game/ui/screens/builder/modifier_logic.py (read-only check in PROJ-498)
- game/simulation/battle_state.py (PROJ-498 logging)
- game/simulation/entities/ship_serialization.py (PROJ-498 logging)
- tests/regression/modifier_ability_snapshots/ (entire dir)
- tests/regression/snapshots/*.json (~65 baselines, in PROJ-499)
- docs/04_SERVICES.md, docs/05_ERROR_HANDLING.md, docs/guides/modifier_system.md, docs/guides/adding_modifiers.md

Batches 4/5 touch tests/unit/strategy/, tests/unit/ui/, tests/conftest.py, tests/fixtures/, tests/integration/. No overlap with Batch 6's surface.

**Risk to monitor**: PROJ-498 Phase 1 changes the `ModifierService` API by adding `check_allowance()`. If any Batch 4/5 test happens to import directly from `game.simulation.services.modifier_service` and asserts something about its public surface, that test could break unexpectedly on the converged branch. Mitigation: `check_allowance()` is additive; `is_modifier_allowed()` remains unchanged. Batch 4/5 tests should continue passing.

**Merge-time policy**: minimal expected merge work for this batch. The only converging-branch concern is the docs files (04_SERVICES.md, modifier_system.md, adding_modifiers.md) which Batch 1's PROJ-489 Phase 2 already edited. Verify your edits compose with PROJ-489's by reading the post-PROJ-489 main state before drafting your docs changes.

================================================================
COMMIT POLICY
================================================================

Do NOT commit unless your environment configuration explicitly authorizes per-batch commits. The original Batch 1 policy was no-commits-from-orchestrator (user inspects converged tree). If this batch runs on a separate machine/branch, the BRANCH OWNER may want batch-end commits to make merge tractable — but that decision belongs to the branch owner, not this orchestrator. Default: no commits.

If your subagents commit (some `/claude-proj-continue` flows do), accept the commits on the current branch and note them in your final report.

================================================================
WHEN ALL 3 PROJECTS COMPLETE
================================================================

Produce a final report for the user with:
- Each project's status (Complete / Blocked / Partial).
- Files modified across the batch.
- The user-decision outcomes from PROJ-497 (record verbatim — they affect downstream behavior).
- Any added Phase N's (per Codex audit remediation) with link to the new checklist.
- Any /claude-di-log entries you filed (note: DI-2026-05-23-004 and DI-2026-05-23-005 already exist; check for duplicates before logging new ones).
- Any user-escalations or stops.
- PROJ-499 specifically: report the baseline-diff scope after the bulk re-shoot. If the diff goes beyond additive key growth, that's a finding to surface.

DO NOT proceed to Batch 4 or Batch 5 work — those are owned by separate orchestrators on separate machines.
