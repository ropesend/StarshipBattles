# PROJ-470: Decisions Log

> **LOG ALL DECISIONS HERE**
> When you make a design choice or the user specifies a preference, add it to this table.
> Future agents will reference this to understand why things were done a certain way.

| Date | Decision | Rationale |
|------|----------|-----------|
| 2026-05-20 | Project initialized | Starting point for Pattern conformance - facade read-path, modal, event-bus + doc/hygiene drift (2026-05-20) |
| 2026-05-20 | Bundled all 15 verified findings from `2026-05-20_075227_pattern-audit` into ONE project, phased by severity (Critical/Major/Minor/Strategic) | Code relatedness over severity per Protocol 18; V=15 < 30 so a single bundle is the protocol default. Doc-drift kept beside related code work per Protocol 18 Note 2. Full bundling discussion in findings/bundling_decisions.md |
| 2026-05-21 | Codex planning consult run (single, autonomous-override) on bundling + UNCERTAIN decisions | Per the autonomous-override contract, replaced the protocol's AskUserQuestion steps with one Codex consult. Codex INPUT: (a) single-project bundling is correct (matches Protocol 18 V<30 rule); (b) agreed excluding UP-003/UP-004/UP-005 (PerPlayerUiState + FacadeSessionState already documented under Pattern #11; Declarative Dispatch Table recurs in only 2 registries, a Pattern #4 variant not a standalone pattern); (c) flagged LOC-001 (69 files) and FAC-001 (135+ sites) as scope-blowout risks that should be capped. MY DECISION (I own the call): accepted all of Codex's advice — single project; defer all 3 UNCERTAIN items; scope Phase 1 FAC-001 as policy+guard+first-slice; scope Phase 3 LOC-001 as top-10 triage with the remaining 59 files recorded as a separate future decomposition project. Consult artifact: AgentCoordination/Scratchpad/Consult/20260521T032218Z_pat-audit-bundling/response.md |
| 2026-05-21 | UNCERTAIN items UP-003, UP-004, UP-005 → DEFER (excluded from this project) | UP-003 PerPlayerUiState and UP-005 FacadeSessionState are already documented as sub-sections under Pattern #11 (docs validator rates #11 ACCURATE); UP-004 Declarative Dispatch Table recurs in only 2 registries, below the audit's 3+ promotion bar. Recorded in findings/verification_report.md for a future audit. |
| 2026-05-21 | OUT_OF_SCOPE: MAJ-H1 (simulation_adapter registry injection), MAJ-H4 (RaceSetup/NewGameSetup screens), Pattern #3 component_layers.py fallback | MAJ-H1 and MAJ-H4 were DISPUTED by the audit's own verification.md (correct injection pattern; setup wizards are not strategy modals). The component_layers.py `get_default_registry_provider()` fallback is strategy-layer (allowed by Pattern #3), narrow legacy-save scoped, with an intentional broad-catch comment — all 3 shard reviewers flagged it as a non-violation MINOR. Treated as accepted convention. |
| 2026-05-20 | **SCOPE REVISION (Protocol 06): DEFER facade read-path migration (FAC-001/FAC-002/FAC-003 → entire Phase 1 + Task 2.1) to PROJ-472.** | Dual independent+Codex review found the facade read-path gap is a deliberately-deferred architecture migration (PROJ-382 / U1–U3), not a cleanup "CRITICAL". Repo search confirms ~93 `game/ui/` files import `game.strategy` directly — this is a multi-PR migration program with prior deferral history, not a contained conformance fix. Moving it into PROJ-470 would balloon a conformance pass into an architecture refactor. Per Protocol 07, the scope is captured in PROJ-472 (plan/design/decisions populated). FAC items removed from PROJ-470 Phase 1 and Phase 2 Task 2.1. |
| 2026-05-20 | **SCOPE REVISION (Protocol 06): DEFER the broad LOC-ceiling triage program (Phase 3 Task 3.5, ten 600+ LOC files) out of PROJ-470.** | The LOC-ceiling decomposition of 69 production files is a maintainability program, not a pattern-conformance fix. Splitting 10 god-modules is independent design work (responsibility analysis, behavior-preservation tests per file) that does not belong in a conformance pass. Recorded as out-of-scope; logged via discovered-issues for future folding into a decomposition project. No LOC split is performed here except any trivially part of a fix already being made (none arose — surviving fixes are localized and stay well under 500 LOC). |
| 2026-05-20 | **SCOPE REVISION: TypeGuard swaps scoped per-site, NOT a mechanical sweep.** TG-001 KEEP, TG-002 KEEP, TG-003 SWAP, TG-004 KEEP. | Codex listed 5 candidate sites; the independent review flagged that `order_types.py` `isinstance(target, Planet/Fleet)` drives DIFFERENT serialized payloads (`planet_ref` vs `fleet_ref`) and the proposed `is_planet`/`is_fleet` TypeGuards are duck-typed (`_has_attrs`, strictly broader than exact `isinstance`). Per-site rulings: **TG-001 (`order_types.py:104,116,119` `to_dict`) — KEEP isinstance**: this is SERIALIZATION emitting branch-specific save payloads; a broader guard risks misclassifying targets into the wrong `*_ref` payload. **TG-002 (`fleet_dto.py:152-183`) — KEEP isinstance**: branches DTO field population (`target_id`/`target_hex`/`target_description`) by Planet vs Fleet; the duck-typed guards are strictly broader and the swap gains nothing while widening the type net on a behavior-branching factory. **TG-003 (`system_slice.py:132` `get_storm_names_at_hex`) — SWAP to `is_storm`**: PROVEN exactly equivalent for the zone domain — the zone spatial index registers stars, storms, and planets (galaxy_entity_registry.py:50,52,83); `storm_type` is unique to `Storm` across `game/strategy/` (only storm.py + storm_generator.py reference it) and `is_storm` requires BOTH `storm_type` AND `abilities`, so no star/planet can pass; read-only filter, not serialization/not value-affecting. Characterization test added. **TG-004 (`build_queue_source.py:294`) — KEEP isinstance**: dispatches DIFFERENT numeric production rates (determinism-adjacent); `is_fleet` is duck-typed and the else-branch silently handles "everything not a fleet" as a planet — keeping exact `isinstance` preserves the value-affecting branch precisely. |
| 2026-05-20 | KEEP the contained verified fixes: SettingsWindow modal-base conformance (MOD-001), event_bus stale docstring path (EVT-001), source_kind enum (ENUM-001), TG-003 swap, doc-drift (DOC-032/DOC-036), and the 3 undocumented-pattern doc-adds (UP-001/UP-002/UP-006). | These are localized, verified-against-live-code conformance fixes with bounded blast radius. They form the surviving PROJ-470 scope after the FAC + LOC + TG-001/002/004 deferrals/keeps above. |
| 2026-05-20 | UP-006 (RoleRegistry) documented as a STANDALONE Pattern #46 (not a Pattern #4 sub-section). | The audit suggested either; I chose a standalone entry because the layered-load (base→mods→user) + `allow_runtime_add` gating + `register_invalidation_callback` + `get_default_/set_default_/reset_default_` accessor recipe is substantial and non-obvious, and the cross-reference "a variant of Pattern #4" in the #46 header preserves the relationship without burying the recipe inside #4. New entries added as Pattern #44 (HabitabilityFactor Registry), #45 (AbilityMetadataRegistry), #46 (RoleRegistry) after #43. |
| 2026-05-20 | ENUM-001 implemented as a `StrEnum` named `SourceKind` (not `Literal`). | StrEnum members ARE `str`, so the existing provider-dict consumers that compare with raw strings (`provider.get('source_kind') == 'storm'`, `!= 'star'`, `f"sector:{source_kind}"`) and the universal-collector payload (`{'source_kind': source.source_kind}`) keep working unchanged — no save/serialization format change. All 7 ability-source adapters return typed members; adding an 8th kind without a new member is a type error. Exported from `game.core.protocols`. |
| 2026-05-20 | Pattern #10 doc: documented the two-bus divergence (`WorkshopEventBus` vs `EventBus`) as INTENTIONAL (different domains), no shared `EventBusProtocol` planned. | Confirmed against live code: `game/core/events/event_bus.py` does not exist (the canonical bus is `game/core/event_logging.py::EventBus`, moved by PROJ-390). The two buses have different payload contracts and non-overlapping scopes; a shared protocol is not warranted. Production docstring path fixed (EVT-001); doc now names both classes explicitly. |
| 2026-05-20 | **Codex audit (one round) run; 4 findings VERIFIED, 2 REJECTED (Codex's own "Not Confirmed").** All 4 verified findings fixed in Phase 5. | Codex completed but its output failed the consult/v1 frontmatter schema, so the harness flagged `missing-response` and moved the report to `...audit.md.invalid-output-*.txt`; full findings recovered from the log. **VERIFIED:** (1) MAJOR — `validate_audit_ready.py PROJ-470` FAILED on unchecked Phase-1 (extracted) + Task-3.2 boxes; stale char-test path cited in phase_3 checklist. (2) MEDIUM — SettingsWindow modality test used `bypass_init`, which skips the production `register_modal()`/`is_blocking` path (characterization gap). (3) MEDIUM — `source_kind` test only grep'd source text; never instantiated adapters or exercised the collector payload (characterization gap). (4) MINOR — `settings_window.py` used legacy `Optional[Callable[...]]` instead of `... | None` (3.13 convention). **REJECTED (Codex did not confirm):** no `isinstance->TypeGuard` payload/behavior regression (only TG-003 swapped; serialization sites correctly kept on isinstance); no SettingsWindow lifecycle/focus break (kill path sound). |
