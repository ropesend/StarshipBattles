# PROJ-430: Decisions Log

> **LOG ALL DECISIONS HERE**
> When you make a design choice or the user specifies a preference, add it to this table.
> Future agents will reference this to understand why things were done a certain way.

| Date | Decision | Rationale |
|------|----------|-----------|
| 2026-05-16 | Project initialized from TD-08 plan (execution rank 9 of 10 in the strategy tech-debt arc) | Soft predecessor PROJ-423 (TD-02) — TD-02's shared bootstrap path lets the `_resolve_economy_config` fallback disappear cleanly in Phase 5. Soft predecessor TD-03 — `FacadeCommands` constructor decouples it from registry reshape. No hard prerequisites. |
| 2026-05-16 | **Red contract first** — Phase 1 rewrites `test_strategy_session_facade_public_api.py` to assert the target shape (10 attrs) before any production code change | Strict TDD per AGENTS.md/CLAUDE.md. The contract test is the canonical anchor; running it against current `main` produces the documented red, proving the plan drives behavior change rather than backfilling it. |
| 2026-05-16 | **Grouped accessors, not flat shrink** — collapse 68 public methods to 2 top-level callables + 9 group accessors (`commands`, `fleets`, `systems`, `planets`, `empires`, `events`, `session_meta`, `economy`, `validation`) plus `facade_state` | PROJ-309 sub-phase 3.7 already did the *internal* split; the boundary stayed flat. Grouping is the only way to make the *cost* of a new top-level entry visible — feature additions now have to be either a new group or a method on an existing group with a clear domain owner. Per the TD-08 plan's "payoff approach". |
| 2026-05-16 | **Strip redundant prefixes** when methods move into a namespace — `dispatch_issue_move` -> `commands.issue_move`; `get_fleet` -> `fleets.get`; `get_fleets_at_hex` -> `fleets.at_hex`; `get_fleet_path_preview` -> `fleets.path_preview`; etc. | The actual API shrink — callers say `facade.fleets.path_preview(...)` instead of `facade.get_fleet_path_preview(...)`. Mechanical, derivable from `specs_by_facade_helper()` for the dispatch case. |
| 2026-05-16 | **Delete cache forwarders root-cause** in Phase 5, after Phase 4 migrates the 3 test files onto a new `FacadeSessionState.seed_*` public seam | Per AGENTS.md rule 3 (no compat shims / parallel old + new path). The `seed_` prefix makes the test-only intent explicit. The forwarders cannot be deleted before tests are migrated — the contract test and 2 consumer tests would go red and block deletion. |
| 2026-05-16 | **Migrate `test_colony_demographic_view.py` via `seed_*` helpers**, not by adding a deprecation warning or keeping the writable property indefinitely | Test pins implementation detail; the behavioral assertions (what `get_colony_demographic_view` returns) stay intact. Only the seeding mechanism changes. Diff before/after must show no `assert` lines dropped. Constructor DI (passing the registry through the slice) is preferred where it can be done without refactor sprawl — assessed task-by-task in Phase 4. |
| 2026-05-16 | **Rewrite `test_facade_indices.py`'s `hasattr` assertion** to a behavioral assertion (`facade.planets.get(42) is facade.planets.get(42)` — cached identity) | The test currently pins an implementation detail (the `_planet_index` field exists). The actual contract is "second call returns the same object." The behavioral form survives the field rename or any future caching refactor. |
| 2026-05-16 | **Delete `_resolve_economy_config` legacy alias** in Phase 5 | Per AGENTS.md rule 3. If `economy.resolve_config()` (the renamed public method on `FacadeEconomyQueries`) covers every caller, the alias is gone. If TD-02 has not yet landed and the warning-log fallback is still load-bearing, the alias becomes `economy.resolve_config()` and the warning-log fallback survives there as documented carried debt until TD-02 cleans it up. |
| 2026-05-16 | **`FacadeCommands` takes `(helper_name, command_cls)` iterable as constructor parameter**, not `command_registry` import | Decouples the namespace from TD-03's potential reshape of `specs_by_facade_helper()`. If TD-03 introduces `OrderMetadataView`, the iterable can be sourced from the new shape without touching `FacadeCommands`. |
| 2026-05-16 | **No top-level shim during migration** — UI callers are rewritten *before* the legacy surface is deleted, not in parallel | Per AGENTS.md no-shim rule and TD-08 plan's weak-LLM guardrail: "Do not keep both the full flat API and the grouped API indefinitely. The grouped API is transitional only until callers are migrated." Phase 3 finishes the UI rewrite before Phase 5 deletes the legacy surface. |
| 2026-05-16 | **One commit per UI screen group** in Phase 3, with `pytest tests/unit/ui tests/integration -x` after each | Keeps diffs reviewable, surfaces breakage early. The auto-installer's registry enumeration makes the rename list generable rather than typed — no hand-curated UI list. |
| 2026-05-16 | **`handle_command(...)` and `process_turn(...)` are the only top-level behavioral entry points that survive** | Per TD-08 weak-LLM guardrail: do not add new top-level facade methods during the migration. New methods land inside the appropriate group from now on. |
| 2026-05-16 | Opt into the 03c phase-aware execution protocol | Per `Projects/protocols/01_initialize_project.md` Phase D, new projects SHOULD opt in. The marker is set in `plan.md` and `phase_state.json` is initialized at scaffold time. |
| 2026-05-16 | Six phases mapped 1:1 to TD-08's six remediation phases | Phase 1 (red contract first) → Phase 2 (grouped accessors as new primary surface) → Phase 3 (UI caller migration, 25 files) → Phase 4 (test seeding migration off cache forwarders) → Phase 5 (root-cause delete of legacy surface) → Phase 6 (docs). No phase reordering or merging from the source plan. |
| 2026-05-17 | Phase 3: actual UI caller count was 16 files, not 25 (plan estimate). Plus 1 strategy service caller (`cargo_transfer_service.py`) and ~50 test files. | Re-counted via `rg -l "facade\.(dispatch_\|get_\|can_)" game/ui`. The TD-08 source plan was generated against an older snapshot; either earlier PROJ-411 work reduced the count or the original number reflected per-call-site (42 sites) rather than per-file. |
| 2026-05-17 | Phase 4: chose `seed_*` helper approach (not constructor DI) for both `seed_planet_index` and `seed_race_registry`. | Constructor DI for race_registry would mean threading it through `FacadeSessionState.__init__`, `EconomySlice.__init__`, and `StrategySessionFacade.__init__`, plus updating every call site. That's the "refactor sprawl" the plan explicitly warned against. The `seed_*` helpers are 4 lines each, clearly marked test-only in docstrings, and let `test_colony_demographic_view.py` migrate with two-line `_facade_for` edits. |
| 2026-05-17 | Phase 3: extended legacy migration to ~50 test files (not just the 3 cache-forwarder test files). | After production callers stopped calling `facade.get_X(...)`, MagicMock-based tests still set `mock_facade.get_X.return_value = ...` — which would silently never be exercised after Phase 5 since production calls `facade.fleets.get(...)` instead. Migrating the test mocks in Phase 3 (when production rewrites happened) keeps the test coverage live; Phase 4 then handles only the *cache-forwarder* writes. |
| 2026-05-17 | Phase 3: extended `MockSession` test doubles with `economy` / `session_meta` `@property` shims rather than re-routing each call site. | 7 build-queue MockSession classes act as both session AND facade in their tests. Adding two `@property` accessors that return inline namespace objects is a 30-line shim per file vs. rewriting hundreds of `self.facade.get_X` test call sites inside those mocks' caller code. Keeps test coverage stable while production code consumes the new shape. |
