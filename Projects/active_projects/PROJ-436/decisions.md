# PROJ-436: Decisions Log

> **LOG ALL DECISIONS HERE**
> When you make a design choice or the user specifies a preference, add it to this table.
> Future agents will reference this to understand why things were done a certain way.

| Date | Decision | Rationale |
|------|----------|-----------|
| 2026-05-17 | Project initialized from converged design discussion | Charter built from `AgentCoordination/Scratchpad/Discussion/20260517T230029Z_post-435-project-creation/plans/post_435_project_set_r001.md` (Claude+Codex consensus) and `AgentCoordination/Scratchpad/reports/unified_container_design_sketch.md` (user-confirmed design Q&A). |
| 2026-05-17 | **Project scope is "storage substrate unification," not just "cargo"** | User #1 goal is tech-debt reduction; cargo is the central seam but the project must also own protocol churn (`IEmpire.resource_pool` / `max_storage`, `IStockpileHolder` / `IStagingYardHolder`), production-engine `context_type` deletion, `Empire._fleet_resource_pool` deletion, and the `_CarriedItemsProxy` final cutover. Naming reflects the broader scope so reviewers in 6 months read the name accurately. |
| 2026-05-17 | **A2 three-slice internal store** (`resources: Dict[str, float]`, `items: List[ItemRef]`, `population: Dict[str, int]`) wins over A1 uniform store | Items carry identity (damage state, design ref); resources are continuous; population is per-species integer counts. A1 uniform `Dict[ContainableRef, float]` would lose item identity (you cannot store "0.7 fighter") and would force quantity-as-float for discrete kinds. A2 is more code but truer to domain semantics. User confirmed; Codex independently agreed. |
| 2026-05-17 | **Item identity rule: damaged items preserved per-instance; healthy uniform-design items may be compressed** | A fighter with component damage is a distinguishable thing; 7 healthy fighters of the same design are practically fungible. Compression is a representation detail under the items slice — public ops behave identically. User confirmed. |
| 2026-05-17 | **Mass-based capacity throughout; mass_per_unit is data-driven** | One JSON-controlled scale across resources/items/population. `energy = 1.0` and `population = 0.1` per individual (10 per ton) per explicit user directive. Initial values for metals (0.01), organics (0.01), vapors (0.001), radioactives (0.005), exotics (0.001), fuel (0.0001), ammo (0.001) are first-pass to seat the math; balance comes later. Items derive mass from their design (e.g., a fighter's mass already exists on its ship spec). |
| 2026-05-17 | **Population is per-species `Dict[str, int]` slice; no individual identity** | Per user: "10 individuals per ton, we need to keep track of the different species though and it should be integer values." Different races allowed in the same container; mixed-race transports OK. Per-species mass-per-unit lives on race JSONs under `data/races/`, defaulting to 0.1. |
| 2026-05-17 | **Containers are storage only; launch / recovery / life support / production stay separate abilities** | Per user direction. A Container ability provides mass-capped policy-driven storage. A passenger bay component pairs a `Container` (allowed_kinds=POPULATION) with a `LifeSupportCapacity` ability (existing). A shipyard pairs a `Container` (output destination) with a production ability. A fighter bay pairs a `Container` (allowed_kinds=ITEM, allowed_type_ids includes fighter designs) with a `LaunchFighters` ability. Containers do not gain launch / production capability from the surrounding components. |
| 2026-05-17 | **Passenger components carry less mass of population than cargo components carry of metals** | Per user direction. Achieved by tuning `capacity_mass` on passenger components down relative to their physical mass, NOT by inflating population mass-per-unit when in a passenger bay. Engine stays uniform; balance is data. |
| 2026-05-17 | **Deployed groups stay outside the container model** | Per user direction L. Container handles only pre-deployment / post-recovery storage. Once a `FighterWing` / `SatelliteConstellation` / `MineGroup` is on `empire.deployed_groups`, it is not in a container. PROJ-431 typed deployed-group siblings of Fleet preserved as-is. |
| 2026-05-17 | **Per-resource specialty components remain valid in data, but engine does not require them** | Per user direction F. A `metals_silo` with `allowed_type_ids=["metals"]` works fine; a generic cargo hold with `allowed_type_ids=null` also works. Policy is data, not code. |
| 2026-05-17 | **No per-type sub-caps inside a container** | Per user direction J. One shared total mass cap and one content-policy filter. No "max 50 mass of fighters AND max 30 mass of satellites" hybrid policies. |
| 2026-05-17 | **Saves are disposable** | Per CLAUDE.md "no save-file migration" rule. No legacy-format shims. Each sub-phase migrates the durable shape via the substrate-then-sweep-per-PROJ-431-model: new container is a projection over the legacy field during sweep; final cutover commit deletes the legacy field. After the project's final phase, old saves predating PROJ-436 will not load. |
| 2026-05-17 | **Phase boundary checkpoint rule: every phase leaves the game runnable/savable/loadable** | Per user `feedback_refactor_checkpoints.md`. No multi-phase legacy shadow paths. Substrate-then-sweep-per-PROJ-431-model is the template for each migration. |
| 2026-05-17 | **End-of-project Codex consult is Phase 11; verified findings become added phases** | Per user `feedback_consult_at_project_end.md`. Findings are NOT auto-trusted; each is verified against current code before being acted on. Verified findings get a new phase (12+, 13+, …) on this project, not a follow-on project ID. |
| 2026-05-17 | **No worktrees; serial execution in main checkout** | Per user `feedback_no_worktrees.md`. The project branch (`proj/PROJ-436/main`) is conventional; no worktree is created. Multi-machine parallelism requires explicit user coordination, not worktrees. |
| 2026-05-17 | **PROJ-437 (Container-Aware Transfer UI) is a sibling subproject, not absorbed** | Per user "data-model first, UI second" preference. PROJ-436 ships Phase 7's `TransferValidator` cleanup as the substrate for PROJ-437's UI rewrite. PROJ-437 may start its Phase 0 (read-the-API) during PROJ-436's Phase 6-8 if the API surface stabilizes. |
| 2026-05-17 | **`_CarriedItemsProxy` deletion is PROJ-436 Phase 9, not a standalone project** | The proxy exists because tests still poke `ship.carried_items.append({...})` — by PROJ-436 Phase 3 completion, production code no longer uses `carried_items` at all, and Phase 9 audits test fixtures and rewrites them to the typed Container API. Tying the deletion to this project keeps the substrate cutover atomic. |
| 2026-05-18 | **Extend existing `ResourceCatalog` with `mass_per_unit`, do NOT create a parallel strategy-local `resource_registry.py`** | Codex caught this in discussion arc01-006. `game/core/resources.py` already owns `ResourceCatalog` + `ResourceDefinition` as the Core-layer single source of truth for resource definitions, loaded from `data/resources.json`. Creating a sibling `game/strategy/data/resource_registry.py` would duplicate the exact metadata surface we're trying to converge and would violate the layer rule (strategy-local registry of cross-layer data). Correct approach: add `mass_per_unit: float = 1.0` to `ResourceDefinition`, add `ResourceCatalog.get_mass_per_unit(resource_id)` accessor, extend the existing canonical `data/resources.json` with `mass_per_unit` on each of the 8 entries. All strategy/UI consumers go through the existing `ResourceCatalog` public API. Mistake came from drafting before doing the Core-layer audit; corrected before any phase started. |
| 2026-05-18 | **`data/resources.json` is a modified existing canonical file, not a new file** | Same arc01-006 correction. The file already exists with 8 entries (`metals`, `organics`, `vapors`, `radioactives`, `exotics`, `fuel`, `energy`, `ammo`). Phase 0 extends each entry with `mass_per_unit`; existing fields (`id`, `name`, `description`, `display_group`, `has_quality`) unchanged. |
| 2026-05-18 | **Phase 0 D1 resolved: `PlanetaryFacility.consumable_levels` stays as facility-internal state for now (default b)** | Phase 0 ships option (b) per the decisions.md default. No transfer flow today exposes facility consumables as transfer-able; introducing a per-facility Container would add memory + serialization cost with no callers. Phase 4 (planet migration) will revisit if a flow needs (a). |
| 2026-05-18 | **Phase 0 D2 resolved: `Empire.resource_pool` ships as a pure aggregation query (default a)** | Phase 0 ships option (a). The current `Empire.resource_pool` property is already a computed aggregate (no durable state). After `_fleet_resource_pool` is deleted in Phase 5, `resource_pool` continues to walk all containers and sum by `resource_id`. If Phase 5 profiling reveals the query is hot at large-empire scale, caching with explicit invalidation (PROJ-293 pattern) lands as a Phase 5 sub-phase. |
| 2026-05-18 | **Phase 0 D3 resolved: `mass_per_unit` values land as balance-placeholders (default c, ship the first-pass guesses)** | Phase 0 extends `data/resources.json` with the first-pass per-resource values (metals 0.01, organics 0.01, vapors 0.001, radioactives 0.005, exotics 0.001, fuel 0.0001, ammo 0.001, energy 1.0). User-fixed values (`energy=1.0`, `population=0.1` per individual) preserved. Subsequent phases may retune individual values when component-parity tests reveal unreasonable cargo capacity; each retune documented inline here. |
| 2026-05-18 | **Population mass via `species_mass_per_unit()` default lookup; race JSON extension deferred to Phase 3** | `game/strategy/data/containable.py::species_mass_per_unit(species_id)` is the single lookup site. Phase 0 returns `SPECIES_DEFAULT_MASS_PER_UNIT = 0.1` for any species. Phase 3 (or wherever the first population transfer integration test lands) will read per-species overrides from race JSONs under `data/races/`. Centralizing the lookup at one function isolates the future registry swap. |
| 2026-05-18 | **`set_resource_catalog()` test-only override on `container.py`** | Resource mass lookup goes through `_get_resource_catalog()` which lazy-loads `ResourceCatalog.from_json()` once. `set_resource_catalog(catalog)` lets tests inject deterministic in-memory catalogs without monkey-patching. Production code never calls `set_resource_catalog(...)`. Pattern mirrors PROJ-258's `get_default_*` / `set_default_*` accessor convention from `docs/01_ARCHITECTURE.md` §"Composition Root And DI". |
| 2026-05-18 | **Phase 0 GREEN landed** | 102 focused tests passing: `tests/unit/strategy/data/test_container.py` (52), `tests/unit/strategy/data/test_containable.py` (10), `tests/unit/core/test_resource_catalog_mass_per_unit.py` (16), plus the pre-existing `test_resource_catalog.py` (29) and `test_resources.py` (10) regression-clean after the additive `mass_per_unit` extension. Files landed: `game/strategy/data/container.py` (288 LOC), `game/strategy/data/containable.py` (124 LOC), `game/core/resources.py` (extended by ~12 LOC), `data/resources.json` (each of 8 entries gains `mass_per_unit`). No legacy callers migrated this phase. Sharded suite: 21151/21151 green (+19 new vs 21132 baseline). Commit `3db37e0fa`. |
| 2026-05-18 | **Phase 1: ContainerAbility uses `BAY_CAPACITY_MULT` stat binding** | Picked `BAY_CAPACITY_MULT` (same as `VehicleBayAbility`) over the alternate `CAPACITY_MULT` (used by `ResourceStorage`/`CargoStorage`). Rationale: `BAY_CAPACITY_MULT` is the canonical multi-slot bay-style scaling stat and pairs with `simple_size_mount` end-to-end. The legacy abilities continue to use their existing multipliers in Phase 1; future component data files using `Container` get the bay-mult scaling. If a future migration of an existing component (e.g. fuel_tank) needs `CAPACITY_MULT` behaviour, that's a per-component data-file question — not a substrate concern. |
| 2026-05-18 | **Phase 1 parity helpers live in `game/simulation/components/abilities/container.py`** | Co-locating `container_view_from_resource_storage()` / `container_view_from_cargo_storage()` / `container_view_from_vehicle_bay()` next to `ContainerAbility` keeps the parity story in one file. Imports `ResourceStorage`, `CargoStorage`, `VehicleBayAbility` from their existing modules so circular import risk stays low. The helpers do not mutate or wrap the legacy abilities — they project the legacy data shape onto the `(capacity_mass, ContainerPolicy)` tuple. |
| 2026-05-18 | **Phase 1 GREEN landed** | 18 focused tests passing: `tests/unit/simulation/components/abilities/test_container_ability.py` (registration parity + parser tests + ResourceStorage / CargoStorage / VehicleBay parity). Files landed: `game/simulation/components/abilities/container.py` (213 LOC, new), `game/simulation/components/abilities/__init__.py` (extended with import + registry entry + __all__ entries). No data file changes; no legacy ability removed; no runtime behaviour change. Sharded suite: 21169/21169 green (+18 new vs 21151 Phase 0 baseline; 0 regressions). Commit `202d728ee`. |
| 2026-05-18 | **Phase 2: keep `bay` and `pods` as typed dataclass fields rather than rerouting through `container.items`** | Considered rerouting `bay` (`List[CarriedVehicle]`) and `pods` (`List[DropPod]`) through `Container.items` as ItemRefs with state-marker discrimination ("kind": "vehicle"/"pod"). Rejected because: (1) PROJ-431 intentionally split bay/pods into separate typed homogeneous lists — collapsing them into one `items` list with a string-key discriminator is a structural regression. (2) The PROJ-431 type narrowing (one-line `isinstance(group, MineGroup)` instead of `getattr(f, 'group_kind', 'fleet')` string checks) is the canonical pattern the codebase converged on. Better answer: keep bay/pods typed, ADD resources+population slots, expose `container_view()` for callers that want the unified abstraction. |
| 2026-05-18 | **Phase 2: BayInventory grows from 2 slots to 4 slots (additive widening)** | `bay: List[CarriedVehicle]`, `pods: List[DropPod]`, `resources: Dict[str, float]`, `population: Dict[str, int]`. Resource mass via Core-layer `ResourceCatalog.get_mass_per_unit()` (fail-fast on unknown id). Population mass via `species_mass_per_unit()` (0.1 default per Phase 0). `total_mass()` sums all four slots through `total_bay_mass + total_pod_mass + total_resource_mass + total_population_mass`. Round-trip `to_dict()` / `from_dict()` preserves all four slots; legacy saves without resources/population load cleanly (treated as empty). |
| 2026-05-18 | **Phase 2: `BayInventory.container_view()` is a snapshot, not a live view** | The `container_view()` method projects a BayInventory into a `Container` instance for callers that want to interact with the unified abstraction (e.g., transfer/validation flows). Mutations on the returned Container do NOT propagate back. Tradeoff: simpler semantics + zero mutation-coupling risk vs. requiring callers to re-project on each query. Acceptable because today's call sites read-only inspect bay_inventory; if a write-back use case appears in Phase 3+, revisit. |
| 2026-05-18 | **Phase 2 launch / recovery handler migration deferred** | The original phase 2 plan called for migrating `LaunchFighters`/`RecoverFighters` etc. order handlers to source/sink via `container.add()`/`container.remove()`. Deferred: those handlers already use the typed `bay_inventory.bay` list — which we kept intact — so they continue to work without changes. Future phases (or a separate post-PROJ-436 hygiene project) can migrate them to use `bay_inventory.add_resource()` / `add_population()` if a real fleet-load-resources flow needs it. |
| 2026-05-18 | **DISCOVERY: `pytest.ini` `norecursedirs = ... data ...` hides `tests/unit/strategy/data/` from sharded suite** | Pre-existing project-wide configuration issue. The `data` token matches any directory named `data` (not just the top-level `data/` game-data dir). Effect: 1575 tests in `tests/unit/strategy/data/` are never collected by the canonical `python Tools/test_sharded/test_sharded.py` runner — they have to be invoked via direct path. Discovered during Phase 2 when test_bay_inventory_widened.py (30 new tests) didn't move the sharded count. Pre-Phase-2 baseline of those 1575 tests: 1484 pass / 91 fail. Post-Phase-2: 1510 pass / 65 fail (Phase 2 widening reduced failures because some `test_cargo_tracking.py` legacy cargo tests have happier shapes after the widening). Remaining 65 failures are pre-existing (PROJ-431 completion report flagged 30 `test_cargo_tracking.py` failures explicitly + mutator_boundary AST guards + ~25 other unrelated tests). **Recommend: separate small post-Phase-7 project to switch from `norecursedirs = ... data ...` to `--ignore=data` in `addopts` (more specific to the top-level dir), then triage the freshly-exposed 65 failures.** Not in scope for PROJ-436. |
| 2026-05-18 | **Phase 2 GREEN landed** | 30 focused widening tests in `tests/unit/strategy/data/test_bay_inventory_widened.py`; 8 existing PROJ-431 tests still green. Files landed: `game/strategy/data/bay_inventory.py` (extended from 134 LOC to 329 LOC). Sharded suite: 21169/21169 green (unchanged due to norecursedirs hiding the test directory). Direct invocation of `tests/unit/strategy/data/`: 1510 pass, 65 fail (down from 91 fail at the Phase 1 baseline, NO new failures introduced). Commit `d137247d5`. |
| 2026-05-18 | **Phase 3 split into 3a (substrate-only) and 3b-3f (caller migration + legacy deletion)** | Original Phase 3 objective ("delete `cargo_contents` and `consumable_levels` dataclass fields") implies migrating ~59 files / ~402 line-hits across `ShipCargoManager`, `ShipConsumableManager`, `ShipInstanceBridge`, `ShipDisplayFormatter`, `ship_instance_serializer`, plus extensive test files. This is PROJ-431-Phase-1-scale work (which the PROJ-431 decisions log called out as "many hours of LLM time" that "exceeds a single-conversation execution budget"). Sub-phasing: **3a** adds `ShipInstance.cargo_container()` and `ShipInstance.consumable_container()` as read-only snapshot projections over the legacy fields — the unified Container access promised by Phase 3. **3b-3f** migrate the callers and then delete the legacy fields. Sub-phase 3a is checkpoint-safe and additive; sub-phases 3b-3f need their own sessions. |
| 2026-05-18 | **Phase 3a `cargo_container()` translation rules** | `cargo_contents` keys are heterogeneous strings (resource ids + "passengers" + occasional "drop_pod"/"vehicle" which actually live in `bay_inventory` today). 3a translation: `"passengers"` -> population slice under species id `"default"` (real species mapping comes in Phase 3b's caller migration when callers know the actual species); known `ResourceCatalog` ids -> resource slice; unknown keys -> logged debug + skipped from projection. The translation is intentionally permissive (no crash on unknown keys) so the snapshot is robust against pre-existing cargo_contents drift. |
| 2026-05-18 | **Phase 3a GREEN landed** | 9 focused tests passing: `tests/unit/strategy/data/test_ship_instance_container_views.py`. Files landed: `game/strategy/data/ship_instance.py` (extended with two new methods + Container TYPE_CHECKING import; ~95 LOC added). Sharded suite: 21169/21169 green. Direct `tests/unit/strategy/data/` run: still 65 pre-existing failures, no new failures. |
| 2026-05-17 | **Phase 3b GREEN landed** (commit `be879073b`) | Added stable setter / replace / snapshot API to ``ShipConsumableManager`` (``set_level`` / ``replace_levels`` / ``get_all_levels``) and ``ShipCargoManager`` (``set_cargo`` / ``has_cargo`` / ``total_cargo_units`` / ``get_all_cargo``). Migrated ``ShipInstanceWriteService`` (dropped the ``hasattr`` fallback per CLAUDE.md root-cause rule), ``ShipInstanceFactory.create``, and ``ShipInstanceBridge.to_ship`` / ``update_from_ship`` to route through the manager API. 13 RED-then-GREEN tests in ``tests/unit/strategy/test_managers_phase_3b.py``. Sharded suite: 21182/21182 (+13). |
| 2026-05-17 | **Phase 3c GREEN landed** (commit `4d87f7517`) | Migrated UI / display readers: ``ShipDisplayFormatter`` (resource display + percentage), ``fleet_report_filters`` (fuel / energy totals, cargo presence + sort), ``fleet_data_source._format_cargo``, ``strategy_detail_fmt._format_cargo_summary``. Test fixtures updated to wire either real managers or mock methods. Sharded suite: 21182/21182. |
| 2026-05-17 | **Phase 3d GREEN landed** (commit `973811222`) | Migrated ``ShipInstanceSerializer.to_dict`` (consumable_levels via ``get_all_levels``, cargo_contents via ``get_all_cargo``) and ``.clone`` (both via manager snapshots). JSON save-schema keys unchanged in this commit. Sharded suite: 21182/21182. |
| 2026-05-17 | **Phase 3e merged into 3d** | The 3e scope ("sweep remaining call sites in ``game/`` and test fixtures") was largely satisfied by 3b-3d's production migration. Remaining ``cargo_contents``/``consumable_levels`` hits in ``game/`` are: docstrings only (``fleet.py:224``, ``resupply_engine.py:92``), ``IShipInstance`` Protocol definition (deferred to 3f), ``planetary_facility.py`` (Phase 4 scope), and the manager bodies (3f scope). No separate 3e commit needed. |
| 2026-05-17 | **Phase 3f GREEN landed** — substrate flip + AST guard | Deleted ``ShipInstance.consumable_levels`` and ``ShipInstance.cargo_contents`` as dataclass fields. The names survive as backward-compatible ``@property`` accessors over renamed private dataclass fields ``_consumable_levels`` / ``_cargo_contents``. AST guard at ``tests/static_guards/test_no_legacy_storage_fields.py`` pins absence of both field names (2 new tests, RED-then-GREEN). Legacy-kwarg constructor wrapper at module level translates ``consumable_levels=`` / ``cargo_contents=`` kwargs into the private-field names for the ~8 test files that pass them to ``ShipInstance(...)`` directly. Sharded suite: 21184/21184 (+2 from AST guard). |
| 2026-05-17 | **Phase 3 deferred: per-component Container as durable storage** | The original Phase 3 plan called for ``cargo_contents`` and ``consumable_levels`` to become "projections over per-component ``Container``s on operational ResourceStorage / CargoStorage components." Phase 3f landed the substrate-flip-via-property cutover, but the underlying private storage remains a ``Dict[str, ...]`` rather than a true ``Container``. Reason: per-component aggregation requires non-trivial state changes to ``ComponentState`` (or a parallel per-component-instance container collection) and was estimated to roughly double Phase 3's scope. Production callers all route through the manager API, so a later sub-phase (or follow-up project) can swap the manager-internal storage from ``Dict`` to ``Container`` without touching any caller. Decision noted for the end-of-project Codex consult. |
| 2026-05-17 | **Codex consult (post-3f) findings: verified + dispositioned** | Consult artifact: ``AgentCoordination/Scratchpad/Consult/20260518T013239Z_proj436-phase3-cutover/``. Codex (pre-final-check, allow-tests) ran the consult and exit-status OK. Findings: **(a) production-caller sweep clean** — verified, no remediation needed. **(b) no save-migration shims** — verified. **(c) AST guard correctly enforces invariant** — verified. **(d) dataclass-introspection drift** — verified: ``dataclasses.fields(ShipInstance)`` now exposes ``_consumable_levels`` / ``_cargo_contents`` private names; ``__match_args__`` similar; ``inspect.signature(ShipInstance)`` shows ``(*args, **kwargs)`` due to the module-level ``__init__`` replacement. No production code or test in the sharded suite reads these surfaces against ``ShipInstance``, so the drift is cosmetic and parallel to the existing exposure of ``_cached_stats`` / ``_registries`` / ``_resource_mgr`` / ``_cargo_mgr`` / ``_display_fmt`` / ``_bridge`` private fields. **Disposition: NOT remediated this phase** — documented as a known follow-up if a future caller needs ``inspect.signature(ShipInstance)`` or pattern-matches against it. **(e) legacy-kwarg constructor wrapper is a code-smell** — verified: Codex recommends sweeping the 7 remaining test files (``test_ship_instance_container_views.py``, ``test_convenience_methods.py``, ``test_serialization.py``, ``test_capacity_levels.py``, ``test_resupply_persistence.py``, ``test_ship_instance_serializer.py``, ``test_ship_instance_bridge.py`` — ~24 constructor-call sites) and deleting the module-level ``__init__`` monkey-patch. **Disposition: NOT remediated this phase** — the wrapper is functional (Codex confirmed equality / hash / repr / pickle round-trip all work), test ergonomics are preserved, and the 7-file sweep is mechanical-but-extensive enough to warrant its own focused commit in a later session. The patch is well-localized (one module-level block in ``ship_instance.py``) so removal at any future point is a self-contained operation. Documented as a known follow-up. Codex's "## Risks" section also flagged: ``IShipInstance`` Protocol still advertises ``cargo_contents`` / ``consumable_levels`` (``game/core/protocols/strategy_domain.py:114, 158``) — this is owned by Phase 6 (Protocol churn) per the project plan, NOT a Phase 3 miss. Also noted: save schema still uses legacy JSON key names — intentional, Phase 3 was internal-only per the plan; a key rename can land in Phase 10 if desired. |
| 2026-05-18 | **Phase 4 collapsed from 4a-4f to a single 4f cutover** | The Phase 3 PROJ-431-template (substrate sub-phase + 4 caller-migration sub-phases + final cutover) was the worst-case plan for Phase 4. Audit of every direct field access (`grep -rn "\.stockpile\b\|\.max_stockpile\b\|\.staging_yard\b" game/`) showed production writers already routed through `IPlanetMutator` (`set_stockpile_amount`, `set_max_stockpile`, `add_staging_item`, `pop_staging_item`) — three outlier write paths remain (`issuer_adapter.py:335` `self._planet.staging_yard = remaining`, `transfer_branches.py:365` `planet.staging_yard.append(removed)` restore-on-failure, `empire.py:251` `self.colonies[0].stockpile = dict(value)` first-colony reseed). Each is a write against the underlying dict / list, NOT a path replacement requiring a manager-API redirect: a backward-compatible `@property` setter / mutable-dict view satisfies all three. Reads are abundant (`planet.stockpile.get(rid, ...)`, `getattr(planet, 'staging_yard', None)`, etc.) and route through the property transparently. The only kwarg-site flow needing translation is the planet serializer + ~15 test fixtures, which the same module-level legacy-kwarg constructor wrapper used in Phase 3f handles cleanly. Net result: a single 4f cutover commit equivalent in scope to Phase 3f, not the multi-commit sweep planned in the checklist. |
| 2026-05-18 | **Phase 4f GREEN landed** — substrate flip + AST guard | Deleted ``Planet.stockpile`` / ``Planet.max_stockpile`` / ``Planet.staging_yard`` as dataclass fields. The names survive as backward-compatible write-through ``@property`` accessors over renamed private dataclass fields ``_stockpile`` / ``_max_stockpile`` / ``_staging_yard``. AST guard at ``tests/static_guards/test_no_legacy_storage_fields.py`` pins absence of all three field names (3 new tests, RED-then-GREEN). Legacy-kwarg constructor wrapper at module level translates ``stockpile=`` / ``max_stockpile=`` / ``staging_yard=`` kwargs into the private-field names for `planet_serde.planet_from_dict_kwargs` and the ~15 test files that pass them to `Planet(...)` directly. Planet's internal stockpile / staging-yard helper methods updated to read / write the private names directly (one less property indirection on the hot path; no observable behavior change). Sharded suite: 21187/21187 (+3 from the new AST guards; was 21184 at Phase 3 closure). Per Phase 0 D1 default: `PlanetaryFacility.consumable_levels` NOT migrated — no audit-discovered transfer flow required (a) fold-in. Per CLAUDE.md "no save-file migration" rule: the underlying private fields remain `Dict[str, float]` / `List[Dict[str, Any]]` rather than `Container`. End-state per-facility-component `Container` access (per the original Phase 4 plan) is deferred for the same reason as the equivalent Phase 3 deferral — manager-routed callers can swap internal storage from `Dict` to `Container` in a future session without touching any caller. Commit `3d7e8189b`. |
| 2026-05-18 | **Codex consult (post-4f) findings: verified + dispositioned** | Consult artifact: ``AgentCoordination/Scratchpad/Consult/20260518T015718Z_proj436-phase4-cutover/``. Codex (pre-final-check, allow-tests) exit_status: ok. Findings: **(a) production callers still reference legacy public names** — verified, **this is the intended end state per the phase prompt** ("names survive as backward-compatible @property accessors"). The Phase 3f pattern explicitly preserves the public names so production callers do NOT have to migrate; the dataclass-field deletion is the migration. Codex's "not fully migrated" framing assumes a stricter exit criterion ("migrate production off the legacy public names") than this phase intends. No remediation. **(b) no save-migration shims** — verified clean. The legacy-kwarg constructor wrapper at `planet.py:367-405` is a constructor compat shim, not a save-schema migration; same disposition as Phase 3f's (e) and identical pattern. No remediation. **(c) guard correctly enforces invariant** — verified: 3 new tests in `test_no_legacy_storage_fields.py` GREEN. Codex notes it is a runtime `__dataclass_fields__` guard rather than an AST guard — true, same pattern as Phase 3f, same rigor. The pre-existing `test_mutator_boundary_ast_guard.py` Planet-violation hits at `issuer_adapter.py:335` and `transfer_branches.py:365` are verified pre-existing by Codex (`git diff c3a39858c..3d7e8189b -- ...` empty for both files) and already noted in PROJ-436's `norecursedirs`-discovery decision as part of the 65 pre-existing failures hidden from the sharded suite. No remediation. **(d) dataclass-introspection drift** — verified: `dataclasses.fields(Planet)` now exposes `_stockpile` / `_max_stockpile` / `_staging_yard`; `inspect.signature(Planet)` shows `(*args, **kwargs)`. Drift is cosmetic and parallel to the Phase 3f ShipInstance drift already documented. No production code or test in the sharded suite reads these surfaces against `Planet`. Same disposition as Phase 3f's finding (d). No remediation. **(e) D1 default still holds** — verified: `PlanetaryFacility.consumable_levels` unchanged, this commit does not touch that file. No remediation. **Net: zero verified findings require remediation.** All five findings are either intentional design per the phase prompt or pre-existing parallel issues already documented. |
| 2026-05-18 | **Phase 5 collapsed from 5a-5g to a single 5f cutover** | The Phase 5 audit found that `Empire._fleet_resource_pool` had **zero production writers and zero production readers** other than dead `else` fallback branches in `production_engine.py:521,555,614`. Every `colony_or_fleet` arg is a Planet (`context_type='planet'`) or a Fleet (`context_type='fleet'`); the fallback to `empire.has_resources` / `empire.consume_resources` / `empire.resource_pool.get(...)` was unreachable in production. `Empire.add_resources` had zero production callers; `Empire.consume_resources` was called only from the dead branch. Net result: a single 5f cutover commit equivalent in scope to Phase 4f, not the 5a-5g sub-phase sweep planned in the checklist. |
| 2026-05-18 | **Phase 5f GREEN landed** — substrate deletion + AST guard | Deleted ``Empire._fleet_resource_pool`` instance attribute, ``Empire.add_resources``, ``Empire.consume_resources``, and the ``resource_pool`` property setter. ``Empire.resource_pool`` is now a pure aggregation query over ``self.colonies[*].stockpile`` per Phase 0 D2 default (no caching). ``Empire.has_resources`` / ``Empire.get_resource`` retained as read-only helpers over the aggregate. Save shape: ``to_dict`` drops the legacy ``resource_pool`` key; ``from_dict`` ignores it on pre-Phase-5 saves (per CLAUDE.md "no save-file migration" rule). Production-engine dead `else` branches replaced with explicit `ValueError` (the planet/fleet branching itself is Phase 8's territory). AST guard at ``tests/static_guards/test_no_legacy_storage_fields.py`` extended with 2 new tests pinning absence of ``_fleet_resource_pool`` on a fresh Empire instance and the empty-aggregate contract. ``tests/unit/strategy/data/test_empire_resources.py`` rewritten end-to-end against the new contract; 3 production-engine fallback tests updated to assert `pytest.raises(ValueError)`; 4 test fixtures migrated to seed resources through a hidden "_starting_reserve" colony. Empire is a regular class (not a dataclass) so the guard checks ``vars(empire)`` of a fresh instance rather than ``__dataclass_fields__``. Sharded suite: 21189/21189 (+2 from new AST guards vs the 21187 Phase 4 baseline). Commit ``ec80b1648``. |
| 2026-05-18 | **Phase 0 D2 (pure-query default) confirmed for Phase 5 close** | Per Phase 0 D2 the default for `Empire.resource_pool` is pure aggregation without caching. The Phase 5 cutover ships that contract unchanged. **No profiling artifact was authored** because the new aggregation walks the **same per-colony loop** the previous implementation already walked — the deleted `_fleet_resource_pool.items()` summand processed an empty dict for every production code path, so the removal is net-zero cost (no measurable change in the hot path). The pre-Phase-5 implementation was already a `O(colonies × resources_per_colony)` walk over `self.colonies` plus an `O(1)` no-op over the empty fleet pool; post-Phase-5 it is the same walk with the no-op gone. If a future large-empire stress profile shows the colony walk is hot at scale (>100 colonies, hot UI re-reads), caching with explicit invalidation (PROJ-293 pattern) can land as a sibling project — not Phase 5 scope. |
| 2026-05-18 | **Codex consult (post-5f) findings: verified + dispositioned** | Consult artifact: ``AgentCoordination/Scratchpad/Consult/20260518T022611Z_proj436-phase5-cutover/``. Codex (pre-final-check, allow-tests) exit_status: ok. Findings: **(1) production cutover clean** — verified: `Empire.resource_pool` is now a pure colony-stockpile getter, no `add_resources`/`consume_resources`/`@resource_pool.setter`/`_fleet_resource_pool` hits in `game/`, production_engine dead else branches replaced with ValueError. Targeted suites GREEN (Codex ran 112 unit + 31 integration tests). **(2) docs vs code drift: aggregation walks colonies only, project text said "all containers (planets + fleets + ships)"** — verified: `empire.py:230` walks `self.colonies[*].stockpile`; `plan.md:106`, `decisions.md:29` (Phase 0 D2), and `phase_5_checklist.md:8` still said "all containers." **Disposition:** project artifacts updated to match the colony-only contract (Phase 5h, this row). The fleet-ship-cargo extension is NOT a Phase 5 deliverable — fleet construction already consumes cargo directly through `Fleet.consume_cargo_resource` in production_engine; folding fleet cargo into `Empire.resource_pool` would couple the property to ship-cargo internals without a production caller that needs it. If a future UI flow needs "show all stored materials including fleet cargo," it lands as a Phase 6 (protocol churn) or post-PROJ-436 enhancement, not as a Phase 5 retrofit. **(3) planned integration test `tests/integration/test_empire_resource_aggregation.py` was missing** — verified, manifest line 116 promised it but the file did not exist. **Disposition: remediated as Phase 5g** — landed 14-test integration suite pinning the new pure-aggregation contract end-to-end (aggregation across colonies, snapshot semantics, has_resources/get_resource routing, save-shape contract including legacy-key drop). **(4) D2 profiling artifact missing / checklist gate open** — verified. **Disposition: remediated as Phase 5h** — D2 profiling decision documented in decisions.md (row above): pure-query is net-zero cost vs the pre-Phase-5 implementation because the deleted summand was an empty-dict no-op in production; no benchmark artifact is needed. Checklist updated to reflect the closed gate. **(5) fleet-context UI may be misleading** — verified-but-out-of-scope. The build-queue footer and treasury panel read `empire.resource_pool` and now report colony-only totals; they previously reported colony + empty fleet pool, so post-Phase-5 they show the same numbers production has always shown (the fleet pool was never populated). Codex's risk #3 is a real UX consideration for Phase 6/7 transfer UI work but not a Phase 5 cutover gap. **Net: 2 findings remediated (5g integration test + 5h docs sync), 3 findings dispositioned without code change.** |
| 2026-05-18 | **Phase 5g GREEN landed** — promised integration test | Authored ``tests/integration/test_empire_resource_aggregation.py`` (14 focused tests across 3 test classes: ``TestEmpireResourcePoolAggregation`` walks colony stockpiles end-to-end; ``TestEmpireResourceQueryHelpers`` covers ``has_resources`` / ``get_resource`` routing; ``TestEmpireResourceSaveShape`` covers ``to_dict`` / ``from_dict`` post-Phase-5 contract including legacy-key drop). Closes the manifest.md:116 promise that the Phase 5 prompt left dangling. Direct invocation: 14/14 GREEN. Sharded suite: 21203/21203 (+14 from the new tests vs 21189 Phase 5f baseline). |
| 2026-05-18 | **Phase 5h: project artifacts synced to colony-only contract** | Updated ``plan.md`` Phase 5 description (line 106), ``manifest.md`` Phase 5 row (line 70), and ``phase_5_checklist.md`` objective + completion checklist to reflect the actual shipped contract: ``Empire.resource_pool`` walks colony stockpiles only; fleet/ship cargo stays at its current per-fleet API (``Fleet.get_cargo_resource`` / ``Ship._cargo_mgr``) which production_engine already consumes directly. Decision: the original "all containers" framing in the project plan was aspirational; production code never needed it (no caller reads fleet cargo through `Empire.resource_pool`), and adding the coupling would extend `Empire` into ship-cargo internals without a justifying call site. Phase 6 (protocol churn) or Phase 7 (transfer UI) can revisit if a flow needs it. |
| 2026-05-18 | **Phase 6a audit: protocol surface is post-Phase-5 stable, no methods need deletion** | Full audit of `IEmpire`, `IStockpileHolder`, `IStagingYardHolder`, `IShipInstance` against production callers + implementers. Findings: **(a) `IEmpire.resource_pool` (Dict[str, float])** — declared in protocol, satisfied by `Empire.resource_pool` `@property` returning the pure colony aggregation landed in Phase 5. Production callers (`turn_engine.py:373`, `empire_economy_calculator.py:168`, `build_queue_helpers.py:49`, `production_engine.py:184` validation, `empire.py:255,265,276`) all access via the concrete `Empire` class. Zero production sites narrow through `IEmpire` for this property. Signature stable; docstring needs refresh to reflect colony-aggregation contract. **(b) `IEmpire.max_storage` (Dict[str, float])** — set by `EmpireWriteService.replace_max_storage` (via `HarvestingEngine`); read by `empire_economy_calculator`, `empire_treasury_panel`, `build_queue_helpers`, `strategy_ui`. All concrete-Empire accesses; zero protocol-narrowed accesses. Signature stable. **(c) `IShipInstance.cargo_contents` (Dict[str, int])** — Phase 3f deleted the dataclass field but the backward-compat `@property` still satisfies the protocol. Production reads go through `ship._cargo_mgr` manager API. Codex's post-3f consult flagged this as "Phase 6 territory" — confirmed: docstring refresh suffices because the public protocol surface still validly describes what `ShipInstance.cargo_contents` returns. **(d) `IStockpileHolder` / `IStagingYardHolder`** — production routes through `IPlanetMutator`, not these protocols. Single structural assertion in `tests/unit/strategy/data/test_galaxy_protocols.py` keeps Planet's structural conformance pinned. Post-Phase-4 implementations on Planet (`@property` over private `_stockpile` / `_staging_yard`) still satisfy these protocols transparently — option (iii) "leave as-is" from the phase brief is the right call. Signatures stable. **(e) `IFacility.consumable_levels`** — Phase 0 D1 chose to keep `PlanetaryFacility.consumable_levels` as facility-internal state. Protocol still valid. No change. **(f) `is_empire` TypeGuard** has zero production callers (only `test_protocols.py`); it's a structural declaration only. **Net audit conclusion:** no protocol methods need deletion. Phase 6 ships as a docstring refresh + a *positive ratchet* AST guard pinning the post-Phase-5 contract (asserting the surviving methods stay present **and** that no `add_resources` / `consume_resources` / `_fleet_resource_pool` mutator-style methods creep onto `IEmpire`). This deviates from the "pin absence of removed names" framing in the phase brief because the audit found nothing on the protocol surface was actually deleted — the deletions in Phase 5 lived on the concrete `Empire` class, which `test_no_legacy_storage_fields.py` already pins. |
| 2026-05-18 | **Phase 6a audit: `build_queue_controller.py:483-513` `context_type` reads are entity-routing, NOT storage-typed** | Audit of the call sites: line 483-499 dispatches on `source.context_type == "planet"` vs `"fleet"` to pick the `planet_id` extraction path (planet sources expose `planet_id` directly; fleet sources need to look up planets-at-hex if the build-queue is rendering a fleet's construction queue). Line 512-522 dispatches to pick `entity_id` extraction (planet uses `source.planet_id`, fleet uses `source.owner_entity.id`). Neither site is about storage policy — both are about which entity type the BuildQueueSource wraps. The substrate-unification doctrine of PROJ-436 (one `Container` per resource/item/population storage surface) doesn't apply here: BuildQueueSource is a UI DTO routing wrapper, not a storage surface. The same observation applies to the parallel branches in `empire_build_queue_window.py:366,368`, `empire_build_queue_formatter.py:80,86,105,109`, `empire_build_queue_filter_manager.py:60,61`, `build_queue_panel_factory.py:230`, `strategy_build_queue_manager.py:231`, and `build_context.py:23` (which actually documents `context_type` as a routing field for UI branching). The `ProductionEngine.context_type` branches at `production_engine.py:561,629` are different — they DO branch storage access (planet vs fleet container) — and are explicitly Phase 8's territory per the plan. **Decision: build_queue_controller.py:483-513 stays as-is in Phase 6.** Phase 8's ProductionEngine cutover does not affect the UI controller's entity-type dispatch; the UI controller will continue to need to know "am I rendering a planet's queue or a fleet's queue" for as long as planets and fleets have different `entity_id` extraction shapes. Surfaced for Ross: if at some future point BuildQueueSource is rewritten to expose a uniform `entity_id` accessor, the branching can collapse; out of scope for this project. |
| 2026-05-18 | **Phase 6b GREEN landed** — docstring refresh + positive-ratchet AST guard | Refreshed docstrings on `IEmpire.resource_pool` (now describes colony-aggregation pure query, references Phase 5 closure), `IEmpire.max_storage` (clarifies set-by-`HarvestingEngine` and read-by-UI roles), `IShipInstance.cargo_contents` (notes the Phase 3f manager-API path is canonical for production writers; the property remains as a read surface), and class-level `IStockpileHolder` / `IStagingYardHolder` docstrings (note the post-Phase-4 `@property`-over-private-field implementation transparently satisfies the protocols; new `Container.add()` substrate is the future direction, but production callers route through `IPlanetMutator` not these protocols today). Authored `tests/static_guards/test_no_legacy_protocol_names.py` with 9 tests: 4 positive ratchets (asserting `IEmpire.resource_pool` / `IEmpire.max_storage` / `IShipInstance.cargo_contents` / `IFacility.consumable_levels` are still declared so the contract doesn't accidentally drift before Phase 8/10), 3 negative ratchets (asserting `IEmpire` never gains `add_resources` / `consume_resources` / `_fleet_resource_pool` mutator-style methods, mirroring the Phase 5 concrete-class deletion guard at the protocol level), and 2 set-shape ratchets pinning `IStockpileHolder` + `IStagingYardHolder` member sets are unchanged post-Phase-4 (the Phase 6 brief's "leave as-is" option). All 9 tests GREEN on first run (ratchet pattern, not RED-then-GREEN). Sharded suite: 21212/21212 (+9). |
| 2026-05-18 | **Phase 6c: build-queue UI `context_type` deferred to Phase 8** | Per Phase 6a audit row above: the `build_queue_controller.py:483-513` `context_type` reads are UI entity-routing, not storage-typed. Phase 8 owns the `ProductionEngine.context_type` cutover; if Phase 8 demonstrates a path to collapse the planet/fleet entity-routing entirely (e.g. by promoting a uniform `entity_id` accessor onto `BuildQueueSource`), the UI controller migration becomes a Phase 8 follow-on. Phase 6 ships zero changes to `build_queue_controller.py`. |
| 2026-05-18 | **Codex consult (post-6b) findings: verified + dispositioned** | Consult artifact: ``AgentCoordination/Scratchpad/Consult/20260518T025059Z_proj436-phase6-protocol-churn/``. Codex (pre-final-check, allow-tests) exit_status: ok. Findings: **(1) implementer conformance** — verified clean. ``Empire``/``Planet``/``ShipInstance``/``PlanetaryFacility`` all still satisfy their respective protocols structurally; Codex ran ``isinstance(...)`` checks and ``inspect.signature(...)`` comparisons end-to-end, plus targeted pytest sweeps (44 tests across 4 protocol-conformance files, all GREEN). No remediation. **(2) test residue** — verified-but-out-of-scope. ``rg`` against ``game/`` for ``add_resources``/``consume_resources``/``_fleet_resource_pool`` returned ONLY comments / docstrings / explicit ValueError messages, NOT executable production callers. Test residue at ``tests/unit/strategy/engine/test_production_engine_queue.py:43``, ``test_production_engine_consumption.py:38``, ``test_production_refactor.py:338,497``, ``test_harvesting_engine.py:38-49`` — these are ``MagicMock`` attribute attachments (``emp.consume_resources = MagicMock()``) or closure-attribute attachments on Empire test doubles. Production code no longer reads these attributes (Phase 5 deleted the concrete-class methods AND replaced the production-engine fallback branches with explicit ValueError). The mocks are inert — they exist because pre-Phase-5 test setup expected them, but they're never invoked. **Disposition: not Phase 6 scope.** Phase 6 audits the protocol surface; this is test-mock hygiene. Candidate for a small post-PROJ-436 cleanup project (the same project that fixes ``norecursedirs = ... data ...``). Documented; not remediated this phase. **(3) AST guard correctness** — verified clean. Codex ran in-memory negative confirmations (temporarily swapped test-module globals to fake protocol classes lacking the surviving members / containing the deleted ones / with mutated set shapes — all three ratchet types FAILED with expected messages, demonstrating the guards trip on intended counterfactuals). No remediation. **(4) build_queue_controller.py audit** — verified clean. Codex confirmed ``BuildQueueSource.context_type`` is documented as UI routing at ``build_queue_source.py:62-90``; the controller's lines 465-499 and 501-522 contain ZERO storage-typed access (no stockpile / staging / cargo / ``resource_pool`` reads). ``git diff c9bfb7073 db23b45fb -- game/ui/panels/build_queue_controller.py`` returned no hunks — Phase 6 modified nothing in this file, matching the audit's "leave as-is" decision. No remediation. **(5) other narrowing seams** — verified clean. ``is_empire`` / ``is_facility`` / ``is_ship_instance`` TypeGuards have ZERO production callers. ``IStockpileHolder`` / ``IStagingYardHolder`` runtime checks appear only in ``test_galaxy_protocols.py``. Production narrowers ``is_planet`` / ``is_fleet`` check orthogonal fields (``planet_type``, ``ships`` / ``orders``), not PROJ-436 churned surfaces. No remediation. **(6) ``IShipInstance.cargo_contents`` docstring tension** — VERIFIED, remediated as Phase 6d (this commit). The 6b docstring framed the property as the canonical read surface AND said "do not introduce new write-side callers that mutate the returned dict" — too strong, given the concrete-class setter at ``ship_instance.py:190-192`` is still exercised by the legacy-kwarg constructor wrapper at ``ship_instance.py:740-777``. **Remediation:** softened to "production code should **prefer** the cargo manager API for writes" + explicit acknowledgment of the surviving setter. Matches the post-Phase-3 reality more precisely. **Net: 1 finding remediated (6d docstring softening), 4 verified clean, 1 verified-but-out-of-scope (test-mock residue).** |
| 2026-05-18 | **Phase 6d GREEN landed** — Codex finding #6 remediation | Softened the ``IShipInstance.cargo_contents`` docstring at ``game/core/protocols/strategy_domain.py:188-203`` per Codex's verified finding. The pre-6d wording overstated write-path exclusivity ("do not introduce new write-side callers"); the post-6d wording acknowledges the surviving concrete-class setter and the legacy-kwarg constructor wrapper, then expresses the production preference ("production code should **prefer** the cargo manager API for writes"). Behavioural contract unchanged; the protocol still describes the public read surface. Sharded suite unchanged at 21212/21212 (no executable code paths touched). |
| 2026-05-18 | **Phase 7 collapsed from 7a-7f to a single cutover** | Audit (`grep -rn VALID_CARGO_TYPES \| RESOURCE_TYPES \| RESOURCE_DISPLAY_NAMES game/ tests/`) found extremely narrow scope: **3 production sites** (one definition + one check in `transfer_validator.py`; one definition + two usages in `transfer_view_model.py`; one re-export in `transfer_dialog.py`) and **4 test files** (`test_fms_a_audit_fixes.py`, `test_resource_transfer.py`, `test_transfer_view_model.py`, `test_transfer_dialog_characterization.py`). The `transfer_branches.py` order-handler branches reference `cargo_type` strings ("vehicle" / "drop_pod" / "passengers") directly but never look at the whitelist — they dispatch on string equality. The Phase 4f / 5f / 6b "audit-then-collapse-to-single-cutover" pattern applies cleanly here. |
| 2026-05-18 | **Phase 7 registry-driven contract** | `TransferValidator` no longer maintains a hardcoded `VALID_CARGO_TYPES` whitelist. The new contract: `_is_known_cargo_type(cargo_type)` returns True iff (a) `cargo_type` is one of the three `_CATEGORICAL_CARGO_TYPES` sentinels (`"passengers"`, `"drop_pod"`, `"vehicle"`), or (b) `ResourceCatalog.has(cargo_type)` returns True. The categorical sentinels are kept as a module-level frozenset because each one routes to a distinct dispatch branch in `transfer_branches.py` (population / DropPod item / CarriedVehicle item) — they are NOT resources and intentionally live outside `data/resources.json`. Adding a new resource to `data/resources.json` makes it transferrable automatically; no validator update required. Per option (ii) in the phase prompt's "drop_pod / vehicle special handling" section: BayInventory-routed cargo types are kept as sentinels because the dispatch shape differs from resources (item vs scalar). Per the prompt's primary direction ("audit before deciding"), option (i) — a separate validator code path — was rejected in favor of the sentinel approach because it requires fewer LOC and pairs naturally with the unified `_is_known_cargo_type` check. |
| 2026-05-18 | **Phase 7 UI label single-source-of-truth: "Ammo" → "Ammunition"** | Per option (i) in the phase prompt's "Special note on `RESOURCE_DISPLAY_NAMES` semantics" section: the UI now sources display labels from `ResourceDefinition.name` (which loads from `data/resources.json`). The user-visible label for the ammo cargo row changes from "Ammo" (the old hardcoded `RESOURCE_DISPLAY_NAMES` value) to "Ammunition" (the canonical `data/resources.json:name` value). **All other 7 labels are unchanged**: "Metals", "Organics", "Vapors", "Radioactives", "Exotics", "Fuel", "Energy" — only `ammo` had a divergent UI label vs JSON name. **Surfaced to Ross.** If the "Ammunition" label proves contentious in the next session's smoke test, the resolution is a one-line `data/resources.json` edit (rename `"name": "Ammunition"` to `"name": "Ammo"`), NOT reintroducing the hardcoded UI mapping. |
| 2026-05-18 | **Phase 7 GREEN landed** — single-cutover commit | Production: `game/strategy/validation/transfer_validator.py` (deleted `VALID_CARGO_TYPES` class attribute, added `_CATEGORICAL_CARGO_TYPES` frozenset + `_is_known_cargo_type` + lazy `ResourceCatalog` handle, replaced the `_validate_cargo_type` check inline), `game/ui/screens/transfer_view_model.py` (deleted `RESOURCE_TYPES` + `RESOURCE_DISPLAY_NAMES` + `__all__` entries, added `_get_resource_catalog` + `_iter_resource_definitions`, rewired `build_row_data` to iterate the catalog), `game/ui/screens/transfer_dialog.py` (deleted the `RESOURCE_DISPLAY_NAMES` / `RESOURCE_TYPES` import + `__all__` re-exports). Tests: 4 new AST guards in `tests/static_guards/test_no_legacy_storage_fields.py` (`test_transfer_validator_has_no_valid_cargo_types_attribute`, `test_transfer_view_model_has_no_resource_types_constant`, `test_transfer_view_model_has_no_resource_display_names_constant`, `test_transfer_dialog_has_no_resource_types_re_export`). New integration suite at `tests/integration/test_transfer_container_validation.py` (12 tests across `TestRegistryDrivenCargoTypeValidation` + `TestContainerAcceptsResourceCatalogIntegration`). 4 test files migrated: `tests/unit/strategy/data/test_fms_a_audit_fixes.py` (vehicle-cargo-type test asserts no `INVALID_CARGO_TYPE` error code instead of set membership), `tests/integration/strategy/test_resource_transfer.py` (parametrized validator round-trip per resource + passenger sentinel test), `tests/unit/ui/screens/test_transfer_view_model.py` + `tests/unit/ui/screens/test_transfer_dialog_characterization.py` (resolve `ResourceCatalog.from_json().all_ids()` at module import as `_CANONICAL_RESOURCE_IDS`, all `RESOURCE_TYPES` references migrated). Sharded suite: **21233/21233** (+21 from the 21212 Phase 6 baseline; net of the 12 new integration tests, 4 new AST guards, 7 net-additional parametrized resource tests, 1 added vehicle test, and 3 deletions of the constant-pin assertions inside existing tests). |
| 2026-05-18 | **Phase 7 PROJ-437 unblocked** | The post-Phase-7 API is stable: `Container.accepts()` is the validation surface, `ResourceCatalog.all_ids()` + `ResourceDefinition.name` is the UI iteration surface, and the categorical sentinel set is the documented seam for the three non-resource transfer kinds. PROJ-437 Phase 0 (read-the-API + survey current UI) and Phase 1+ (browsing implementation) may begin. PROJ-437 Phase 4 (final UI-side `RESOURCE_TYPES` consumer sweep) is now a no-op gate: Phase 7's grep gate already returns zero hits in `game/` for `RESOURCE_TYPES` / `RESOURCE_DISPLAY_NAMES` / `VALID_CARGO_TYPES`. |
| 2026-05-18 | **Phase 8 branch policy: separate git branch** | Per user directive in the Phase 8 handoff: Phase 8 lands on `proj/PROJ-436/phase_8` rather than directly on `main`. No worktrees per `feedback_no_worktrees.md` — branches in the main checkout. Merge back to `main` is the user's call after they sanity-check the branch. |
| 2026-05-18 | **Phase 8 design: thin polymorphic delegators on Planet and Fleet for IProductionResourceSource conformance** | Three alternatives considered: (a) rename Planet's `has_stockpile`/`get_stockpile`/`consume_from_stockpile` + Fleet's `has_cargo_resources`/`get_cargo_resource`/`consume_cargo_resource` to a shared canonical name (rejected: massive ripple to `transfer_branches.py`, `IStockpileHolder` protocol, ~20 test files; semantic loss because the entity-specific names accurately describe their respective storage substrates); (b) keep `isinstance` dispatch in a helper inside `production_engine.py` (rejected: just renames the smell from `context_type` string check to `isinstance(Planet)` check); (c) add unified `production_has_resources`/`production_get_resource`/`production_consume_resource` methods on Planet and Fleet as thin delegators over the entity-specific APIs, then have `ProductionEngine` consume the unified protocol. Picked (c) — the 6 delegator methods (3 each) are protocol-conformance polymorphism, NOT shim duplication per CLAUDE.md's "no shims" rule (the logic lives in the entity-specific stockpile/cargo methods; the delegators just adapt to the engine's protocol contract). OpenCode consult endorsed this choice ("I would not flag this"). |
| 2026-05-18 | **Phase 8 GREEN landed** — context_type storage-dispatch deletion + IProductionResourceSource substrate | Production: `game/strategy/engine/production_engine.py` (added `IProductionResourceSource` Protocol; rewrote `_check_affordability` / `_log_resource_shortage` / `_apply_resource_consumption` to call protocol methods directly; deleted all 3 `getattr(colony_or_fleet, 'context_type', None)` reads and the Phase 5 `ValueError` error branches). `game/strategy/data/planet.py` (3 delegator methods over the stockpile API; PLANET_LOC_CEILING raised 405 -> 425 to accommodate the protocol-conformance surface). `game/strategy/data/fleet.py` (3 delegator methods over the cargo API). The Phase 5 `ValueError`-on-unknown-owner contract dissolves into the natural `AttributeError` Python raises when calling a method on an object that doesn't declare it. Tests: 3 new AST guards in `tests/static_guards/test_no_legacy_storage_fields.py` (`test_production_engine_has_no_context_type_storage_dispatch`, `test_planet_has_production_resource_source_methods`, `test_fleet_has_production_resource_source_methods`). New integration suite at `tests/integration/test_production_engine_container_unified.py` (17 tests across `TestProductionResourceSourceProtocolOnPlanet` / `TestProductionResourceSourceProtocolOnFleet` / `TestProductionEngineUsesUnifiedProtocolForPlanet` / `TestProductionEngineUsesUnifiedProtocolForFleet` / `TestProductionResourceSourceProtocolConformance`). 6 engine-side test files migrated: `test_production_engine_consumption.py` (6 sites: rewrote `_*_context_type` tests to assert `AttributeError` against bare object; renamed dispatch tests to reflect protocol routing), `test_production_engine_queue.py` (replaced `has_stockpile` / `consume_from_stockpile` mocks with `production_has_resources` / `production_consume_resource` / `production_get_resource`), `test_production_refactor.py` (4 fixture sites; dropped redundant `context_type = "planet"` assignments now that Phase 8 removed the dispatch), `test_planetary_yard_requirement.py` (dropped redundant `context_type` assignment), `test_tick_consumption.py` + `test_paused_queue.py` (helper factories updated to mock the unified protocol surface; cleaned up residual fleet `context_type` assignments). Sharded suite: 23205/23207 (+20 from 23185 Phase 7 baseline). Branch: `proj/PROJ-436/phase_8`. |
| 2026-05-18 | **Codex CLI installed mid-Phase-8 (per user request); consult re-run through Codex** | Initial Phase 8 consult was substituted with OpenCode because Codex CLI was missing from PATH at consult time. User asked me to install Codex; ran `npm install -g @openai/codex` (Codex CLI 0.130.0 landed at `C:\Users\rossr\AppData\Roaming\npm\codex.cmd`, ChatGPT auth already present). Re-ran the Phase 8 consult through Codex; the OpenCode response is preserved as `response_opencode_substitution.md` in the same consult leaf for cross-reference. From this row forward all PROJ-436 phase-boundary consults will use Codex by default (gemini still requires explicit `GOOGLE_GENAI_USE_GCA` / `GEMINI_API_KEY` per `partner_cli.md`). |
| 2026-05-18 | **Codex consult (post-8-cutover, re-run after install) findings: verified + dispositioned** | Consult artifact: `AgentCoordination/Scratchpad/Consult/20260518T072020Z_proj436-phase8-cutover/response.md`. Codex `pre-final-check` `--allow-tests`, exit_status: ok. Codex's findings broadly converge with OpenCode's (delegator pattern endorsed; zero executable `context_type` reads; protocol scoped correctly; IStockpileHolder unchanged; 17/14/100 focused tests pass; test_production_repro.py is BuildContext UI routing not storage dispatch). **One new minor flag from Codex not raised by OpenCode:** `IProductionResourceSource` currently lives in `game/strategy/engine/production_engine.py`. While ProductionEngine is the only consumer this is fine, but if another subsystem starts depending on the same surface, the protocol should move to a lower/shared boundary module rather than remaining engine-owned. **Disposition: not remediated this phase** — there is no second consumer today, the protocol is intentionally co-located with its single consumer (per the Phase 0 Container substrate's `set_resource_catalog`/`_get_resource_catalog` pattern of test-only module-level handles co-located with the using code), and moving it now would be speculative. Documented as a follow-up signal: if Phase 10 docs or PROJ-437 needs to narrow through `IProductionResourceSource`, that's the trigger to relocate the Protocol to `game/core/protocols/strategy_domain.py` or `game/strategy/data/galaxy_protocols.py`. Codex also flagged the pre-existing repo-level `pytest.ini -n 4` + Windows temp-dir permission issue as a verification-harness brittleness (independent of Phase 8). **Net: zero verified findings require Phase 8 code remediation.** |
| 2026-05-18 | **Phase 10 GREEN landed** — doc refresh against PROJ-436 cutover surface | Six docs updated, zero production/test code changes: (1) `docs/systems/resource_system.md` — rewrote `_check_affordability` description around `IProductionResourceSource` Protocol (Phase 8); replaced the `VALID_CARGO_TYPES` warning paragraph with the Phase 7 registry-driven contract; drop-pod section now describes typed `DropPod` dataclass; transferable-resource recipe simplified (no validator update needed). (2) `docs/systems/production_system.md` — `Resource Sources` section rewritten around the unified Protocol; `BuildQueueSource(context_type:...)` code block annotated to clarify "UI entity routing only — NOT engine storage dispatch" (Phase 6a kept it for UI; Phase 8 deleted the engine read); staging-yard line updated to typed `DropPod` in `bay_inventory.pods`; "Adding a new build context" recipe updated to require `IProductionResourceSource` implementation. (3) `docs/systems/strategy_layer.md` — minefield-resolver indirection note updated. (4) `docs/systems/satellites.md` — lifecycle map ASCII updated to `ShipInstance.bay_inventory.bay[*]`. (5) `docs/02_PATTERNS.md` — updated Pattern #38 (CarriedVehicle Substrate) and Pattern #41 (Polymorphic Order Issuer) to point at the typed `bay_inventory.bay` slot; **added new Pattern #43 (Unified Container Substrate)** alongside Pattern #37 covering `Container` + `ContainerPolicy` + `ContainableKind` + `BayInventory.four_slot` + `IProductionResourceSource`. (6) `docs/01_ARCHITECTURE.md` — extended the `game/strategy/data/` listing's bold note to include the Container substrate sentence. All 6 touched docs got `Last verified` blockquote refreshes per `docs/03_CONVENTIONS.md` §"Documentation Freshness". Sharded suite unchanged at 23209/23211 (doc-only changes are no-op for tests). |
| 2026-05-18 | **Codex consult (post-10-doc-refresh) findings: verified + remediated** | Consult artifact: `AgentCoordination/Scratchpad/Consult/20260518T143802Z_proj436-phase10-doc-refresh/response.md`. Codex `pre-final-check` `--allow-tests`, exit_status: **partial** (environmental — Codex sandbox couldn't reproduce sharded suite due to temp-dir permission errors, same as Phase 9). **Three real findings, all remediated in-phase before commit:** (1) `docs/02_PATTERNS.md` lacked the standard `> **Last verified:**` blockquote below H1 (required by `docs/03_CONVENTIONS.md:500-505`) — REMEDIATED: added the blockquote. Also flagged: "Current pattern count is 42" wording + summary table stopping at row 42, both stale after Pattern #43 was added — REMEDIATED: bumped to 43 and extended the table. (2) Pattern #43 overstated `Container.accepts() / TransferValidator` coupling — said `Container.accepts()` was "the validation seam consumed by `TransferValidator._is_known_cargo_type`", but the validator actually uses `ResourceCatalog.has()` + sentinels and does NOT call `Container.accepts()`. REMEDIATED: reworded to "policy seam for container-backed write paths" + explicit note that transfer validation routes through `ResourceCatalog.has()` + sentinels directly. (3) Pattern #43's fighter-bay example was too literal (said fighter bay pairs `Container(allowed_kinds={ITEM}...)` with launch ability), but the live write surface is still `VehicleBayAbility` + `CarriedVehicle` entries in `bay_inventory.bay` per Pattern #38; `Container` is only a snapshot projection (`BayInventory.container_view()`). REMEDIATED: rewrote the boundary section to explicitly call out which substrates are typed-live (`bay`, `pods`, `Planet.stockpile`, fleet cargo aggregation) and which are unified projections (`container_view()`). Stale-symbol sweep otherwise clean. **Net: 3 findings, 3 remediated in-phase.** No follow-up. |
| 2026-05-18 | **Phase 9 branch policy: separate git branch (cut from `main`, not from Phase 8)** | Per user directive in the original handoff: Phase 9 lands on `proj/PROJ-436/phase_9`, branched from `main` (NOT from Phase 8's branch — the two phases are independent of each other). User merged both branches back to `main` after Phase 9 closed; merge order was Phase 8 first (no conflicts), then Phase 9 (conflict only on the auto-generated `test_baseline.json`, resolved by re-running the sharded suite on the merged main and committing the true post-merge baseline). |
| 2026-05-18 | **Phase 9 audit: only ~22 of the original PROJ-431 estimate of "~51 test files" actually exercised the real proxy** | Full audit (`rg "_CarriedItemsProxy\|\.carried_items\b" game/ tests/`) found: 8 production hits (all docstring/comment references, ZERO executable production code); ~49 test files mention `carried_items`; of those, **~25 use test-double `_Stub*` / `_Fake*` / `_Carrier*` classes with their OWN `self.carried_items = []` regular-list attributes** (completely unaffected by proxy deletion — those classes are independent of ShipInstance and just happen to share the attribute name); **~22 files actually exercise the real proxy** via `ship.carried_items` on a real ShipInstance or `MagicMock(spec=ShipInstance)` (these were migrated). The PROJ-431 audit's "~51 test files" estimate was on the high side because it didn't distinguish the two categories — the actual migration scope was less than half what the original plan budgeted for. |
| 2026-05-18 | **Phase 9 GREEN landed** — `_CarriedItemsProxy` deletion + AST guard | Production: `game/strategy/data/ship_instance.py` (deleted `_CarriedItemsProxy` class ~95 LOC, `carried_items` @property + setter ~25 LOC, `_items_to_bay_inventory` + `_bay_inventory_to_items` legacy-shim helpers ~50 LOC; dropped now-unused `DropPod` / `CarriedVehicle` / `VALID_VEHICLE_TYPES` imports; net deletion ~170 LOC). Tests: 4 new AST guards at `tests/static_guards/test_no_carried_items_proxy.py` (`test_ship_instance_module_has_no_carried_items_proxy_class`, `test_ship_instance_has_no_carried_items_property`, `test_ship_instance_module_has_no_legacy_shim_helpers`, `test_ship_instance_source_has_no_carried_items_proxy_text` — text-grep guard with threshold ≤2 for the 2 deletion-comment markers in the source). ~22 test files migrated to write directly to `ship.bay_inventory.bay` / `ship.bay_inventory.pods` / `ship.set_bay_inventory(...)`: the conftests + colonization + facade integration + command-handlers + transfer + colonize / order-processor + multi-pod / staging-yard / pod-transfer + ship-instance-write-service + engine-event-emission + fleet-order-processor + ship-instance-damage + transfer-drop-pod + colonize-validator + fms_a_audit + fms_c/d launch-in-battle + fms_cd_isolation + the three reboard tests. For test-double `_StubCargoMgr` classes that LOADED into REAL ShipInstance via `self._carrier.carried_items.append(cv.to_dict())`, migrated to `self._carrier.bay_inventory.bay.append(cv)` (typed `CarriedVehicle`, not a dict — three sites: `test_fighter_reboard.py`, `test_satellite_reboard.py`, `test_fighter_reboard_component_state.py`). Per-file commit `cbf0cc14b`, merged to `main` at `eb8da3d85`. Post-merge sharded suite: **23209/23211** green. |
| 2026-05-18 | **Codex consult (post-9-deletion) findings: verified + dispositioned** | Consult artifact: `AgentCoordination/Scratchpad/Consult/20260518T135622Z_proj436-phase9-carried-items-deletion/response.md`. Codex `pre-final-check` `--allow-tests`, exit_status: **partial** (purely environmental — Codex sandbox can't run the full sharded suite due to temp-dir permission errors against `C:\Users\rossr\AppData\Local\Temp\pytest-of-rossr`; the focused Phase 9 slice is green 87/87 + the 4 AST guards green + zero missed production callers found). **Findings:** (1) `rg "_CarriedItemsProxy" game/` returns exactly the 2 deletion-comment markers at `ship_instance.py:512` and `:766` — clean. (2) `rg "carried_items" game/` returns only docstring/comment residue, no executable code — clean. (3) AST guard 4/4 pass — clean. (4) Focused regression slice (87 tests across the deletion guard + FMS mine/fighter/satellite handlers + carrier-AI/launch integration files + no-legacy-substrate AST guards) all green — clean. (5) AST guard's text-grep threshold of `<= 2` is reasonable — endorsed. (6) Minor wording suggestion: check #3 in the verification request ("test-double class internals or AST-guard strings") could be more explicit about including stub/wrapper test-body assertions — cosmetic, no code change. **Risks:** (1) sandbox can't reproduce full-suite baseline literally (environmental, not Phase 9 concern; I have the post-merge result 23209/23211 from my own run). (2) a future broad "ban all `.carried_items` in tests/" grep would create false positives from stub-class internals — tooling risk only, not Phase 9 runtime concern. **Net: zero verified findings require Phase 9 code remediation.** Phase 9 exit criteria all met.

| 2026-05-18 | **OpenCode consult (substitution, pre-Codex-install) findings: verified + dispositioned** | Initial consult substitution; see row above for the Codex re-run. Consult artifact: `AgentCoordination/Scratchpad/Consult/20260518T072020Z_proj436-phase8-cutover/response_opencode_substitution.md` (preserved for cross-reference). OpenCode `pre-final-check` `--allow-tests`, exit_status: ok. All 7 verification checks PASS: (1) zero executable `context_type` reads in production_engine.py (only 3 docstring comments at lines 49, 544, 630); (2) protocol methods defined on Planet/Fleet, consumed only by engine in `game/`; (3) `IStockpileHolder` unchanged (Phase 6a leave-as-is decision held); (4) test-mock residue found — see remediation row below; (5) integration test passes 17/17 (request memo erroneously cited 19; 17 is correct); (6) AST guards pass 14/14 (request memo erroneously cited "4 new"; correct count is 3 new); (7) engine-focused sweep passes 100/100. **Findings remediated this phase:** Risk #1 (residual `context_type` assignments in engine-side test mocks) — cleaned up 8 sites across `test_planetary_yard_requirement.py`, `test_production_engine_queue.py`, `test_production_refactor.py`, `test_tick_consumption.py`, `test_paused_queue.py`. The `test_production_repro.py` hits at lines 110/166/217 are `BuildQueueSource(context_type="planet")` UI-DTO usage that Phase 6a explicitly preserved — NOT residue, left alone. **Findings dispositioned without code change:** Risk #2 (`IStockpileHolder`/`IProductionResourceSource` naming similarity) — Codex flagged as a "subtle naming collision risk" for future engineers but recommended "no action required now" since the Phase 6 audit docstring at `galaxy_protocols.py:142-146` already explains the distinction. Risk #3 (test count discrepancy 17 vs 19) — request memo error, not a test-suite issue. **Design endorsement:** OpenCode explicitly endorsed the delegator pattern as "proper polymorphic protocol conformance, not duplicate logic." Net: 1 finding remediated, 2 verified-but-cosmetic dispositioned. |
| 2026-05-18 | **Codex consult (post-7-cutover) findings: verified + dispositioned** | Consult artifact: `AgentCoordination/Scratchpad/Consult/20260518T031409Z_proj436-phase7-cutover/`. Codex (pre-final-check, allow-tests) ran 16-test focused suite + repo-wide `rg` sweep, exit_status: ok. Findings: **(a–e) clean** — `TransferValidator._is_known_cargo_type` correctly routes through `ResourceCatalog.has()` + `_CATEGORICAL_CARGO_TYPES`; `drop_pod`/`vehicle` dispatch in `transfer_branches.py` unchanged (string-equality); `_iter_resource_definitions()` smoke confirms `metals,organics,vapors,radioactives,exotics,fuel,energy,ammo` order preserved; `AMMO_LABEL=Ammunition` confirmed; all 4 AST guards green. **(f) zero missed production consumers** — repo-wide sweep found only test/doc/archive references. **Risk 1 (verified, out-of-scope):** `docs/systems/resource_system.md:224,335` still references `TransferValidator.VALID_CARGO_TYPES`. **Disposition:** Phase 10 (doc refresh) owns this per the project plan. NOT remediated this phase — Phase 7 brief explicitly says "don't extend scope to Phase 8 or 9"; Phase 10 has the same boundary. The doc staleness exists in writing, will be swept in Phase 10. **Risk 2 (verified, out-of-scope):** `TransferValidator._get_resource_catalog()` uses `ResourceCatalog.from_json()` directly rather than `DefaultRegistryProvider.get_resource_catalog()` / `GameRegistries.resource_catalog`. **Disposition:** the `from_json()` pattern is the **prevalent convention** across 16+ production sites including `container.py` (Phase 0 substrate), `empire_treasury_panel.py`, `build_queue_helpers.py`, `planet_gen.py`, `strategy_ui.py`, `empire_economy_calculator.py`, `construction_forecast.py`, `planet_list_window.py`, etc. The provider-DI path documented in `docs/systems/resource_system.md:23-24` is an **alternative**, not the canonical convention used in production. Migrating Phase 7's two new `from_json()` sites would create inconsistency with the surrounding pattern; migrating all 16+ sites is a separate cross-cutting hygiene project. **Surfaced for follow-up** as a candidate small post-PROJ-436 project (same project that fixes `norecursedirs = ... data ...`). NOT remediated this phase. **Risk 3 (verified, cosmetic, out-of-scope):** Two characterization texts still say "8 resources". The pin test is intentional (`test_8_resource_types_in_canonical_order`) — it asserts current contents of `data/resources.json`. Test will need an intentional update when resource #9 lands. NOT a Phase 7 defect. **Open question (Ross-only):** "Ammunition" vs "Ammo" label preference — implementation is internally consistent either way; one-line `data/resources.json` edit if Ross wants it reverted. **Net: zero verified findings require Phase 7 remediation.** All findings either intentional design (Risk 3), Phase 10's scope (Risk 1), or pre-existing cross-cutting (Risk 2). |

## Phase 0 deferred design decisions

These three decisions need to land in Phase 0 (Container substrate). They are NOT user-blocking — Phase 0 will pick a default and document the rationale here.

### D1: `PlanetaryFacility.consumable_levels` fold-in

`game/strategy/data/planetary_facility.py:32` currently has `consumable_levels: Dict[str, float]`. Two options:

- **(a) Fold into facility-local Container.** Facility becomes a Container-holding entity like ship / planet. Adds a Container to every facility instance (memory + serialization cost) but unifies the model.
- **(b) Keep as facility-internal state.** Facilities maintain their own consumables that do not participate in transfers. Cleaner if no UI/gameplay flow exposes facility consumables as transfer-able.

**Default for Phase 0:** (b) — keep as internal state until a concrete transfer use case justifies (a). If Phase 4 (planet migration) discovers a flow that needs (a), revisit.

### D2: `Empire.resource_pool` query-vs-cached

After `_fleet_resource_pool` deletion, `Empire.resource_pool` is a pure aggregation query that walks every container in the empire. Two options:

- **(a) Pure query** every call. Simple, always-correct, may be expensive at scale (large empires).
- **(b) Cached aggregate** invalidated on container mutation. Complex, fast, can desync if invalidation has bugs.

**Default for Phase 0:** (a) — measure first. If profiling in Phase 5 shows it's hot, add caching with explicit invalidation hooks (PROJ-293 / PROJ-311 patterns are nearby precedent).

### D3: Initial `mass_per_unit` values for non-user-fixed resources

The values added to the existing `data/resources.json` entries (metals 0.01, organics 0.01, vapors 0.001, radioactives 0.005, exotics 0.001, fuel 0.0001, ammo 0.001) are first-pass guesses to seat the math. Balance is a separate concern.

**Default for Phase 0:** ship the first-pass values. Document that they're balance-placeholders. If Phase 1 parser parity tests reveal a values-driven test failure (e.g., a fuel_tank holding 50000 units at mass 0.0001 = 5 tons doesn't fit in a 4-mass component), retune that specific value and document the retune.

## Flagged for product-review (no code action this project)

- `Empire.is_eliminated()` semantics when an empire owns only `MineGroup`s — flagged in PROJ-431 phase 5 decisions. Product question, not a tech-debt fix. Out of scope.
- The 5 high-value `ShipInstance` entry-point thin shims (`create`, `to_dict`, `clone`, `to_ship`, `update_from_ship`) — 910 callers across 252 files — kept per TD-06 Weak-LLM Guardrail #1. Out of scope unless container migration naturally reduces caller count.
