# PROJ-476: Decisions Log

> **LOG ALL DECISIONS HERE**
> When you make a design choice or the user specifies a preference, add it to this table.
> Future agents will reference this to understand why things were done a certain way.

| Date | Decision | Rationale |
|------|----------|-----------|
| 2026-05-21 | Project initialized | Starting point for Facade read-path: tooling/editor screens (battle_setup, galaxy_test, race_setup) reader migration (follow-on from PROJ-472) |
| 2026-05-21 | Created + scoped as the **tooling/editor screens** tail of PROJ-472. **GATED on PROJ-472's guards; informed by PROJ-475.** | Tooling/sandbox surfaces (`battle_setup` x4, `galaxy_test` x3, `race_setup` x4, `builder` x3 — verified 2026-05-21) are not live strategy screens and likely need broader/file-scoped exemptions than the main strategy UI (consult §4 + Open Question 1). Migrated last so the live-UI policy is settled first; exemptions must be file+reason scoped, not blanket waivers. |
| 2026-05-22 | **PROJ-476 is import-guard-only — no session-read migration.** Pre-flesh Codex consult + live grep confirmed ZERO `.session`/`._session`/`.facade_state.session` reads in `battle_setup/`, `galaxy_test/`, `race_setup/`, `builder/` (and the in-scope screens-root files). The session-read guard allowlist has no tooling-dir entry. | The tooling screens do not read a live `GameSession`; their `game.strategy.*` imports are detached pre-session editors / sandbox harnesses / authoring services / design-editor metadata. There is nothing to migrate to the facade — the deliverable is codifying the import exemptions, not a DTO migration. Consult `advice.md` §1/§2 (file:line evidence). |
| 2026-05-22 | **Gating widened to 474 + 475 + 477** (not just 472/475). Execute LAST; Phase 1 is a mandatory re-inventory against post-474/475/477 live code. | PROJ-474 promotes the pure value/enum/static-metadata symbols (`RaceConfig`, `RacePointBudget`, `FieldStatus`, `PlanetType`, `BattleRole`, `CombatPolicy`, `VALID_GALAXY_TYPES`, `StrategicKind`, `abilities_with_kind_tag`, `SUPERWEAPONS`) out of the tooling files' `TAIL` lines into `_UISAFE_SYMBOLS` (PROJ-474 design.md `:82-96`). 475/477 close the live read path. The legitimate tooling residue is only knowable once those subtract their share; the 2026-05-22 triple set is a snapshot to RE-VERIFY at execution. |
| 2026-05-22 | **Exemptions expressed as a machine-checkable `_TOOLING_EXEMPTIONS` category (exact `(file, module, member)` triples + tag + reason), NOT comment-only `TAIL` parking and NOT folder waivers.** Tags: `prebattle-editor`, `sandbox-harness`, `race-authoring`, `design-editor`. | Consult §4. `galaxy_test`/`race_setup`/`builder` mix promotable pure symbols with genuine tooling imports, so a folder/subpackage waiver would let net-new live imports sneak in. Mirrors how PROJ-474 makes `UISAFE` machine-checkable. A named category states "intended to stay outside the facade" (the real end state) instead of `TAIL`'s implied "migration pending". Add a no-misfile invariant (disjoint from `_UISAFE_SYMBOLS` and from residual `TAIL`/`CLUSTER`/`FLEETCAP`) + a positive control that a non-exempt live import in a tooling file is still flagged. |
| 2026-05-22 | **Scope boundary for screens-root files:** `battle_setup_state.py` IN (model behind the `battle_setup` package — `battle_setup/screen.py:32`, `controller.py:33`, `spec_compiler.py:77`); `design_selector_window.py` + `workshop_event_router.py` IN (`design-editor` tooling per PROJ-474 design.md `:132-141`); `build_queue_panel_factory.py` OUT (live build-queue screen, `compute_planet_production` → PROJ-475). | Consult §3. Classify by ownership/usage, not directory name; re-confirm exact triples for the screens-root design-editor files at execution. |
| 2026-05-22 | **Post-flesh review Finding 1 — build-queue `compute_planet_production` handoff to PROJ-475 made explicit.** The `build_queue_panel_factory.py` (`:18`/`:247`) + `build_queue_screen.py` (`:27`/`:211`/`:321`) `compute_planet_production` triples are OUT of PROJ-476 but were not enumerated in any sibling plan. Added an explicit owning row to PROJ-475 plan.md ("Deferred-from-472 reader set" table). | Codex post-flesh review Finding 1 (only blocker; in-plan fix, no user decision). PROJ-474 design.md `:118-119` already defers `compute_planet_production` to PROJ-475, but 475's plan only named the `strategy_detail_formatter.py` site. Closing the orphan risk so these live imports are owned. PROJ-476 stays import-guard-only and tooling-only. |
| 2026-05-22 | **Post-flesh review Finding 6 — Phase 3 doc/guard parity made a concrete (not optional) task.** Phase 3 Task 3.2 now adds a token/tag-based parity assertion (the four category tags in Pattern #5 == the tags used by `_TOOLING_EXEMPTIONS`), mirroring PROJ-474's fenced-token approach, instead of relying on review only. | Codex review Finding 6 soft spot: a review-only doc check lets Pattern #5 wording drift from the implemented tag shape while the guard stays green. Cheap fenced-tag parity prevents that. |
| 2026-05-22 | **Phase 1 re-inventory: NO DRIFT vs the 2026-05-22 snapshot.** Gate cleared (474=`11439ea71`, 475=`83ba24ea1`/`e2af24373`, 477=`95a07ea83`). All 30 snapshot tooling triples present + live; zero `.session` reads in any in-scope file. | Execution-time verification against post-474/475/477 live code, as the plan mandated. The snapshot was accurate. |
| 2026-05-22 | **Phase 4 (Codex exec-audit, 1 round): 2 findings VERIFIED + fixed; 0 rejected.** F1 (Medium): `DesignCatalog` in `design_selector_window.py:21` was annotation-only under PEP 563 (used only at param annotation `:59` + docstrings) — root-cause fix per AGENTS.md is to move it under `TYPE_CHECKING`, not codify a removable import as policy. Moved it; dropped the triple (30→29). F2 (Medium): no AST-liveness test asserted exemption triples are real runtime imports — added `test_tooling_exemptions_are_live_runtime_imports` (+ non-vacuity twin). The audit's two Risks (RaceRandomizer test-seam debt; drift) are subsumed by F2's liveness test + the existing prune-if-removed note. | Codex `proj476_exec_audit/audit.md`. Verified both against live code (PEP 563 + grep showed zero runtime `DesignCatalog` use). Sharded suite 24659 passed after fix; the `DesignCatalog` move is behavior-neutral. |
