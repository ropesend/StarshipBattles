# PROJ-477: Decisions Log

> **LOG ALL DECISIONS HERE**
> When you make a design choice or the user specifies a preference, add it to this table.
> Future agents will reference this to understand why things were done a certain way.

| Date | Decision | Rationale |
|------|----------|-----------|
| 2026-05-22 | Project initialized | Starting point for Facade read-path: delete galaxy/empires/systems StrategyScreen pass-throughs + renderer/read-model boundary (deferred from PROJ-475) |
| 2026-05-22 | **Fleshed to execution-ready. Pre-flesh Codex consult (`Scratchpad/Consult/proj477_preflesh/advice.md`) verified consumers live and EXTENDED the deferred set.** | The stub UNDERCOUNTED. Consult §1 added direct readers `strategy_build_queue_manager`, `strategy_game_state_manager`, `strategy_screen_assets`, `strategy_screen_selection`, `strategy_screen.current_empire` (`:201-210`), wrapper-only `strategy_fleet_ops`/`strategy_superweapons`, AND the transitive raw-domain fan-out (menu builders→`fleet_menu_items`/`planet_menu_items`; list windows→`planet_list_window`/`star_list_window`; build-queue manager→`build_queue_screen`→`build_queue_controller`). All verified live 2026-05-22. |
| 2026-05-22 | **Boundary = HYBRID (consult §2): a scene-owned `StrategyWorldAccess` live-traversal seam for render-hot + raw-window handoffs, PLUS narrow cold facade queries.** REJECT a `facade.render.*` DTO namespace. | `facade.systems.all()`/`facade.empires.all()` allocate DTO lists per call (`grouped_namespaces.py:200-202,240-246`); `EmpireInfo.from_empire` rebuilds `ResourceCatalog.from_json()` per call (`empire_dto.py:102-110`) — categorically wrong per-frame. `FacadeSessionState` stays a kept-by-design per-turn cache holder (`_facade_state.py:49-61`, pinned by `test_game_session_projection_boundary.py`), NOT a public raw-domain render API. `StrategyWorldAccess` hands back live collections/O(1) map lookups → NO per-frame DTO allocation, and gives guard #3 a SINGLE allowlist seam. |
| 2026-05-22 | **New cold facade surfaces:** `facade.systems.by_name`, `facade.systems.of_object`, `facade.systems.at_map_hex(hex, radius=50)`, and a NEW `facade.spatial.contents_at_hex` namespace. | Consult §3: existing `facade.systems.at_hex`/`near_hex` + `facade.planets.at_hex` do NOT cover the live readers. `near_hex(max_dist=8)` ≠ `get_system_at_hex(radius=50)` ownership semantics (`system_slice.py:86-114` vs `galaxy_pathfinding_service.py:113-128`); `planets.at_hex` is exact-center only (`planet_slice.py:83-89`) so misses multi-hex zones (Dyson Spheres). `get_system_by_name`/`get_system_of_object`/`get_zones_at_global_hex` have no facade equivalent. Do NOT add `systems.all_live()`/`empires.all_live()` returning domain objects (raw-domain leak) — that's what `StrategyWorldAccess` is for. |
| 2026-05-22 | **Guard #3 added FIRST (Phase 1), ratcheted down — not last.** Match Load-context property reads on SCOPED shapes only (`scene.X`, `_screen.X`, `r.X`, `screen.X` in `strategy_screen_*` helpers, `self.galaxy` only in `strategy_screen.py`). Do NOT match generic `self.galaxy`/`self.empires`/`self.systems`. | Consult §4: adding the guard early blocks net-new bypasses during the migration. Generic `self.galaxy` matching would false-positive on constructor-injected LOCAL attrs in `build_queue_screen`/`build_queue_controller`/`planet_list_window`/`star_list_window`/`planet_list_helpers`. Load-context-only so a store `x.galaxy = scene.galaxy` flags only the RHS. |
| 2026-05-22 | **Harden the SESSION guard for dynamic `getattr(obj, "session")` extraction.** | `system_tree_panel.py:418-425` resolves `getattr(scene,'session')` then reads `.active_empire`/`.registries` — bypasses the current AST matcher entirely (consult §4). Must be caught before the getter is retired. |
| 2026-05-22 | **`StrategyScreen.session` GETTER retired by making it raise `AttributeError`; SETTER kept. Category B WRITE seams routed through a narrow scene-owned write handle (`screen.order_writes`), NOT the facade and NOT a reopened getter.** | Consult §6: `system_tree_panel` rewires to `scene.active_empire_id` + `scene.registries`. Writes (`strategy_game_state_manager.py:160-164` set-active-empire; `strategy_screen_order_editing.py:56-67`/`:89-92` set-path/pop-order) get a tiny composition-root write handle backed by `_session.active_empire`/`_session.fleet_mutator`. Getter raises with a pointer to the right surface; setter (`strategy_screen.py:294-311`) stays so `screen.session = mock` works (split-brain guard). |
| 2026-05-22 | **Phasing (consult §5): guard scaffold → cold surfaces + write handle → session-getter retirement + getattr hardening → cold consumer migration → `StrategyWorldAccess` + render migration → delete pass-throughs/re-exporters + ratchet guard.** DELETE each property LAST, only after all consumers migrated, guard green, suite green. | Consult §5 deletion order: `session` getter after readers migrated + getattr hole closed; `empires` after assets/state-manager/click/render/owner-lookup migrated; `systems` after assets/selection/camera_nav/colonization/superweapons/render migrated; `galaxy` LAST (widest fan-out). A removed property surfaces missed callers as `AttributeError` immediately. |
| 2026-05-22 | **CAPPING: planned WHOLE (option A) but with a documented Phase 4/5 cut line (option B) flagged to the user.** | Consult §8: as a single execution pass this is at the upper bound now the missed consumers are included. Whole is feasible ONLY by making `StrategyWorldAccess` the single raw-domain seam (render migration becomes a mechanical source-swap, not a DTO rewrite). If a single agent pass proves too long, an executing agent splits at the Phase 4/5 boundary, extracting Phases 5-6 (render-hot migration + final property deletion) to a new stub via `create_project.py`. No further stub created pre-emptively. **Orchestrator to confirm A-vs-B with the user before executing.** |
| 2026-05-22 | **GATED on PROJ-475; RE-SCAN the consumer inventory against post-475 live code before executing.** | PROJ-475 is planned, NOT executed (`_facade_state.py:65` still `self.session = session`; session-guard Category A still allowlists `_session.galaxy/empires/systems`). PROJ-475 migrates several session-getter readers (event_router, lifecycle, transfer_controller, empire_panel_ctrl) so the getter tail shrinks before PROJ-477 deletes it (consult §6 live-code nuance). |
| 2026-05-22 | **Post-flesh Codex review (`Scratchpad/Consult/proj477_postflesh/advice.md`): VERDICT BLOCKERS — all addressable in-plan, NO user-decision blocker. Revised per all findings. Verdict on whole-vs-split: keep WHOLE.** | Review verified the plan against live code. Blocker 1 = the external PROJ-475 gate (already documented; not a plan defect — readiness is conditional). Blocker 2/3 = plan-completeness gaps, fixed below. Cap stays WHOLE with the Phase 4/5 cut line; the inventory of DIRECT readers is confirmed complete (only transitive helper files were missing). |
| 2026-05-22 | **B2 fix: live-`StarSystem` consumers use `scene.world.system_for_object`/`system_at_map_hex` (LIVE), NOT the summary facade queries.** | `SystemInfo` carries no `planets`/`warp_points`/`stars` (`system_dto.py:118-161`). `strategy_event_router.py:401-411` iterates the resolved system's live `.planets`; `strategy_colonization.py:246-257` + `strategy_superweapons.py:362-379` read live `.planets`/`.warp_points`. `facade.systems.of_object`/`at_map_hex` remain for true summary-only callers (Phase 2 still builds them). |
| 2026-05-22 | **B3 fix: added `world.planet_by_id()` to the `StrategyWorldAccess` contract (or route to existing `facade.facade_state.get_planet_by_id`); added `planet_list_filters.py`/`star_list_filters.py` to the Phase 4 inventory + manifest.** | `strategy_build_queue_manager._resolve_live_yard()` (`:301-319`) needs a live planet-by-id lookup. `planet_list_filters.py:38-87` + `star_list_filters.py:20-64` walk `galaxy.systems.values()` — transitive fan-out once the windows stop receiving raw galaxy. |
| 2026-05-22 | **B5 fix: `strategy_superweapons.py:109` uses EXACT planet membership → migrate to `world.planets_at_exact_hex`, NOT `facade.spatial.contents_at_hex`.** | `contents_at_hex` is multi-hex-aware; using it here would start matching Dyson-Sphere edge hexes where the exact-membership read does not today (semantic drift). `contents_at_hex` is correct for the zone-membership sites (`colonization:83-88,170-176`, `click_dispatcher:594-598`). |
| 2026-05-22 | **B1/B4 minor: Phase 5 identity test pins ELEMENT identity (same live `StarSystem` objects), not `dict_values` container identity; Phase 1 guard positive-control adds nested `c.scene.galaxy`/`self.scene.galaxy` samples; Task 5.1 promoted to a hard PREREQUISITE executed before Phase 4.** | `galaxy.systems.values()` returns a fresh view per call — the invariant is "same live objects, no DTO projection". Most live property matches take the nested `c.scene.galaxy` shape. Many Phase 4 tasks are not executable until `scene.world` exists. |
