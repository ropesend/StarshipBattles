# PROJ-377: Decisions Log

> **LOG ALL DECISIONS HERE**
> When you make a design choice or the user specifies a preference, add it to this table.
> Future agents will reference this to understand why things were done a certain way.

| Date | Decision | Rationale |
|------|----------|-----------|
| 2026-05-06 | Project initialized | Closes PROJ-372 review MAJ-001 (golden-save fixture) + MIN-002 (pathfinding shim sweep). Original PROJ-372 decisions.md row 2026-05-07 named these as "PROJ-376 follow-up", but PROJ-376 was already allocated to BuildQueueScreen — this work moved to PROJ-377. |
| 2026-05-06 | Three phases: golden fixture (1), partial shim sweep (2), shim-scope freeze + AST guard (3). | Two distinct deliverables; partial sweep is decision-heavy enough to warrant its own phase; AST guard locks the result. Could compress to 2 phases by merging Phase 1 into Phase 2 — kept separate so Phase 1 can ship even if Phase 2 stalls on a deferred site discovery. |
| 2026-05-06 | **Golden-fixture format: JSON** (rejected pickle). | JSON is human-diffable, version-control-friendly, immune to Python-version unpickling drift (relevant after PROJ-295's 3.13 bump). `Galaxy.to_dict()` already produces plain JSON-shaped dicts. Pickle would re-introduce the exact form of cross-version risk PROJ-295 just removed. |
| 2026-05-06 | **Two fixtures, not five.** Baseline (5-system synthetic, seed=2, no planets) + populated (10-system, planets, warp lanes, owned planet w/ atmosphere/deposits/stockpile lit up, seed=100). | The five synthetic test functions in `test_save_round_trip.py` already exercise five shapes. PROJ-377's added value is the **checked-in artifact** — two is enough to catch field-drift regressions while keeping CI cost and fixture maintenance burden low. The populated fixture's manually-added owned planet is the critical add: synthetic generation often leaves planets with default values, masking drift in `Planet.to_dict` for owned-planet fields (populations, stockpile, intrinsic_abilities). |
| 2026-05-06 | **Capture script lives at `tests/fixtures/saves/_capture_baseline.py`** with leading underscore so pytest's discovery rules (default `test_*.py` glob) ignore it. | The script must NOT run as a test — it overwrites the fixture. Humans run it manually when capturing a new baseline; CI loads-and-asserts only. |
| 2026-05-06 | **Capture script strips storms** before calling `galaxy.to_dict()`. | `Storm.to_dict()/from_dict()` has pre-existing drift documented at `tests/integration/strategy/test_save_round_trip.py:34-38`; PROJ-372 explicitly scoped it OUT. Stripping storms in the capture mirrors the synthetic test's approach. |
| 2026-05-06 | **JSON written via `json.dump(..., indent=2, sort_keys=True)`.** | `sort_keys=True` normalizes dict key order, which protects against insertion-order drift in `Galaxy.to_dict()` output. `indent=2` keeps diffs readable in PRs. |
| 2026-05-06 | **Round-trip identity assertion: `Galaxy.from_dict(fixture).to_dict() == fixture`.** | Catches every structural drift class the synthetic test catches (missing field, type narrowing, lazy-built index not restored). Does NOT catch semantic drift (a field's MEANING changes). PROJ-377 accepts the same trade-off the synthetic test already accepts. |
| 2026-05-06 | **Pathfinding shim sweep is partial: 5 of 14 sites migrated by default; 1 site verified-then-decided in Phase 2 Task 2.1.** | The 6 deferred sites have active `unittest.mock.patch('game.strategy.data.pathfinding.X')` reaching them through their production code paths. Migrating a deferred site to `galaxy._pathfinder.X(...)` would silently break those patches — production runs unmocked, tests pass against the unmocked output. Defer them with a documented rationale rather than break test-patch transparency. |
| 2026-05-06 | **Migrate target: `galaxy._pathfinder.X(...)` for the Class A sites.** | Direct service access on production galaxies is intentional; `_pathfinder` is constructed in `Galaxy.__init__` (`galaxy.py:65`). The leading underscore is preserved (matches `_state`, `_registry`, `_spatial`, `_warp_gen`, `_sys_gen`). A future ticket may add a public `pathfinder` property; out of PROJ-377 scope. |
| 2026-05-06 | **Class C sites (intercept_calculator.py:121, 169) STAY shim-routed by design.** | Per PROJ-372 verifier_report.md and intercept_calculator.py's own docstrings: routing through `_pf_shim` preserves test-patch transparency for tests patching `pathfinding.find_hybrid_path` / `pathfinding.project_fleet_path`. Decision pre-dates PROJ-377 and is preserved. |
| 2026-05-06 | **`pathfinding.py` is NOT deleted at the end of PROJ-377.** Plan G7 explicitly conditions deletion on co-migrating tests, which is out of scope. | PROJ-372 plan said "delete shims at Phase 5 close". PROJ-377 formalizes that the deletion is conditional on test-patch unification — a separate concern owned by a successor ticket. The shim's role is upgraded from "deprecated, slated for deletion" to "permanent test-patch transparency surface for the four functions test files actively patch", with the AST guard pinning the surface. |
| 2026-05-06 | **An AST guard test pins the surviving shim free-function set in Phase 3.** | Without it, the four "permanent" functions could silently grow back to the original 8 if a future PR re-adds a forwarder. The guard is a small AST-walk test asserting the module's top-level function names exactly equal the Phase 2 final list (plus helpers `_pathfinder_for`, `_intercept_for`). |
| 2026-05-06 | **Phase 1 ships independently.** | The golden fixture has no code dependency on the shim sweep; both can land in either order. Sequencing 1→2→3 keeps reviewer continuity. If Phase 2 stalls on a deferred-site discovery, Phase 1's value (closing MAJ-001) is preserved. |
| 2026-05-06 | **Per-phase round-trip tests `test_save_round_trip_phase{1,2,3,4}.py` are NOT deleted.** | PROJ-372 Phase 5 plan marked them "kept for now as boundary checks". Deleting is a separate concern. PROJ-377 is purely additive on the test side. |
| 2026-05-06 | **PROJ-372 `decisions.md` row 2026-05-07 is updated, not replaced.** A new row notes "PROJ-377 closed MAJ-001 (golden fixture) and MIN-002 (partial sweep + AST guard); see PROJ-377 plan for the deferred-site set". | Preserves the historical record of why Phase 5 closed without these deliverables, while pointing future agents at the resolution. |
| 2026-05-06 | **`Galaxy._pathfinder` access pattern: read-through-attribute, not via a `get_pathfinder()` accessor.** | Matches existing access patterns in PROJ-372's facade (`galaxy._registry`, `galaxy._spatial`, `galaxy._warp_gen`, `galaxy._sys_gen` — all attribute reads). Adding an accessor is a separate API design choice; out of PROJ-377 scope. |
| 2026-05-06 | **`tests/fixtures/saves/` is the fixture home.** Mirrors the existing `tests/fixtures/captions/` and `tests/fixtures/test_components.json` convention. | Locates the fixture next to other test fixtures; near `tests/integration/strategy/test_save_round_trip.py` via path; pytest discovery is unaffected by JSON files. |
| 2026-05-06 | **Phase 2 final migration count is data, not a target.** Plan declares "5 by default + 1 verify-then-decide", but the architect explicitly avoids committing to "X of 14" in advance. | If Task 2.1's verification reveals an additional Class B coupling (or, conversely, that a presumed Class B site is actually safe), the count flexes. The decision is "migrate sites where test-patch transparency is preserved", not a numeric target. |
| 2026-05-06 | **`tectonic_activity` field is NOT renamed.** | PROJ-372 review MAJ-003 was a model hallucination — the word is correctly spelled (t-e-c-t-o-n-i-c). Verified at `planet.py:68`, `planet_serde.py:44/102/151`. PROJ-372 verifier rejected the finding; PROJ-377 re-confirms and explicitly excludes it from scope. |
| 2026-05-06 | **`remove_warp_link` is NOT moved.** | Already covered by PROJ-372 decisions.md row 2026-05-07 (plan deviation accepted with rationale). Out of PROJ-377 scope. |
| 2026-05-06 | **`Galaxy._intercept` deletion is NOT in scope.** | Already remediated as PROJ-372 review MIN-001 (decisions.md row 2026-05-07). PROJ-377 G8 is to verify no new `galaxy._intercept` callers re-appeared during Phase 2. |
| 2026-05-06 | **Phase 3 AST guard pins shim FUNCTION NAMES, not their bodies.** | Bodies are 1-line forwarders; PROJ-372 already lint-checks them via the existing `test_no_method_body_over_5_loc.py` LOC guards. The new guard's job is to prevent the function-name set from growing back to the pre-PROJ-372 8-name set. |
| 2026-05-07 | **OpenCode review (req_20260507_044410_7cfefd) + verifier**: 0 CRIT, 1 MAJ, 3 MIN, 12 INFO. MAJ-001 + MIN-001 (PROJ-372 cross-link reported "5 of 14" instead of "4 of 14"; superweapon attempt-and-revert missing from deferred list) **fixed in `Projects/active_projects/PROJ-372/decisions.md` last row** — corrected count to 4, removed superweapon from migrated parenthetical, added to deferred Class B list with attempt-and-revert note. MIN-002 (capture-script double-seed fragility) **deferred** — verifier confirmed docstring at `_capture_baseline.py:9-23` already documents the seeding limitation accurately and explicitly states the CI contract is round-trip identity not byte-equality; inline comment would be redundant. MIN-003 (closeout-readiness drift) **auto-resolved** by the MAJ-001 fix. All 12 INFOs CONFIRM. Independent verifier sweep: zero `patch.*strategy_screen.*{find_hybrid_path,get_system_at_hex,find_nearest_system}` or `patch.*strategy_colonization.*get_system_at_hex` patches in `tests/` — the 4 migrated sites are safe from the local-import-patch trap that caught reverted site #3. Reports: `Reviews/results/2026-05-07_044412_code_proj-377-.../{report,verifier_report}.md`. | One material doc-drift fix landed; capture-script docstring is sufficient as-is. PROJ-377 closes with the cross-link consistency restored. |
| 2026-05-07 | **Phase 2 — final Class A migration count: 4 sites (sites #10, #11, #12, #14).** Site #3 (`superweapon_order_processor.py`) was attempted in Phase 2 but Phase 3's sharded run revealed 40 test regressions — `tests/unit/strategy/engine/test_superweapon_order_processor.py` and `..._gaps.py` patch `'game.strategy.engine.superweapon_order_processor.get_system_at_hex'` (the **local module-level import name**, not `'game.strategy.data.pathfinding.get_system_at_hex'`). The original planning grep searched only `pathfinding.X` patches; module-local-import patching reaches site #3 the same way Class B sites are reached. Site #3 reverted and reclassified **Class B**. The strategy_screen.py and strategy_colonization.py migrations remained green (verified via grep — no `patch('game.ui.screens.strategy_screen.X')` or `patch('game.ui.screens.strategy_colonization.X')` against the migrated names). Site #9 (`planet_slice.py:66`) **deferred** per Task 2.1 verification: `tests/unit/strategy/facade/test_facade_robust_resolution.py:73,87` patches `pathfinding.get_system_at_hex` while exercising `facade.get_planets_at_hex(...)` which routes through `planet_slice.py:66`. Class B sites (1, 2, 3, 4, 5, 6, 9, 13) remain on shim by design — pinned in this row + r004 row. | Original planning grep had a blind spot: it searched `patch('game.strategy.data.pathfinding.X')` but tests can also patch the **target module's local re-export** of an imported name (`patch('game.strategy.engine.superweapon_order_processor.get_system_at_hex')`). Both patch idioms produce the same test-isolation effect when the local import is `from … import X`. Future patch-site sweeps must search both forms. Final sharded delta: 19057 passed / 1 pre-existing failure unchanged. The 4 successful migrations (3 strategy_screen methods + 1 strategy_colonization method) preserved the pathfinding shim's role as test-patch transparency surface; the one revert is logged here for the cross-link audit trail. |
| 2026-05-07 | **Capture script idempotence relaxed to best-effort.** Original design assumed byte-identical re-capture; in practice `PlanetImageRegistry.get_random_image` / `get_random_rotation`, `StarImageRegistry.get_random_image`, `_apply_warp_point_intrinsic_abilities`, and `_roll_warp_type` each instantiate an *unseeded* `random.Random()` when no `rng` is threaded through. Threading rngs through every call site is out of scope for PROJ-377 (it touches PROJ-301/302/304 production code). Mitigation: planet `image_id` / `image_rotation` normalised to deterministic placeholders post-generation; system placement + names + planet body shapes seeded explicitly. Star image_ids and warp-point fields remain re-roll-able. **CI contract is the round-trip identity assertion**, not byte-identical re-capture — the committed fixture is frozen and round-trips identically; re-running the script produces a new internally-consistent snapshot. Documented in `_capture_baseline.py` module docstring. |
| 2026-05-07 | **Codex+Claude joint review (arc 20260507T033233Z_proj-376-377-378-plans-review)**: applied r004 edits — see `outcome.md` for the full edit list. | CC-1 added `phase_state.json` + `findings_ledger.md` per 03c protocol. CC-2 demoted three inline `### Phase N:` plan summaries to `#### Phase N summary:` so `project_status.py` / `validate_phase.py` parse the standalone phase checklists. E377-1 added `**Depends on:**` + `**Review Mode:**` metadata to all three phase checklists (Phase 1 + 3 lightweight, Phase 2 standard). E377-2 reordered Phase 1 to TDD-first (round-trip tests first → confirm FAIL → capture script → fixtures → re-run round-trip tests → confirm PASS). E377-3 fixed the runtime-broken JSON-load snippet (`json.loads(_FIXTURE_DIR / "...")` → `json.loads((_FIXTURE_DIR / "...").read_text())`; the original would fail because `json.loads` does not accept `Path` objects). E377-4 reclassified pathfinding site #9 (`planet_slice.py:66`) as Class B (test-patched), removing the previous "no patch reaches" / "patches ARE for this site" contradiction; defer per OQ-3 unless the migration also rewrites the relevant facade tests. |
