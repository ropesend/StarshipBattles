# PROJ-473: Decisions Log

> **LOG ALL DECISIONS HERE**
> When you make a design choice or the user specifies a preference, add it to this table.
> Future agents will reference this to understand why things were done a certain way.

| Date | Decision | Rationale |
|------|----------|-----------|
| 2026-05-20 | Project initialized | Starting point for Thread per-instance RNG through planet/star generation to enable global random.seed removal (deferred from PROJ-471) |
| 2026-05-20 | Scope populated from PROJ-471 deferral (Protocol 07) | PROJ-471 (state-hygiene) findings ST-04-010 / ST-04-011 flagged two global `random.seed()` calls as Pattern #18 violations, but dual independent + Codex review (re-verified inline against live code) found them LOAD-BEARING: `star_generator.py` (26 bare `random.*` draws) and planet/atmosphere/naming/warp generation use the bare global `random` API, and placement strategies fall back to global random when `rng` is None. Removing the seed without first threading an explicit `rng` breaks galaxy reproducibility. The seed removal therefore depends on this project's rng-threading work and was moved here. |
| 2026-05-20 | ~~Removal of the two `random.seed()` calls stays at the END of Phase 1~~ **SUPERSEDED 2026-05-21: seed removal is Phase 3, not end of Phase 1** | Original rationale (still valid): the seed delete is only safe once NO generation code reads global `random` state. **Correction:** the re-phased plan (next two rows + the actual `phase_3_checklist.md`) moves the seed removal to **Phase 3**, after stars/planets (P1) and warps (P2) are all on seeded streams. This row is kept for history only; the authoritative phase is Phase 3. |
| 2026-05-20 | Generation output must remain reproducible byte-for-byte for a fixed seed; this is a "where randomness comes from" change, not a "what is generated" change | Moving `random.X(...)` → `rng.X(...)` with an identically-seeded `rng` draws the same sequence, so a fixed `galaxy_seed` must yield the same galaxy before and after. Any divergence indicates a draw-order error, not an intended balance change. No save-format change. |
| 2026-05-21 | **Reproducibility target = FULL save-visible equivalence** | Orchestrator decision. The before/after fixed-seed snapshot must include *all* save-visible generation outputs, not just coordinates/counts. Per system: `name`, `global_location`, `region_id`, `archetype`, `intrinsic_abilities`, and per warp point `destination_id` / `location` / `warp_type` / `intrinsic_abilities`. Per star: `name`, `mass`, `radius_hexes`, `temperature`, `luminosity`, `spectrum`, `star_type`, `color`, `age`, `location`, `image_id`, `intrinsic_abilities`. Per planet: `name`, `location`, `orbit_distance`, `mass`, `radius`, `surface_area`, `density`, `surface_gravity`, `surface_pressure`, `surface_temperature`, `atmosphere`, `planet_type`, `surface_water`, `tectonic_activity`, `magnetic_field`, `deposits`, `image_id`, `image_rotation`, `intrinsic_abilities`. Plus storms. **Do NOT inherit the save-fixture image-field skiplist** (`tests/fixtures/saves/_build_galaxy_fixture.py` normalizes `image_id`/`image_rotation` to placeholders for golden-save stability; for *this* refactor those fields are generation outputs and will drift if draw order drifts, so they must be in the snapshot). `StarSystem.to_dict()` already serializes this whole nested tree (verified `game/strategy/data/star_system.py:99-115`), so the snapshot anchors on `[sys.to_dict() for sys in galaxy.systems.values()]`. Rationale: a partial migration that keeps coordinate determinism but silently changes names / images / warp types / intrinsics would pass the existing shallow tests (`test_deterministic_generation.py` only checks coords + counts + star types) and ship a regression. **REFINED 2026-05-21 (sign-off Blocker 1/2):** this full field list still holds, but the fields split into TWO enforcement classes — class (a) already-seeded (incl. warp-point `location`/S9 geometry) compared against a GOLDEN BASELINE, and class (b) currently-unseeded (`name`, `image_id`, `image_rotation`, `warp_type`, warp `intrinsic_abilities`) asserted for determinism+presence only. See the two sign-off decision rows below for the authoritative class split. |
| 2026-05-21 | **Phasing = Phase 0 (root-RNG boundary + name shuffle) → Phase 1 (per-system star+planet pipeline + image registries) → Phase 2 (warp generation incl. the `generate_warp_lanes` facade rng gap) → Phase 3 (remove the two global seeds + normalize the rng-is-None fallbacks)** | Orchestrator decision, matches the Codex consult (`AgentCoordination/Scratchpad/Consult/proj473_preflesh3/advice.md` §1). **Why this order:** (1) Name order is *already outside* the seeded stream today — `GameInitializer.initialize` constructs `Galaxy` (which shuffles names in `NameRegistry.load_data`, `naming.py:42`) *before* it builds the generation RNG (`game_initializer.py:248`). If names aren't brought in first, "unchanged output" is impossible because the name stream is unseeded. So the root-RNG boundary + name shuffle must move first. (2) Stars/planets are the bulk of per-system draws and run in a fixed per-system loop (`galaxy_system_generator.py:171-209`); thread them next so the seeded sequence is correct before warps consume from it. (3) Warps run as a clean second pass *after* `generate_systems` completes at both entry points (`game_initializer.py:271`, `galaxy_mode.py:287`), so they migrate independently — but the global seed can't be deleted until they're seeded too. (4) Delete the two `random.seed()` calls only after 0–2 land, then normalize fallbacks. Deleting the seed earlier yields non-reproducible galaxies; hardening fallbacks earlier breaks the no-rng public-API tests. |
| 2026-05-21 | **Scope expansion: add 6 files the stub missed; keep 4 off-path files OUT** | Orchestrator decision, verified against live code. ADDED (on the new-game galaxy path): `game/strategy/data/galaxy.py` (the `generate_warp_lanes` facade drops `rng` — `galaxy.py:256-266` — and constructs `NameRegistry`/`StarGenerator`/`PlanetGenerator` in `__init__`, `galaxy.py:44-60`); `game/strategy/data/galaxy_system_generator.py` (the per-system orchestration loop + the seeded sub-RNG derivation at `:158-169` + the `generate_planets`/`generate_system_stars` calls that currently pass NO rng for *physics*, `:71`/`:193`); `game/strategy/data/naming.py` (the load-time `random.shuffle`, `:42`); `game/strategy/generation/star_image_registry.py` and `game/strategy/generation/planet_image_registry.py` (already accept `rng` but `StarGenerator._get_image_id` / `PlanetGenerator._create_single_planet` never forward one — `star_generator.py:51-58`, `planet_gen.py:397-398`); `game/strategy/generation/storm_generator.py` (INSPECTED — already fully rng-threaded: every draw uses the passed `rng`, fed a derived `storm_rng` at `galaxy_system_generator.py:165`; storm output fields for the snapshot: `name`, `storm_type`, `description`, `location`, `hex_offsets`, `abilities`, `image_variant`, `intensity`. No production change needed — include only in the snapshot + as a regression guard). KEPT OUT (not on the new-game galaxy path, per consult §3): `density_map.py`, `system_blueprints_loader.py`, `race_randomizer.py`, `minefield_resolver.py` — separate cleanup candidates; log via `/claude-di-log` if desired, not scoped here. |
| 2026-05-21 | **Test/caller cleanup is in scope** | Orchestrator decision. Several tests exercise the no-rng public path or seed module-level `random`, pinning the current global-random contract. They must be updated so the `rng=None` fallbacks can be normalized in Phase 3 without breaking them: `tests/unit/strategy/data/test_planet_gen.py:42-45` (calls `random.seed(42)` then constructs `PlanetGenerator`); `tests/integration/strategy/test_planet_gen.py:17-18` (`galaxy.generate_systems(count=5)` with no rng); `tests/integration/strategy/test_galaxy_gen.py` (`generate_systems(..., placement_strategy=strategy)` with no rng); `tests/unit/strategy/generation/test_placement_strategies.py:44-45` (`sample_location(...)` with no rng); `tests/unit/strategy/data/test_galaxy_system_generator.py:544-549` (explicitly asserts `rng=None` still works). Cleanup = pass an explicit seeded `random.Random(...)` (or accept the normalized contract) rather than relying on global state. |
| 2026-05-21 | **Pattern #18 guard correction: `game/strategy` is NOT currently guarded — Phase 3 ADDS it, it does not "tighten an exclusion"** | Verified against live code. `tests/unit/quality/test_no_unseeded_random.py:37` sets `GUARDED_DIRECTORIES = ("game/simulation", "game/engine", "game/ai")` — `game/strategy` is absent entirely. The stub plan/manifest assumed a "strategy-generation exclusion" to tighten; there is none. The real Phase 3 task is to ADD `game/strategy/generation` + `game/strategy/data` (the now-clean modules) to `GUARDED_DIRECTORIES` so the guard would catch a regression. The `# excluded: PROJ-301-304` comment at `:32-36` is prose, not an executable exclusion list. |
| 2026-05-21 | **RNG-topology contract: REPRODUCE the existing independent streams, do NOT consolidate to one root** | Codex post-flesh review (`AgentCoordination/Scratchpad/Consult/proj473_postflesh/review.md` §2, §7), re-verified against live code. The code does NOT use one root rng. Today: system PLACEMENT uses `rng = random.Random(galaxy_seed)` (`game_initializer.py:248`), which also yields the seeds for two child streams (`storm_rng`, `intrinsic_rng`) via two `randint` draws at `galaxy_system_generator.py:163-164`; star/planet PHYSICS draw from the **module-level `random`** separately seeded by `random.seed(galaxy_seed)` (`game_initializer.py:250`), passed NO rng at `galaxy_system_generator.py:193`/`:197`; storms/intrinsics use the derived child streams; images + warp-type/intrinsics use fresh **unseeded** `random.Random()` fallbacks (`star_image_registry.py:81-82`, `planet_image_registry.py:76-77`/`:107-108`, `galaxy_warp_generator.py:408-409`). **Contract:** to preserve byte-for-byte output, physics generation must get its OWN `random.Random(galaxy_seed)` (a separate instance, same seed) — NOT derived from the already-consumed placement rng — kept distinct from placement/storm/intrinsic streams. Because CPython's module-level `random.*` is one `Random` instance, `random.seed(galaxy_seed)` + fixed draw order == `random.Random(galaxy_seed)` + same order, so the dedicated `physics_rng` reproduces the global sequence exactly when star-then-planet draw order is preserved (H5). Full topology table is in design.md **H7**. This makes the vague "derive or reuse a seeded stream" (old phase_1/phase_2 wording) a HARD, non-free choice. **Refined 2026-05-21 (sign-off Blocker 1):** warps add NO separate stream — warp geometry (S9) and warp type/intrinsics (S10) CONTINUE the same `physics_rng`; see the warp-stream-contract decision row below. |
| 2026-05-21 | **Caller-cleanup contract: backward-compatible public facades + hard-required rng only on internal helpers** | Codex review §5 named many omitted no-rng / `random.seed(...)` callers. Per the revision directive (pick the backward-compatible-facade approach unless code says otherwise), the contract is: **public facades keep accepting `rng=None` and NORMALIZE it internally to a seeded stream at a single composition site**; only INTERNAL leaf helpers hard-require a concrete rng. This is Task 3.3 **option (b)**, chosen over option (a). Facades that STAY normalized (accept `rng=None`): `Galaxy.generate_systems`, `Galaxy.generate_planets` (currently no rng param at `galaxy.py:212-214` — add an optional one that normalizes), `Galaxy.generate_warp_lanes` (P2 adds optional rng), and the `StarGenerator()` / `PlanetGenerator()` direct-construction path used by tools/tests. **Consequence:** the broad caller surface the review listed does NOT all need editing — it keeps working through the normalized facades: `tests/integration/strategy/test_galaxy_generation_storms.py` (`:22-24`,`:42-44`,`:56-58`,`:80-82`,`:108-110`,`:156-158`), `tests/integration/strategy/test_save_round_trip.py` (`:70-113`, incl. `galaxy.generate_planets(system)` at `:97`,`:110`), `tests/integration/colonization/conftest.py` (`:121-124`), `tests/integration/gameplay_loop/conftest.py` (`:103-105`), `tests/performance/bench_galaxy_planet_star.py` (`:48-56`), `tests/integration/strategy/test_star_generation.py` (`:25-70`), `tests/unit/strategy/data/test_stars.py` (`:468-745`), `tests/unit/strategy/planet_atmosphere/test_generation.py` (`:91-101`), and `game/ui/screens/galaxy_test/system_mode.py` (`:245-263`, direct `StarGenerator()/PlanetGenerator()` construction). The ONLY callers that Phase 3 Task 3.1 must actively edit are the ones that pin the *unseeded fallback* contract being normalized at the placement/leaf layer (the five files already listed in Task 3.1) — because those assert behavior that changes. The `random.seed(...)` calls in the still-using-facade tests become harmless no-ops once the facade normalizes internally; they MAY be cleaned up opportunistically but are not required for correctness. Verified caller list against live code 2026-05-21. |
| 2026-05-21 | **Output contract split into class (a) golden-baseline-preserved vs class (b) newly-deterministic** (Codex sign-off Blocker 2) | Codex sign-off review (`AgentCoordination/Scratchpad/Consult/proj473_signoff/review.md` §7, blocker 2), verified against live code. The Phase 0 / Phase 2 tests as previously written were two-run SAME-implementation determinism checks — they prove the NEW code is reproducible but NOT that it produces the SAME galaxy as the CURRENT code; a deterministic-but-sequence-shifting rewrite would pass them while changing output. Fix = split the contract and add a golden baseline. **Class (a) — already-seeded, MUST be byte-for-byte preserved vs CURRENT code:** system placement / `global_location` (S1), star physics (S4: `mass`, `radius_hexes`, `temperature`, `luminosity`, `spectrum`, `star_type`, `color`, `age`), planet physics (S5: `orbit_distance`, `mass`, `radius`, `surface_area`, `density`, `surface_gravity`, `surface_pressure`, `surface_temperature`, `atmosphere`, `planet_type`, `surface_water`, `tectonic_activity`, `magnetic_field`, `deposits`), **warp GEOMETRY (S9): warp-point `location` per `destination_id`**, storms (S2), intrinsic abilities + system `archetype` (S3). **ENFORCEMENT:** capture a GOLDEN BASELINE snapshot of these fields from the CURRENT (pre-change) code BEFORE any threading, store it as a fixture (Phase 0 Task 0.0), and assert equality against it after EVERY phase. NOT merely two same-impl runs. **Class (b) — currently-unseeded, values WILL change (allowed, not a regression):** system `name` (S8), star/planet `image_id` + `image_rotation` (S6/S7), `warp_type` + warp `intrinsic_abilities` (S10). **ENFORCEMENT:** asserted ONLY for determinism (stable across two runs of the NEW code, per phase) + presence — NEVER against the golden baseline. The expected-RED full-snapshot test concerns exactly these (b) fields becoming deterministic; the (a) fields are guarded by the golden baseline from the start. |
| 2026-05-21 | **Warp-stream contract correction: S9 warp geometry CONTINUES `physics_rng` (no fresh `warp_rng`); only S10 type/intrinsics are newly-deterministic** (Codex sign-off Blocker 1) | Codex sign-off review §3 + §7 (blocker 1), verified against live code. The prior plan treated ALL warp randomness as movable to a brand-new independent `warp_rng = random.Random(galaxy_seed)`. That is WRONG. **S9 — warp GEOMETRY** (`_calculate_warp_distance` jitter `galaxy_warp_generator.py:47`, `_should_add_density_edge` acceptance `:273`) ALREADY consumes the seeded module RNG today, continuing the SAME module-`random` stream right after the star/planet physics draws (warps are a second pass after `generate_systems`, `game_initializer.py:271`). Nothing else consumes the module RNG in between — storms (S2) and intrinsics/archetype (S3) use the child streams derived from the placement rng (`galaxy_system_generator.py:163-166`), not the module RNG. So same-seed runs ALREADY produce identical warp geometry (`warp_geometry_equal=True`, runtime-probe verified). To preserve it byte-for-byte, warp geometry must continue the **same dedicated `physics_rng = random.Random(galaxy_seed)` instance** that stars/planets used, in the same draw order — a fresh `warp_rng` would reset the sequence and change geometry (a regression vs current output, even though still deterministic). **S10 — warp TYPE / intrinsic rolls** (`_apply_warp_point_intrinsic_abilities`, fresh fallback `Random()` at `:408-409`) ARE unseeded today; making them deterministic is the intended fix and their values change from per-run-random to fixed (allowed). They continue the same `physics_rng` at the tail of `generate_warp_lanes` (`:351-352`). Net: warps add NO new stream — they continue `physics_rng`. design.md H7 (S9/S10 rows + stream-instance plan) and phase_2_checklist Tasks 2.1/2.2 rewritten accordingly. |
| 2026-05-21 | Reference consult artifact | Code-grounded pre-planning consult from Codex at `AgentCoordination/Scratchpad/Consult/proj473_preflesh3/advice.md`. Line citations re-verified against live code 2026-05-21 (most accurate; the `game_initializer.py:83-122` retry-loop range is actually `:83-136`, and the `galaxy_warp_generator.py:408-409` warp-intrinsics fallback is at `:408-409` exactly). Consult §6's `system_count==1` seed-bump sensitivity confirmed at `game_initializer.py:83-91` (`replace(config, galaxy_seed=config.galaxy_seed + attempt)`). |
