# PROJ-414 Phase 1a: Migration Table

Generated by Phase 1a audit (2026-05-13). One row per import or patch site
that references `game.strategy.data.pathfinding`. Patch targets are decided
per-site based on where the migrated production code looks up the name.

## Conventions

- `GPS` = `GalaxyPathfindingService` (in `game.strategy.services.galaxy_pathfinding_service`).
- `IC` = `InterceptCalculator` (in `game.strategy.services.intercept_calculator`).
- For tests that patch the shim, the new patch target must match the
  attribute lookup in the migrated SUT (Python `mock.patch` binds at the
  attribute lookup site, not the definition site).

## Production import sites

| Site (file:line) | Kind | Current target | New target | Notes |
|---|---|---|---|---|
| `game/strategy/services/fleet_warp_resolution.py:15` | import | `from ...pathfinding import find_hybrid_path` | construct `GPS(galaxy).find_hybrid_path(...)` directly | Module-level import; replaced by direct service call inline. |
| `game/strategy/services/fleet_navigation_service.py:24` | import | `from ...pathfinding import find_hybrid_path, strip_start_hex` | inline `GPS(galaxy).find_hybrid_path(...)` and `GPS.strip_start_hex(...)` | Module-level. `strip_start_hex` is a staticmethod on GPS. |
| `game/strategy/services/fleet_navigation_service.py:185` | import | `from ...pathfinding import calculate_intercept_point` | construct `IC(GPS(galaxy)).calculate_intercept_point(state, target_fleet, galaxy)` | Local import inside `get_destination`. |
| `game/strategy/engine/superweapon_order_processor.py:27` | import | `from ...pathfinding import get_system_at_hex` | helper `_get_system_at_hex_impl(galaxy, hex)` calling `GPS(galaxy).get_system_at_hex(hex)` | Module-level. Three call sites in same file (lines 352, 365, 491). Two are method calls already (`_get_system_at_hex`), the staticmethod is the patch surface. |
| `game/strategy/engine/game_session.py:347` | import | `from ...pathfinding import find_hybrid_path, strip_start_hex` | inline `GPS(self.galaxy).find_hybrid_path(...)` and `GPS.strip_start_hex(...)` | Local import inside `preview_fleet_path`. |
| `game/strategy/engine/game_session.py:366` | import | `from ...pathfinding import project_fleet_path` | `from game.strategy.services.intercept_calculator import project_fleet_path` | Local import inside `get_fleet_path_projection`. |
| `game/strategy/engine/handlers/base.py:22` | import | `from ...pathfinding import find_hybrid_path, strip_start_hex` | inline `GPS(session.galaxy).find_hybrid_path(...)` and `GPS.strip_start_hex(...)` | Module-level. Used in `add_move_order_if_needed`. Tests patch `game.strategy.engine.handlers.base.find_hybrid_path` (~14 sites) — after migration, `find_hybrid_path` is no longer a module attribute of `handlers.base`, so those SUT-local patches must be redirected. |
| `game/ui/screens/strategy_superweapons.py:364` | import | `from ...pathfinding import get_system_at_hex` | `GPS(self.galaxy).get_system_at_hex(hex_coord)` | Local import in `_get_system_at_hex` method. |
| `game/strategy/facade/slices/planet_slice.py:66` | import | `from ...pathfinding import get_system_at_hex` | `GPS(self._state.session.galaxy).get_system_at_hex(hex_coord, radius=50)` | Local import. |
| `game/strategy/services/intercept_calculator.py:121` | import | `from ...data import pathfinding as _pf_shim` (call `_pf_shim.project_fleet_path`) | direct call to module-level `project_fleet_path` already defined in same module (no shim hop) | The shim's `project_fleet_path` just re-imports `intercept_calculator.project_fleet_path`. The deliberate routing was for "test patches of the shim still apply" — irrelevant once the shim is gone. Tests that want to mock `project_fleet_path` for intercept tests will patch `game.strategy.services.intercept_calculator.project_fleet_path`. |
| `game/strategy/services/intercept_calculator.py:169` | import | `from ...data import pathfinding as _pf_shim` (call `_pf_shim.find_hybrid_path`) | direct call to `self._pathfinding.find_hybrid_path(...)` (the GPS already on the calculator) | Remove the `galaxy is not None` branch entirely — both branches now route through `self._pathfinding`. The `galaxy` parameter on `_evaluate_intercept_candidates` becomes unused; remove or keep-for-signature stability. Tests that need to mock `find_hybrid_path` for intercept tests should patch `GalaxyPathfindingService.find_hybrid_path`. |

## Test import sites

| Site (file:line) | Kind | Current target | New target | Notes |
|---|---|---|---|---|
| `tests/unit/strategy/pathfinding/test_strip_start_hex.py:9` | import | `from ...pathfinding import strip_start_hex` | `from ...services.galaxy_pathfinding_service import GalaxyPathfindingService` then `GalaxyPathfindingService.strip_start_hex` | Tests a staticmethod directly. |
| `tests/unit/strategy/pathfinding/test_basic_paths.py:8-13` | import | `find_path_deep_space, find_path_interstellar, get_system_at_hex, find_nearest_system` | `from game.core.hex_math import hex_linedraw` for `find_path_deep_space`; `GalaxyPathfindingService` for the rest (wrap mock_galaxy: `GalaxyPathfindingService(mock_galaxy)`) | Update each call site to use service method invocation. |
| `tests/unit/strategy/pathfinding/test_hybrid_and_intercept.py:8-12` | import | `find_hybrid_path, project_fleet_path, calculate_intercept_point` | `GalaxyPathfindingService(galaxy).find_hybrid_path(...)`; `from ...services.intercept_calculator import project_fleet_path`; `InterceptCalculator(GPS(galaxy)).calculate_intercept_point(...)` | Update each call site. Patch sites (~12 inside this file) also need updating. |
| `tests/unit/strategy/pathfinding/test_edge_cases.py:8-13` | import | `find_path_deep_space, find_path_interstellar, find_hybrid_path, calculate_intercept_point` | as above (hex_linedraw + GPS + IC) | 4 patch sites also. |
| `tests/unit/strategy/pathfinding/test_intercept_recursion.py:23-26` | import | `calculate_intercept_point, project_fleet_path` | `InterceptCalculator(GPS(galaxy)).calculate_intercept_point(...)`; `from ...services.intercept_calculator import project_fleet_path` | |
| `tests/unit/strategy/services/test_galaxy_pathfinding_service.py:131` | import | `from ...pathfinding import find_path_deep_space` | `from game.core.hex_math import hex_linedraw` | The "forwarding correctness" test loses meaning; rewrite to test `hex_linedraw` directly or delete. |
| `tests/integration/strategy/test_fleet_movement.py:4` | import | `from ...pathfinding import find_path_interstellar` | `GalaxyPathfindingService(galaxy).find_path_interstellar(start_sys, end_sys)` | |
| `tests/integration/strategy/test_path_projection.py:5` | import | `from ...pathfinding import project_fleet_path` | `from ...services.intercept_calculator import project_fleet_path` | |
| `tests/unit/strategy/test_advanced_fleet_orders.py:194,316` | import | `from ...pathfinding import calculate_intercept_point` (local) | `InterceptCalculator(GPS(galaxy_mock)).calculate_intercept_point(...)` | These tests use real intercept logic with mocked path/project. |
| `tests/performance/bench_galaxy_planet_star.py:75` | import | `from ...pathfinding import find_path_interstellar` | `GalaxyPathfindingService(galaxy).find_path_interstellar(...)` | Bench code. |

## Patch sites (shim path)

Mock-patch `game.strategy.data.pathfinding.X` rewrites; target depends on
the migrated SUT's lookup point.

| Site (file:line) | Kind | Current patch | New patch | Notes |
|---|---|---|---|---|
| `tests/integration/strategy/turn_engine/test_basics.py:14,51` | patch decorator | `'game.strategy.data.pathfinding.find_hybrid_path'` | `'game.strategy.services.galaxy_pathfinding_service.GalaxyPathfindingService.find_hybrid_path'` | After migration handlers.base inlines `GalaxyPathfindingService(galaxy).find_hybrid_path(...)`, so patch the method on the class. |
| `tests/integration/strategy/test_command_handlers.py:84` | patch | same as above | `'game.strategy.services.galaxy_pathfinding_service.GalaxyPathfindingService.find_hybrid_path'` | |
| `tests/integration/strategy/test_command_handlers.py:310` | patch | same | same | |
| `tests/unit/strategy/turn_engine/test_tick_mechanics.py:80` | patch | `'game.strategy.data.pathfinding.calculate_intercept_point'` | `'game.strategy.services.intercept_calculator.InterceptCalculator.calculate_intercept_point'` | The intercept now constructed inline in `get_destination` calls method on class. |
| `tests/unit/strategy/test_advanced_fleet_orders.py:98` | patch | `'game.strategy.data.pathfinding.project_fleet_path'` | `'game.strategy.services.intercept_calculator.project_fleet_path'` | After migration `IC.calculate_intercept_point` calls module-level `project_fleet_path` in `intercept_calculator`. |
| `tests/unit/strategy/test_advanced_fleet_orders.py:99` | patch | `'game.strategy.data.pathfinding.find_hybrid_path'` | `'game.strategy.services.galaxy_pathfinding_service.GalaxyPathfindingService.find_hybrid_path'` | The IC after migration always uses `self._pathfinding.find_hybrid_path(...)`. |
| `tests/unit/strategy/test_advanced_fleet_orders.py:156` | patch | `'game.strategy.data.pathfinding.calculate_intercept_point'` | `'game.strategy.services.intercept_calculator.InterceptCalculator.calculate_intercept_point'` | |
| `tests/unit/strategy/test_advanced_fleet_orders.py:190` | patch | `'game.strategy.data.pathfinding.find_hybrid_path'` | `'game.strategy.services.galaxy_pathfinding_service.GalaxyPathfindingService.find_hybrid_path'` | |
| `tests/unit/strategy/test_advanced_fleet_orders.py:191` | patch | `'game.strategy.data.pathfinding.project_fleet_path'` | `'game.strategy.services.intercept_calculator.project_fleet_path'` | |
| `tests/unit/strategy/test_advanced_fleet_orders.py:305,306` | patch | same as 190/191 | same as 190/191 | |
| `tests/unit/ui/screens/test_strategy_superweapons.py:495` | patch | `'game.strategy.data.pathfinding.get_system_at_hex'` | `'game.strategy.services.galaxy_pathfinding_service.GalaxyPathfindingService.get_system_at_hex'` | UI screen's `_get_system_at_hex` constructs GPS inline. |
| `tests/unit/strategy/pathfinding/test_hybrid_and_intercept.py:200,249,287,313,336,366,394,429,460` | patch (project_fleet_path) | `'game.strategy.data.pathfinding.project_fleet_path'` | `'game.strategy.services.intercept_calculator.project_fleet_path'` | These tests directly call `calculate_intercept_point(...)` via IC; the IC's `calculate_intercept_point` calls module-level `project_fleet_path`. |
| `tests/unit/strategy/pathfinding/test_hybrid_and_intercept.py:337,395,430` | patch (find_hybrid_path) | `'game.strategy.data.pathfinding.find_hybrid_path'` | `'game.strategy.services.galaxy_pathfinding_service.GalaxyPathfindingService.find_hybrid_path'` | |
| `tests/unit/strategy/pathfinding/test_edge_cases.py:208,256,297` | patch (project_fleet_path) | as above | `'game.strategy.services.intercept_calculator.project_fleet_path'` | |
| `tests/unit/strategy/pathfinding/test_edge_cases.py:257,298` | patch (find_hybrid_path) | as above | `'game.strategy.services.galaxy_pathfinding_service.GalaxyPathfindingService.find_hybrid_path'` | |
| `tests/unit/strategy/fleet_movement_engine/test_warp.py:36` | patch | `'game.strategy.data.pathfinding.calculate_intercept_point'` | `'game.strategy.services.intercept_calculator.InterceptCalculator.calculate_intercept_point'` | |
| `tests/unit/strategy/facade/test_facade_robust_resolution.py:73,87` | patch | `'game.strategy.data.pathfinding.get_system_at_hex'` | `'game.strategy.services.galaxy_pathfinding_service.GalaxyPathfindingService.get_system_at_hex'` | facade/slices/planet_slice migrated to GPS. |

## SUT-local patch sites (NO change needed, but verify)

These patches target attributes on SUT modules. After migration the
attribute may not exist anymore (the import is removed) — these need
auditing for each affected SUT.

| Site | Patch target | Status after migration |
|---|---|---|
| `tests/unit/strategy/test_command_handlers.py:382,687,712` | `'game.strategy.engine.handlers.base.find_hybrid_path'` | BROKEN — `find_hybrid_path` import removed from `handlers.base`. Redirect to `GalaxyPathfindingService.find_hybrid_path`. |
| `tests/integration/strategy/test_command_handlers.py:349,420` | `'game.strategy.engine.handlers.base.find_hybrid_path'` | Same as above. |
| `tests/unit/strategy/engine/test_superweapon_handler_validation.py:224` | `'game.strategy.engine.handlers.base.find_hybrid_path'` | Same. |
| `tests/unit/strategy/engine/test_superweapon_edge_cases.py:113,136,158,181` | same | Same. |
| `tests/unit/strategy/engine/test_superweapon_command_handlers.py:390,411,437,469,502,534,562,618` | same | Same. |
| `tests/integration/strategy/test_fleet_navigation_consistency.py:184` | `'game.strategy.services.fleet_navigation_service.find_hybrid_path'` | BROKEN — module-level `find_hybrid_path` import removed from `fleet_navigation_service`. Redirect to `GalaxyPathfindingService.find_hybrid_path`. |
| `tests/integration/strategy/turn_engine/test_resources.py:275,319,346,371` | same | Same. |
| `tests/unit/strategy/fleet_movement_engine/test_warp.py:37,155,177` | same | Same. |
| `tests/unit/strategy/fleet_movement_engine/test_basics.py:46` | same | Same. |
| `tests/unit/strategy/turn_engine/test_tick_mechanics.py:47,81` | same | Same. |
| `tests/unit/strategy/test_advanced_fleet_orders.py:157` | same | Same. |
| `tests/unit/strategy/engine/test_superweapon_order_processor*.py` | `'game.strategy.engine.superweapon_order_processor.get_system_at_hex'` | After migration, the `_get_system_at_hex` staticmethod will call GPS internally. The patch site `superweapon_order_processor.get_system_at_hex` will no longer exist as a module attribute. Decision: KEEP the module-level `get_system_at_hex` lookup intact by leaving the import shape (`from game.strategy.services.galaxy_pathfinding_service import GalaxyPathfindingService` then a module-level def `get_system_at_hex(galaxy, hex)`) OR redirect all ~11 patches to `GalaxyPathfindingService.get_system_at_hex`. **Decision: redirect** — fewer compatibility-shim semantics, single canonical patch surface. The SUT loses its private helper free function. |
| `tests/unit/ui/screens/test_strategy_superweapons.py: many `patch.object(superweapon_ops, '_get_system_at_hex', ...)` ` | patches a method on the **screen module's** instance | NOT BROKEN — these patch the method `_get_system_at_hex` on the superweapon screen's operations object, which remains. No change needed. |

## Guard test

| File | Action |
|---|---|
| `tests/unit/strategy/data/test_pathfinding_shim_scope.py` | DELETE entire file in Phase 1c. |

## Pathfinding test directory

| Comment files in `tests/unit/strategy/pathfinding/` |
|---|
| The whole directory currently exercises the shim's public surface as the SUT. After migration these tests should target `GalaxyPathfindingService` / `InterceptCalculator` / module-level `project_fleet_path` directly. Treat as Group D in the checklist. |

## Conftest / fixtures

- `tests/unit/strategy/pathfinding/conftest.py` provides `mock_galaxy` (a `MagicMock` with `.systems` and `.get_system_by_name`). Once the pathfinding tests call `GalaxyPathfindingService(mock_galaxy)`, the mock still satisfies the `IGalaxySystemGraph` duck-type since GPS only reads `.systems` and `.get_system_by_name`. No conftest changes required.
