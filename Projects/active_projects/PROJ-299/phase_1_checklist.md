# Phase 1: Caption schemas + Gemini prompts + validation tool [Medium]

> **BEFORE MARKING THIS PHASE COMPLETE:**
> 1. Run `python Projects/scripts/validate_phase.py PROJ-299 1`
> 2. Only proceed if output shows PASSED
> 3. Update plan.md phase table AND Current State

**Status:** Not Started
**Objective:** Three asset-specific JSON schemas. Three Gemini capture prompts the user can copy/paste. A `Tools/` script that scans `assets/` and reports which sidecars are missing or malformed. NO captions generated by code.

---

## Tasks

### Task 1.1: Create directory structure [Simple]
**File:** `Tools/captioning/{schemas,prompts}/` (NEW directories)
**Tests:** N/A

- [ ] Create `Tools/captioning/schemas/`
- [ ] Create `Tools/captioning/prompts/`
- [ ] Add `Tools/captioning/README.md` explaining the workflow (user runs prompts in Gemini → saves JSON output to sidecar locations → runs validator)

**Notes:**

### Task 1.2: Define `flag.schema.json`, `portrait.schema.json`, `theme.schema.json` [Medium]
**File:** `Tools/captioning/schemas/{flag,portrait,theme}.schema.json` (NEW)
**Tests:** `pytest tests/regression/test_caption_schemas_validate.py`

- [ ] Write failing test in `tests/regression/test_caption_schemas_validate.py`:
  - Each schema file is valid JSON
  - Each has `schema_version: 1`
  - Each declares the required fields per design.md (flag has 6, portrait has 6, theme has 6)
  - Mood enum values match design.md (8 values)
  - Tech-level enum values match design.md (6 values)
- [ ] Implement the three schema files per design.md spec
- [ ] Run test, confirm pass

**Notes:**

### Task 1.3: Write Gemini capture prompts [Medium]
**File:** `Tools/captioning/prompts/{flag,portrait,theme}_prompt.md` (NEW)
**Tests:** Manual — paste into Gemini and inspect output for one sample asset of each type

- [ ] For each asset type, write a markdown prompt file that:
  - Sets role: "You are an art critic for a 4X space strategy game"
  - Specifies the schema (embed it inline in the prompt for self-containment)
  - Includes ONE worked example of the desired output JSON
  - Ends with: "OUTPUT ONLY THE JSON. No prose, no preamble, no markdown fences."
- [ ] Save outputs as `flag_prompt.md`, `portrait_prompt.md`, `theme_prompt.md`
- [ ] Update `Tools/captioning/README.md` with the workflow:
  1. Open Gemini
  2. Upload an image (e.g., a flag PNG)
  3. Paste the corresponding `*_prompt.md` content
  4. Save Gemini's JSON output to the sidecar location (per design.md)
  5. Run `python Tools/captioning/validate_captions.py` to verify

**Notes:**

### Task 1.4: Implement `validate_captions.py` [Medium]
**File:** `Tools/captioning/validate_captions.py` (NEW)
**Tests:** `pytest Tools/captioning/test_validate_captions.py` (NEW)

- [ ] Write failing tests:
  - Reports OK when all expected sidecars exist and are valid
  - Reports MISSING when a flag/portrait/theme has no sidecar
  - Reports INVALID when a sidecar's JSON parses but doesn't match the schema (e.g., wrong fields, wrong enum value, missing required field)
  - Reports MALFORMED when a sidecar's JSON doesn't parse at all
  - Exit code 0 when no issues; non-zero when there are
- [ ] Implement the script:
  - Discover assets:
    - Flags: `assets/Images/Flags/Processed/flag_*` directories
    - Portraits: files in `assets/Images/Race Portraits/` matching `*.{jpg,jpeg,png}`
    - Themes: directories in `assets/ShipThemes/` containing `theme.json`
  - For each, look up the sidecar at the canonical path (per design.md)
  - Validate against the corresponding schema using stdlib `json` + manual checks (no `jsonschema` dep — keep it stdlib)
  - Print a summary report: `OK: 35 / MISSING: 2 / INVALID: 0` and a per-asset detail list for failures
- [ ] Run test, confirm pass
- [ ] Run the validator against the actual `assets/` — it should report MISSING for all 37 assets (no captions exist yet; that's the user's task)

**Notes:**

---

## Phase Completion Checklist
- [ ] All task checkboxes above are checked
- [ ] ~6 new tests added
- [ ] `pytest tests/regression/test_caption_schemas_validate.py Tools/captioning/test_validate_captions.py` — all green
- [ ] `python Tools/captioning/validate_captions.py` runs without crash (will report 37 MISSING — that's expected)
- [ ] No regression in baseline
- [ ] Update status at top of this file to `Complete`
- [ ] Update `plan.md` phase table row to `Complete`
- [ ] Update `plan.md` Current State to point to Phase 2
