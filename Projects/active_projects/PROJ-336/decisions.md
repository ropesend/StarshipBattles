# PROJ-336: Decisions Log

> Add decisions as they're made. Future agents reference this for "why".

| Date | Decision | Rationale |
|------|----------|-----------|
| 2026-05-04 | Project initialized | Row 6 of test-coverage arc (PROJ-331..340). MED risk; ~1.5 session estimate. |
| 2026-05-04 | **D-001:** fleet_navigation gets gap-fill, not full re-characterization | 74 unit tests already exist across 5 files in `tests/unit/strategy/fleet_navigation/`. Re-pinning would be churn. New file `test_fleet_navigation_gaps.py` covers only paths the existing files miss (see phase_1_checklist Task 1.1). |
| 2026-05-04 | **D-002:** Mock at the service boundary, use real domain objects where cheap | `Galaxy`, `StarSystem`, `Star`, `Planet`, `Empire`, `Fleet` are dataclass-style and constructible without pygame. Building real ones in tests pins more behavior than `Mock(spec=...)` and matches the existing integration tests' style. Reserve `Mock` for `component_registry` and `event_bus` (DI seams), and for `find_abilities_in_scope` in stabilizer tests (avoiding the registry plumbing). |
| 2026-05-04 | **D-003:** One new test file per production file | Per master-plan default. Each lands in its own commit so bisect/revert is per-file. |
| 2026-05-04 | **D-004:** No shared `GalaxyState` fixture across files | Each test file builds its own minimal galaxy in conftest or per-test helpers. Tested files share little state shape (cargo projector touches only fleet.resources/orders; system_destroyer touches Galaxy + Empire + StarSystem; stabilizer_registry touches Galaxy + Planet + Empire). A shared fixture would over-couple the four test files and make each test harder to read in isolation. Local helpers also match the style of the existing `tests/integration/strategy/test_system_destruction.py`. |
| 2026-05-04 | **D-005:** Mock `find_abilities_in_scope` for stabilizer_registry unit tests | The integration test already covers the live registry path. The unit-test value is in pinning the iteration order: `STABILIZERS` outer loop × `empires` × `spec.scopes`, with first-hit-wins semantics. Mocking the scanner makes that order observable and decouples from registry plumbing. |
| 2026-05-04 | **D-006:** Pin `_consume_ticks` as a public-shaped pure function despite leading underscore | It's a `@staticmethod`, has no `self` references, and is part of how `_project_action_order` and the main projection loop coordinate turn boundaries. Tests are cheap (pure inputs/outputs) and protect a class of off-by-one bugs that are hard to catch through `project_path`'s integration shape. |
| 2026-05-04 | **D-007 (observation candidate):** `find_blocking_stabilizer` short-circuits on `reference_planet is None` BEFORE checking `order_type` | If a future caller passes a system-scope order with no reference planet, it returns None silently. Pinning this as the current behavior; if user wants this to raise instead, that's a separate ticket. |
| 2026-05-04 | **D-008 (observation candidate):** `FleetCargoProjector.get_projected_cargo` accepts negative amounts implicitly | An order with `amount=-50, direction='load'` would compute `delta=-50` and reduce `projected`. Loading negative is meaningless but not validated here. Pin as-is; flag as observation. |
| 2026-05-04 | **D-009 (observation candidate):** `collect_system_contents` accepts `radius` kwarg but `destroy_system` cannot be re-parameterized after planning | The plan is frozen; any post-plan radius change requires a new collection pass. Document the contract; do not change. |
| 2026-05-04 | **D-010 (observation candidate):** `destroy_system` mutates `plan.system.stars = []` even though the plan is `frozen=True` | The dataclass freeze prevents reassigning plan attributes, but `plan.system` is a reference and the inner `stars` list is mutated. Intended (the plan snapshots references, not deep copies), but worth pinning a test that exercises this so the contract is explicit. |
| 2026-05-04 | **D-011:** Use `monkeypatch` over `unittest.mock.patch` decorator for the few module-level imports we need to swap | Matches existing `tests/unit/strategy/services/` style. |
