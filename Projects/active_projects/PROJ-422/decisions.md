# PROJ-422: Decisions Log

> **LOG ALL DECISIONS HERE**
> When you make a design choice or the user specifies a preference, add it to this table.
> Future agents will reference this to understand why things were done a certain way.

| Date | Decision | Rationale |
|------|----------|-----------|
| 2026-05-16 | Project initialized from TD-09 plan | First project in the 10-plan strategy tech-debt arc; symbol-preserving and lowest-risk per EXECUTION_ORDER.md §"Recommended Linear Order #1". |
| 2026-05-16 | Split `engines.py` into a **package** (`engines/__init__.py` + 9 leaf modules), not into N sibling modules at the same level | Keeps the import path `game.strategy.interfaces.engines` stable for all 30 existing consumers. Sibling modules at the `interfaces/` level would require every consumer to change its import line — that is sweeping churn the TD plan explicitly avoids. |
| 2026-05-16 | `engines/__init__.py` is a **symbol-preserving public seam**, not a "fallback shim" | The seam is the package's API surface, exactly equivalent to the prior single-file module path. The AGENTS.md "no compatibility shims / no fallback systems" rule targets duplicate runtime paths and save-file migrations, not package `__init__.py` files. The seam stays indefinitely; deleting it would be a breaking change to 30 import sites. |
| 2026-05-16 | **Delete** `engines.py` in Phase 1; do not leave a parallel "old + new" path | AGENTS.md root-cause rule: when a system is replaced, remove the old path and update all callers. The package re-export makes all callers happy without the old file. |
| 2026-05-16 | Domain partitioning into 9 leaf modules (movement / orders / combat / production / logistics / population / planet_ops / terraforming / components) | Mirrors the natural domain clusters identified in the verification inventory. All leaves end under 200 LOC; largest is `logistics.py` at ~140 LOC. Comfortably under the project's 500-LOC ceiling. |
| 2026-05-16 | **Hard rule:** leaf modules are private — consumers import via `game.strategy.interfaces.engines` (package path), never via `game.strategy.interfaces.engines.movement` | Keeps the public API surface narrow. Future domain regrouping (e.g. splitting `logistics` into fuel/cargo subdomains) can happen inside the package without breaking any consumer. The layout test asserts package-root importability as the canonical surface. |
| 2026-05-16 | Close pre-existing `__all__` drift as part of this change (add `IComponentActivationEngine` to `engines.__all__`; add the 5 missing names to `interfaces/__init__.py.__all__`) | These gaps already exist and the verification step uncovered them. Fixing them here is **not scope creep** — they are existing bugs in the same files being rewritten. The new layout test enforces both invariants forever after. |
| 2026-05-16 | Do **not** rename `IOrganicsConsumptionEngine` (PROJ-286 deferred rename) in this project | Out of scope per the TD plan. Renames are sweeping consumer churn and would conflate a mechanical layout change with a contract change. Deferred to a separate project. |
| 2026-05-16 | Do **not** edit concrete engine modules under `game/strategy/engine/` | Symbol-preserving split means no consumer needs to change. If the executor finds themselves editing a concrete engine, that's a signal to stop and prove with a failing test that package-root re-exports are insufficient. |
| 2026-05-16 | TDD anchor: author `tests/unit/strategy/interfaces/test_engines_package_layout.py` **before** the split (Phase 0, red) and drive the split (Phase 1, green) | Strict TDD per AGENTS.md. The test enforces package-hood, per-leaf `__all__` correctness, symmetric re-export with `game.strategy.interfaces.__all__`, and absence of a leftover `engines.py` module file. |
| 2026-05-16 | Opt into the 03c phase-aware execution protocol | Per `Projects/protocols/01_initialize_project.md` Phase D, new projects SHOULD opt in. The marker is set in `plan.md` and `phase_state.json` is initialized at scaffold time. |
