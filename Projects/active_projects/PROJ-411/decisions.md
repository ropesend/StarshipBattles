# PROJ-411: Decisions Log

> **LOG ALL DECISIONS HERE**
> When you make a design choice or the user specifies a preference, add it to this table.
> Future agents will reference this to understand why things were done a certain way.

| Date | Decision | Rationale |
|------|----------|-----------|
| 2026-05-10 | Project initialized | Starting point for Optimize Strategy Panel Load Times |
| 2026-05-10 | Project title: "Optimize Strategy Panel Load Times" | User-selected. Action-oriented, 5 words, mirrors triage filename. |
| 2026-05-10 | Scope: bundled 5 panels under one project (rather than 5 separate issues) | User directive in triage: "if there are similar issues they must be coordinated together". Swarm confirmed all 5 share `@fast_panel` opt-out + per-turn-static data they could cache. |
| 2026-05-10 | Phase 1 includes profiling + low-risk shared wins (not measurement-only) | User answer to Phase A question. Reasoning: the shared wins (DesignLibrary cache, lazy Empire icons, `@fast_panel`, `gather_*` caches) are obvious from current code without needing Scalene evidence. Phase 1 lands them; Phase 2 uses the resulting profile to find anything that remains. |
| 2026-05-10 | Acceptance: soft wall-clock target (<100 ms imperceptible target, pragmatic acceptance) + hard count-based regression tests | User: "I would like the delay to be imperceptible, but that may not be realistic. I will accept an honest attempt to make it faster and profile things." `docs/guides/performance_profiling.md` recommends count-based assertions over wall-clock thresholds. We do both: count-based as the regression gates, wall-clock as informational reporting. |
| 2026-05-10 | Smoke scenario: turn 1, 2 empires, 2 systems, 1 planet each, 0 fleets | User: "now things are slow on the 1st turn, so no ships, 1 planet each, 2 systems. Let's get that to be somewhat performant then we can worry about later game issues later." Larger-galaxy perf deferred to a future project. |
| 2026-05-10 | Empire Overview: lazy-load on tab activation (Treasury icons on first Treasury render; portrait/flag on first Population render) | User-chosen approach. Background-thread asset loading rejected (would need Pattern #28 worker — overkill for static assets). Lazy-on-tab matches the existing Planet/Star Registry lazy-icon-on-render pattern (Pattern #11). |
| 2026-05-10 | Land Issue #17 fix before PROJ-411 Phase 1 begins | Risk Assessor flagged that PROJ-410 stale-rows correctness regression (#17) + new DesignLibrary cache could double-mask failures. Sequencing keeps each PR reviewable in isolation. #17 is already `status:in-progress`. |
| 2026-05-10 | `@fast_panel` rollout: all 5 panels in one Phase 1 task | User-chosen. Single coherent visual review; ~3 s per-panel saving reported; easy to revert as a unit if visuals regress. |
| 2026-05-10 | No DISABLE_DESIGN_CACHE rollback flag | User-chosen. Matches CLAUDE.md Rule 3 (root-cause fixes, no compat shims). Rollback by PR revert if needed. |
| 2026-05-10 | Phase 1 size: single phase containing all instrumentation + shared wins | User-chosen. ~7 tasks but coherent: profile_action spans + smoke fixture + DesignLibrary cache + `@fast_panel` rollout + `gather_*` caches + EmpireEconomyService cache + Event Log copy fix + lazy Empire icons. Split alternative (1a measure, 1b fix) rejected as redundant — the wins don't need profile evidence first. |
| 2026-05-10 | DesignLibrary cache home: `FacadeSessionState`, not module-level singleton | Architecture Analyst recommendation. Per-turn lifecycle matches `FacadeSessionState.invalidate_all()` (line 61) called from `StrategySessionFacade.process_turn()` (line 202). Pattern #11's module-level singleton is for immutable assets, not per-turn data. |
| 2026-05-10 | DesignLibrary write-through invalidation: explicit `.pop()` in `save_design()`, no mtime check | Pattern Scout: matches `ShipInstance.invalidate_stats_cache()` style. Mtime check adds disk I/O cost; version counter adds complexity. Explicit invalidation is the established repo idiom. |
| 2026-05-10 | `DesignLibrary` accepts optional `facade_state: FacadeSessionState | None` ctor param (Option A) | Option B (callback injection) was rejected as more flexible but unnecessary for one use case. Option A: UI-side callers pass it; engine-side callers omit it (their work runs inside the turn loop where a cache is irrelevant). Adding one kwarg has zero ripple at sites that don't care. |
| 2026-05-10 | LOC budget: edits in `planet_list_window.py` (737), `empire_panel_window.py` (572), `build_queue_screen.py` (877) capped at ≤15 LOC each | Architecture Analyst guidance. These files are already over the 500-LOC ceiling. Heavier code (cache lookup, instrumentation, lazy state) goes in collaborators (data sources, filters, controllers, facade slice) which have headroom. |
| 2026-05-10 | Wall-clock perf tests: print measured value, fail only on >2× regression | Risk Assessor recommendation. Hardware variance across local/CI/different boxes is high. Count-based assertions are the hard gates; wall-clock numbers are informational. |
| 2026-05-10 | Smoke-scenario fixture lives at `tests/fixtures/perf_smoke_scenario.py` | Test Impact Analyst recommendation. No existing fixture matches "turn 1, 2 empires, 2 systems, 1 planet each, 0 fleets". Reuses `tests/integration/gameplay_loop/conftest.py::two_empire_setup` as a starting point. |
| 2026-05-10 | `TestRowPoolReuseGuard` extended to Planet/Star/Event Log in Phase 3, not Phase 1 | Test Impact Analyst recommendation. Current guard covers only Build Queue's virtual table (PROJ-373 phase 3 lock). Extending it is a regression-gate concern that belongs in Phase 3 after wins land. |
