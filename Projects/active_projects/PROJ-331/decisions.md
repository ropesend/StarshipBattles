# PROJ-331: Decisions Log

> Add decisions as they're made. Future agents reference this for "why".

| Date | Decision | Rationale |
|------|----------|-----------|
| 2026-05-04 | Project planning landed | Tier 1 of test-coverage arc. Master plan: `AgentCoordination/Scratchpad/plans/test_coverage_master_plan_v1.md`. |
| 2026-05-04 | **D-001:** Reframe project as "gap closure" not "pin from zero" | Pre-planning research found all three in-scope files already carry meaningful test coverage (battle_state: ~65 tests; battle_controller: ~70 tests; conflict_resolution_engine: ~24 tests). The explore-agent's "0 unit tests" report was wrong. Plan reflects actual gap inventory in `manifest.md`. **Surfacing to user:** the master plan's session estimate (3) still stands because gap-closure with deep mock setup is comparable in effort to pin-from-zero on simpler code. |
| 2026-05-04 | **D-002:** New test files instead of editing existing ones (mostly) | `test_battle_state_serialization.py` is already 1395 LOC and at the soft 500-LOC ceiling (per `docs/03_CONVENTIONS.md` §2.4). Adding ~16 more tests would push it well over. Same for `test_core.py` in conflict_resolution. New files: `test_battle_state_live_object_bridges.py`, `test_start_from_spec.py`, `test_logging_and_lookups.py`. The exception is `test_state.py` (battle_controller) which is small enough to absorb 4-5 more tests. |
| 2026-05-04 | **D-003:** `to_ship` / `from_ship` tests use mocks for Ship + registries, not real Ship | Real `Ship` construction requires registries + theme + components — heavy and beyond the unit-test boundary. Tests use `MagicMock(spec=Ship)` and assert on the call shape (which registry methods get called, which fields get assigned). Live-Ship round-trip is already covered in `tests/integration/save_load/test_roundtrip_ships.py` — that integration test is the place to assert end-to-end fidelity. Surface this in `design.md` so future readers don't add redundant integration-style tests at unit layer. |
| 2026-05-04 | **D-004:** `capture_from_engine` tested with mock BattleEngine, not real | Same reasoning as D-003. Real engine requires real ships + boundary + end_condition + projectiles — a heavy constructor chain. Mock `engine.ships`, `engine.projectiles`, `engine.tick_counter`, `engine.end_condition` per test. |
| 2026-05-04 | **D-005:** `start_from_spec` tests stub `start_engine_from_spec` rather than running real engine | The function delegates to `game.simulation.battle_runner.start_engine_from_spec` which is its own subsystem with own tests. Patch it; assert the controller wires the boundary, ai_factory, spec, and ship_id_map correctly. |
| 2026-05-04 | **D-006:** Multi-fleet-per-empire ordering determinism gets ONE pinning test | The PROJ-320 design rationale (deterministic `(empire_id, fleet_id)` iteration for replay stability) is load-bearing. One characterization test using a 3-empire-with-2-fleets-each setup pins the iteration order observed in current code. If a refactor changes order, this test fires. |
| 2026-05-04 | **D-007:** `_collect_team_modifiers` exception-swallowing is pinned, not fixed | The current code does `except Exception as e: logger.warning(...); return None`. This is a broad catch (master plan rule says document, don't fix). One test pins the swallow behavior: when `collect_combat_modifiers` raises, `_collect_team_modifiers` returns `None` and logs at warning level. **Observation:** broad catches like this are exactly what characterization tests are good at detecting if the swallow ever stops happening. Recorded as a separate bug-observation note (line below). |
| 2026-05-04 | **OBSERVATION-A:** broad `except Exception` in `_collect_team_modifiers` (line 552) | Has an inline reason comment ("Intentional broad catch: external collector"), so it's per-convention legitimate. But the contract is undocumented (does the caller see the modifier-failure? No — silently None). Test pins observed behavior; future ticket may decide whether to surface the failure. **Not blocking.** |
| 2026-05-04 | **OBSERVATION-B:** `BattleController.load_state` defaults boundary to `UnboundedRegion` | Production zero callers (per inline comment line 614). Saves are disposable per CLAUDE.md. Test pins the unbounded-default behavior (no edge-retreat after load). If a future feature persists+restores battles, the inline comment warns it must thread `BattleState.boundary` through. **Not blocking.** |
| 2026-05-04 | **OBSERVATION-C:** `BattleController._extract_outcome_on_battle_end` replay-id capture failure swallows broadly | Broad `except Exception` with inline comment "capture must not crash visual-mode battle end" — legitimate per convention. Test pins the swallow: when `get_default_capture_sink().on_battle_ended` raises, the outcome is still set and battle end completes. **Not blocking.** |
| 2026-05-04 | **D-008:** No new fixtures introduced | Reuse `tests/unit/simulation/battle_controller/conftest.py` (`mock_service`, `mock_ship`, `basic_config`, `controller`) and `tests/fixtures/strategy_entities.py` where they fit. New tests build per-test synthetic dataclasses inline (`ComponentState(...)`, `ShipState(...)`, etc.) — same pattern as existing tests in `test_battle_state_serialization.py`. |
| 2026-05-04 | **D-009:** No regression baselines captured | Per master plan: characterization tests are the regression baseline. Run the new test files green once, then they ARE the contract. No need for byte-identical pytest-output comparison the way PROJ-329A required (those tests were retrofit-paired; these are new pins). |
