# PROJ-496 Codex Audit Verification (2026-05-23)

Consult leaf: `AgentCoordination/Scratchpad/Consult/20260523T154936Z_audit-PROJ-496/`
Codex response: `AgentCoordination/Scratchpad/Consult/20260523T154936Z_audit-PROJ-496/response.md`

| id | verdict | evidence | action |
|----|---------|----------|--------|
| F1: persistence_adapter schema guard weakened beyond plan | VERIFIED + IN-SCOPE | `tests/unit/strategy/engine/session/test_persistence_adapter.py:97-151` claims to validate "stable contract surface — top-level keys, nested key sets, and the intended values per slot" (docstring line 108) but in fact never checks nested key sets (e.g., `set(config.keys()) == _EXPECTED_CONFIG_KEYS`, `set(players[0].keys()) == _EXPECTED_PLAYER_KEYS`) and never asserts `config["save_name"]` — which is emitted at `game/strategy/engine/game_config.py:243`. A missing or renamed `save_name`, or an extra/missing nested player key, would now pass silently. PROJ-480 T4.1 (`Projects/archived_projects/PROJ-480/phase_4_checklist.md:15-20`) asked for a narrower "stable subset" relaxation, not this weaker check. | Phase 3: add `set(config.keys()) == _EXPECTED_CONFIG_KEYS` (incl. save_name), assert `config["save_name"] == <value-from-frozen-fixture>`, add `set(players[0].keys()) == _EXPECTED_PLAYER_KEYS` per-player. |
| F2: T1.8 drop unjustified (colony_output happiness re-derivation) | VERIFIED + IN-SCOPE | `tests/unit/strategy/formulas/test_colony_output.py:447-451` still does `rate_giddy == pytest.approx(rate_normal * 2.0, rel=1e-9)` — the exact relational re-derivation PROJ-480 T5.17 (`Projects/archived_projects/PROJ-480/phase_5_checklist.md:127-132`) explicitly asked to be replaced with "a pre-computed expected value from formula documentation. Test external value, not re-derived logic." The implementer's drop rationale ("relational scaling check, not a formula re-derivation") doesn't match PROJ-480's wording — calling production twice and comparing ratios IS re-deriving the formula. | Phase 3: replace with precomputed expected value. The docstring at line 437-439 already documents the formula: max(0, happiness) scales the logistic term linearly. Compute the expected rate offline (or via a one-shot derivation comment) for happiness=2.0 and assert `rate_giddy == pytest.approx(<precomputed>, rel=1e-9)`. Keep the happiness=1.0 case as the comparator anchor if useful for documentation. |
| F3: T1.5 weak drop rationale (battle_engine_tick hardcoded loops) | VERIFIED + IN-SCOPE (lower priority) | `tests/unit/simulation/systems/test_battle_engine_tick.py:610-617` (`test_multiple_ticks_increment_counter`, n=10) and `:740-748` (`test_rapid_succession_ticks`, n=100) are the 2 hardcoded-loop tests PROJ-480 T5.4 (`Projects/archived_projects/PROJ-480/phase_5_checklist.md:36-41`) asked to consolidate into `@pytest.mark.parametrize("n", [1, 10, 100])`. They differ only in the loop count and the second test's extra `engine.end_condition = TickLimitCondition(max_ticks=1000)`. The implementer's drop rationale ("different-class loop tests, intent diverges") is weak — both pin the same `tick_counter == n` contract. Codex flagged this but said it's "not a behavioral regression" — lower priority than F1/F2. | Phase 3: parametrize the 2 tests into one `test_tick_counter_increments_per_update` on `n ∈ {1, 10, 100}` with a fixture-controlled `end_condition` (TickLimitCondition with `max_ticks=n+1` or higher) to cover the n=100 case's requirement. |
| F4 (clean): T1.6 drop genuinely justified | CONFIRMED CORRECT | `tests/unit/simulation/systems/test_battle_engine_tick.py:383-388` already has `max(ai_indices) < min(ship_indices)` which is *logically equivalent* to PROJ-480 T5.5's `all(i < j for i in ai_indices for j in ship_indices)`. The implementer was right — no upgrade needed. | None. |
| F5 (clean): T1.2 static-guards split | CONFIRMED CORRECT | `tests/static_guards/test_turn_engine_architectural_guards.py:37-90` preserves all former `inspect.getsource` + AST guards from `test_turn_engine_lazy_properties.py`. The behavioral `_battle_resolver is None` sanity assert still lives in `tests/unit/strategy/turn_engine/test_turn_engine_lazy_properties.py:116-127`. Selector-level caveat: PROJ-480's task selector was the single file, while PROJ-496 now requires running both directories. PROJ-496's `phase_1_checklist.md:25-32` already documents the dual selector. | None. |
| F6 (clean): T1.1, T1.4, T1.7, T2.2, T2.4 all sound | CONFIRMED | Spot-checked per Codex: T1.1's 17 default-engine cases at `tests/unit/strategy/turn_engine/test_turn_engine_lazy_properties.py:34-80` match the archived task; T1.4's derived `expected` at `tests/unit/regressions/test_bug_regressions_2026_01.py:47-66` matches `game/simulation/components/abilities/crew.py:86-97`; T1.7's seeded `generate_atmosphere` path is real and removes a former silent-pass branch; T2.2's parametrize matches the archived task; T2.4 still catches the queue-clearing concern (turn = 100 ticks, speed=100 → 1-tick movement interval). | None. |

## Decision

- 3 in-scope findings → spawn Phase 3 (the next phase after Phases 0-2) for remediation. Estimated ~8 min LLM work, under the 15-min threshold.
- Codex itself ranked the items: F1 > F2 > F3 in priority. All three are mechanical edits.
- No discovered-issue logs needed for PROJ-496.
