# PROJ-412: Decisions Log

> **LOG ALL DECISIONS HERE**
> When you make a design choice or the user specifies a preference, add it to this table.
> Future agents will reference this to understand why things were done a certain way.

| Date | Decision | Rationale |
|------|----------|-----------|
| 2026-05-10 | Project initialized from `Projects/Triage/turn_processing_performance.md` via `/claude-triage-to-proj` | Triage scoped this as cross-cutting and profiling-first, warranting a full project rather than a single bug ticket |
| 2026-05-10 | Project title: **"Reduce Strategy Turn Processing Time"** | User selected (outcome-focused framing) over "Profile and Optimize" and "Investigation" alternatives |
| 2026-05-10 | Reference scenario: 2 empires, 2 planets, a handful of ships, no active combat | User: "right now I just want to make small games playable. … Right now the criteria is 2 empires 2 planets and a handful of ships." |
| 2026-05-10 | Phase 1 scope: everything except combat (`ConflictResolutionEngine`) | User: "Worry about everything except combat, combat is a completely separate issue, and it takes a long time." |
| 2026-05-10 | 100-tick-per-turn subturn loop is preserved | User: "I want to maintain the 100tick system" |
| 2026-05-10 | No forward assumptions across ticks | User: "we cannot make assumptions about production 2 turns later, because a battle could occur that destroys the producing infrastructure at some point during the turn" |
| 2026-05-10 | Long-term port to Rust/C++ is out of scope here | User: "Eventually the entire game will be ported to rust or C++ and be sped up a lot, but right now I just want … the existing system to be coded as efficiently as possible" |
| 2026-05-10 | Phase plan: 1 Measure → 2 Cheap wins → 3 Harvesting cache → 4 Orchestration → 5 Secondary phases (conditional) | Triage notes called for "Phase 1: measurement; phases 2+: subsystem-specific optimization work". Swarm findings point to harvesting (~50%) and orchestration overhead (~30%) as the two main targets. |
| 2026-05-10 | Cache strategy: per-turn key + dirty-flag invalidation set by write services | Mirrors the PROJ-285 per-turn habitability cache already in the codebase; matches the documented `docs/02_PATTERNS.md` conventions; smaller invalidation surface than version counters |
| 2026-05-10 | Phase ordering (descriptor lists) is frozen | Enforced by golden tests `test_default_tick_phase_list.py` / `test_default_end_of_turn_phase_list.py`; all optimizations live inside sub-engines or orchestration, not in the descriptor list |
| 2026-05-10 | New benchmark `tests/performance/bench_turn_processing.py` is the acceptance gate | Per `tests/performance/bench_galaxy_planet_star.py` convention: fixed seed, fixed scenario, N min-of-runs, baseline JSON sibling, CI budget < 30 s |
| 2026-05-10 | Full sharded test-suite baseline run deferred to Phase 1 start | Profiling-first project — no production code changes in planning; the baseline run is the first action of Phase 1 |
| 2026-05-10 | Phases 3-5 are conditional on Phase 1 findings | If the profile contradicts the pre-profile hypothesis (harvesting dominant, snapshot/callback heavy), the phase ordering will be re-justified before Phase 2 starts |
| 2026-05-10 | Aspirational target: **~10× speedup** on the tiny scenario (~7.5 s → ~0.75 s) | User: "I have no idea what is possible, I'm hoping that it can be at least 10x as fast, but we will see what we can turn up and improve." This is the *aspiration*, not a hard gate — Phase 1 will measure the ceiling and the final acceptance number is agreed at the end of Phase 4 |
| 2026-05-10 | Progress-callback coarsening is in scope for Phase 4 | User: "I am open to reducing UI call backs." Phase 4.3 may reduce per-tick callback to every Nth tick (proposed default: every 5 ticks → 20 callbacks/turn). Final cadence still surfaced for confirmation if it changes UX visibly |
| 2026-05-10 | All Phase 2 "skip when nothing to do" short-circuits are in scope | User: "I'm fine with skipping anything that has nothing to do at any time." Confirms full Phase 2 task list (storms, orders, planet actions, component activation transitions, etc.) |
| 2026-05-10 | Codex consult: agreed plan corrections applied | See `AgentCoordination/Scratchpad/Consult/20260512T044934Z_proj412-plan-review/response.md`. Key corrections: (1) swarm_02 booster vs storage ranking softened to co-top; (2) Phase 1 progress-callback probe promoted to "early" since UI callback verified to redraw + flip per tick; (3) Phase 5.1 (PlanetEnergyEngine cache) reduced — `_energy_cache` + fingerprinting already exist; (4) Phase 1.6 retired — `test_recalculate_storage_called_each_tick` is actually `test_process_harvesting_tick_calls_recalculate_storage` and asserts behavior not call count; (5) Phase 1 benchmark turns/runs made configurable to fit < 30 s CI budget (10 turns × 7.5 s overshoots); (6) Risks expanded: rollback-and-retry cache staleness, don't-cache-harvest-amounts, cache observability, narrower floating-point statement, superweapon scope note. |
| 2026-05-10 | Phase 1 Test C ("mid-turn booster arrival via fleet") deferred pending design decision on fleet-carried `ResourceHarvestBooster` | Codex found current `HarvestingEngine._get_harvest_booster_mult` calls `find_abilities_in_scope` which only resolves planets → operational facilities (`strategic_ability_scanner.py:64-99`). Fleet-carried boosters can only reach harvesting via the universal `IAbilitySource` pipeline (`ability_iterator.py:261-339`, `ability_sources/fleet.py:1-14`), which the current harvest path does NOT use. Test C as originally written would test behavior the code does not implement. Pending user direction: (a) change Test C to a planet/facility-based booster arrival, or (b) accept a design decision to migrate harvesting boosters to the universal pipeline first (additional scope). |
| 2026-05-12 | **Option B chosen**: migrate harvesting boosters to the universal `IAbilitySource` pipeline as part of this project | User: "go with B". This is a deliberate game-behavior change — fleet-carried `ResourceHarvestBooster` becomes functional in harvesting. Added as new **Phase 3** (booster pipeline migration); old Phases 3-5 bumped to 4-6. New Phase 3 lands BEFORE the booster cache (Phase 4) so the cache is built around the universal pipeline and its invalidation hooks correctly include fleet movement / ship death. Phase 1 Test C is split into C1 (facility-based positive control, passes today) and C2 (fleet-based, xfail until Phase 3.3 lands). |
| 2026-05-12 | Phase 1 complete — baseline green, bench captured, 4 characterization tests + 1 xfail-pending, overhead probes confirm swarm_03 hypothesis | Test suite: 20155/20155 passing in 132 s. Bench: ~62 ms / turn on synthetic scenario (harvesting 88%). Probe A (mock snapshot capture): −0.7 ms / turn — negligible. Probe B (5 ms synthetic UI callback): +543 ms / turn — strongest evidence that the user's 2.5–3.7 s unaccounted gap is dominated by the per-tick UI redraw at `strategy_game_state_manager.py:170-177`. Findings persisted at `findings/profile_baseline_cpu.md`. |
| 2026-05-12 | **Phase reordering**: UI callback coarsening promoted from old Phase 5.3 → new Phase 4 (was harvesting cache); old Phase 4 → Phase 5; old Phase 5 (now without callback task) → Phase 6; old Phase 6 → Phase 7 | User: "RE-arder if you feel that is better." Probe B measured the callback at +543 ms / turn — the largest single per-turn cost on the user's real game. Promoting it ahead of the harvesting cache produces the biggest absolute win earliest. Phases 6 and 7 are explicitly gated on remeasurement: drop if < 50 ms / turn savings remain after Phases 4 and 5 land. |
| 2026-05-12 | Phase 2 complete | Late imports moved out of `_get_harvest_booster_mult` (imported via `strategic_ability_scanner` module to preserve test monkeypatch contract) and `_time_phase`. `PlanetModifierEffectEngine` lazy-cached on `TurnEngine` (was constructed fresh + late-imported every tick). Two short-circuits kept: environmental (skip-on-no-storms, defensively gated on `isinstance(galaxy.systems, dict)`) and component_activation (skip-on-no-transitions). Three short-circuits attempted and reverted (order_processor / action_execution / planet_action) — sub-millisecond savings each and broke MagicMock-driven tests. Bench delta: −1.3% total, `activation_timers` −16.6%, `planet_modifier_effects` −4.6%. Full sharded suite green (20159/20164). |
| 2026-05-12 | Phase 3 complete: booster pipeline migrated to universal `IAbilitySource` | New `strategic_ability_scanner.find_harvest_boosters_for_colony(colony, galaxy, empire, registries=)` walks `iter_ability_sources_at_hex` + `iter_ability_sources_in_system`, enforces booster scope semantics on each entry, and handles owner filtering. `HarvestingEngine._get_harvest_booster_mult` swapped to call it. The legacy `find_abilities_in_scope` planet/facility-only path remains for other consumers (build-rate booster, geologic stabilizer). Test C2 (fleet booster) flipped from failing → passing; all 5 mid-turn characterization tests green; the previous unit test that pinned the 4-scope call pattern was rewritten to assert the `resource_type`-filter contract against the new helper. Bench impact: **harvesting +60.5%** (~55 ms → ~88 ms / turn) — universal pipeline walks more providers; Phase 5 cache reclaims this and more. `docs/systems/strategy_layer.md` updated. Behavior change shipped: fleet-carried `ResourceHarvestBooster` now scales harvest, matching the existing Sector/System Effects panels which already showed fleet-projected abilities. |
| 2026-05-12 | Phase 4 complete: UI callback coarsened to cadence-5 | TurnEngine fires `progress_callback` at tick 1, every 5th tick, and tick 100 (21 invocations/turn instead of 100). User: "N can = 5, make it a setting in a .json file." Interval lives in `output/settings/turn_engine_settings.json` (`progress_callback_interval`, default 5, clamped to `[1, 100]`); loader at `game/strategy/engine/turn_engine_settings.py` follows the `replay_settings.json` pattern (missing/corrupt file silently falls back to defaults). Repeated probe with 5 ms synthetic callback: pre-Phase-4 +543 ms/turn → post-Phase-4 **+114 ms/turn (~80% reduction)**. UX-visible effect: "Tick N / 100" overlay advances in jumps of 5; both endpoints display. Tests: 5 cadence tests + 10 settings tests; `test_turn_engine_progress_callback.py` rewritten for new contract. |
| 2026-05-12 | Phase 5 complete: harvesting cache | Added `_storage_dirty` / `_booster_dirty` transient flags on `Empire`. `PlanetWriteService.add_facility`/`remove_facility` take optional `empire` kwarg and flip both flags; production_spawner + colonize handler updated. `FleetMovementEngine.apply_movements` post-hook flips `_booster_dirty` for empires with moved fleets. `HarvestingEngine` grows per-turn `_storage_cache_turn[empire_id]` + `_booster_cache[(turn, colony_id, resource_type)]`; `invalidate_turn_caches()` exposes the cache-clear contract for the rollback path. `TurnEngine.process_turn`'s `EnginePhaseError` handler now calls it after `snapshot.restore(session)`. Observability via `_cache_counters` dict (hits, misses_turn, misses_dirty). **Bench: total 96 ms → 11 ms / turn (−88%); harvesting 89 ms → 5 ms (−94%).** 4 cache-contract tests + characterization-test updates (Test A flips `_storage_dirty`; Test D calls `invalidate_turn_caches`). Task 5.4 (per-component ability cache) **skipped** — harvesting at 5 ms is already sub-budget. |
| 2026-05-12 | Phase 6+7 **gated on remeasurement** | After Phase 5 the bench scenario is at 11 ms/turn — well below any sensible threshold for further synthetic-bench optimization. Phase 6 (snapshot capture / `_run_phases` micro-tuning) was already conditional per the plan; recommend dropping it unless user remeasurement on real game shows >50 ms/turn remaining. Phase 7 (secondary engines: PlanetEnergyEngine already has its own cache; Environmental short-circuit shipped in Phase 2; FleetMovementEngine pathfinding memoization remains as the largest possible win but only matters in games with many moving fleets — out of scope for the tiny-game target). |
