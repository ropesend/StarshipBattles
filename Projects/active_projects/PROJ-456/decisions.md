# PROJ-456: Decisions Log

> **LOG ALL DECISIONS HERE**
> When you make a design choice or the user specifies a preference, add it to this table.
> Future agents will reference this to understand why things were done a certain way.

| Date | Decision | Rationale |
|------|----------|-----------|
| 2026-05-18 | Project initialized | Starting point for UI back-compat shim retirement sweep (9 shim clusters + transfer_dialog characterization) |
| 2026-05-17 | Phase 1 executed | F-C-002 (broad-catch marker added at `transfer_dialog.py:412`); F-C-005 (deleted `draw_grid` free function — only `test_grid_and_storms.py` reached the free-function form, migrated to `GridLayer().draw(...)`); F-C-007 (deleted `_description_controller` property+setter — verified zero test callers reach through the shim at HEAD; the 12-ref count cited in the original finding was inflated by canonical `controller._description_controller` storage references and the `attach_description_controller`/`on_description_controller_change` method names, which are NOT shim accesses); F-C-010 (deleted `_get_order_description` shim; migrated 3 test files to `OrderDescriber().describe(order, entity)` + deleted the shim-delegation test); F-C-012 (Option B half-measure: refreshed docstring to drop the "back-compat for tests" framing + made 10 test sites pass explicit `empire_name="Test Empire"`; left the `Optional[str] = None` signature + title fallback branch intact for production because the controller's `getattr(empire, "name", None)` legitimately propagates None when the upstream empire has no `.name` attribute. Option A — full required-str — would require an upstream-controller audit that's out of scope here). Sharded 23362/23362 green (delta -1 = the deleted shim-delegation test). |
| 2026-05-17 | F-C-012 deviation: Option B instead of Option A | Per the plan checklist, Option A (required keyword-only `empire_name: str`, drop the title-fallback branch) was the recommended path. Investigated the production caller `event_log_window_ctrl.open_all` (line 48): it computes `empire_name=getattr(empire, "name", None)`. The `, None` fallback is a defensive default for empire-less calls. Option A would require also fixing every code path that might reach this controller without an empire (out of scope). Option B closes the test-side back-compat (no test now relies on the implicit None default) while leaving production tolerant. The DI catalog could re-open this as a follow-up if the upstream None-path is ever proven impossible. |
