# PROJ-449: Decisions Log

> **LOG ALL DECISIONS HERE**
> When you make a design choice or the user specifies a preference, add it to this table.
> Future agents will reference this to understand why things were done a certain way.

| Date | Decision | Rationale |
|------|----------|-----------|
| 2026-05-18 | Project initialized | Starting point for Strategy entity wrapper retirement (Planet + ShipInstance legacy-kwarg + property shim cluster) |
| 2026-05-19 | Plan + manifest + 7 phase checklists populated from Codex r4 redesign job 1 | Codex r4 redesign at `AgentCoordination/Scratchpad/Consult/20260519T004841Z_stages-1-2-audit-and-redesign/response.md` re-bundled wrapper retirement as a single job-oriented project (vs the PROJ-444..447 layer-bucket split that hit cycles). PROJ-449 owns the full sweep end-to-end. Sequencing: audit (Phase 0) → fixture (Phase 1) → sweep + serde (Phase 2) → delete Planet shims (Phase 3, unblocks PROJ-450) → delete ShipInstance shims (Phase 4, closes PROJ-443 5b) → protocol caveat drop (Phase 5) → profile resource_pool (Phase 6, profile-gated). |
| 2026-05-19 | PROJ-443 Phase 5b deferred-deletion supersession | PROJ-443 Phase 5b (2026-05-17) retained the ShipInstance wrapper because deleting it before the test sweep produced 19 failures + 16 errors. PROJ-449 reverses the ordering: sweep first (Phase 2) with wrappers alive; delete after (Phase 4) with bodies unreached. The PROJ-443 audit-of-record (18 files) is the Phase 0 verification input. |
| 2026-05-19 | F-A-007 explicitly OUT of scope | Codex r4 redesign: "F-A-007 should not be smuggled in as a side quest; if it still sits at 839 LOC after job 1, spin it as its own next-touch project." Phase 4 Task 4.5 measures post-deletion LOC and records the next-touch trigger. |
| 2026-05-19 | F-A-025 deposits-resources legacy alias = Phase 2 free-rider | While rewriting `planet_from_dict_kwargs`, the same hunk carries the `data.get("resources", {})` legacy alias removal per CLAUDE.md "saves are disposable." Zero added cost; no separate finding tracking needed. |
| 2026-05-19 | Codex audit fixes applied (Bucket A audit, `response.md`) | Codex's audit at `AgentCoordination/Scratchpad/Consult/20260519T014918Z_audit-bucket-A-substrate-triad/response.md` found 3 issues: (1) plan.md:100 wrongly assumed `PlanetaryFacility` had been renamed to `_consumable_levels=`; verified at HEAD that the facility kwarg is still public (`game/strategy/data/planetary_facility.py:32`, deprecated fuel wrappers at `:203-217`). Phase 1 Task 1.1 reframed as a no-op verification step; F-A-012 facility-kwarg rename added to plan.md Out of Scope. (2) manifest.md omitted the sibling-project SHA-signal write — resolved by removing that mechanism entirely (Phase 3 Task 3.7 no longer writes PROJ-450/plan.md). (3) phase_0_checklist.md ShipInstance sweep used raw `rg` counts that match helper-call false positives (e.g. `tests/unit/strategy/data/test_facility_resource_tracking.py:111`, fixture defaults at `tests/fixtures/strategy_entities.py:140,318,320`). Task 0.1 now requires constructor-context filtering before the `<=25 / 25-40 / >40` gate is applied. |
| 2026-05-19 | Facility constructor-kwarg = OUT of scope (F-A-012 follow-up) | F-A-012's generic consumable API landed but the `PlanetaryFacility(...)` constructor kwarg was never renamed. PROJ-449 only retires the Planet + ShipInstance wrappers + property clusters. Renaming `consumable_levels=` to `_consumable_levels=` on PlanetaryFacility would be a separate sweep across every facility-construction test site plus a serde-key reconciliation; future projects should split it out if taken on. |
| 2026-05-19 | Cross-project gate to PROJ-450 = direct code inspection (no SHA signal) | Codex audit flagged two incompatible gate mechanisms: (a) PROJ-449 Phase 3 Task 3.7 wrote PROJ-450's plan.md with a commit-SHA signal, (b) PROJ-450 Phase 0 Task 0.1 inspects current code directly. PROJ-449 dropped (a). Direct inspection is robust to sequencing errors and avoids the manifest-incompleteness paradox (cross-project write would have required PROJ-450/plan.md in PROJ-449's manifest). |
| 2026-05-18 | Phase 3 scope adjustment: deleted wrapper + 3 SETTERS only; kept the 3 read-only @property GETTERS | Phase 0 audit treated `planet.stockpile`/`.max_stockpile`/`.staging_yard` strictly as a write/test-fixture surface. After Phase 3 deleted the getters, the sharded suite revealed ~16 production read sites (transfer_branches, lay_mines handlers, harvesting_engine, organics_consumption_engine, empire.resource_pool aggregation, planet_report_panel, etc.) AND ~50 test read sites. Re-introduced the getters as read-only views over the private fields. Setters were a real surface (2 production setter sites: `planet_write_service.set_max_stockpile` + `issuer_adapter` recovered-staging-yard rewrite; ~25 test setter sites). Migration sweep applied via subagent: 19 files (2 production + 17 tests), ~45 attribute-setter migrations + 9 ctor-kwarg leftovers + 2 mock-fixture refactors (`test_harvesting_engine.py` promoted from `MagicMock(spec=Planet)` to a real Planet; `MockColony` in `test_harvesting_size_scaling.py` got property aliases). PROJ-450 will refine the `staging_yard` getter's return type to a typed substrate; that is its concern. Net F-A-004 outcome: setter cluster fully deleted (the substrate-widening seam closed); read-only views survive as the stable read surface. |
| 2026-05-18 | Phase 1 scope crept by 3 lines (test_roundtrip_ships.py) | The wrapper at `ship_instance.py:809-833` raises `TypeError` when BOTH `consumable_levels=` and `_consumable_levels=` (or `cargo_contents=`/`_cargo_contents=`) are passed. After Phase 1 renamed strategy_entities.py:318/320 defaults to `_consumable_levels=`/`_cargo_contents=`, any test calling `create_test_ship_instance(consumable_levels=...)` or `create_test_ship_instance(cargo_contents=...)` collides because `defaults.update(overrides)` leaves both private (default) and public (override) names in `defaults`, unpacked together to the wrapper. Only 3 such callers exist (`test_roundtrip_ships.py:46, 56, 62`); migrating them in the Phase 1 commit keeps Phase 1 "independently green" per the plan's intent. Phase 2's sweep set shrinks by 1 file. Planet side does not collide because `create_test_planet`'s `defaults` does not include `stockpile`/`max_stockpile`/`staging_yard` keys — those only enter via `**overrides` so callers passing `stockpile=` still translate cleanly through the wrapper. |
