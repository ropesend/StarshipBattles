# PROJ-497: Decisions Log

> **LOG ALL DECISIONS HERE**
> When the user specifies a preference, add it to this table.
> Future agents will reference this to understand why things were done a certain way.

| Date | Decision | Rationale |
|------|----------|-----------|
| 2026-05-23 | Project initialized | Carved out of PROJ-489 audit follow-up. See `findings/source_review.md`. |
| 2026-05-23 | Split data-intent (PROJ-497) from engineering hardening (PROJ-498) | Codex consult (`AgentCoordination/Scratchpad/Consult/20260523T120100Z_plan-PROJ-489-blast-radius/response.md`) recommended split because PROJ-498's rejection-matrix test should encode the *chosen* truth, not today's accidental truth. PROJ-497 runs first. |
| 2026-05-23 | Data-edit phase is BLOCKED on user decisions | Each of the three smells (efficient_engines, mini_capital_missile, facing/turret_mount seeker allowance) is a genuine design call, not a foregone fix. Agents must not unilaterally pick. Decision space recorded in `plan.md` "User Decision Points". |
| 2026-05-23 | Codex/Claude agreement: `efficient_engines` is doubly broken | Both agree allow_abilities namespace is wrong (0 valid targets) AND `consumption_mult: -0.2` against default `multiply` operation would drive consumption negative if reachable. See Codex response and `game/simulation/components/modifiers.py:18-48`. |
| 2026-05-23 | New insight from Codex: allowed-implies-mandatory coupling | `get_mandatory_modifiers()` returns every allowed modifier and `ShipComponentManager.ensure_mandatory_modifiers()` auto-applies on add (`game/simulation/services/modifier_service.py:108-125,222-234`; `game/simulation/entities/ship_component_manager.py:72-80`). Any allowlist broadening also broadens auto-application surface. This raises the stakes of option (b) for `efficient_engines` and any seeker-allowance edit. |
| 2026-05-23 | Mid-project review (Codex): `mini_capital_missile` retype Task 2.2 was incomplete | Original wording said "change `type` field" only. The live allowance service keys on `component.abilities` payload keys, NOT on `type`. A type-only edit is a no-op for `is_modifier_allowed()`. Task 2.2 updated to require explicit decision on type vs ability-payload vs both, and per-pair user approval for newly-allowed seeker_* modifiers. See `AgentCoordination/Scratchpad/Consult/20260523T120300Z_mpr-PROJ-497-498/response.md` Q1/Q4. |
| 2026-05-23 | Static-scan script corrected: `deny_abilities` is NOT enforced by live service | Original scan in `findings/source_review.md` applied `deny_abilities` filter, undercounting `hardened_mount` as 162 valid. Live count is 169. PROJ-498's matrix test will be false-red on 7 armor components if it copies the original scan. Findings doc updated with canonical-rule warning. See Codex MPR Q2. |
| 2026-05-23 | ~~**Decision 1 (`efficient_engines`): REDESIGN** with corrected keys + effect (user choice option (b))~~ **SUPERSEDED 2026-05-23 — see next row.** | (Historical) User initially picked redesign over delete/keep-inert. Redesign specifics were never specified; orchestrator presented 3 concrete options (Options A/B/C in earlier subagent report); user reversed to DELETE after review. Retained in log for traceability. |
| 2026-05-23 | **Decision 1 (REVISED — `efficient_engines`): DELETE** (user choice option (a), reversing earlier REDESIGN) | User verbatim: "The efficient engine ability should be eliminated, continuing to work with it is a mistake as it is too specific to a specific type of component/ability." Aligns with Codex's original recommendation (least regression risk; row was doubly malformed and reachable by zero components). Applied via Phase 2 Task 2.1: TDD failing test at `tests/unit/validation/test_proj497_efficient_engines_deleted.py` -> row deleted from `data/modifiers.json` (was lines 447-465) -> stale icon-map entry removed from `game/ui/services/modifier_icon_service.py:30` -> green. Zero `data/designs/*.json`, `tests/`, or `tests/regression/snapshots/` references existed (verified by grep). Orphaned asset `assets/Images/Modifier Icons/mod_efficient_engines.png` left in place (asset cleanup out of scope; UI never asks for it after the dict-key removal). DI-2026-05-23-004 pruned via `triage_discovered_issues.py --prune`. |
| 2026-05-23 | **Decision 2 (`mini_capital_missile`): RETYPE to `SeekerWeaponAbility`** (user choice option (b)) | Aligns name with type and game-design intent (a "mini capital missile" should be a seeker, mirroring `capital_missile`). Per Phase 2 Task 2.2 Codex-MPR caveat (Q4), the retype must change BOTH the `type` field AND the ability-payload key (`abilities.BeamWeaponAbility` → `abilities.SeekerWeaponAbility` with a seeker-shaped payload); a type-only edit would be a no-op for `ModifierService.is_modifier_allowed()`. |
| 2026-05-23 | Decision 2 cascade: 4 modifiers gain `mini_capital_missile`, 2 modifiers lose it | Static-scan cascade after retype: `seeker_endurance`, `seeker_damage`, `seeker_armored`, `seeker_stealth` each grow by 1 (1→2). `range_mount` shrinks by 1 (6→5). `precision_mount` shrinks by 1 (5→4). `turret_mount`, `facing`, `rapid_fire` unchanged (already matched Seeker via prior allow-list, and Seeker replaces Beam — net zero). `simple_size_mount`, `hardened_mount`, `efficient_engines`, `efficiency_mount`, `automation` unchanged. `mini_capital_missile` is not referenced by any shipped design in `data/designs/*.json` (verified), so the auto-mandatory broadening has zero production-design impact today. PROJ-499 will re-shoot bulk; this project does not pre-empt (no existing snapshots reference `mini_capital_missile`). |
| 2026-05-23 | **Decision 3 (`facing`/`turret_mount` seeker allowance): KEEP, document as intentional** (user choice option (b)) | User clarified actual seeker semantics: "Seekers do not ignore firing arc or facing direction for initial launch direction, but they do ignore firing arc for targeting and the ability to launch. The seeker can be launched with the target in any location, but it must be launched in a direction consistent with the facing direction and firing arc." Therefore: launch direction IS arc-constrained → `facing`/`turret_mount` on seekers IS meaningful (controls launch direction); target acquisition is NOT arc-constrained → seekers can hit anywhere after launch. The doc claim in `docs/systems/ability_reference.md:287` ("seekers ignore firing arc") is at best ambiguous; flagged as a doc-clarification candidate (defer to PROJ-498 docs phase or a future doc PR; NOT edited in PROJ-497). |
| 2026-05-23 | **Decision 4 (PROJ-498 pre-question): override pairs — NONE** | Pre-recorded so PROJ-498 does not have to re-ask. No override-pair edits performed in PROJ-497. |
| 2026-05-23 | Task 1.4 (re-validate static scan): no NEW broken rows since project creation | Re-ran the canonical scan from `findings/source_review.md` (live-rule, no `deny_abilities` enforcement). Result identical to the table in `findings/source_review.md` (`efficient_engines` 0; all others as recorded). No new broken rows. |
| 2026-05-23 | **Decision 2.1 (mini_capital_missile endurance — Phase 4 Task 4.1 / Codex audit F1): reduce endurance from 3.0 to 0.05, yielding range = 240** | User verbatim (2026-05-23): "reduce the endurance of the mini, so that it comes in at close to a 200 range." Codex audit F1 flagged that the original Task 2.2 retype dropped the explicit `range: 200` from the old BeamWeaponAbility payload, and `SeekerWeaponAbility.__init__` (`game/simulation/components/abilities/weapons.py:369-373`) defaults range to `int(projectile_speed * endurance * 0.8)`. With projectile_speed=6000 and the inherited capital_missile endurance=3.0, that defaulted to **14400** — a 72x range jump the user did not approve. Chosen value: **endurance = 0.05 -> range = int(6000 * 0.05 * 0.8) = 240**. Picked over the alternative endurance=0.04 (range=192, exact-200) because 0.05 yields a rounder lifetime number, a small buffer over 200, and reads cleanly. Intentional side-effect: `endurance` also controls projectile lifetime (`game/simulation/combat/families/seeker.py:69-70`), so the mini missile now dies quickly — consistent with the Fighter-mounted "short-range mini missile" role. Applied via TDD: new test class `TestMiniCapitalMissileEffectiveRange` in `tests/unit/validation/test_proj497_mini_capital_missile_retype.py` (2 tests — raw payload check + instantiated `SeekerWeaponAbility.range` check) -> RED -> data edit at `data/components.json:1079` -> GREEN. Snapshot suite + sharded suite stable. |
| 2026-05-23 | DI-2026-05-23-007 logged (Phase 4 Task 4.3 / Codex audit F5 out-of-scope) | Doc claim at `docs/systems/ability_reference.md:287` ("Guided missile; ignores firing arc") is half-true per Decision 3 — seekers ignore firing arc for target acquisition but honor it for launch direction. Logged as a `doc` / `low` DI for future doc-PR pickup. PROJ-497 explicitly scoped out of `docs/systems/`. |
| 2026-05-23 | DI-2026-05-23-008 logged (Phase 4 Task 4.4 / Codex audit F6 out-of-scope) | Inert `"Weapon"` namespace tokens at `data/modifiers.json:58` (turret_mount) and `:284` (rapid_fire) — same broken-namespace pattern PROJ-497 fixed via DELETE for efficient_engines, but the rows still function via co-listed ability keys, so it's cosmetic-only dead data. Logged as a `dead-code` / `low` DI. PROJ-497 scope was 3 user-decided rows; these are out-of-scope (turret_mount and rapid_fire weren't audit-surfaced as design decisions). |

## Pending user follow-up (Phase 2 Task 2.1 blocker)

**RESOLVED 2026-05-23.** User reversed Decision 1 from REDESIGN to DELETE after reviewing the three redesign options. See the "Decision 1 (REVISED)" row above. The `efficient_engines` row has been deleted; Phase 2 Task 2.1 is complete.

**Historical background (kept for traceability) about the follow-up that no longer applies:**
- The sibling row `efficiency_mount` already targets `ResourceConsumption` with a parameterized 0.1-1.0 `consumption_mult` (operation=multiply default) — covering the same design space generally. This is why the user concluded a fixed `efficient_engines` would be "too specific to a specific type of component/ability".
- `consumption_mult` default value is 1.0 (multiplicative stat per `StatKey.get_default`). Per the deleted row's original `formula: "-0.2"` with default `operation: multiply`, the effect (if ever reachable) would have driven consumption negative — a second bug on top of the empty allow-list.
- `ResourceConsumption` is the ONLY ability keying onto `consumption_mult` in the consumer code (`game/simulation/components/abilities/resources.py:49`). The deleted row's `allow_abilities: [Engine, Generator, Weapon, Thruster]` matched zero components because those strings are not real ability keys.
