# PROJ-366: Decisions Log

> **LOG ALL DECISIONS HERE**
> When you make a design choice or the user specifies a preference, add it to this table.
> Future agents will reference this to understand why things were done a certain way.

| Date | Decision | Rationale |
|------|----------|-----------|
| 2026-05-05 | Project created | PROJ-354B's plan asserted codex was handling the production sink wiring. User clarified codex is NOT handling it. PROJ-354B Phases 5-6 are blocked on that wiring. PROJ-366 owns the prereq + the Phase 5-6 work end-to-end. |
| 2026-05-05 | `ReplayStore` constructed inside `bootstrap()` (not lazy) | The sink must be registered before any code path that calls `start_engine_from_spec` runs (`battle_runner.py:156` queries `get_default_capture_sink()` at battle start). Lazy construction risks losing the first battle's record. |
| 2026-05-05 | Coordinator constructed and started inside `bootstrap()` (not lazy) | Same reasoning — the coordinator must be subscribed before any battle persists. The first persisted record otherwise has no listener and verification is silently skipped. |
| 2026-05-05 | Add `replay_store` and `replay_verification_coordinator` fields to `BootstrapResult` | `RunLoop.run()` needs the coordinator reference for `shutdown_all_coordinators(timeout=...)`. Adding two frozen-dataclass fields is the canonical extension shape; no new global is introduced. |
| 2026-05-05 | DI inputs for the coordinator: fresh `AIControllerFactory()` + `get_default_registry_provider()` + `load_replay_settings()` + `load_combat_lab_design` fallback | Matches what `screen_router.py` already does for in-game combat. No new abstractions invented. |
| 2026-05-05 | Combat Lab fallback wired in `bootstrap()`, not at coordinator-call-site | Per PROJ-354B Phase 6 plan: the coordinator's `__init__` accepts `fallback_ship_builder`. Wiring at construction means every verification call uses the same fallback policy without per-call decisions. |
| 2026-05-05 | Add invariants 7 & 8 to `tests/unit/test_app_bootstrap_invariants.py` rather than a new file | The existing module is the canonical home for bootstrap-order invariants. Concentrating them in one place lets future readers see the full contract. |
| 2026-05-05 | Headless-vs-visual equivalence test boundary at `BattleController.start_from_spec`, NOT `BattleScreen` | Inherited from PROJ-354B r004 Codex correction. Avoids Pygame UI dependency in tests; both paths flow through `start_engine_from_spec` → `run_battle`, so equivalence here proves equivalence downstream. |
| 2026-05-05 | Verifier-import lint as a unit test (AST parse), not a static analyzer | Lightweight; no new tooling dependency; locks in PROJ-354B audit-remediation `27e297815`'s layer-violation fix (AR-001) so future changes can't silently re-introduce the violation. |
| 2026-05-05 | Update PROJ-354B `plan.md` Quick Status to "Complete (via PROJ-366)" for Phases 5-6 at project end | Keeps the older project's status accurate without forging signatures or rewriting the plan body. PROJ-366 is the canonical owner of the work delivered. |
| 2026-05-05 (r001) | Add Phase 0 — `replay_ship_builder` registry-provider contract repair (CRIT) | Codex review found `replay_ship_builder.py:58` calls `registry_provider.get_registries()` which the protocol does not expose. Reference fix is `build_context_ship_builder` at `battle_runner.py:240-247`. Without this, every coordinator verification call raises `AttributeError` and writes ERROR sidecars. |
| 2026-05-05 (r001) | Combat Lab fallback hoisted from Phase 4 to Phase 2 — adapter via `DesignOnlyMaterializer` | Codex review found two issues: (a) import path was wrong (`game.combat_lab.design_loader` vs actual `combat_lab.design_loader`), and (b) callable shape mismatch — `load_combat_lab_design(design_id) -> Dict` but coordinator needs `Callable[[ShipSpec, int], Ship]`. Resolved via `DesignOnlyMaterializer` (already used by Combat Lab itself) wrapped in a closure. Coordinator constructor takes the fallback at start time, so wiring belongs in Phase 2 alongside coordinator construction. |
| 2026-05-05 (r001) | Phase 3 PASSED test must drive production `ship_instance_lookup` snapshots, not no-snapshot path | Codex review found `test_replay_playback.py:99-106` does not populate `ship_instance_lookup`; copy-paste from there validates the fallback path, not production materializer. Resolution: drive Phase 3 PASSED through the strategy adapter (`game/strategy/adapters/simulation_adapter.py:426-445`) so `ShipInstanceSerializer.to_dict` is real. Reserve no-snapshot records for Phase 4 Combat Lab fallback test. |
| 2026-05-05 (r001) | Bootstrap test modules need autouse cleanup fixture | Codex review: once bootstrap starts the coordinator for real, every existing bootstrap test (`test_app_bootstrap_invariants.py`, `test_app_bootstrap_profiling.py`) spawns a non-daemon worker thread. Add an autouse fixture in BOTH modules calling `shutdown_all_coordinators(timeout=5.0)` + `reset_default_capture_sink()` + `set_replay_store(None)` after each test. Pattern from `test_replay_verification_coordinator.py:93-98`. Lands inside Phase 1. |
| 2026-05-05 (r001) | Tests monkeypatch SOURCE modules, not lazy-import sites | Codex review: `RunLoop.run()` and the new `bootstrap()` wiring use late imports inside the function body. Patching `game.run_loop` or `game.app_bootstrap` for those names misses the local imports. Targets: `game.services.llm.background.shutdown_all_calls`, `game.strategy.services.replay_verification_coordinator.shutdown_all_coordinators`, `game.simulation.replay.replay_capture.set_default_capture_sink`, `game.strategy.systems.save_game_service.set_replay_store`. |
| 2026-05-05 (r001) | Insertion-point standardization: "after `InputMapper.load(...)`, before profiler total/save_history/return" | Codex review noted `plan.md:42` said "immediately after `ApplicationContext.create_production()`" but `phase_1_checklist.md:36-42` and `design.md:43-64` placed it after input loading. Standardize on the later point everywhere — no battle can run before bootstrap returns and `Game.__init__` constructs `ScreenRouter`. |
| 2026-05-05 (r001/r002) | Shutdown rationale wording (r002 supersedes r001) | r001 used "producers, not consumers, of replay records" framing for `shutdown_all_calls` first. Codex's arc01-004 correction: that framing is unsupported (no LLM path produces replay records). Replaced with a wording that grounds the order in the current AI factory shape and the existing LLM shutdown invariant. The ordering test name pins the current invariant so a future flip is loud. |
| 2026-05-05 (r001) | Phase 3 tests use `coordinator.wait_for_idle(timeout=30)` THEN file-existence check | `wait_for_idle` (`replay_verification_coordinator.py:413-429`) exists for exactly this test problem. Combining wait_for_idle + file check gives clearer failure diagnostics — wait_for_idle reveals worker failures, file check confirms atomic sidecar landing. |
| 2026-05-05 (r001) | Phase 3 adds no-recursion assertion (live battle path) | After `coordinator.wait_for_idle()` returns, the store still contains exactly one replay record. PROJ-354B Phase 4 has a narrow regression test, but a PROJ-366-level assertion through the wired caller pins the guarantee end-to-end. |
| 2026-05-05 (r001) | Single shared `settings = load_replay_settings()` instance | `bootstrap()` constructs one `settings` and passes it to BOTH `ReplayStore(settings=settings)` and `ReplayVerificationCoordinator(settings=settings)`. Avoids two file reads and two potentially-divergent settings instances. |

## Audit Remediation
| Cycle | Date | Findings | Resolution |
|-------|------|----------|------------|
| 1 | | | |
