# PROJ-466: Decisions Log

> **LOG ALL DECISIONS HERE**
> When you make a design choice or the user specifies a preference, add it to this table.
> Future agents will reference this to understand why things were done a certain way.

| Date | Decision | Rationale |
|------|----------|-----------|
| 2026-05-20 | Project initialized | Starting point for Error handling cleanup - session-init crash + exception hygiene (2026-05-20) |
| 2026-05-20 | Bundled findings from `2026-05-20_065518_error-audit` into ONE project by code locality across all six layers (27 verified, V<30) rather than splitting per layer | Bundling driven by code relatedness rather than severity to maximize implementation continuity; the CRITICAL session-init path spans two files that must be fixed together, and the rest is low-effort exception hygiene. Codex consult (planning) concurred: "keep this as one project... splitting would add project overhead faster than it reduces implementation risk." Full bundling discussion in findings/bundling_decisions.md |
| 2026-05-20 | Phase 4 added from Codex audit: removed the router-level `SessionInitializationError` catch on the new-game path (kept it on quickstart) | Codex CRITICAL: the per-component Phase 1 catches conflicted in the real wiring — the router catch made the controller catch dead and still killed the window + left `showing_new_game_setup` stuck True. Root-cause fix is single ownership: controller owns the new-game failure UX, quickstart (no controller) keeps the router guard. 3 other VERIFIED Codex findings (weak run_battle test, missing OSError test, minefield missing-file visibility) also remediated. 1 Codex finding REJECTED (untracked-test-files is an artifact of not committing). Verification: `AgentCoordination/Scratchpad/Consult/proj466_audit/verification.md` |
| 2026-05-20 | Resolved 4 borderline UNCERTAIN MINOR findings: INCLUDE satellite_controller.py:108 (logger.debug); DEFER construction_queue.py:186, strategy_screen_assets.py:76, star_list_window.py:395 | Ran one Codex consult per the autonomous-override contract. Codex advised include 2b / drop 2a,2c,2d. I weighed and agreed: (b) is the only one of the four that improves observability with no behavior change and matches an audit-flagged asymmetry; (a) is comment-only and risks normalizing an ambiguous zero-cost fallback (real design question, not wording); (c) is already a narrow typed catch with a warning and the broad-catch comment rule targets `except Exception`, not narrow tuples; (d) the audit itself calls defensible UI responsiveness — log noise from routine typos. Deferred items recorded in findings/verification_report.md and findings/bundling_decisions.md. Codex response: AgentCoordination/Scratchpad/Consult/20260521T031047Z_err-audit-bundle/response.md |
