# PROJ-437: Decisions Log

> **LOG ALL DECISIONS HERE**
> When you make a design choice or the user specifies a preference, add it to this table.
> Future agents will reference this to understand why things were done a certain way.

| Date | Decision | Rationale |
|------|----------|-----------|
| 2026-05-17 | Project initialized as sibling subproject of PROJ-436 | Per user "data-model first, UI second" preference. PROJ-436 owns the unified `Container` substrate; PROJ-437 rebuilds the transfer UI on top once the API is stable. Both projects converged via [discussion arc01](../../../AgentCoordination/Scratchpad/Discussion/20260517T230029Z_post-435-project-creation/). |
| 2026-05-17 | **UI is its own project, not absorbed into PROJ-436** | Two reasons. (1) PROJ-436 is already large (12 phases including consult); folding UI in would balloon it and break the substrate-then-sweep checkpointing model. (2) The current transfer UI works adequately against the legacy storage patchwork; rewriting it after the substrate is settled lets us design against the final API shape, not a moving target. |
| 2026-05-17 | **Preserve existing slider/arrow/Max UX shape** | Per user: "we already have a transfer UI that works OK, it is somewhat similar to a slider system." This project re-plumbs the UI to the unified `Container` API but does not redesign the input affordances. `MAX_LOAD` / `MAX_DROP` sentinels preserved; arrow-after-Max behavior preserved. |
| 2026-05-17 | **Phase 0 may start during PROJ-436 Phase 6-8** | Phase 0 is read-the-API + survey-the-current-UI work that produces a findings document. It does not edit production code and does not depend on the final cutover of PROJ-436 Phase 7. Phase 1+ are gated to PROJ-436 Phase 7 completion. This lets the two projects overlap modestly without breaking the checkpoint rule. |
| 2026-05-17 | **No worktrees** | Per user `feedback_no_worktrees.md`. Serial execution in main checkout. |
| 2026-05-17 | **End-of-project Codex consult is Phase 5; verified findings become added phases** | Per user `feedback_consult_at_project_end.md`. Same workflow as PROJ-436 Phase 11. |
| 2026-05-17 | **Mixed-content row model with per-kind specialization** | Resources display as float amounts with per-resource icons; items display as discrete counts with design-name labels and damage indicators; population displays as per-species integer counts. All three render through one unified row contract but with kind-specific formatting hooks. Rejected alternative: separate panels per kind — would proliferate UI surface and contradict the "one transfer screen" goal. |
| 2026-05-17 | **Drop-pod-name handling folds into items-row presentation** | Currently `transfer_view_model.all_pod_names` is a special-cased always-show list for known drop-pod designs. After unification, drop pods are just `ItemContainable` instances; their presentation rule (show even at 0/0) becomes a per-item display flag on the design, not a special view-model field. |
| 2026-05-18 | **OD1 resolved → (a) every container per entity** | Phase 0 audit ([findings/transfer_ui_migration_map.md §5](findings/transfer_ui_migration_map.md#5-open-decisions-od1od2od3--phase-0-resolution)) confirmed the default. The existing `collect_sources_and_targets` already enumerates per-entity; option (a) is the natural extension. Noise concern mitigated by the existing filter-empty toggle. |
| 2026-05-18 | **OD2 resolved → (a) cross-kind transfer in one operation** | Phase 0 confirmed default — preserves existing UX where `pending_transfers` already mixes resources + items + passengers in one confirm pass. |
| 2026-05-18 | **OD3 resolved → (a) per-input mass-remaining preview** | `Container.add()` is O(1) over hash + small slice-map sum; per-arrow-click recompute is well within frame budget for the realistic row counts (≤ 50). Profile only on capacity bump. |
| 2026-05-18 | **Manifest correction: `fleet_data_source.py` is NOT a Phase 1 target** | Phase 0 audit ([findings/transfer_ui_migration_map.md §3.6](findings/transfer_ui_migration_map.md#36-gameuiscreensfleet_data_sourcepy--manifest-entry-is-wrong)) discovered this file is the fleet-report `VirtualTable` data source, unrelated to the transfer dialog. The actual source/target enumeration lives in `transfer_controller.py::collect_sources_and_targets`. The Phase 1 target list will be `transfer_view_model.py` + `transfer_controller.py` + a new DTO/facade `get_containers(id)` accessor (introduced as substrate in Phase 1a). Manifest update deferred to Phase 1 start. |
| 2026-05-18 | **Phase 1 split recommendation: 1a substrate / 1b cutover** | Per the PROJ-431 sub-phase model. Phase 1a introduces `ContainerRef` + `ContainerSnapshotInfo` types and a parallel `facade.fleets.get_containers(id)` / `planets.get_containers(id)` accessor as additive projections; Phase 1b cuts the view model and controller over. Collapse to single commit if Phase 1a turns out to be a ≤30-line patch with no breaking surface. |
| 2026-05-18 | **Pre-existing pytest.ini `norecursedirs` issue is fixed (out of scope note)** | PROJ-443 Phase 4 (commit `e12603992`) flipped `pytest.ini norecursedirs` so 1953 previously hidden tests under `tests/unit/strategy/data/` are now visible. The PROJ-437 prompt's "NOT YOURS TO FIX" caveat about `data` token in `norecursedirs` is moot — the issue has been resolved upstream. PROJ-437 baseline sharded count will reflect the new visible-tests total. |
| 2026-05-18 | **Tangential audit findings flagged (NOT taken in this project)** | `fleet_dto.py:217-226` and `builder/stat_rows_dynamic.py:179,252` still hardcode the eight resource IDs / display names. PROJ-437's UI rework routes around these (Container snapshots are the new SoT) but the leaks should not survive a final grep. Documented in [findings/transfer_ui_migration_map.md §4](findings/transfer_ui_migration_map.md#4-tangential-finding-out-of-scope-for-proj-437-flagging-for-visibility) for a future TD ticket or PROJ-436 Phase 7 cleanup pass. |
| 2026-05-18 | **Phase 1a substrate landed: ContainerSnapshotInfo + facade.{fleets,planets}.get_containers(id)** | Additive — zero caller migration. UI layer can now ask the facade for an immutable per-container projection (`container_id`, `owner_kind`, `owner_id`, `label`, `capacity_mass`, `mass_used`, `allowed_kinds`, `entries: tuple[ContainerEntry, ...]`) without the mutable `Container` crossing the facade boundary. Per fleet: one snapshot per ship via `bay_inventory.container_view(capacity_mass=inf)` then the snapshot reports the real bay capacity from `ship._cargo_mgr.get_vehicle_bay_capacity()` — `inf`-projection avoids `REJECTED_CAPACITY` for ships whose contents already exceed nominal cap; the snapshot can then report `mass_used > capacity_mass` and the UI can flag over-cap. Per planet: stockpile snapshot (capacity = `sum(max_stockpile.values())` as a rough proxy; revisit in Phase 2 once mass-remaining preview needs a real cap model) + staging-yard snapshot (capacity = `planet.max_staging_mass`). 10 new tests at [tests/unit/strategy/facade/test_container_snapshots.py](../../../tests/unit/strategy/facade/test_container_snapshots.py). |
| 2026-05-18 | **Phase 1 split kept as 1a / 1b per Phase 0 recommendation** | Phase 1a is the substrate commit. Phase 1b (cutover) will (a) extend `transfer_controller.collect_sources_and_targets` to attach `containers: tuple[ContainerSnapshotInfo, ...]` per entry (additive), (b) introduce a parallel `transfer_view_model.get_amounts_from_containers(snapshots)` reader without breaking the existing DTO path, (c) wire the dropdown source-of-truth selection through container snapshots. Existing DTO-driven path remains alive through Phase 1b to keep `tests/unit/ui/screens/test_transfer_view_model.py` row-ordering tests green; final DTO retirement is Phase 3-4 territory. |
| 2026-05-18 | **Phase 1b landed: per-entry `containers` field + `get_amounts_from_containers` reader** | Browse-only by spec — outer dropdown shape (`{label, type, id}`) preserved; the new `containers` tuple is additive. Source-fleet fallback branch in `collect_sources_and_targets` also receives container snapshots, so injected source-fleet entries are uniform with the facade-listed ones. `get_amounts_from_containers` mirrors `get_amounts(info_obj)`: RESOURCE entries → `{type_id: int}`, POPULATION entries → `passengers_<species>: int`, ITEM entries skipped (Phase 3 mixed-content). Two pre-existing `test_transfer_controller.py` assertions relaxed from exact-dict-eq to subset checks (`label`/`type`/`id` + `"containers" in entry`) — the field is additive and tests that check exact dict equality on a structure callers extend are over-specified. 7 new tests at [tests/unit/ui/screens/test_transfer_view_model_container.py](../../../tests/unit/ui/screens/test_transfer_view_model_container.py). |
| 2026-05-18 | **Outer dropdown shape stays per-entity in Phase 1 (per Phase 0 finding §3.2)** | The OD1 (a) "every container per entity" decision is interpreted as the nested `available_containers` model from Phase 0 finding §3.2, not as a flat per-container dropdown. Reason: Phase 1 is browse-only and a flat per-container dropdown would force `current_source` / `current_target` shape changes that cascade into row-data construction — pulling Phase 3 work forward and breaking the existing pin (`test_source_and_target_selection_updates_labels_and_defaults`) without a behavior-driven reason. Per-container dropdown UX, if needed, lands in Phase 2 or 3 alongside the row-data cutover. |
| 2026-05-18 | **Phase 2 landed: mass-remaining preview math + chrome indicators** | `MassPreview` dataclass + `TransferViewModel.compute_mass_preview` (delegating to a sibling helper at `game/ui/screens/transfer_mass_preview.py`) project source/target mass-remaining after every pending transfer applies. Per OD3 (a) the dialog refreshes on every arrow / Max / Zero / Clear-All mutation. Two new `UILabel`s in the chrome (one per side, below their dropdown) show `"<side>: <remaining>.<1f> / <capacity>.<1f>t"`; infinite capacity collapses to `"<side>: —"`; over-capacity target shows explicit `OVER (cap Xt)`. Mass-per-unit lookup covers resources (catalog) + population (default 0.1 t/individual); ITEM-kind cargo keys (`drop_pod:`, `vehicle:`) intentionally mass-neutral until Phase 3 mixed-content folds them in. Unknown keys are mass-neutral too — drift never crashes the preview. 16 new tests at [tests/unit/ui/screens/test_transfer_mass_preview.py](../../../tests/unit/ui/screens/test_transfer_mass_preview.py). |
| 2026-05-18 | **Phase 2: extracted mass-preview helpers to a sibling module to stay under the 500-LOC ceiling** | `transfer_view_model.py` reached 573 LOC with the in-place Phase 2 additions. Extracted `MassPreview` + `compute_mass_preview` + private mass-per-unit / qty helpers to a new `transfer_mass_preview.py` (209 LOC). View model re-exports `MassPreview` and exposes `TransferViewModel.compute_mass_preview` as a thin classmethod wrapper that injects the view-model's `MAX_LOAD` / `MAX_DROP` sentinels — preserves the single-entry-point public surface while moving the cohesive Phase 2 block to its own module. Post-extraction view model: 423 LOC. |
| 2026-05-18 | **Phase 2 deferred: `transfer_dialog.py` LOC ceiling (522 vs 500)** | Pre-Phase-2 the dialog was at ~489 LOC (just under the 500 ceiling). Phase 2's `_refresh_mass_preview` method + label placeholders + 5 event-handler wire calls added ~33 LOC, pushing it to 522. The dialog's natural decomposition target is the back-compat property shim cluster (lines ~191-253, ~60 LOC) which Phase 0 finding §3.3 already flagged for retirement during Phase 3 (when `all_pod_names` and the legacy DTO row-builder path retire together). Splitting now would create churn that Phase 3 immediately undoes. **Decision: keep the 522-LOC dialog through Phase 2; Phase 3 cleanup naturally restores it under the ceiling.** |
| 2026-05-18 | **Phase 3a substrate: container-driven row builder additive** | New `TransferViewModel.build_row_data_from_containers(source_containers, target_containers, *, filter_empty=False)` classmethod, delegating to `game/ui/screens/transfer_container_rows.py` (module extracted to keep view model under the 500-LOC ceiling — 547 → 453 LOC). Row dicts gain an additive `kind: ContainableKind` field. The canonical 8 resources always emit (matches legacy UX so users can see all load targets even when empty); population/items only emit when present on either side. Items use `drop_pod:<design_id>` prefix uniformly — vehicle vs drop-pod discrimination needs `ItemRef.state` on the snapshot (Container substrate change, PROJ-436 Phase 9 territory; the legacy DTO path retains the discrimination through `_build_pod_rows`). Dialog still calls legacy `build_row_data(source_obj, target_obj)` DTO path — Phase 3a is risk-isolated substrate; cutover is Phase 3b. 9 new tests at [tests/unit/ui/screens/test_transfer_mixed_content.py](../../../tests/unit/ui/screens/test_transfer_mixed_content.py). |
| 2026-05-18 | **Phase 3b minimal cutover: dialog switches to container row builder; legacy retirement deferred to Phase 4** | `TransferDialog._build_grid` now reads `current_source["containers"]` / `current_target["containers"]` (Phase-1b additive field) and calls `TransferViewModel.build_row_data_from_containers(...)`. No "if-X-has-Y" branching — the container path is the only path the dialog now exercises. The legacy `vm.build_row_data(source_obj, target_obj)` + `get_amounts` + `_build_pod_rows` + `all_pod_names` + dialog back-compat shims (`_get_amounts`, `_add_pod_rows`, `_all_pod_names`, `_row_data`, `_current_source`, `_current_target`, `_filter_empty`) are now dead code; Phase 4 owns the deletion + pinning-test retirement. Rule 9 ("two ways briefly during a sweep, legacy deleted in final cutover") satisfied — Phase 4 IS that final cutover. MagicMock iterates as empty by default, so characterization tests that don't wire `facade.fleets.get_containers` explicitly produce canonical-8 zero rows (same UX as the legacy DTO path with empty DTOs); no test ripple. |
| 2026-05-18 | **Phase 3 visual polish (icons, damage indicators, species labels) out of scope** | The design.md §"Per-kind row presentation" table calls for per-resource icons + damage indicators on damaged items + species icons. These are UI polish that doesn't fit the PROJ-437 data-model-migration scope. The `kind: ContainableKind` field in the row dict (Phase 3a) unlocks them when a future ticket picks them up — the substrate is ready. |
| 2026-05-18 | **Phase 4 final cutover: legacy DTO row-builder + dialog shims retired** | Deleted from view model: `get_amounts(info_obj)`, `build_row_data(source_obj, target_obj)`, `_build_pod_rows`, `all_pod_names` constructor param + field. Deleted from dialog: `_all_pod_names` property, `_get_amounts` method, `_add_pod_rows` method, the `self.view_model.all_pod_names = self._controller.discover_pod_designs(scene)` call in `__init__`. Retired pinning tests: `test_transfer_view_model.py::TestTransferViewModelRows::test_worker_i_transfer_vm_species_key_ordering`, `test_transfer_dialog_characterization.py::TestGetAmounts` (3 tests), `test_transfer_dialog_characterization.py::TestAddPodRows` (1 test) — 5 tests total. `controller.discover_pod_designs` method KEPT (still tested in isolation; no production caller but the method's contract is documented public). View model: 423 → 325 LOC. |
| 2026-05-18 | **Phase 4 AST guard for `RESOURCE_TYPES` is already pinned by `test_no_legacy_storage_fields.py`** | The Phase 0 manifest's `tests/static_guards/test_no_resource_types_constant.py` is redundant — PROJ-436 Phase 7 added pins at `tests/static_guards/test_no_legacy_storage_fields.py:217-265` covering both `transfer_view_model.RESOURCE_TYPES` / `RESOURCE_DISPLAY_NAMES` absence and `transfer_dialog`-re-export absence. Skipping the planned file rather than authoring a duplicate. |
| 2026-05-18 | **Phase 4 deferred follow-up: dialog LOC ceiling (523 vs 500)** | Pre-Phase-4 the dialog was 546 LOC; Phase 4 retirement brought it to 523. The remaining 23 LOC over the ceiling is the back-compat property shim cluster (`available_sources`, `available_targets`, `pending_transfers`, `_row_data`, `_filter_empty`, `_current_source`, `_current_target`) that other tests (TestPendingTransferMath, TestConfirmCommandEmission) still reach through. Retiring those tests is mechanical but touches ~10-15 test functions that would need to read `dialog.view_model.X` instead of `dialog._X`. **Decision: defer to a future TD ticket; PROJ-437's main goal (data-model migration) is complete and the file is moving in the right direction.** Not blocking project completion. |

## Open decisions (to resolve at phase start)

> **All three were resolved at Phase 0 start (2026-05-18) with their
> documented defaults.** See the 2026-05-18 rows above. The original
> option statements are retained below for traceability.

### OD1: Source/destination enumeration scope (Phase 1)

What counts as a "source" or "destination" container in the dropdown?

Options:
- **(a) Every container on the entity.** A ship with 5 cargo holds shows 5 source options. Most flexible, potentially noisy.
- **(b) One aggregated container per entity.** The ship appears once; UI shows aggregate contents across all its containers. Cleaner, loses some control.
- **(c) Grouped by content kind.** A ship appears as "Ship X — Resources," "Ship X — Items," "Ship X — Population." Mid-ground.

**Default for Phase 1:** (a) most flexible, document if (b) or (c) emerges as cleaner during implementation.

### OD2: Cross-kind transfer in one operation

Can a single staged transfer move resources AND items AND population from one container to another in one confirm?

Options:
- **(a) Yes, all three at once.** Matches existing slider-per-row UX.
- **(b) No, one kind at a time.** Forces clarity, may annoy users.

**Default for Phase 3:** (a) — preserves existing UX. If validation rejection messaging gets confusing across kinds, revisit.

### OD3: Mass-remaining preview granularity (Phase 2)

How often does the preview recompute as the user adjusts sliders?

Options:
- **(a) Per-input (every arrow click).** Most responsive, may be expensive on large containers.
- **(b) On debounce / on confirm.** Cheaper, slightly delayed.

**Default for Phase 2:** (a) — `Container.add()` validation should be cheap. Profile if it isn't.

## Flagged for product-review

None at this time. Open decisions OD1-3 are implementer choices, not user-input dependencies.
