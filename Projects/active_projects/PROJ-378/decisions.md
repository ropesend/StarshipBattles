# PROJ-378: Decisions Log

> **LOG ALL DECISIONS HERE**
> When you make a design choice or the user specifies a preference, add it to this table.
> Future agents will reference this to understand why things were done a certain way.

| Date | Decision | Rationale |
|------|----------|-----------|
| 2026-05-06 | Project initialized as PROJ-378 from PROJ-372 carryover. | 15 setup-errors in `tests/unit/strategy/data/test_galaxy_cleanup.py` use the pre-PROJ-372 pattern (`Galaxy.__new__(Galaxy)` + direct attribute assignment); the very first line `galaxy.radius = 100` invokes `radius.setter` at `galaxy.py:73-75` which does `self._state.radius = value`, but `_state` was never assigned because `__init__` was patched out. PROJ-372's verifier explicitly flagged this as out-of-scope-but-trailing. |
| 2026-05-06 | **Fix option B chosen: shared `make_galaxy_stub()` fixture.** Rejected option (a) direct `_state` assignment per fixture, and option (c) real `Galaxy(radius=N)` construction. | (a) duplicates boilerplate across 3 fixtures and doesn't help `test_empire.py` / `test_fleet_registration_lifecycle.py`. (c) costs ~33 ms per call (measured) and reintroduces the disk I/O dependency PROJ-372's `GalaxyState` extraction was designed to remove from unit tests. Option (b) is the only one that fixes all 15 failures, generalizes to the 6 sibling call sites, and keeps unit-test setup at ~0 ms. |
| 2026-05-06 | Place the new fixture at `tests/unit/strategy/data/conftest.py`, NOT `tests/fixtures/`. | Co-location with `test_galaxy_cleanup.py` (where 12 of 15 callers live) matches the more common pytest idiom. Cross-`unit`/`integration` imports for `test_empire.py` and `test_fleet_registration_lifecycle.py` are acceptable (precedent: `tests/fixtures/test_scenarios.py` from PROJ-279). If review pushes back, relocate to `tests/fixtures/galaxy_fixtures.py` — same content, different path. |
| 2026-05-06 | `make_galaxy_stub()` wires `_state`, `_registry`, `_spatial` (NOT generators). | The 15 failing tests exercise `unregister_planet` (delegates to `_registry`), `remove_warp_link` (reads `_state` directly), and `get_all_fleets_in_system` (delegates to `_spatial`). None calls a generator. Wiring `_warp_gen` / `_sys_gen` / `naming` adds ~33 ms of constructor cost for no test benefit. If a future test needs a generator, it should call `Galaxy(radius=...)` instead — that's what real construction is for. |
| 2026-05-06 | Sweep `test_empire.py` (5 sites) and `test_fleet_registration_lifecycle.py` (1 site) in Phase 2 even though they pass today. | Both use the legacy `Galaxy.__new__(Galaxy)` pattern. `test_empire.py` happens to work because it touches `_next_fleet_id` first, which routes through `_ensure_state()` (the lazy escape hatch). The codebase reads more cleanly with one canonical stub. Migration is mechanical. |
| 2026-05-06 | Do NOT touch `game/strategy/data/galaxy.py` (or any production file). | Brief: "DO NOT touch `tests/` or `game/`. Planning only." — wait, brief actually says "DO NOT touch `tests/` or `game/`. Planning only." for the architect; implementation will touch tests. Reading correctly: the architect (this doc) does not touch anything; the implementer touches only tests. `_ensure_state()` stays in production for now; its potential removal is a follow-up project decision, not a PROJ-378 decision. |
| 2026-05-06 | Skip optional AST-guard test for "no new `Galaxy.__new__(Galaxy)` calls". | 30+ LOC of test infrastructure for marginal benefit. The fixture's existence + a docstring note in `make_galaxy_stub()` is sufficient soft enforcement. If a future review surfaces drift, add it then. |
| 2026-05-06 | `make_galaxy_stub()` does NOT accept `**state_kwargs` for pre-populating `GalaxyState` fields. | Keep the factory minimal. Tests layer state via `galaxy._state.systems[k] = v` after construction — the pattern already proven in `test_fleet_registration_lifecycle.py:62-80`. A kwargs-rich factory invites scope bloat. |
| 2026-05-07 | Phase 2 Task 2.4 (`docs/02_PATTERNS.md` doc note) skipped per design Q4 recommendation. | Soft enforcement (the fixture's docstring + the canonical-location convention) is sufficient; a 10-line doc section adds churn without changing outcomes. If a future reviewer surfaces drift, add it then. The `make_galaxy_stub()` docstring already states the safe-method / unsafe-method contract. Filed as a future opportunity in this file. |
| 2026-05-07 | **Codex+Claude joint review (arc 20260507T033233Z_proj-376-377-378-plans-review)**: applied r004 edits — see `outcome.md` for the full edit list. Implementation module relocated to `tests/fixtures/galaxy_fixtures.py` from the originally-planned `tests/unit/strategy/data/conftest.py` because the latter is not importable from integration-test directories (no `__init__.py` exists at `tests/unit/strategy/data/`); `tests.fixtures.*` is the established cross-tree convention per `tests/fixtures/README.md` and 8+ existing imports. | CC-1 added `phase_state.json` + `findings_ledger.md` per 03c protocol (PROJ-378 `plan.md` already opted in). E378-1 corrected stale test counts: 18 collected (not 15), baseline `3 passed, 15 errors`, final `18 passed`, sharded delta `+15`. E378-2 fixed `findings/initial_review.md` line 9 wording: "3 fixtures × 18 distinct tests, 15 of which currently error in setup with `AttributeError: 'Galaxy' object has no attribute '_state'`". r004 fixture-location refinement: `tests/fixtures/galaxy_fixtures.py` is the canonical implementation; an optional thin `tests/unit/strategy/data/conftest.py` exposes a `galaxy_stub` `@pytest.fixture` wrapper that delegates to it. `phase_1_checklist.md`, `phase_2_checklist.md`, `design.md`, `manifest.md`, and `plan.md` updated accordingly. The 2026-05-06 row above (which leaned co-located conftest) is preserved for audit-trail; this row supersedes it. |

## Future opportunities (logged, NOT in scope)

- **Remove `Galaxy._ensure_state()` from production** (`galaxy.py:96-105`). After PROJ-378 ships, no remaining test calls `Galaxy.__new__(Galaxy)`, and `_ensure_state()` becomes dead code. A 5-minute follow-up project to delete it + update the property forwarder docstrings.
- **Generalize the stub pattern to other heavy-init classes** (`GameSession`, possibly `Empire`). Each would get its own `make_<x>_stub()` factory in a sibling `conftest.py`. Out of scope for PROJ-378.
- **Document `make_galaxy_stub()` in `docs/02_PATTERNS.md`** alongside the PROJ-279 fixture-bridge pattern. Marked optional in Phase 2.
