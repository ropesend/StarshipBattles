# PROJ-319: Decisions Log

> **LOG ALL DECISIONS HERE**
> When you make a design choice or the user specifies a preference, add it to this table.
> Future agents will reference this to understand why things were done a certain way.

| Date | Decision | Rationale |
|------|----------|-----------|
| 2026-05-02 | Project initialized | Starting point for Audit-shrink cleanup 2026-05-02 |
| 2026-05-02 | Acted only on findings that passed independent verification of `Reviews/results/2026-05-02_184210_audit_shrink/` | Audit-shrink reports have produced false positives in past runs (e.g. `_eval_least_armor_rule` reachable via `data/targeting_policies.json`); rejected and uncertain items recorded in `findings/verification_report.md` |
| 2026-05-02 | Skipped Phase 3 (dead classes / dead files) entirely | Source audit's Section 3 Tier 1 (dead files) and Tier 2 (dead classes) had zero verified items; `GroupTargetCoordinator` was the only candidate and the audit's own verifier downgraded it to PRODUCT_DECISION |
| 2026-05-02 | Surface zero-rejection rate from verifier explicitly in design.md | Source audit's own verifier corrected one CRITICAL misclassification and one false finding in the same run; a downstream skeptical pass that finds zero additional issues across 30 candidates is unusual and warrants per-task safety nets during implementation |
| 2026-05-02 | Ran a Round-2 strict re-verification with stricter agent prompts; kept all 30 items after reviewing every challenge | Round-2 agents raised 5 REJECTED + 1 UNCERTAIN, but human spot-check of each challenge against the actual code showed: 3 were verdict-label flips (agent's evidence agreed with the audit but they labeled it REJECTED), 2 were misreads of the audit's recommendation against an architectural strawman (DUP-X-04, DUP-X-13: the audit's proposals already preserve layer separation / parameter-driven anchor distinction), 1 was a minor line-number drift (DEEP-04-005, fixed in checklist). Full record in `findings/verification_report.md`. |
| 2026-05-02 | Marked Phase 2 complete despite a 1-of-3 sharded-suite failure in `tests/unit/services/llm/test_background.py::test_elapsed_seconds_is_monotonic_then_frozen` | Pre-existing flaky timing test — uses `time.sleep(0.01)` then asserts `elapsed_seconds > 0`, but Windows' default `time.sleep` resolution is ~15.6ms. Reproduced the same intermittent failure on a clean tree (Phase 1+2 stashed) and on the working tree, with no correlation to the `_extract_weapon_summaries` or `_planet_has_shield_facility` deletions (different subsystem entirely). Fix is out of scope for PROJ-319 — should be a separate ticket bumping the sleep, switching to event-based synchronization, or adding `@flaky`. |
| 2026-05-02 | `tests/unit/strategy/data/test_build_context.py::test_fleet_satisfies_build_context_protocol` is broken on main, not by PROJ-319 | Reproduced on a clean tree with all PROJ-319 changes stashed. PROJ-210 Phase 2 ("Fleet pass-through elimination") removed the direct `has_space_shipyard` and `can_build_type` attributes from `Fleet`; the test still expects `isinstance(fleet, BuildContext)` to return True. Test passes in the sharded suite (presumably due to import ordering side effects) but fails when run in isolation. Out of scope for PROJ-319 — needs either updating the test to use `fleet.capabilities.has_space_shipyard`, or restoring pass-through properties on Fleet. |
| 2026-05-02 | DUP-X-02 Task 4.12 partially scoped: consolidated the click dispatcher (5 handlers → `_handle_superweapon_click(designation_method)` helper + 5 thin wrappers) and the direct command handler tail (6 handlers → `_emit_validated_order` helper on `BaseCommandHandler`); did NOT extract `_resolve_superweapon_target` from `strategy_superweapons.py` or convert mission handlers | The two consolidations done covered the highest-value duplication in the audit's recommendation set. `_resolve_superweapon_target` was deferred because the audit itself noted that "limited by the UI divergence (confirmation dialog vs system picker)" the helper would only cover ~30 LOC across 2 of 5 designation handlers. Mission handlers' move-then-action pattern is structurally distinct from the direct handlers; consolidation would require a bigger base-class change and the marginal LOC savings is small. |
| 2026-05-02 | `tests/integration/strategy/test_mutual_join_rendezvous.py` failed 4 tests in the second-to-last sharded run but passed in isolation and on a re-run of the sharded suite | Intermittent test-ordering flake. Not caused by PROJ-319; the rendezvous code path was not touched. Recorded for future agents so they know to re-run the sharded suite once before treating these tests as a regression. |
