# PROJ-443: Decisions Log

> **LOG ALL DECISIONS HERE**
> When you make a design choice or the user specifies a preference, add it to this table.
> Future agents will reference this to understand why things were done a certain way.

| Date | Decision | Rationale |
|------|----------|-----------|
| 2026-05-18 | Project initialized from PROJ-436 Phase 2 discovery | `pytest.ini` `norecursedirs = ... data ...` matches any directory named `data` at any depth (not just the top-level `data/`), silently hiding 1575 tests in `tests/unit/strategy/data/` from the canonical sharded suite. Long-standing issue — predates PROJ-436. Surfaced when PROJ-436 Phase 2's new `test_bay_inventory_widened.py` (30 tests) didn't move the sharded count. |
| 2026-05-18 | **Scope expanded from `data` only to `data` + `combat_lab` + `Assets`** | Per Codex pre-execution consult, `norecursedirs` has THREE problematic tokens collectively hiding **126 test files across 6 directories**: `data` hides `tests/integration/data/` (1), `tests/unit/data/` (3), `tests/unit/strategy/data/` (95); `combat_lab` hides `tests/unit/combat_lab/` (24); `Assets` hides `tests/unit/assets/` (2) and `tests/unit/ui/assets/` (1) — on Windows `fnmatch` is case-insensitive so `Assets` matches lowercase `assets`. Narrowing to just `data` would leave the stated goal ("every test under `tests/` runs in the sharded suite") provably unmet and the regression guard would fail on the un-fixed tokens. Wider scope adds ~31 test files to triage in Phase 3 (24 combat_lab + 3 data + 2 assets + 1 ui/assets + 1 integration_data) on top of the ~95 strategy/data ones. |
| 2026-05-18 | **Fix `pytest.ini` AFTER triage, not before** | Flipping the config first would expose 65 pre-existing failures and turn the sharded gate red. Triage them in Phases 1-3 with the config still hiding them; flip the config in Phase 4 once the hidden directory is green via direct invocation; sharded gate then jumps from ~21233 to ~22700+ collected without introducing failures. |
| 2026-05-18 | **(SUPERSEDED 2026-05-18) Use `--ignore=./data` in `addopts` instead of `norecursedirs = data`** | Originally proposed: `norecursedirs` is a glob-style match against directory names (any depth, anywhere); `--ignore=./data` would be a path-anchored exclusion. SUPERSEDED by the row below after Codex pre-execution consult traced pytest 9.0.3 source and found `--ignore=path` is cwd-relative via `Path(os.path.abspath(path))` (`_pytest/pathlib.py:998-1004`), not config-root-anchored. |
| 2026-05-18 | **Phase 4 config change is removal-only: drop `data`, `combat_lab`, `Assets` from `norecursedirs`; add NO `--ignore` flag** | Per Codex pre-execution consult at `AgentCoordination/Scratchpad/Consult/20260518T034917Z_proj443-charter-review/response.md`: pytest 9.0.3's `--ignore` is cwd-relative (`_pytest/pathlib.py:998-1004`), not config-root-relative. Adding `--ignore=./data` to `addopts` would work for the sharded runner (which uses `cwd=PROJECT_ROOT`) but anchor unpredictably for any non-canonical invocation (subdirectory, IDE plugin). `pytest.ini` already sets `testpaths = tests`, which restricts default collection to `tests/` regardless of `norecursedirs` — so the original intent of the tokens (skip top-level `data/` / `Assets/` / `combat_lab/`) is already covered. Removing the tokens is sufficient and avoids the relativity foot-gun. |
| 2026-05-18 | **Bundle 4 PROJ-436 deferred items in Phase 5 rather than spawning four follow-up projects** | PROJ-436's consults (Phase 3 finding (d) + (e), Phase 5 D2 caching, Phase 6 mock residue) each flagged a small, focused cleanup. Spawning four PROJ-44X projects would inflate project-management overhead. One umbrella phase here, with each sub-item as its own commit, is cleaner. |
| 2026-05-18 | **Phase 0 baseline ledger is mandatory** | PROJ-436 Phases 3/4/5/6/7 touched code that hidden tests cover (cargo manager API, Planet storage, Empire pool aggregation, protocols, transfer validator). The Phase 2 baseline of 65 failures is stale by 7 commits. Phase 0 captures the actual current state before any triage work begins. |
| 2026-05-18 | **Phase 5d (D2 large-empire profiling) only executes on a real perf signal** | Phase 5 D2 of PROJ-436 documented that `Empire.resource_pool` aggregation is net-zero cost vs the pre-PROJ-436 baseline (the deleted `_fleet_resource_pool.items()` summand was always an empty dict). No production stress test has shown a hot path. If a real signal emerges during this project or later, add caching with explicit invalidation per the PROJ-293 pattern. Otherwise document and close. |
| 2026-05-18 | **Regression guard in Phase 4 is an AST-level collection check, not a count check** | Asserting "sharded suite collects N tests" is brittle (every test addition changes N). Asserting "every `test_*.py` file under `tests/` is in the collection set" is structural and stable. |
| 2026-05-18 | **No worktrees; serial execution in main checkout** | Per user `feedback_no_worktrees.md`. PROJ-436 Phases 8/9 may run in parallel branches that the user manages on different machines; this project stays on `main`. |
| 2026-05-18 | **End-of-project Codex consult is Phase 6** | Per user `feedback_consult_at_project_end.md`. Same pattern as PROJ-436. |
| 2026-05-18 | **Phase 4 regression guard scope is "no hidden test files" (file-level), not "no collection drift" (general)** | Per Codex pre-execution consult: a structural file-level guard catches every flavor of `norecursedirs`/`--ignore`/`testpaths`/`python_files` regression that drops a `test_*.py` from collection. It does NOT catch function-level drift via `python_functions`/`python_classes`/collection hooks (e.g., a hook that silently filters out a class of tests inside a still-collected file). We accept this scope limit — that's a different class of bug than PROJ-443 found, and a function-level guard would require more invasive instrumentation. |
| 2026-05-18 | **Phase 4 also updates `docs/guides/testing_infrastructure.md`** | Per Codex pre-execution consult: lines 187-194 of that doc embed a `pytest.ini` snippet showing the current `norecursedirs` value. Phase 4 refreshes the snippet and adds a brief rationale line about why the three tokens were removed. Bound the touch — don't over-scope into a doc refresh. |
| 2026-05-18 | **Phase 4 documents a one-time `.testmondata` rebuild** | `[unverified]` Testmon's official docs don't strictly mandate a clean rebuild after collection-membership changes, but a one-time `rm .testmondata && pytest tests/ --testmon` is the safe default and avoids stale-state surprises after the ~126-test collection growth. Phase 4 adds the recommendation in `decisions.md` and the project plan; actual rebuild is a local op each contributor runs as needed. |
| 2026-05-18 | **PROJ-431 citation chain corrected** | Per Codex pre-execution consult: the original charter draft pointed `design.md` link at `../PROJ-431/decisions.md`, but PROJ-431 has been archived to `Projects/archived_projects/PROJ-431/` (not active). Additionally, the archived `PROJ-431/decisions.md` does not mention `test_cargo_tracking` directly. The real provenance trail for "30 cargo-tracking failures were already flagged" is in [Projects/active_projects/PROJ-436/plan.md:34-37](../PROJ-436/plan.md) and the post-435 discussion at [arc01_001_claude_to_codex.md:56-61](../../../AgentCoordination/Scratchpad/Discussion/20260517T230029Z_post-435-project-creation/arc01_001_claude_to_codex.md). Citations corrected. |
| 2026-05-18 | **`ShipThemes` token retained in `norecursedirs`** | Per directory audit `find tests -type d -name ShipThemes`: no matches. The token is harmless. Removing it provides no benefit and the directory `assets/ShipThemes/` still exists at top level (though `testpaths = tests` would already skip it). Keep the token to minimize the config diff and avoid future-surprise if a `ShipThemes` directory ever appears under `tests/`. |
| 2026-05-17 | **03c execution protocol dropped; project runs serially on `main` per legacy 03a** | First execution session of PROJ-443 found `phase_state.json` missing canonical schema fields (`schema_version`, `project_id`, `project_branch`, `reviews`), causing `phase_dag.py` / `phase_complete.py` / `pending_reviews.py` to fail with `ValueError: unsupported schema_version: None`. Same bug present in PROJ-436 and PROJ-437 state files; only PROJ-438 has the canonical shape. Rather than repair the state file inline (scope-creep into a scaffolder bug affecting multiple projects), user direction was to drop 03c on this project and work serially on `main` per legacy 03a — which matches the project's already-documented "no worktrees, serial on `main`" preference (`feedback_no_worktrees.md` row above). `phase_state.json` retained as a reference but is not authoritative; `plan.md` Execution Protocol marker changed from `03c-phase-aware-execution` to `03a-continue-working`. The scaffolder repair is a separate concern, not in scope here. |
| 2026-05-17 | **Phase 0 baseline numbers refined; plan estimates partially off** | Direct-invocation pytest runs across all 6 hidden directories at HEAD `42ac82eec` produced **75 failures across 1952 tests** in 126 hidden files. Key deltas from plan estimates: Phase 1 (`test_cargo_tracking.py`) **30 actual** vs "~30 expected" — exact match. Phase 2 (`test_mutator_boundary_ast_guard.py`) **4 actual** vs "~9 expected" — PROJ-436 protocol consolidation already trimmed most drift. Phase 3a strategy/data long-tail **33 actual** vs "~26 expected" — `test_build_queue_source.py` dominates with 19 failures (likely PROJ-436 build-queue surface drift), `test_fleet_consumable_aggregator.py` 9 (cargo manager API parity). Phase 3b combat_lab **0 actual** vs plan-stated risk "may surface its own cluster" — confirmed no-op for combat_lab. Phase 3c small-dir **8 actual** (7 in `tests/unit/data/test_data_validation.py`/`test_test_infrastructure.py` — formation-file/utility-script naming drift; 1 in `tests/unit/assets/test_asset_manager_resolutions.py` — MagicMock signature drift). Post-flip projection corrected: plan said "~21359+" but conflated file count (126) with test count; real delta is **+1952 tests → ~23185 post-flip**, conservative band 23110–23185. Sharded visible baseline confirmed at exactly 21233 — no drift since PROJ-436 Phase 7. |
