# PROJ-372: Decisions Log

> **LOG ALL DECISIONS HERE**
> When you make a design choice or the user specifies a preference, add it to this table.
> Future agents will reference this to understand why things were done a certain way.

| Date | Decision | Rationale |
|------|----------|-----------|
| 2026-05-05 | Project initialized as PROJ-372 from `AgentCoordination/Scratchpad/reviews/strategy_layer_tech_debt_2026-05-05.md` item #5. | Galaxy/Planet/Star god-classes (2,126 LOC) ranked #5 on the strategy tech-debt review. PROJ-86/87/88/89 deliberately deferred them. |
| 2026-05-05 | Use facade/delegate pattern, NOT a full split into separate packages or a dataclass-only ECS. | Preserves the existing import surface (54 reader files across `galaxy.systems` / `planet.populations` / `star.luminosity`); matches the predecessor pattern from PROJ-86/87/88/89. Alternatives C and D rejected in design.md. |
| 2026-05-05 | PROJ-86/87/88/89 are predecessors, NOT superseded. PROJ-372 is the bounded follow-up for the three classes they deliberately deferred. | Verified in `Projects/deep_archive/PROJ-051-100/PROJ-{86,87,88,89}/plan.md` — none of the four "Out:" sections mention Galaxy / Planet / Star, and all four shipped without touching them. |
| 2026-05-05 | **Sequencing (initial recommendation): PROJ-372 ships independently and BEFORE PROJ-370.** | Original architect recommendation; superseded by 2026-05-06 row below. |
| 2026-05-06 | **Sequencing (FINAL, Codex+Claude joint review): PROJ-370 first; ALL of PROJ-372 follows.** | Phase 0 creates `game/strategy/data/galaxy_protocols.py` (`IHabitabilityCalculator`, `IStockpileHolder`, `IStagingYardHolder`, `IGalaxySystemGraph`, `IGalaxySpatialQuery`) and modifies `game/context.py` habitability accessors — those are exactly the contract surfaces PROJ-370 should own. Splitting Phase 0 to enable Star (Phase 1) parallelism with PROJ-370 was rejected as marginal benefit. Open question Q1 in design.md resolved. |
| 2026-05-05 | Introduce `GalaxyState` dataclass as the shared mutable-state owner; services receive `GalaxyState` by reference, NOT `_galaxy: Galaxy` back-pointer. | Today the four PROJ-173-Phase-2 delegates hold `_galaxy: Galaxy` back-references that force `if TYPE_CHECKING: from galaxy import Galaxy` in 4 files and require constructing a real `Galaxy()` (which loads naming registries from disk) to unit-test a delegate. `GalaxyState` is a 5-minute extraction that pays for itself in test cleanliness. Alternative H rejected in design.md. |
| 2026-05-05 | Service naming follows the `*_service.py` containing class `*Service` convention already established in `game/strategy/services/`. | Verified — `planet_economy_projector.py`, `cargo_transfer_service.py`, `empire_economy_service.py`, etc. New services: `planet_query_service.py`, `planet_habitability_service.py`, `galaxy_pathfinding_service.py`, `intercept_calculator.py`. Open question Q8 in design.md. |
| 2026-05-05 | `_kelvin_to_rgb`, Wien's law constants/helpers, Stefan-Boltzmann luminosity helper, `_WAVELENGTHS` move to `game/core/spectrum_math.py`. | Pure math with zero domain knowledge — `core/` is the correct architectural layer (mirrors PROJ-257's placement of `FormulaEvaluator` in `core/`). Open question Q4 in design.md. |
| 2026-05-05 | `_map_solar_radius_to_hex_radius` STAYS in `generation/star_generator.py` (does NOT move to `core/`). | It encodes a game design choice (compact remnants → 1, supergiant → 6) using hex-radius semantics specific to the strategy layer's multi-hex-zone system. Borderline; choosing the less-pure side. Open question Q5 in design.md. |
| 2026-05-05 | `Spectrum` dataclass moves to its own file `game/strategy/data/spectrum.py`; `Star` dataclass stays in `stars.py`; `StarGenerator` moves to `game/strategy/generation/star_generator.py`. | `stars.py` today is 770 LOC of dataclass + dataclass + generator + math + serde. Splitting along well-known seams gets four files all under their LOC ceilings. |
| 2026-05-05 | `StarSystem` and `WarpPoint` STAY in `galaxy.py` (do NOT move to their own file). | They are tight, focused, ~150 combined LOC. Moving them adds churn for marginal benefit. **Deferred to Phase 3 implementer** if `galaxy.py` cannot fit ≤ 350 LOC after services are extracted. Open question Q2 in design.md. |
| 2026-05-05 | `Planet` keeps all 47 dataclass fields exactly verbatim (save format invariant). | Risk R1 in design.md. Phase 5 includes a "load 5 fixture saves" round-trip equality check. |
| 2026-05-05 | `Planet._cached_habitability_multiplier` and `_cached_multiplier_turn` (PROJ-285 transient cache fields) STAY on `Planet` as dataclass fields with `init=False, repr=False, compare=False`. The `PlanetHabitabilityService` reads/writes them; it holds no cache state of its own. | The cache is logically part of the planet's per-turn lifecycle; relocating it to a service would require a service-side dict keyed by planet identity. Cleaner to keep cache where it is. Risk R4 in design.md. |
| 2026-05-05 | `PlanetHabitabilityService` is injectable via `ApplicationContext` (PROJ-258 pattern). `Planet.get_cached_habitability_multiplier` becomes a 1-line facade calling `get_default_planet_habitability_service().get_cached(self, race_registry, turn)`. | Closes the user-visible "modders can't inject custom habitability calculators" complaint from the source review. Mirrors all 10 PROJ-258 service-bridge sites. |
| 2026-05-05 | Stockpile / staging / order methods STAY on `Planet` as 1-line methods that conform to (new) `IStockpileHolder` / `IStagingYardHolder` protocols. The protocols are DEFINED in PROJ-372; their CONTRACT TESTS (capacity invariants, resource conservation) are PROJ-370's scope. | PROJ-372 closes god-class status; PROJ-370 enforces invariants. Don't conflate. Risk R8 in design.md. |
| 2026-05-05 | Pathfinding shims in `pathfinding.py` (`find_path_deep_space`, `find_path_interstellar`, `find_hybrid_path`, `project_fleet_path`, `calculate_intercept_point`, `find_nearest_system`, `get_system_at_hex`, `strip_start_hex`) become 1-line deprecated wrappers calling the new services. **Deleted at Phase 5 close.** | 82 call sites across 15 files use these as free functions. Phased deprecation lets the migration happen in chunks; the AST guard at Phase 5 close confirms zero remaining callers before the wrappers are removed. Open question Q7 in design.md. |
| 2026-05-05 | AST guard tests use a custom AST walker, NOT `radon`. | `radon` measures cyclomatic complexity; PROJ-372 needs LOC ceilings + "no non-facade method body > 5 LOC on Galaxy/Planet/Star" + "no direct mutation of `GalaxyState` private indexes outside services" — all bespoke checks. Open question Q6 in design.md. |
| 2026-05-05 | Save format unchanged — old saves load bit-identically. | Goal G9. The 47 Planet fields, 13 Star fields, 9 Spectrum fields, all `intrinsic_abilities` dicts, `_next_planet_id` / `_next_fleet_id` counters preserved. Phase 5 round-trip equality test. |
| 2026-05-05 | Perf budget: pathfinding + spatial query + habitability lookup within ±5% of pre-PROJ-372 baseline. | Risk R3 in design.md. Indirection cost is ~1 attribute access per O(1) lookup; expected to be well within tolerance, but reasserted in Phase 5. |
| 2026-05-05 | Phase ordering: Star first (Phase 1), then Planet (Phase 2), then Galaxy queries (Phase 3), then Galaxy algorithms (Phase 4). Phase 0 establishes the facade-template + AST scaffolding before any extraction begins. | Star is the smallest of the three (single-file split, no external state); good warm-up. Planet has the most external readers (57 occurrences across 20 files for `populations` / `stockpile` / `facilities` etc.) and benefits from Star's lessons. Galaxy queries (Phase 3) and algorithms (Phase 4) depend on `GalaxyState`, which Phase 3 establishes. |
| 2026-05-05 | Phase 5 covers AST guards + perf bench + doc updates + final cleanup; functions as the audit gate before user verification. | Mirrors PROJ-367's final-phase pattern. |
| 2026-05-05 | Plan reflects 03c-phase-aware-execution opt-in. | `**Execution Protocol:** 03c-phase-aware-execution` marker in plan.md per `Projects/protocols/03c_phase_aware_execution.md` config. |
| 2026-05-06 | Codex+Claude joint review: applied r004 edits — see `AgentCoordination/Scratchpad/Discussion/20260506T024652Z_strategy-tech-debt-plans-review/outcome.md`. | E372-1..4: Sequencing flipped — PROJ-370 first, ALL of PROJ-372 follows (no Star parallelism). Phase 0 creates `game/strategy/data/galaxy_protocols.py` (`IHabitabilityCalculator`, `IStockpileHolder`, `IStagingYardHolder`, `IGalaxySystemGraph`, `IGalaxySpatialQuery`) and modifies `game/context.py` habitability accessors that PROJ-370 must define first. Quick Status table now declares `**Depends on:** PROJ-370 verified` on every phase including Phase 0. Q1 marked Resolved. Original 2026-05-05 sequencing entry preserved above for history. |
