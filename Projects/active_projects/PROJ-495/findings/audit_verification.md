# PROJ-495 Codex Audit Verification (2026-05-23)

Consult leaf: `AgentCoordination/Scratchpad/Consult/20260523T152252Z_audit-PROJ-495/`
Codex response: `AgentCoordination/Scratchpad/Consult/20260523T152252Z_audit-PROJ-495/response.md`

| id | verdict | evidence | action |
|----|---------|----------|--------|
| F1: T3.3 partially false drop | VERIFIED + IN-SCOPE | `tests/unit/strategy/data/test_squadron_characterization.py:125-171` — verified read of the 4 round-trip tests after the default-fields case: `test_round_trip_with_battle_role_enum` (:125-134), `test_round_trip_with_combat_policy` (:136-147, multi-attr nested), `test_round_trip_preserves_spatial_behavior_when_set` (:149-160), `test_round_trip_preserves_flagship_id` (:162-171). Three of them — battle_role, spatial_behavior_when_set, flagship_id — share the identical shape `Squadron(name=, node_id=, <kwarg>=value); restored = Squadron.from_dict(original.to_dict()); assert restored.<attr> == value`. The combat_policy case has nested-attr assertions (`policy.targeting`, `.movement`, `.retreat`) and remains heterogeneous. | Phase 5: parametrize the 3 single-attribute roundtrip tests on `(kwarg_name, kwarg_value, attr_path)`. Keep combat_policy distinct. |
| F2: T2.12 — 7 missed-caller cleanups in test_resupply_engine.py | VERIFIED + IN-SCOPE | `tests/unit/strategy/engine/test_resupply_engine.py:560-561`, `:589-590`, `:621-624` (two sites), `:666-667`, `:713-714`, `:743-744` — each is the pattern `empire = MagicMock(); empire.fleets = [fleet]`. The canonical helper `make_mock_empire(fleets=...)` at `tests/unit/strategy/engine/conftest.py:21-52` supports this exactly. The file already imports the helper as `_make_mock_empire_canonical` at `tests/unit/strategy/engine/test_resupply_engine.py:97-101` (used by the colony-side `_make_empire` wrapper); the fleet-side tests bypass it. | Phase 5: extend the file's local `_make_empire` wrapper to accept a `fleets=` kwarg (or add a sibling `_make_fleet_empire(fleets)` helper), then replace the 7 ad-hoc MagicMock empire stubs with the helper call. |
| F3: `_assert_roundtrip_property` orphan helper | VERIFIED + OUT-OF-SCOPE | `tests/conftest.py:380-392` defines the helper; repo-wide grep finds zero call sites. Codex's audit confirms PROJ-479 introduced it but PROJ-495 T3.28 did not adopt it. The implementer's choice of inline parametrize for T3.28 was defensible (the helper has no comparator hook, and ship_serialization needs `tuple(...)` color normalization at lines `:147,:268,:281,:292`). Cleaning up the orphan requires either deleting PROJ-479's deliberate addition or redesigning its API — both belong to PROJ-479, not PROJ-495. | Log via `/claude-di-log` (or equivalent) with cross-reference to PROJ-479 DUP-003. Do NOT include in Phase 5. |
| F4 (clean): no scope leak | CONFIRMED | Touched set is test-only and within `tests/unit/{simulation,strategy,modifiers}/` + `tests/regression/`. Matches plan.md Scope. | None. |
| F5 (clean): CAT-11 join_fleet_handler relaxation per-plan | CONFIRMED | `tests/unit/strategy/engine/order_handlers/test_join_fleet_handler.py:217-259` still pins load-bearing fields/values rather than collapsing to a smoke check. Matches `phase_4_checklist.md:17-23`. | None. |
| F6 (clean): deleted facade meta-test did not lose gate coverage | CONFIRMED | Gate coverage still lives at `tests/unit/strategy/engine/test_order_processor_no_legacy_helpers.py:22,47,55` and `tests/unit/strategy/engine/order_handlers/test_handler_registry_completeness.py:20`. The deleted test was only `hasattr`-style import meta-coverage. | None. |
| F7 (clean): T3.10 (planet_energy) drop | CONFIRMED CORRECT | Four cited tests in `tests/unit/strategy/engine/test_planet_energy_engine.py:208-267` have materially different state shapes (generator+battery, near-capacity-clamp, battery-only, activation-state shield drain). Codex agrees a single param table would need per-case builders. | None. |
| F8 (clean): T3.13 (fleet_pursuer_tracker) drop | CONFIRMED CORRECT | Three cited tests in `tests/unit/strategy/fleet/test_fleet_pursuer_tracker.py:59-110` assert different invariants (order rewrite / transfer to new tracker / source clearing). Codex agrees param is not clearly better. | None. |
| F9 (clean): T3.28 inline parametrize defensible | CONFIRMED CORRECT | `tests/conftest.py:380-392` shows no comparator hook; color comparison needs `tuple(...)` normalization. Local `(field, compare)` parametrize at `tests/unit/simulation/entities/test_ship_serialization.py:337-355` is reasonable. | None — though F3 above is the residual debt this exposes. |

## Decision

- 2 in-scope findings → spawn Phase 5 remediation (~5 min LLM time, well under 15-min threshold).
- 1 out-of-scope finding (F3) → log via `/claude-di-log` after Phase 5 completes.
- 6 confirmations of correct judgment — no action.
