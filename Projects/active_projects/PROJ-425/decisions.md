# PROJ-425: Decisions Log

> **LOG ALL DECISIONS HERE**
> When you make a design choice or the user specifies a preference, add it to this table.
> Future agents will reference this to understand why things were done a certain way.

| Date | Decision | Rationale |
|------|----------|-----------|
| 2026-05-16 | Project initialized | Strategy tech-debt #04/10 — TD-06 ShipInstance slimming. `ship_instance.py` is 845 LOC with ~50 thin forwarders and three substantive in-class blocks; the existing delegates (`ShipConsumableManager`, `ShipCargoManager`, `ShipDisplayFormatter`, `ShipInstanceBridge`, `ShipInstanceSerializer`, `ShipInstanceWriteService`) already exist and are healthy — this is mostly migration + demolition, not redesign. |
| 2026-05-16 | Adopt 03c-phase-aware-execution protocol | Matches the TD-06 plan's six-phase TDD extraction and gives the project a phase-state contract that the validate/phase-complete scripts can enforce. |
| 2026-05-16 | Extract one responsibility at a time per TDD (Phase 1 = stats, Phase 2 = inspector, Phase 3 = factory, Phase 4 = writes) | TD-06's Phased Remediation Plan is explicit about extracting one block per phase with focused tests in between. Doing more than one extraction per phase makes regression triage hopeless if any focused suite goes red. |
| 2026-05-16 | Forwarder demolition runs in sub-batches with grep gates between batches, not as one sweep | TD-06 §"Phase 5 — Remove forwarders in controlled sub-batches" mandates this. A single-edit demolition would invariantly miss caller migrations and force a rollback. |
| 2026-05-16 | Sub-batch order: 5a (display) → 5b (consumable) → 5d (serializer) → 5e (bridge) → 5c (cargo/deployable, gated) | The TD-10-independent sub-batches run first so that as much of the demolition as possible lands without waiting on PROJ-431. 5c is split out into its own project phase (Phase 6) to make the cross-project gate unmissable. |
| 2026-05-16 | Cargo/deployable forwarder demolition deferred behind [PROJ-431](../PROJ-431/plan.md) Phase 1 | TD-06 §"Ordering Constraints" + [EXECUTION_ORDER.md](../../../Reviews/results/2026-05-16_strategy-layer-tech-debt-review/Verified%20Problem%20Remediation%20Plans/EXECUTION_ORDER.md) §"Phase Gates" — migrating cargo callers before TD-10's typed `bay_inventory` exists would force a double-migration (today's untyped accessor first, then again post-TD-10). The cross-project gate avoids that waste. |
| 2026-05-16 | Preserve `ShipInstance.create(...)` as a thin shim through Phase 3 (and beyond, if grep still finds callers) | TD-06 Weak-LLM Guardrail #1 — do not remove `create` / `to_dict` / `from_dict` / `to_ship` / `clone` until grep proves their callers were migrated. Phase 3 *extracts the body* but does *not* migrate every call site; the shim stays as long as `rg -n "ShipInstance\.create\(" game tests` is non-empty. |
| 2026-05-16 | Stats calculation moves out before `_cached_stats` storage moves | TD-06 Weak-LLM Guardrail #2 — splitting these into separate phases (logic in Phase 1, storage move deferred or skipped) keeps cache-invalidation regressions debuggable. Moving both in one phase risks silent cache breakage. |
| 2026-05-16 | Extend [`component_inspector.py`](../../../game/strategy/services/component_inspector.py) rather than creating a second inspector module | TD-06 Weak-LLM Guardrail #4 — avoid duplication. Only split if extension would breach the 500-LOC ceiling on `component_inspector.py` itself; verified at the start of Phase 2. |
| 2026-05-16 | Manager / accessor name standardization happens *inside* Phase 4 prep, before any caller migration through the write service | TD-06 Verification Summary explicitly flags that `_cargo_manager` / `_consumable_manager` may not exist with those names on live `ShipInstance` instances. Phase 0 captures the actual names by grep; Phase 4 reconciles them with the write service before broader migration. |
| 2026-05-16 | Use existing `tests/unit/strategy/ship_instance/` modules for Phase 0 characterization; do not invent a parallel test layout | TD-06 §"Phase 0" + Weak-LLM Guardrails are explicit: the focused test modules already exist (`test_convenience_methods.py`, `test_component_toggles.py`, `test_capacity_levels.py`, `test_registries_di.py`, `test_ship_instance_bridge.py`, `test_ship_instance_serializer.py`, `test_serialization.py`, `test_cost_queries.py`). Adding a new characterization file would fragment the test surface. |
| 2026-05-16 | `phase_state.json` records cross-project dependency on PROJ-431 Phase 1 only via project status (`Blocked` row in plan.md + body text in `phase_6_checklist.md`), not via `depends_on` field | `phase_state.json` `depends_on` is intra-project. The cross-project gate is recorded in three other places (plan.md Dependencies, plan.md phase row marker, phase_6_checklist.md Depends on line) so it cannot be missed even if `phase_state.json` is consulted alone. |
| 2026-05-17 | Phase 1: extract stats logic into new `game/strategy/data/ship_stats_cache.py` rather than extending an existing module | No existing module is a natural home: `calculate_design_stats` lives in `game.simulation.entities.ship_design_stats` (simulation layer, can't depend on strategy), and the registry-DI plus cache-invalidation behavior is strategy-side. New module is ~30 LOC. Co-located with the entity in `data/`. |
| 2026-05-17 | Phase 5 scope reduction: only 5a (display) fully demolished; 5b/5d/5e kept as documented thin shims | 5d (serializer) + 5e (bridge) entries are explicitly listed in TD-06 Weak-LLM Guardrail #1 as "do not remove until grep proves callers migrated" — grep shows ~18 (serializer) + ~10 (bridge) live production callers. 5b (consumable) has ~80 tests that mock the entity methods directly (`ship.consume_resource = Mock(...)`); the demolition would require parallel mock migration that exceeds the slimming benefit. Per TD-06 plan: "if any high-value entry point still has many live callers, leave it as a documented thin shim rather than forcing a risky all-callers migration." All four shim groups now carry inline comments labeling them as 5b/5d/5e-deferred. |
| 2026-05-17 | Phase 4 rename direction *reversed* after grep audit: keep entity names `_cargo_mgr` / `_resource_mgr` / `_display_fmt` / `_bridge`; update the write service to query these | Initial Phase-0 decision proposed renaming the entity. Mid-Phase-4 grep showed ~50+ production + test callers using `_cargo_mgr` short names (production: `transfer_validator`, `issuer_adapter`, `fleet_dto`, `transfer_branches`, `production_spawner`, `fighter_reboard`; tests: many FMS integration suites). Renaming the entity would have meant migrating 50+ sites in one phase; renaming the dead-code write-service references was 2 edits. The write service was the broken side (its `_cargo_manager` / `_consumable_manager` getattr lookups returned None today — dead branch). |
| 2026-05-17 | Add Phase 7 from Codex consult: label 5d/5e shims explicitly (comment-only) | Codex consult on the shipped PROJ-425 work flagged that the 5d serializer and 5e bridge forwarder groups in `ship_instance.py` lacked the explicit "retained shim" comment block that the 5b consumable group already had — despite this `decisions.md` row claiming all four shim groups were labeled (Phase 5 entry). Phase 7 lands the missing comment blocks (matching the 5b format: why retained, canonical delegate, removal condition). Comment-only — no behavior change. Ran independently of the still-gated Phase 6 because it only depends on Phase 5's outcome (5d/5e forwarders intact). |
| 2026-05-17 | Spin out component_inspector split into PROJ-433 (not a PROJ-425 phase) per Codex's explicit recommendation | Codex's consult noted that `game/strategy/services/component_inspector.py` is 537 LOC after PROJ-425 Phase 2 added the layer-view helpers — above the 500-LOC convention but not large enough to reopen PROJ-425. Clean split point: ability iteration helpers vs layer-view helpers (`component_inspector.py:404-501`). PROJ-433 scaffolded today; execution deferred to a separate run. |
| 2026-05-17 | Tooling gap noted in findings_ledger (not a tech-debt item) | Codex also flagged that `Projects/scripts/validate_phase.py` (lines 85-154) and `Projects/scripts/phase_complete.py` (lines 194-247) do not read `cross_project_blockers` from `phase_state.json`. PROJ-425 Phase 6's gate to PROJ-431 Phase 1 is documented in plan.md / phase_6_checklist.md / phase_state.json but is not enforced by tooling — a future agent could bypass it. This is project-system infrastructure (not strategy-layer tech debt) so the gap is recorded in `findings_ledger.md` for separate project-system work rather than absorbed into PROJ-425's scope. |
| 2026-05-17 | **Phase 6 unblocked — PROJ-431 Phase 1 typed `bay_inventory` shipped on `proj/PROJ-431/main`** | PROJ-431 Phase 1f completed 2026-05-17 (commit on `proj/PROJ-431/main`). `ShipInstance.bay_inventory: BayInventory` is now the canonical typed storage; `ShipInstance.carried_items` is no longer a dataclass field (only a backward-compatible test-shim property survives); `CarriedVehicle.from_any(...)` is deleted. The cargo / deployable forwarder demolition (TD-06 batch 5c) can now run against the stable typed surface (`bay_inventory.bay` / `bay_inventory.pods` / `ShipCargoManager.load_vehicle` / `unload_vehicle`). Phase 6 status flipped from `Blocked` to `Eligible to start` in `plan.md`. |
