# PROJ-435: Decisions Log

> **LOG ALL DECISIONS HERE**
> When you make a design choice or the user specifies a preference, add it to this table.

| Date | Decision | Rationale |
|------|----------|-----------|
| 2026-05-17 | Project initialized as a PROJ-429 Phase 8 spin-off | Codex consult finding 4 (PROJ-429 Phase 8) flagged `_ACTIVATABLE_ABILITIES` in `stat_rows_dynamic.py:381-463` as a hardcoded literal set, but the UI map has UI-specific structure (display labels + 2 abilities not in the registry) that prevents a mechanical inline migration. Spun off rather than forcing migration in PROJ-429. |
| 2026-05-17 | Phase 1 must decide registry-extension vs UI-side label map (Options A/B/C in design.md) | The two viable migrations make different layering trade-offs; the call is not pre-determined by existing patterns. |
| 2026-05-17 | Policy: **hybrid C+B** — register `GravityModifier` and `RadiationShield` in `AbilityMetadataRegistry` with the `ENERGY_DRAINING` kind tag and an `EnergyFacet`, then iterate `abilities_with_kind_tag(ENERGY_DRAINING)` from the UI while keeping display labels in a UI-side label dict. | (1) Both abilities are genuine strategy-layer activatable energy-draining planet abilities (verified: they appear in `ABILITY_REGISTRY` at `game/simulation/components/abilities/__init__.py:187,189`, in `data/components.json:3855,3903`, and are queried by `planet_modifier_effect_engine.py:67,84` via `_has_active_ability`). Their omission from `AbilityMetadataRegistry` was an oversight, not a design choice. (2) `EffectFacet.display_name` is a per-facet field, not a top-level UI label — extending the dataclass with a generic UI-display field would push a UI concern into the strategy registry for negligible gain (one UI screen consumes these labels). (3) The `_SUPERWEAPON_LABELS` precedent at `stat_getters.py:307-314` already shows the project pattern: iterate from a strategy-layer registry; keep display labels in a UI-side dict. After registration, `abilities_with_kind_tag(StrategicKind.ENERGY_DRAINING)` returns exactly the six names the UI currently shows — zero behaviour change. |
| 2026-05-17 | Policy for `modifier_abilities` dict: iterate the union `COMBAT_MODIFIER ∪ BUILD_RATE_BOOSTER ∪ RESOURCE_BOOSTER` and filter to abilities that have a UI label in the local label dict. | The current dict shows 4 abilities (ShieldModifier, DamageModifier, BuildRateBooster, ResourceHarvestBooster). The union tag set covers them plus `ThrustModifier` (COMBAT_MODIFIER) and `QualityImprovement` (RESOURCE_BOOSTER) which the UI does NOT show today. Out-of-scope says "Behaviour changes to which abilities are shown" — so we filter by UI-label presence. This eliminates the hardcoded name-set (membership is now registry-driven via kind tags + intersection with UI label dict). |
| 2026-05-17 | Phase 2 manifest: extend `game/strategy/services/ability_metadata.py` to register 2 new entries; rewrite `_ACTIVATABLE_ABILITIES` and `modifier_abilities` usage in `stat_rows_dynamic.py` to iterate from registry; add regression test asserting no hardcoded membership literals remain. | Keeps blast radius minimal. The registry change is additive (no signature change, no behaviour change for existing consumers). |
