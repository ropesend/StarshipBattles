# PROJ-291: Decisions Log

> **LOG ALL DECISIONS HERE**
> When you make a design choice or the user specifies a preference, add it to this table.
> Future agents will reference this to understand why things were done a certain way.

| Date | Decision | Rationale |
|------|----------|-----------|
| 2026-04-18 | Project initialized from dual cross-project audit of PROJ-283..290 | Two independent reviews (mine + the user's prior 5-skeptic audit in `Temp Review Docs/`) converged on 3 Critical findings. Two impartial subagents settled the disagreements. User confirmed all three design choices via AskUserQuestion. |
| 2026-04-18 | Two-project split: PROJ-291 (Critical) + PROJ-292 (High/Major/Minor) | User answered Q4. Sharper sign-off gating — Criticals block PROJ-283..290, the rest are quality cleanups that can land independently. |
| 2026-04-18 | C3 fix approach: full IRaceRegistry retrofit on HappinessEngine + PopulationEngine (not the bandaid `return None` on mismatch) | User answered Q1, choosing Option (b). Per CLAUDE.md Rule 3, the proper fix is to wire the registry that PROJ-287 built. The single-line bandaid would leave the original sin (engines blind to multi-species) in place. The retrofit reverses PROJ-287's decisions.md line 16 deferral — log the reversal as a forward-link in PROJ-287/decisions.md. |
| 2026-04-18 | C3 fix preserves backward-compat: `race_registry: Optional[IRaceRegistry] = None` kwarg with None-fallback path | Mirrors PROJ-285's already-shipped pattern on `HarvestingEngine`/`ProductionEngine`. Preserves 850+ pre-PROJ-285 MagicMock tests that don't pass a registry. Without this fallback the engine retrofit becomes a much larger PR with a higher regression risk. |
| 2026-04-18 | C3 fix tightens the legacy fallback: when registry is None AND `race_id != empire.race_config.race_id`, return `None` instead of returning the empire's primary race | The dual-return on lines 93-95 of happiness_engine.py is the actual bug. Even on the legacy path, returning the wrong race_config is incorrect. Returning None causes the species to be gracefully skipped (matches the existing `if race_config is None: return` early-out path). |
| 2026-04-18 | C2 fix approach: migrate FoodAllocationEditor to iterate `economy.population_consumption` (not the temporary `food_per_pop_per_turn` shim) | User answered Q2, choosing Option (b). PROJ-286's plan.md explicitly meant this migration ("PROJ-289's UI migration"); PROJ-289 didn't pick it up. Adding a shim now would be CLAUDE.md Rule 3 violation. The migration retires the `population_food_resource` shim as a free side-effect (auto-fixes prior-audit m4). |
| 2026-04-18 | C1 fix is a 1-line addition to `total_expenses` aggregation | The bug shape (data computed but silently dropped from summary) is identical to the PROJ-269/270 `_team_bonuses` bug. The fix is trivial; the test is what matters — `TestTreasuryTotalIncludesUpkeep` + `tests/integration/strategy/test_treasury_panel_e2e.py` together pin both the unit-level math AND the end-to-end render path so this can't recur. |
| 2026-04-18 | M3 prior-audit concern (Planet cache survival across `TurnStateSnapshot.restore()`) is CLEARED | Impartial subagent verified: `restore()` does `session.galaxy = Galaxy.from_dict(...)` which discards the old planet objects entirely. The `init=False` cache fields can never carry stale data across rollback because the receiving objects are fresh. No action needed. |
| 2026-04-18 | Audit reports moved to `findings/` rather than left in `Temp Review Docs/` | Per CLAUDE.md § Subagent Report Output, durable audit records belong in `Projects/active_projects/PROJ-XX/findings/`. The temp dir is the user's working location and can be deleted after they verify the archive. Phase 4 handles the move. |
| 2026-04-18 | Reverses PROJ-287/decisions.md line 16: "Do NOT migrate PopulationEngine._get_race_config / HappinessEngine._get_race_config to the new registry" | The deferral was made before multi-species colonies were a reachable game state. Post-PROJ-284/286/289 they are. PROJ-291 must update PROJ-287/decisions.md with a forward-link to PROJ-291's reversal so the historical context is preserved. |
