# PROJ-472: Decisions Log

> **LOG ALL DECISIONS HERE**
> When you make a design choice or the user specifies a preference, add it to this table.
> Future agents will reference this to understand why things were done a certain way.

| Date | Decision | Rationale |
|------|----------|-----------|
| 2026-05-20 | Project initialized | Starting point for Facade read-path migration: route game/ui access through strategy facade DTOs (deferred from PROJ-470) |
| 2026-05-20 | **Scope populated from PROJ-470 Protocol-06 revision.** FAC-001/FAC-002/FAC-003 (facade read-path migration) extracted here from PROJ-470. | Dual independent+Codex review (2026-05-20) found the facade read-path gap is a deliberately-deferred architecture migration (PROJ-382 / U1–U3), not a cleanup CRITICAL. Repo search confirmed 93 `game/ui/` files import `game.strategy` directly (`grep -rln "import game.strategy\|from game.strategy" game/ui/` → 93). A single-pass migration would balloon PROJ-470's conformance pass into a structural refactor. Per Protocol 07, the scope lives here as policy + read-path static guard + first migration slice; remaining sites migrate incrementally under the guard. |
| 2026-05-20 | Phase 1 scoped as **policy + read-path static guard + first slice**, NOT a 93-site single-pass migration. | Mirrors how the write-path guard (`tests/static_guards/test_facade_bypass_guard.py`) was rolled out. The first slice covers the densest FAC-002 sites (build_queue_screen, build_queue_controller, fleet_data_source) and the FAC-003 `StrategyScreen.session` consumers. The remaining ~85 sites are migrated in per-subpackage batches under the guard, decomposed into further phases/projects as scope demands. |
| 2026-05-20 | Read-path policy (option a read DTOs vs option b documented UI-safe read surface) is **deferred to Phase 1 Task 1** of this project — not pre-decided here. | The choice has real trade-offs (DTO surface/conversion cost vs convention+allowlist) and should be made by the implementing agent against live code, then recorded in Pattern #5 of `docs/02_PATTERNS.md`. PROJ-470 deliberately did not pre-commit the policy. |
| 2026-05-21 | **Policy = option (b):** documented UI-safe read surface enforced by static guard + exact allowlist + convention. NOT a blanket DTO layer. Recorded in Pattern #5 (`docs/02_PATTERNS.md`) — doc edit is a Phase 1A task, not done at planning time. | Confirmed with user. The facade is already a real read boundary for session-shaped queries (grouped namespaces, `BuildQueueSourceDTO`), but ~93 UI `game.strategy` imports include pre-session config/value/enum reads (`GameConfig`/`RaceConfig`/`ContainableKind`/`ActivationPhase`), render-hot reads, and editor surfaces that a blanket DTO rule would churn without boundary value (consult §1, verified live 2026-05-21). Use facade for session-owned/mutation-adjacent/cross-screen-cached reads; allow a documented UI-safe surface for immutable config/value/enums/protocols. Do NOT allowlist live traversal helpers (`BuildQueueSource`, `collect_build_queues_at_hex`) just because they are "read-only" — they return mutable owner refs / live lists. |
| 2026-05-21 | **Scope CAPPED at a contained Phase 1 (1A policy+guards / 1B build-queue cluster / 1C session-consumer cleanup).** The remaining ~75 files are NOT scoped here. | Per orchestrator decision + consult §4 stopping-point recommendation: the 93-file spread (57 screens-root, 15 panels, 4 battle_setup, 4 race_setup, 3 each strategy_render/strategy_windows/builder/galaxy_test, 1 widget — verified 2026-05-21) is too wide for one envelope. Capping mirrors how the write-path guard rolled out: policy → guard → first slice → incremental. |
| 2026-05-21 | **Deferred tail split into three follow-on project stubs, created and populated:** PROJ-474 (value/config UI-safe allowlist consolidation), PROJ-475 (remaining live strategy-screen + render readers + deprecation of `StrategyScreen` pass-through properties and `FacadeSessionState.session` public exposure), PROJ-476 (tooling/editor screens: battle_setup / galaxy_test / race_setup / builder). | Consult §4/§5 grouping. All three are **gated on PROJ-472's two guards landing** — they extend the allowlists / tighten the matchers PROJ-472 establishes. PROJ-474 is mostly doc + allowlist; PROJ-475/476 are code motion with the live-vs-tooling split per consult. |
| 2026-05-21 | **Guard strictness = pragmatic.** Two guards over all `game/ui/`, but pre-session config/value/enum types AND `StrategyScreen` pass-through properties (`galaxy`/`empires`/`systems`/`active_empire`/`human_player_ids`) are allowlisted-with-reason as documented TRANSITIONAL read surfaces. | Consult §1/§2 + risk "overstating read path closed" / "guard scope drift". The plan HONESTLY states Phase 1 tightens the read path (blocks net-new bypasses) but does NOT fully close it — pass-throughs + `FacadeSessionState.session` (`_facade_state.py:63-86`) + the ~75-file tail remain. Deprecating the pass-throughs is PROJ-475. Do not overclaim. |
| 2026-05-21 | **Two separate guards** (not one): (1) `test_facade_read_path_imports_guard.py` — runtime-import scan over `game/ui/**/*.py` that IGNORES `if TYPE_CHECKING:` and fails on non-allowlisted runtime `game.strategy.*` imports of live domain/session/query helpers; (2) `test_facade_read_path_session_guard.py` — matches `.session.<attr>`, `._session.<attr>`, AND `.facade_state.session.<attr>`. Both mirror the write-path guard incl. positive controls. | Consult §2. The `TYPE_CHECKING` exemption prevents pointless churn on `build_queue_controller.py:18-20` (type-only). The `facade_state.session` form is mandatory or the guard misses live bypasses like `strategy_build_queue_manager.py:82-83`. Allowlist by file+path+reason, not bare name. |
| 2026-05-21 | **Build-queue DTO: enrich `BuildQueueSourceDTO` ONCE, do not add an adapter layer or leave mixed representations.** Add `is_paused` + owner-derived SCALAR projection fields; do NOT expose live `owner_entity`. | Consult §3 + DTO-drift risk. Verified live (2026-05-21): callers read `source.owner_entity` (`build_queue_input_router.py:128,164`; `empire_build_queue_window.py:365,425`) and `source.is_paused` (`build_queue_input_router.py:174`), but `BuildQueueSourceDTO` (`build_queue_dto.py:6-42`) carries neither. Exposing the live `owner_entity` ref would re-leak the mutable domain object the DTO exists to hide, so project scalars instead. Finishing 1B with both domain `BuildQueueSource` and DTOs live in the cluster is explicitly disallowed. |
| 2026-05-21 | **Citation corrections applied (verified against live code 2026-05-21).** | (a) 93-file count still live. (b) `build_queue_controller.py:18-20` is TYPE_CHECKING-ONLY — stale as a "runtime bypass"; real coupling is class-level live-BQS use (`:66-79`,`:117-150`,`:421-522`). (c) `hex_outlines.py` lives at `game/ui/screens/strategy_render/hex_outlines.py` (not `game/ui/screens/`), active-empire read `:30`, turn read `:76-79`. (d) FAC-003 consumer list was incomplete: added `strategy_detail_formatter.py:278`, `list_windows.py:70`, and the `facade_state.session` bypass `strategy_build_queue_manager.py:82-83`. (e) Leak model incomplete: `StrategyScreen` pass-throughs (`:160-189`) + `FacadeSessionState.session` (`_facade_state.py:63-86`) persist after Phase 1. |
| 2026-05-21 | **Phase ordering: 1A → 1B → 1C (guards first, then cluster, then session consumers).** | Guards-first so 1B/1C migrations are enforced as they land (TEMPORARY allowlist entries removed file-by-file as each migrates). 1B before 1C because `strategy_build_queue_manager.py` has both a BQS import (1B) and a `facade_state.session` read (1C); migrating BQS first means 1C touches a file whose build-queue path is already DTO-based. |
| 2026-05-21 | **Determinism/save-compat: projection-only.** No serialized payload, domain-persistence, or save-format changes while touching readers. | Consult save/schema risk. The existing DTO pattern is safe precisely because it copies read data without changing owner objects (`build_queue_dto.py:24-41`). Render-hot reads (`hex_outlines`, `fleets`) keep existing caches; no per-frame DTO allocation introduced. |
| 2026-05-21 | **Post-flesh review Blocker 1 — 1C made honest about remaining session readers.** Migrate `strategy_render/fleets.py:85` IN 1C (1C.6); explicitly DEFER `strategy_event_router.py:223/368`, `strategy_screen_selection.py:93`, `empire_panel_ctrl.py:62`, `strategy_screen_order_editing.py:42` to PROJ-475 as allowlisted-with-reason (1C.7); rewrote the false "only pass-throughs + mutator seam remain" claim (1C.8). | Codex post-flesh review §3/§6: `phase_1c_checklist.md:85-87` overclaimed against live code. Verified live 2026-05-21: the deferred readers are real session reads; `facade.fleets.path_projection(fleet_id, max_turns)` exists (`grouped_namespaces.py:141-145` → `fleet_slice.py:96-103`) so `fleets.py` migrates now (NOTE the live call passes a `fleet` object → must pass `fleet.id`). Final 1C allowlist = pass-throughs + deferred readers + mutator write seams. Session-read guard MUST match full attribute chains (`facade_state.session.<...>`, `scene.session.<...>`) to catch/allowlist `strategy_build_queue_manager.py:82-84` and `strategy_event_router.py:223/368`. |
| 2026-05-21 | **Post-flesh review Blocker 2 — 1B owner_entity replacement + fleet-state read pinned.** Add DTO scalars `owner_global_hex: HexCoord \| None` (fleet `entity.location`; planet `system.global_location + entity.location`) and `owner_system_name: str \| None` (resolved via galaxy at slice projection), replacing all live `owner_entity` location/system reads in `empire_build_queue_formatter.py:79-114` + `empire_build_queue_window.py:363-374`. Fleet closeout in `strategy_build_queue_manager.py:226-276` reads BUILD-order state from `facade.fleets.get(fleet_id)` → `FleetInfo` (`orders`, `is_building`, `construction_queue_size`), NOT a live fleet. | Codex post-flesh review §2/§6. Verified live 2026-05-21: `FleetInfo` carries the needed fields (`fleet_dto.py:67-73`, populated `:221-227`); `facade.fleets.get` exists (`fleet_slice.py:60-65`). DTO `from_domain` cannot reach the galaxy, so the slice resolves the scalars. `_add_item_to_source` (`:425-436`) uses DTO `context_type`+`entity_id`, not the live entity. Full ~13-caller read set is satisfied with no live `owner_entity` left. |
| 2026-05-21 | **Post-flesh review Blocker 2 — `FleetCapabilityCalculator` late-imports DEFERRED, not migrated in 1B.** Three identical sites (`fleet_data_source.py:241-245`, `fleet_report_filters.py:163`, `:302`) stay import-guard allowlisted-with-reason; recorded in PROJ-475. | Codex review §6 flagged the additional `fleet_report_filters.py` sites. Verified live 2026-05-21: all three are `INTENTIONAL LATE IMPORT` of `FleetCapabilityCalculator.ship_has_spaceyard(ship)` — pure per-ship capability checks with no existing facade query. Migrating piecemeal in a build-queue phase has low boundary value; defer the cluster rather than invent a facade method out of scope. |
