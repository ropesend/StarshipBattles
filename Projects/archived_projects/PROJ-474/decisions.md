# PROJ-474: Decisions Log

> **LOG ALL DECISIONS HERE**
> When you make a design choice or the user specifies a preference, add it to this table.
> Future agents will reference this to understand why things were done a certain way.

| Date | Decision | Rationale |
|------|----------|-----------|
| 2026-05-21 | Project initialized | Starting point for Facade read-path: value/config UI-safe read-surface allowlist consolidation (follow-on from PROJ-472) |
| 2026-05-21 | Created + scoped as the **value/config allowlist** deferred tail of PROJ-472. **GATED on PROJ-472's import-guard landing.** | PROJ-472 caps scope at policy + guards + build-queue cluster + session consumers; the value/config allowlist consolidation is mostly doc + allowlist work and belongs in its own pass per consult §4. Do not start until `tests/static_guards/test_facade_read_path_imports_guard.py` and its UI-safe allowlist exist. |
| 2026-05-22 | **Gate cleared; plan fleshed to execution-ready.** PROJ-472's two read-path guards have landed and are green (import guard: 340 passed 2026-05-22). | Pre-flesh Codex consult (`AgentCoordination/Scratchpad/Consult/proj474_preflesh/advice.md`) + independent live-code verification confirm the gate is satisfied. |
| 2026-05-22 | **The real work is structural, not prose.** PROJ-472 already documented the UI-safe surface (Pattern #5) and parked the types in a `UISAFE`-**commented** block of a flat allowlist. 474 turns `UISAFE` into machine-checkable data + reconciles drift + promotes misfiled TAIL entries. | The current `UISAFE` is a comment, not an invariant; it has already drifted three ways (consult §0, verified live 2026-05-22): `VALID_GALAXY_TYPES` UISAFE-vs-TAIL split; `RaceConfig`/`RacePointBudget` documented-safe but parked in TAIL; `ComponentActivationState` in UISAFE block but absent from the doc. |
| 2026-05-22 | **Structure = symbol-level `_UISAFE_SYMBOLS: frozenset[(module, member)]`, NOT module prefixes.** Matcher allows facade write path OR `(module,member)∈_UISAFE_SYMBOLS` OR exact `(file,module,member)` in the transitional file-scoped set. | Consult §2 option (a). Module prefixes would wrongly bless mixed modules — `race_description_llm_controller` has a safe `FieldStatus` enum (`:47`) AND a live `RaceDescriptionLLMController` (`:90`); `economy_config` has a safe getter (`:135`) AND a mutating setter (`:143`). Transitional allowlist stays file-scoped because the same symbol (`RaceLibrary`, `DesignCatalog`) is safe in one file and live in another. |
| 2026-05-22 | **Two new invariant tests (TDD-first):** doc↔guard parity (`_UISAFE_SYMBOLS` == a parseable canonical token list embedded in Pattern #5) and no-misfile (no UISAFE symbol also in a transitional triple). | Consult §2.4/§2.5. Parity uses a fenced one-token-per-line block in the doc (NOT prose scraping) to avoid brittleness (consult §6). |
| 2026-05-22 | **Membership criteria pinned** (design.md §"Membership criteria"): pre-session/detached AND no live-object require/return AND enum/const/frozen-metadata-or-detached-config-or-static-loader AND no live owner refs. A pure helper that *takes* a live domain object is NOT UI-safe. | Consult §1. Canonical traps explicitly excluded: `calculate_habitability(planet,...)`, `project_fleet_position(fleet)`, `SectorEnvironment(local_hex, system)`. |
| 2026-05-22 | **Promote 12 detached value/config/enum/static-metadata symbols out of TAIL** (design.md table): `VALID_GALAXY_TYPES`, `RaceConfig`, `RacePointBudget`, `OrderType`, `PlanetType`, `BattleRole`, `CombatPolicy`, `get_default_economy_config`, `FieldStatus`, `StrategicKind`, `abilities_with_kind_tag`, `SUPERWEAPONS`. | Consult §3A, each verified live 2026-05-22. Membership is a property of the SYMBOL, not the file — so tooling files (galaxy_test/battle_setup/builder) may import these UI-safe enums while their live/generation imports stay TAIL for PROJ-476. |
| 2026-05-22 | **Open item 1 resolved: bless `ComponentActivationState`.** Add to `_UISAFE_SYMBOLS` AND the Pattern #5 token list. | It is a detached `@dataclass` of a phase enum + scalar tick counters (`component_activation_state.py:33-52`), no live session ref → meets the criteria. Closes the doc/guard drift in the safe direction. |
| 2026-05-22 | **Open item 2 resolved: DELETE the `EmpireEconomySnapshot` runtime import** (move under `TYPE_CHECKING`); drop its TAIL entry. Do NOT promote. | Used only as a type annotation in `empire_treasury_panel.py` (`:65`,`:301`,`:306`); the UI never touches it at runtime, so the right fix is removing the runtime import, not broadening policy (consult §3B). |
| 2026-05-22 | **Recommended execution order: PROJ-474 → 475 → 476.** | Consult §5. 474 first: cheap category stabilization removes false positives (the `VALID_GALAXY_TYPES` split) so 475/476 don't wade through category noise. 475 second: closes the live gameplay boundary (remaining live readers + pass-through deprecation). 476 last: its legitimate exemptions are only knowable once the live boundary is closed (per PROJ-476's own plan). |
| 2026-05-22 | **Post-flesh Codex review: READY-TO-EXECUTE, no blockers** (`AgentCoordination/Scratchpad/Consult/proj474_postflesh/advice.md`). Applied 3 corrections. | Review re-verified all 12 promotions as detached/safe at exact line refs, confirmed `EmpireEconomySnapshot` annotation-only + `from __future__ import annotations` present, and the structure as implementable. Corrections: (1) no-misfile test compares against ALL `_IMPORT_ALLOWLIST` triples not just TAIL (§5.4); (2) added an explicit TDD RED step in Task 1.4 so the no-misfile invariant is not vacuous against an empty `_UISAFE_SYMBOLS` (§4.2); (3) corrected the stale Pattern #5 range in plan.md to `:168-230` (§1.4). Review notes `planet_physics` constants / `RaceRandomizer` could be argued into scope later but keeping them in PROJ-476 is "a coherent policy choice, not a mistake" — no user decision required. |
| 2026-05-22 | **Scope CAPPED to one execution pass: categorization only.** No live-reader migration, no facade expansion, no tooling exemptions. | Consult §4. The only production edit is the `EmpireEconomySnapshot` `TYPE_CHECKING` move. Everything else is the guard test + the doc. Live/tooling triage explicitly deferred to 475/476. No further deferred stub needed — the cap fits one pass. |
