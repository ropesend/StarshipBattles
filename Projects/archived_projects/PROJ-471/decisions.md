# PROJ-471: Decisions Log

> **LOG ALL DECISIONS HERE**
> When you make a design choice or the user specifies a preference, add it to this table.
> Future agents will reference this to understand why things were done a certain way.

| Date | Decision | Rationale |
|------|----------|-----------|
| 2026-05-20 | Project initialized | Starting point for State hygiene — singleton-divergence consolidation + collection/RNG hygiene (2026-05-20) |
| 2026-05-20 | Bundled findings from `2026-05-20_082533_state-audit` into ONE project by singleton-or-mechanism, phased by severity | Bundling driven by singleton-or-mechanism rather than severity to maximize implementation continuity; the shared root cause is the `create_production()` bridge hub. Protocol Phase D math (V=19 < 30) + Codex consult both pointed to one project. Full bundling discussion in `findings/bundling_decisions.md`. |
| 2026-05-20 | One project (not split _default_provider into its own foundation project) | Codex consult (planning) agreed: although `_default_provider` is the heaviest item (68+ sites), it is the same context-vs-module-default conversation as `_default_cache_manager`/`_default_policy_manager`/`_default_manager`. Mitigated execution-sprawl risk by making Phase 1 an explicit exit gate around `_default_provider` + `ShipCombatEngine` before any tail cleanup. Final call: Claude (consult advises, initiator owns). |
| 2026-05-20 | `exit_dialog` rects kept IN scope as the lowest-priority MAJOR | Real module-level mutable shared state (`exit_dialog.py:11-12,24`), but audit verifier downgraded CRITICAL→MAJOR (derived values, modal, no corruption). Codex advised "include as opportunistic, first to drop if trimming." Decision: include, placed last in Phase 2 with an explicit "drop first" note. |
| 2026-05-20 | `ST-02-003` density_map unseeded `random.Random()` ruled OUT_OF_SCOPE | Codex consult agreed: instance RNG is Pattern #18-compliant (Pattern #18 targets module-level `random.*` in sim/engine/AI; the guard test `tests/unit/quality/test_no_unseeded_random.py:32-37` excludes strategy generation). Only a docstring-accuracy issue, not a state bug. Logged for the refinement proposal as a doc-drift note, not a project item. |
| 2026-05-20 | `_default_profiler` bridge reframed as a *design cleanup*, not dead-code deletion | Codex consult corrected the audit's "removable bridge" framing: `set_default_profiler()` is still called in `create_production()` (`context.py:165-176`) and `profile_action`/`profile_block` still depend on `_default_profiler` as their live hook (`profiling.py:116-149`). Task 3.2 requires verifying the decorator fallback before any removal. `_default_game_settings` and `_default_image_provider` confirmed truly dead (0 consumers) and kept as straightforward MINOR deletions. |
| 2026-05-20 | **REVISION (scope cut): DROP the lead `_default_provider`/`DefaultRegistryProvider` CRITICAL — verified FALSE POSITIVE** | Dual independent + Codex review, re-verified inline against live code: `game/core/registry.py:380-394` — every `DefaultRegistryProvider` accessor (`get_components`/`get_modifiers`/`get_vehicle_classes`/`get_resources`) re-calls `get_default_registry_manager()` on each call. The `_default_provider` singleton (`registry.py:466-483`) caches only the *stateless provider wrapper*, never a manager. There is no captured/cached manager and therefore no singleton-divergence bug. The `get_resource_catalog` lazy `_resource_catalog` is per-provider-instance, not a manager fork. Task 1.1 removed; the divergence-window claim was an artifact of mis-reading the wrapper as capturing a manager. The genuinely-dual `_default_manager` (`registry.py:284-315`) is a separate, real item retained as Task 2.3. |
| 2026-05-20 | **REVISION (defer): MOVE `random.seed()` removals (ST-04-010/011) to PROJ-473 via Protocol 07** | Verified galaxy-determinism regression as written: `game/strategy/engine/game_initializer.py` creates a per-instance `random.Random(galaxy_seed)` AND calls `random.seed(galaxy_seed)` ("Also seed global random for star/planet generation"); `star_generator.py` + planet/atmosphere/naming generation use BARE global `random.*`, and placement strategies fall back to global random when `rng` is None. The global seed is LOAD-BEARING for reproducible generation. Deleting it before threading an explicit `rng` through generation breaks seed reproducibility. PROJ-473 populated with the rng-threading scope that must precede any global-seed removal. Tasks 2.11 + 2.12 removed from PROJ-471. |
| 2026-05-20 | **REVISION (scope cut): DROP the "dead bridge" deletions — `_default_game_settings`/`_default_image_provider` are NOT dead** | Codex verified `get_default_game_settings()`/`set_default_game_settings()` are still tested and injectable (`tests/unit/ui/services/test_game_settings.py:88-93`); image-provider defaults are part of the application-context contract (`tests/unit/ui/services/image/test_defaults.py`, `tests/unit/core/test_application_context.py:192-195`). Task 3.1 removed. (Task 3.2 profiler design-cleanup and Task 3.3 llm-bridge removal are independent and retained pending their own re-verification.) |
| 2026-05-20 | **REVISION (keep): real state-hygiene fixes retained** | `ShipCombatEngine` class-level `_targeting_system`/`_damage_calculator`/`_weapon_firing_system` → per-instance via a per-battle `CombatSubsystems` bundle owned by `BattleEngine` (Task 1.2 — re-verified at `ship_combat_engine.py:40-63`); the `battle_setup` global `_damage_calculator` overwrite re-verified at `game/simulation/systems/battle_setup.py:48-49` (manifest's `entities/battle_setup.py` path was wrong; corrected to `systems/`); plus the genuine test-isolation seams (`_SERIALIZABLE_REGISTRY`, `_catalog`, `_next_fleet_id`, crew-priority registry, etc.). Each re-verified against live code before changing. |
| 2026-05-21 | Codex one-round audit: 4 findings, ALL VERIFIED against live code, all remediated in new Phase 4 | Codex completed the audit (harness only rejected the file for missing `consult/v1` frontmatter; content captured at `AgentCoordination/Scratchpad/Consult/proj471_audit/audit.md.invalid-output-*.txt`). F1 (Major, VERIFIED): `BattleSetupState.from_dict` reused loaded fleet ids — reproduced `[1001,1001,1002]`; fixed by advancing the counter past max loaded id (Task 4.1). F2 (Major, VERIFIED): `reset_component_caches` swapped a new instance, diverging from a live `ctx.component_cache`; fixed to clear the existing manager's fields in place (Task 4.2). F3 (Medium, VERIFIED as test-strength gap, NO code regression — Codex confirmed `test_battle_determinism.py` passes): determinism test only compared raw rng draws; strengthened with a real same-seed battle-outcome equivalence test (Task 4.3). F4 (Low, VERIFIED): serializable-seam test left the process registry empty; fixed with baseline snapshot/restore (Task 4.4). 0 findings REJECTED. |
| 2026-05-21 | Codex finish-audit (one round): 1 Medium finding, REJECTED-as-defect (advisory only); (b)(c)(e) clean | Codex completed (harness rejected the wrapper only for missing `consult/v1` frontmatter; content at `AgentCoordination/Scratchpad/Consult/proj471_finish_audit/audit.md.invalid-output-*.txt`). Sole finding: Task 2.6's regression test `tests/unit/ui/test_sprite_default_no_autocreate.py` is *untracked* in git, so it is not in `git diff HEAD`. VERIFIED-as-fact but REJECTED-as-defect: the file exists and passes (20/20 in Codex's own run); "untracked" is the expected pre-commit state because the orchestrator commits, not the executing agent (per ground rules). No code action needed — orchestrator's commit must stage the new file. Codex explicitly confirmed NO combat/RNG/ordering/payload changes (b), NO stale references to deleted exit-dialog helpers (c), NO layer/convention violations (e). 0 findings requiring a remediation phase. |
| 2026-05-21 | **Task 2.13 DONE**: `exit_dialog` rect globals encapsulated in an `ExitDialog` instance class | Removed module-level `_exit_yes_rect`/`_exit_no_rect` and the `global` reassignment; `ExitDialog` (`game/exit_dialog.py`) now owns per-instance rects with `draw`/`handle_click`/`handle_cancel`. `RunLoop` (`game/run_loop.py`) owns one instance. No module-level mutable shared state remains. Characterization test rewritten in `tests/unit/test_exit_dialog.py` (asserts globals are gone + per-instance isolation). |
| 2026-05-21 | **Task 2.6 DONE (root cause) + consumer partially migrated**: removed silent auto-create fallback in `get_default_sprite_manager()` | The genuine ST-01-005 state bug — `get_default_sprite_manager()` silently created a fresh, unloaded, divergent `SpriteManager()` when accessed before `create_production()` ran — is fixed: the accessor now raises a descriptive `RuntimeError` (`game/ui/renderer/sprites.py`). Migrated the one ctx-reachable consumer (`app_bootstrap.py:265` → `ctx.sprite_manager`). The remaining consumer (`workshop_screen.py:109`) has no `ApplicationContext` in scope (it takes a `WorkshopContext`); threading ctx there is a separate UI-DI change with zero divergence benefit (production always primes the default at startup), so it keeps the accessor. Test: `tests/unit/ui/test_sprite_default_no_autocreate.py`. |
| 2026-05-21 | **Tasks 2.4 / 2.5 / 2.7 DROPPED as moot for state-hygiene; 3.3 DROPPED (gated on 2.7)** | Verified against live code: every `set_default_ship_theme_manager` / `set_default_asset_manager` / `set_default_llm_provider` call site is exactly ONE invocation, inside `create_production()` (`game/context.py:179-183`); nothing re-sets these mid-run. Therefore `get_default_X() is ctx.X` always holds in production — there is NO singleton-divergence bug to fix, only an accessor-style preference. The migrations require threading `ApplicationContext` through UI constructor chains that currently carry no ctx (`new_game_setup_controller` → `RaceSetupScreen` → `panel_factory`; multiple UI panels for ship_theme/asset). That is a cross-cutting UI-DI refactor far larger than the task sizings, with zero state-hygiene payoff. Per CLAUDE.md root-cause/no-gold-plating, dropped. 3.3 (llm-bridge removal) is gated on 2.7 and dropped with it; the bridge remains the intentional module-level hook like profiling's. |
| 2026-05-21 | **Task 3.2 RESOLVED: keep the `_default_profiler` bridge as the intentional module-level hook** | Re-verified: `profile_action`/`profile_block` (`game/core/profiling.py:116-149`) read `_default_profiler` directly; `set_default_profiler()` is called once in `create_production()` (`context.py:176`). These decorators/context managers are applied at module-import / definition time across the codebase and have no `ctx` available at their call sites. Migrating them to require `ctx` would mean threading ctx into every decorated function — a large change for no state-divergence benefit (single startup setter). Decision (b): keep the bridge, documented as the intentional profiling hook. No code change. |
| 2026-05-21 | **Task 2.3 RESOLVED: document the `_default_manager` dual pattern as an accepted, single-instance bridge** | Re-verified `game/core/registry.py:284-360`: `get_default_registry_manager()` auto-creates a `RegistryManager()` only when `_default_manager is None`. In production `create_production()` calls `set_default_registry_manager()` once at startup (`context.py:175`); the 8 convenience wrappers (`freeze_registry`, `set_validator`, `clear_registry`, etc.) all route through `get_default_registry_manager()`, so they share the one manager. The auto-create is the test-time convenience that lets registry tests run without standing up a full ApplicationContext — analogous to the accepted bridges. The only divergence window (touching a wrapper before bootstrap in production) does not occur because bootstrap runs first. Decision: keep as documented bridge (the wrappers already centralize on the single getter); no behavior change. |
| 2026-05-20 | `ShipCombatEngine` per-instance design: per-battle `CombatSubsystems` bundle, not naive per-instance fresh objects | The class-level subsystems are LOAD-BEARING shared-within-battle state: `battle_setup.py:48-49` sets the seeded `DamageCalculator(rng=engine.rng)` so the per-battle RNG reaches every ship; `battle_engine.py:612,642-643` read `ShipCombatEngine._damage_calculator` to share that seeded calc with ram/mine resolvers; `battle_engine.py:242-250` wires the shared `WeaponFiringSystem` event bus. Naive per-instance fresh objects would (a) lose the seeded RNG → determinism regression, (b) break ram/mine resolver damage sharing. Root-cause fix: `BattleEngine` owns a per-battle `CombatSubsystems(targeting, damage_calculator, weapon_firing)` bundle, injected into each ship's `ShipCombatEngine` via constructor; class attributes removed. Within a battle all ships share the engine's bundle (preserving current behavior + determinism); across battles/tests no class state leaks. |
