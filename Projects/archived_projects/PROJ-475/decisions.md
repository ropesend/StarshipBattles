# PROJ-475: Decisions Log

> **LOG ALL DECISIONS HERE**
> When you make a design choice or the user specifies a preference, add it to this table.
> Future agents will reference this to understand why things were done a certain way.

| Date | Decision | Rationale |
|------|----------|-----------|
| 2026-05-21 | Project initialized | Starting point for Facade read-path: remaining live strategy-screen + render readers migration (follow-on from PROJ-472) |
| 2026-05-21 | Created + scoped as the **remaining live readers + pass-through deprecation** tail of PROJ-472. **GATED on PROJ-472's guards + first slice landing.** | PROJ-472 honestly leaves the read path tightened-but-not-closed: `StrategyScreen` pass-throughs (`strategy_screen.py:160-189`) and `FacadeSessionState.session` (`_facade_state.py:63-86`) stay as documented transitional surfaces. Their deprecation + the remaining live readers belong here per consult §4/Risks. Respect render-hot-path caution (no per-frame projections). |
| 2026-05-22 | **Fleshed to execution-ready. Pre-flesh Codex consult (`Scratchpad/Consult/proj475_preflesh/advice.md`) confirmed the key finding and drove a SCOPE CAP.** | The two PROJ-472 guards only measure `.session`/`._session`/`.facade_state.session` chains + `game.strategy.*` imports — NOT the pass-through *properties* `scene.galaxy`/`scene.empires`/`scene.systems`/`scene.active_empire`/`scene.human_player_ids`. So the `.session`-allowlisted readers are a small tail (~7 sites) while `galaxy`/`empires`/`systems` are a BROAD raw-domain bus (renderer re-exporters `strategy_renderer.py:124-138`, render-hot per-frame traversal, context menus, list/build-queue windows, `system_tree_panel`). Verified live 2026-05-22. |
| 2026-05-22 | **SCOPE CAPPED to the contained first slice.** PROJ-475 = (1) add 4 new facade read surfaces, (2) migrate the explicit `.session` reader tail + remove their guard allowlist entries, (3) retire the THREE NARROW pass-throughs `enemy_empire`/`human_player_ids`/`active_empire` + replace the `StrategyScreen.session` getter with a test-only swap seam, (4) privatize `FacadeSessionState.session`. **DEFER** `galaxy`/`empires`/`systems` deletion + the render/read-model boundary to a NEW stub (**PROJ-477**, created 2026-05-22, GATED on PROJ-475). | Consult §1c/§3/§5: deleting `galaxy`/`empires`/`systems` is too large for one pass and is a fundamentally different problem (render/read-model boundary, needs a renderer-only access layer NOT per-frame DTOs, plus a third property-consumer guard). The honest split keeps PROJ-475 a coherent "close the small `.session` tail + retire the narrow pass-throughs" project. **Orchestrator to confirm this cap with the user before executing.** |
| 2026-05-22 | **`enemy_empire` deletes outright.** | Verified live: zero external consumers in `game/ui` — only the property body at `strategy_screen.py:184-185`. |
| 2026-05-22 | **Four new facade surfaces (Phase 1):** `facade.empires.race_config(empire_id)` (race identity — `EmpireInfo` carries none, `empire_dto.py:77-120`); `facade.session_meta.save_current_game(save_name=None)` (facade owns session, calls `SaveGameService.save_game` internally — `save_game_service.py:93-118` needs a live `GameSession`); colony `has_build_yard` projection on `PlanetInfo`/`ColonySummary` (planetary-yard OR space-shipyard, for `strategy_detail_formatter:277`); per-ship `has_spaceyard` on the fleet-report ship DTO (collapses 3 `FleetCapabilityCalculator` late-imports). | Consult §2: distinguished surfaces that already exist (`session_meta.registries()`/`human_player_ids()`, `facade_state.get_design_catalog_for_empire`, `active_empire_id`) from those that must be added. Build them first (Phase 1) so Phase 2 migrations have a target. |
| 2026-05-22 | **`FacadeSessionState.session` is PRIVATIZED (rename → `_session`), not deleted.** Cache holder stays public. | Verified the facade's OWN slices (`system_slice`/`planet_slice`/`fleet_slice`/`event_slice`/`empire_slice`/`command_dispatch_slice` + `_facade_state` helpers) read `_state.session.<x>` extensively — deletion would break them. The leak to close is the PUBLIC reachability `facade.facade_state.session` from UI; no UI runtime code reads it today (the 4 remaining `facade_state.session` hits in `game/ui` are docstring/comment residue). UI legitimately reads `facade_state.planets_for_empire_cache`/`stars_cache_new` (kept-by-design perf boundary, pinned by `test_game_session_projection_boundary.py`) — those stay. |
| 2026-05-22 | **Keep the `StrategyScreen.session` SETTER; replace only the getter with a test-only swap seam.** Rewire screen-internal `active_empire_id`/`current_empire` to the private `_session`. | The setter rebuilds the facade in lockstep to prevent split-brain (`strategy_screen.py:294-311`); tests swap via `screen.session = mock`. The getter is the leak. `active_empire_id`/`current_empire` are the screen's own helpers (composition root owns the only legitimate `_session`), so they read `_session` directly once the public `active_empire` property is gone (consult §1 screen-internal, §6). |
| 2026-05-22 | **transfer_controller migrates to `viewing_empire_id` anchor, not active-turn empire.** | `strategy_screen.py:247-266` documents `viewing_empire_id` as the canonical "whose data should this UI show" anchor; consult §6. |
| 2026-05-22 | **Post-flesh Codex review (`Scratchpad/Consult/proj475_postflesh/advice.md`): VERDICT BLOCKERS (all addressable in-plan, no user-decision blocker). Revised per all findings.** | Review verified the plan against live code 2026-05-22. Four blockers were plan-completeness gaps, not scope problems; fixed below. The cap line (475 vs 477) and the privatize-not-delete call were both CONFIRMED correct. |
| 2026-05-22 | **B1 fix: Phase 4 = `_facade_state` + SEVEN slices (added `economy_slice.py:67,119`).** | The original Phase 4 list named 6 slices; `economy_slice` also dereferences `_state.session` and would ship a latent `AttributeError` if missed. |
| 2026-05-22 | **B2 fix: the `StrategyScreen.session` GETTER retirement is DEFERRED to PROJ-477, not done in Phase 3.** Phase 3 retires only the three narrow READ pass-throughs. | Review found live production consumers of the getter outside the Phase 2 list: `system_tree_panel.py:418-425` resolves `scene.session` dynamically (`getattr(..., 'session')` — guard-missed), and the Category B mutator WRITE seams (`strategy_game_state_manager.py:164` active-empire rotation, `strategy_screen_order_editing.py:66/:92` fleet_mutator) write through `screen.session.<x>`. "test-only getter" was overclaiming. Retire it in PROJ-477 with the system_tree_panel + write-seam cleanup. Setter STAYS. |
| 2026-05-22 | **B3 fix: per-ship `has_spaceyard` needs a CONSUMER BRIDGE (Phase 2 Task 2.6 reclassified Complex).** | The fleet report runs on raw `ShipInstance`, not `ShipInfo` DTOs (`fleet_report_window.py:185,238`, `fleet_report_view_model.py:49,152`). Adding `ShipInfo.has_spaceyard` (Task 1.4) is cheap; the report must be bridged: fetch `facade.fleets.get(fleet_id)`, build an `instance_id → has_spaceyard` lookup (held in the view-model), and rewire the format/filter/sort. Escalate if too entangled. |
| 2026-05-22 | **B4 fix: `has_build_yard` needs a QUERY-WIRING step (Phase 2 Task 2.7 reclassified Medium).** | `_show_planet_report` renders a live `Planet`, not a `PlanetInfo` (`strategy_detail_formatter.py:134,193,241,277`). Phase 2 must add `facade.planets.get(obj.id)` inside `_show_planet_report` and read `has_build_yard` off the DTO; the field alone does not replace the `colony_has_planetary_yard` import. |
| 2026-05-22 | **Phase 1 EXECUTED.** `facade.empires.race_config(empire_id)` returns the RAW `empire.race_config` (via `getattr`); the `RaceLibrary` fallback (when `race_config` is absent but a `race_id` exists) STAYS in the caller (event_router) — Phase 2. `has_build_yard` resolves the planetary-yard bit in `PlanetSlice` against `session.registries` and ORs with `has_space_shipyard` in `from_planet`; the resolver degrades to `False` on `(AttributeError, TypeError)` for stub colonies (a bare `Mock().facilities` raises `TypeError: not iterable`). `ShipInfo.has_spaceyard` calls `FleetCapabilityCalculator.ship_has_spaceyard` once per ship in `from_fleet`, degrading to `False` on `(ValidationException, AttributeError)`. | Per checklist Task 1.1 (decide raw-vs-fallback), B12 (no `EmpireIdentityInfo` DTO), and the slice-resolution pattern for registry-dependent projections (B4). Defensive catches keep existing facade tests (which pass `Mock`/`SimpleNamespace` stubs through the slices) green without changing their fixtures, except two `test_planet_slice.py` `from_planet` stubs that needed `**kwargs` to absorb the new keyword. |
| 2026-05-22 | **Phase 2 EXECUTED.** event_router race-config migration DROPPED the dead `RaceLibrary`/`race_id` fallback (`Empire` has no `race_id` attr, so `getattr(empire,'race_id',None)` was always `None`); `RaceLibrary` import stays (still used by `resolve_race`). Save seams: `on_design_click` hands a SCALAR `save_path` (from `facade.session_meta.save_path()`); `app._create_workshop_context` gate is now empire-only (was empire+game_session) since a scalar `save_path` can legitimately be `None`. Spaceyard bridge HELD ON THE VIEW-MODEL (`FleetListViewModel._spaceyard_lookup`); the window builds it from `facade.fleets.get(fleet_id).ships` and pushes it in on init + every refresh; `filter_ships`/`sort_ships` gained optional `spaceyard_lookup` params. transfer_controller anchors on `viewing_empire_id`. | All Phase 2 migrations completed without escalation. Bridge fit cleanly (B3's escalation clause not triggered). Allowlist tightened: removed 4 Category C + 3 Category E session entries, 2 FLEETCAP + 1 CLUSTER import entries, 2 SaveGameService TAIL import entries. Both guards green. |
| 2026-05-22 | **B10/B12 fixes:** manifest's "(manual-save caller — locate)" replaced with the pinned site `strategy_screen_lifecycle.py:155` (`on_save_game_click`); `facade.empires.race_config` returns the live `RaceConfig` (NO `EmpireIdentityInfo` DTO — `Empire` owns `race_config` directly, no `Empire.race_id` field). | Review findings 10 + 12. Returning `RaceConfig` (an immutable race-definition value object) is an acceptable read-surface shape here, distinct from leaking live mutable session state. |
| 2026-05-22 | **Phase 3 EXECUTED.** `enemy_empire` deleted (zero consumers). `human_player_ids` external consumers (`strategy_click_dispatcher:387`, `strategy_screen_selection:41`, `strategy_game_state_manager` :88/:136/:159/:194) migrated to `facade.session_meta.human_player_ids()`; public property deleted. `active_empire` external consumer (`strategy_screen_assets.focus_on_player_home`) needed the LIVE empire's `.colonies` (identity-matched vs `screen.systems[].planets`), so it now resolves the live empire from the raw `screen.empires` bus keyed by `screen.active_empire_id` — `empires`/`systems` stay deferred to PROJ-477. `active_empire_id`/`current_empire` rewired to read `self._session.active_empire`/`.empires`/`.human_player_ids` directly. Public `active_empire` deleted; getter+setter UNTOUCHED. | The `focus_on_player_home` migration keeps the live-object semantics the facade DTO surfaces can't provide (`ColonySummary` is not a live `Planet`), consistent with the rest of that asset-bootstrap module already running on the raw bus. |
| 2026-05-22 | **Phase 3 allowlist outcome:** `_session.enemy_empire` REMOVED. `_session.human_player_ids` + `_session.active_empire` KEPT (re-purposed comments) as Category-A composition-root self-reads backing `current_empire`/`active_empire_id` — NOT as public pass-throughs. | Per Phase 3 Task 3.4 / design Risk 2: the screen IS the composition-root boundary and owns the only legitimate `_session` handle; routing the screen's OWN helpers through the facade would be circular. The honest shrink is "no external consumer reads these properties" — verified by grep + the public property deletions. |
| 2026-05-22 | **Phase 4 EXECUTED.** `FacadeSessionState.session` attribute renamed → `_session` (constructor keyword stays `session`, a test-internal seam). Chose **slices read `_state._session` directly** (option a) over a named accessor (option b) — simplest, no new surface. All 7 slices migrated (command_dispatch/event/empire/economy/system/planet/fleet) plus `_facade_state` helpers. Cache holder stays public (perf boundary, pin green). | Per Phase 4 Task 4.2 decide-the-accessor subtask. Option (a) is the least-surface change and the `_state._session` form is honestly engine-internal (slices are the facade's own composition). |
| 2026-05-22 | **End-of-project Codex audit (`Scratchpad/Consult/proj475_exec_audit/audit.md`): 2 findings (1 Medium, 1 Low) + test-gap notes. All re-verified against live code → Phase 5 created.** | Audit verified the core deliverables (race_config, save seam, transfer_controller, BUG-125 gates, empire_panel registries, build-yard, spaceyard bridge, the 3 retired pass-throughs, FacadeSessionState privatization, projection-only/no-persistence-change) as correct. |
| 2026-05-22 | **Audit Finding 1 (Medium) VERIFIED → Phase 5 Task 5.1.** The plan (`plan.md:140`) assigned the build-queue `compute_planet_production` import tail to PROJ-475, but no phase covered it and the 2 import-allowlist entries remained — so "all phases complete" overstated the import-guard shrink. Disposition: KEEP allowlisted-with-reason (the plan.md:140 fallback) + pin the deferral; defer the clean migration to PROJ-477. | Both call sites render a LIVE `Planet`/`yard` the build-queue screen already holds and call a pure read-only calculator with `facade.session_meta.registries()` — no `.session` leak. A clean `production_rates` facade projection needs the live-`Planet`→DTO bridge that IS PROJ-477's render/read-model boundary work (PROJ-477 already scopes build_queue_screen/controller/manager). Migrating here would duplicate 477's boundary. |
| 2026-05-22 | **Audit Finding 2 (Low) VERIFIED → Phase 5 Task 5.3.** The import-guard comment for the StrategyScreen `GameSession` import said "deprecation is PROJ-475"; the `session` getter + that import are deferred to PROJ-477. Retagged the comment. | Documentation drift only; no runtime change. |
| 2026-05-22 | **Audit Finding 3 (test gaps) VERIFIED-partial → Phase 5 Task 5.2.** Added degrade-path coverage for the 3 new defensive branches (`planet_slice` `(AttributeError,TypeError)`, `ShipInfo.has_spaceyard` `(ValidationException,AttributeError)`, `fleet_report_window._build_spaceyard_lookup` → `{}`). The `system_tree_panel.py:418-425` string-getattr `session` is **REJECTED for PROJ-475** — it is the explicitly PROJ-477-deferred getter consumer (PROJ-477 Phase 1 hardens the dynamic-getattr guard). | The defensive branches were untested; the system_tree_panel getattr is out of 475 scope by design (the getter retirement is 477). |
| 2026-05-22 | **Phase 4 found + fixed a missed external `.session` reader NOT in the plan's list:** `workshop_ship_io.py:84` `_design_catalog` read `getattr(facade_state, "session", None).services.design_catalogs_by_empire` (string-getattr, so the AST session-guard MISSED it). Root-cause fix: route through the existing public `FacadeSessionState.get_design_catalog_for_empire(empire_id)` accessor (which already does that exact resolution internally). | Honest closure: the plan claimed "no UI runtime reads `facade_state.session` (residue is comments)" but this getattr-by-name reader survived. Migrating it (not re-allowlisting) is the only way Phase 4's "live session no longer reachable" claim is true. Added a runtime guard pin (`test_facade_state_session_attribute_is_privatized`) so getattr-by-name leaks are caught going forward. |
