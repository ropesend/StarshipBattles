# PROJ-324: Decisions Log

> **LOG ALL DECISIONS HERE**
> When you make a design choice or the user specifies a preference, add it to this table.
> Future agents will reference this to understand why things were done a certain way.

| Date | Decision | Rationale |
|------|----------|-----------|
| 2026-05-04 | Project initialized | Continuation of PROJ-322 deferred work (14 of 25 items gated by UIWindow blocker) and Task 4.3 (LLM polling). Source: continuation review at `AgentCoordination/Scratchpad/plans/proj_321_322_323_continuation_plan.md`. |
| 2026-05-04 | **D-001:** Use Option (a) — production-side `bypass_init=True` flag | Both Claude's Explore-agent investigation and the OpenCode 322-review (CRIT-001) independently arrived at this recommendation. Option (b) couples to pygame_gui internals (library upgrade could silently break it). Option (c) loses test fidelity for RaceSetupScreen and FleetReportWindow. Option (a) is opt-in, low-risk, and unblocks all 14 PROJ-322 deferrals in one move. |
| 2026-05-04 | **D-002:** Bundle LLMBackgroundCall refactor into PROJ-324 (not its own project) | OpenCode 322-review CRIT-002: production change is +4 lines. The class already has `threading.Lock`, `threading.Event` (for cancel), and full thread-safe state transitions. Adding a `_done_event` + `wait()` method is mechanically simple and bundles cleanly with the UIWindow work. |
| 2026-05-04 | **D-003:** Guard placement is FIRST executable statement in `__init__` (after docstring); use `type(self)` not class-defining-class | Risk #1 + #3 in design.md. If parameter validation runs before the guard, the guard hides bugs. If the check uses `StrategyModalWindow` instead of `type(self)`, flags set on subclasses are ignored. |
| 2026-05-04 | **D-004:** Provide a `bypass_init(Cls)` context manager in `tests/fixtures/ui_widget_factory.py` | Risk #2 in design.md. Bare `Cls.bypass_init = True` assignment in test bodies leaves the flag set if the test crashes between set and unset. Context manager pattern is enforceable in code review. |
| 2026-05-04 | **D-005:** RaceSetupScreen migration provisionally in PROJ-324 Phase 3, but may roll to PROJ-325 if `bypass_init` alone is insufficient | RaceSetupScreen has 6 declared parameters expanding to 10+ collaborators and 8 lazy panels (see [`game/ui/screens/race_setup/screen.py:104-166`](game/ui/screens/race_setup/screen.py#L104-L166)). The `bypass_init` flag handles construction but the test-side wiring may still be onerous. Decision deferred to Phase 3 reality-check. |
| 2026-05-04 | **D-006:** Out-of-scope items rolled to PROJ-327, NOT silently dropped | User feedback: "I do not want the additional issues forgotten." Items rolled: PROJ-322 Task 3.14 (virtual_table @patch sweep — high test-runtime impact), Task 3.25 (strategy_screen 50-test refactor), DUP-001 / HLP-001 (Tasks 6.1 / 6.4 — accepted disposition reconsideration), mutable-mock fixture rescopes (Tasks 2.6 / 2.11 / 2.15 / 2.19 / 3.15). PROJ-327 is the explicit owner. |
| 2026-05-04 | **D-007:** Branch strategy: continue on `feat/03c-phase-aware-execution` (where PROJ-321/322/323 landed) unless user directs otherwise | Awaiting user confirmation. If branched off `main`, downstream merge of the PROJ-321/322/323 work needs to land first. |
| 2026-05-04 | **D-008:** Parallelism: PROJ-324 and PROJ-326 are file-disjoint and may run in parallel; PROJ-325 Phases 1-2 also parallel-safe; PROJ-325 Phase 3 BLOCKS on PROJ-324 Phase 1 | See parallelism map in [plan.md](plan.md). PROJ-327 starts after PROJ-326 completes; the deferred items it owns must remain documented and queued throughout. |
