# PROJ-320: Decisions Log

> **LOG ALL DECISIONS HERE**
> When you make a design choice or the user specifies a preference, add it to this table.
> Future agents will reference this to understand why things were done a certain way.

| Date | Decision | Rationale |
|------|----------|-----------|
| 2026-05-02 | Project initialized | Conversion from `Projects/Triage/strategic_combat_round_budget.md` (QA session 20260502_184736, Obs 2a + 2d) |
| 2026-05-02 | Title: "Strategic Combat Round Budget" | User-confirmed; matches triage filename and describes both the new state (round budget) and the layer (strategic combat) |
| 2026-05-02 | **Combat trigger rule:** on each fleet's movement-opportunity tick (`tick % get_tick_interval(fleet.speed) == 0`), combat fires UNLESS the fleet successfully left the contested hex on that tick | User specified the model directly: "at each of these points combat should be initiated if a) the fleet moves into a sector(hex) occupied by another player, or b) if the fleet has no movement orders (is not leaving the sector(hex))." Eliminates the per-tick scanning waste. |
| 2026-05-02 | **No new persistent Fleet state.** Model is stateless — derive everything from `tick % interval == 0` + post-Phase-3 fleet location | User: "Currently each fleet gets the opportunity to move at certain intervals based on how many movement points it has." The existing tick-interval system already encodes movement opportunities; no `remaining_moves` counter needed. Save format unchanged. |
| 2026-05-02 | **No new round-scheduling registry.** Re-derive contested-hex membership each tick from `Empire.fleets` | User: "rounds occur based entirely on when the fleets either encounter each other the 1st time, or any time that one fleet has a movement option and is not moving out of the sector." This rule operates entirely on per-tick state — no encounter-tracker needed. Pattern Scout swarm agent confirmed stateless approach is consistent with existing `_resolve_conflicts` design. |
| 2026-05-02 | **Action orders (COLONIZE, TRANSFER, BUILD, etc.) DO trigger combat** when fleet is at a contested hex on its movement-opportunity tick | User-confirmed. Action-ordered fleets ARE staying put; the spoken rule "fleet is not leaving the sector" applies. A colonizing fleet under enemy fire is still under fire. |
| 2026-05-02 | **Idle (no orders) fleets DO trigger combat** on their movement-opportunity ticks at contested hexes | User-confirmed. Idle = staying put = fights. Matches the "stalemate keeps firing" semantic from the Round-1 questions. |
| 2026-05-02 | **Blocked / pathfind-failed moves DO trigger combat** | User-confirmed. The fleet HAD a movement order but didn't actually leave; the result (staying put) is what matters for the combat trigger. |
| 2026-05-02 | **Entry into an enemy-occupied hex via WARP/MOVE triggers combat at the destination** | User-confirmed in Round 1: "Combat occurs when one fleet enters a sector(hex) that another fleet occupies." Treated as the same trigger family — the fleet "did not leave the destination hex this tick" because it just arrived. |
| 2026-05-02 | **Multi-fleet per empire:** every fleet at a contested hex contributes its own movement-opportunity rounds. Today's `Dict[int, Fleet]` (one per empire) becomes `Dict[int, List[Fleet]]` | User-confirmed in Round 2. Eliminates the silent "extra fleets sit idle" bug at `conflict_resolution_engine.py:298-300`. Also: spec compiler already maps fleets to teams by `owner_id`, so multi-fleet-per-team is a natural extension. |
| 2026-05-02 | **N-team encounters (3+ empires) compose by sum-of-engaged-fleet-speeds** | User confirmed in Round 1: "A 3rd fleet means that there are now the possibility of additional combat rounds when it either enters the sector, or if it remains in the sector then on every tick it could have moved combat occurs." Falls out naturally from the per-fleet-per-tick rule with no special N-team math. |
| 2026-05-02 | **Stalemated fleets keep firing every opportunity tick until one side dies or the turn ends** | User-confirmed in Round 1. Matches the original spoken-commentary example: A=6 + B=4 ⇒ 10 rounds total. |
| 2026-05-02 | **Leaving fleet skips combat:** a fleet whose movement-tick takes it OUT of a contested hex does NOT fire combat that tick | User-confirmed in Round 2. Matches the spoken rule "if the fleet has no movement orders (is not leaving the sector)" — explicitly leaving is the negation. |
| 2026-05-02 | **In-scope:** fix pre-existing bug where `OrderProcessor._execute_fleet_merge` does not call `FleetSpeedCalculator.update_fleet_speed(target)` after merging | Risk Assessor (HIGH). With the new model dependent on per-fleet movement intervals, an incorrect post-merge speed produces wrong opportunity-tick cadence. Pre-existing bug exposed by the new model; small, isolated fix in Phase 2. |
| 2026-05-02 | **CORRECTION (Phase 1):** the merge speed-recalc bug DOES NOT exist. `Fleet.merge_with` (`game/strategy/data/fleet.py:459`) already calls `other_fleet.trigger_speed_recalculation()` which dispatches to `FleetSpeedCalculator.update_fleet_speed`. The Risk Assessor agent's HIGH-severity finding was wrong. Verified by reading the code AND by running `tests/unit/strategy/engine/test_order_processor_fleet_merge.py::test_fleet_merge_recalculates_target_speed` (passes immediately on Phase 1). Phase 2 reframed from "bug fix" to "speed-recalc invariant audit" (sweep other Fleet.ships-mutation sites) plus "lock in regression guard" (the test is already written). | Trust-but-verify: the Risk Assessor read line 86-98 of `order_processor.py` and stopped before following through to `Fleet.merge_with` line 459. The plan author should have caught this in Phase A code review but didn't read `fleet.py::merge_with` directly — relied on the agent's summary. Lesson recorded for handoff: **always verify swarm agent claims against the actual code before locking the plan**. |
| 2026-05-02 | **Out of scope:** issues #6 (Race Setup LLM panel), #7 (tick number on Processing Turn overlay), #8 (replay button disabled for shortcut branches) | All three are independent of the combat-scheduling change. Verified by the UI Impact swarm agent — PROJ-320 neither fixes nor worsens them. |
| 2026-05-02 | **Out of scope:** event-driven hex-map scan (only re-scan after a fleet moves in Phase 3) | Performance Analyst flagged as a separate optimization opportunity worth ~1× more. PROJ-320's 10× win is sufficient; bundling another mechanism increases risk surface. |
| 2026-05-02 | **Out of scope:** Tick-budget tunability per engagement composition (originally Obs 2d in the triage doc, item §8 in scope notes) | The new model already produces fewer rounds per encounter; the user's "reduce ticks when no weapons present" concern is largely answered by the round-budget change. Defer further tick-budget tuning to a follow-up if the per-round simulator runtime is still the dominant cost after PROJ-320 lands. |
| 2026-05-02 | **No backward-compat shim for the old per-tick combat path** | Per CLAUDE.md "Eradicate Backward Compat Shims" rule and docs/03_CONVENTIONS.md §6.5 ("System Migration"). The old path is deleted in Phase 4; no fallback flag, no opt-in. Save files are disposable. |
| 2026-05-02 | **No public API or DTO changes** | API Contract Reviewer swarm agent confirmed: `IConflictEngine`, `IBattleResolver`, `BattleResult`, `ConflictResult`, `BattleSpec`, `Fleet` public surfaces all stay stable. Zero mock implementations need updates. |
| 2026-05-02 | **Implementation lives inside `ConflictResolutionEngine` (not a new sub-engine)** | Pattern Scout swarm agent recommendation — change is local to the spatial collision module; no `TurnEngineConfig` field, no DI wiring, no new `ITickPhase`. Adds ~15 lines to `ConflictResolutionEngine` + ~5 lines to `TurnEngine` Phase 4. |
| 2026-05-02 | **Trigger predicate naming:** new private helper `_should_trigger_combat_for_fleet(fleet, tick) -> bool` on `ConflictResolutionEngine` | Pattern Scout — mirrors `FleetNavigationService` "should/is" predicate style. Annotated `-> bool` per docs/03_CONVENTIONS.md §8. |
| 2026-05-02 | **Iteration order:** scheduling iterates `(empire_id, fleet_id)` tuples in sorted order | Preserves deterministic battle seeding (`_battle_seed_counter`). Mirrors existing `sorted(fleets_by_empire.keys())` pattern at `conflict_resolution_engine.py:305`. |
| 2026-05-02 | **Roster re-derivation:** when multiple combat rounds fire at the same hex in one tick, each round re-fetches the live fleet list from `Empire.fleets` (no cached `hex_map` reuse) | HIGH risk from Risk Assessor — destroyed-mid-encounter fleets must not appear in subsequent rounds. Trivial cost; correctness-critical. |
| 2026-05-02 | **No new event payload fields** (e.g. `triggering_fleet_id`, `round_number`) | Data-Flow Tracer recommendation — current `COMBAT_RESOLVED` payload (`participating_fleet_ids`, `surviving_fleet_ids`, `destroyed_fleet_ids`, `replay_id`) is sufficient. Optional debug fields can be added later if combat replay attribution becomes a feature ask. |
| 2026-05-02 | **`get_tick_interval` becomes a second consumer in `ConflictResolutionEngine`** (currently only called by `FleetMovementEngine`) | API Contract Reviewer noted this is a safe read-only expansion. Function signature unchanged; semantics preserved. No caching introduced — pure-arithmetic call cost is negligible (~2× O(fleets×100) per turn). |
| 2026-05-02 | **Performance regression test scope:** new test at `tests/performance/test_contested_hex_round_budget.py` — 5 contested hexes, 3 empires, 2 fleets each, 100 turns | Performance Analyst recommendation. Asserts total battle invocations ≤ 150 (today: 500+). Locks in the ~10× win and gates against future regressions. |
| 2026-05-02 | **Test assertion updates:** existing tests in `test_event_log_integration.py`, `test_combat_shortcut_paths.py` that assert specific event counts need updating | UI Impact agent flagged. Production UI is volume-agnostic; only test-level event-count expectations change. |
