# PROJ-446: Decisions Log

> **LOG ALL DECISIONS HERE**
> When you make a design choice or the user specifies a preference, add it to this table.
> Future agents will reference this to understand why things were done a certain way.

| Date | Decision | Rationale |
|------|----------|-----------|
| 2026-05-18 | Project initialized | Starting point for Post-refactor residue: UI + Core + Tests layer (Bucket C) |
| 2026-05-18 | F-C-025 regression-snapshot wallpaper: Option (b) — replace `pytest.skip(...)` with `pytest.fail(...)` via new `fail_missing_baseline(name, snapshot)` helper in `tests/regression/modifier_ability_snapshots/conftest.py`. | The 65 baseline files are already tracked in the repo, so the skip path only ever fires on a corrupted/missing checkout, which is exactly the case where a silent skip masks a regression. Option (a) (commit baselines) was already done; the residue was the self-healing skip. The new helper still writes the baseline so a maintainer can inspect/commit it, but the test now fails loudly per the F-C-025 finding text. 21 sites migrated across `test_weapon_modifiers.py` (13) and `test_utility_modifiers.py` (8). |
| 2026-05-18 | F-C-021 (Tech tree JSON wallpaper skip) — SUPERSEDED by PROJ-447 F-D-020. | The original Bucket C finding cited `tech_tree.json` which doesn't exist; PROJ-447 closes the real `data/techtree.json` wallpaper. No work needed in PROJ-446 Phase 1. |
| 2026-05-18 | F-C-016: `tests/fixtures/README.md` rewritten to point at Pattern #33 and the `bypass_init` scoped helper. Cross-bucket coordination note: the sibling "Stale-doc warning" paragraph in `docs/known-issues.md` (between the UI Widget Testability section and LLM Background Calls) is now outdated, but `docs/**` is owned by PROJ-447 in this stage's file partition. PROJ-447 should drop that paragraph as part of its sweep. | F-C-016 task limited Phase 1 edits to `tests/fixtures/README.md`; not editing `docs/known-issues.md` here is correct per the hard ownership partition between PROJ-446 and PROJ-447. |
| 2026-05-18 | F-C-030 PEP-604 sweep: `Mapping[str, int]` for `IShipInstance.cargo_contents` is imported from `collections.abc`, not `typing.Mapping`. All other legacy `typing.Dict / List / Optional / FrozenSet / Tuple` annotations across the 7 protocol modules (boundary, combat, common, persistence, registry, strategy_domain, strategy_entities) are rewritten to PEP-585/604 (`dict[K,V]`, `list[X]`, `X \| None`, `frozenset[X]`, `tuple[A,B]`). Each module now has `from __future__ import annotations` at the top so forward references stay string-compatible. | Modern syntax per `docs/03_CONVENTIONS.md` "Type Annotations". `Mapping` from `collections.abc` matches the post-PEP-585 idiom (PEP 585 marked `typing.Mapping` as the legacy alias). `from __future__ import annotations` is added to all 7 modules even on 3.13+ because the protocol layer is heavily forward-referenced (TYPE_CHECKING imports in strategy_domain.py, string-typed `IResourceReader \| None` in boundary.py); the future import is the lowest-friction guarantee. |
| 2026-05-18 | F-C-018 / F-C-019 static guards: AST-scan helpers are co-located inside each guard file rather than extracted into a shared `tests/static_guards/_helpers.py`. | Two AST helpers in two files is below the duplication threshold; extracting now would be premature abstraction. If a third static guard lands needing the same scan pattern, then extract. |
