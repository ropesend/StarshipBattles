# PROJ-465: Decisions Log

> **LOG ALL DECISIONS HERE**
> When you make a design choice or the user specifies a preference, add it to this table.
> Future agents will reference this to understand why things were done a certain way.

| Date | Decision | Rationale |
|------|----------|-----------|
| 2026-05-20 | Project initialized | Starting point for Audit-shrink cleanup 2026-05-20 |
| 2026-05-20 | Acted only on findings that passed independent verification of `2026-05-20_060020_audit_shrink` | Audit-shrink reports have produced false positives in past runs (e.g. `_eval_least_armor_rule` reachable via `data/targeting_policies.json`); rejected and uncertain items recorded in `findings/verification_report.md` |
| 2026-05-20 | **Scope revision (Protocol 06) from dual independent+Codex review.** DEFER/EXCLUDE Cluster 2 (planetary stat-modifier abilities inheriting `SimpleMultiplierAbility`). | Verified against live code: `SimpleMultiplierAbility` (base.py:480) models ONE scaled value (parsed via `_parse_primary_value`, recalculated via a `stat_key` multiplier) and emits exactly ONE UI row. `ShieldModifierAbility`/`DamageModifierAbility` (stat_modifiers.py:50/125) parse FOUR data fields (`multiplier`, `energy_drain_rate`, `activation_time`, `deactivation_time`), have empty `STAT_BINDINGS`/no `stat_key`, and emit 2-4 UI rows (value + Scope + conditional Energy Drain + conditional Activation). Collapsing them would drop the scope/energy/activation UI rows and the activation/energy data fields = behavior loss. Not a mechanical shrink. Excluded; logged as a discovered-issue for a possible future scoped refactor. |
| 2026-05-20 | **Path correction for DUP-X-1 `_find_ship`.** Target base is `order_handlers/base.py::BaseOrderHandler`, NOT `handlers/base.py::BaseCommandHandler` as the manifest/checklist stated. | All 5 `_find_ship` copies live in `game/strategy/engine/order_handlers/{launch_fighters,launch_satellites,lay_mines,recover_fighters,recover_satellites}.py` and all 5 classes inherit `BaseOrderHandler`. The orchestrator's note that "lay_mines declares it `@staticmethod` while others are instance methods" is STALE — all 5 are already `@staticmethod` with byte-identical bodies. Reconciled by keeping it a `@staticmethod` on `BaseOrderHandler`. |
| 2026-05-20 | Prioritized the byte-identical / trivially-equivalent mechanical dedups. Re-verified each against live code before merging. | Orchestrator directive: keep pure mechanical dedups that are byte-identical or trivially equivalent; treat determinism (combat/strategy dispatch) as sacred. |
| 2026-05-20 | **IMPLEMENTED (7 clusters):** DUP-X-1 (`_find_ship` -> `BaseOrderHandler`); DUP-X-7 (inline ship loops -> `BaseCommandHandler._find_ship`); Cluster 8 (`launch_*_in_battle` -> shared `_launch_carried_in_battle`); Cluster 10 (`_draw_radial_hit`); Cluster 7 (`DismissableModalDialog` mixin); DUP-X-5 (FighterWing/Satellite `_from_dict_payload` -> `_ShipBearingDeployedGroup`); Cluster 19 (`_contribute_launch`). | Each verified byte-identical / trivially-equivalent against live code, with characterization tests added/confirmed before refactor. None touch RNG, ordering, or serialized output (DUP-X-5 round-trips identical bytes; Cluster 19 keeps field-write order). |
| 2026-05-20 | **DEFERRED Cluster 9** (`BackgroundCall.cancel`). | The two `cancel()` bodies are functionally identical, BUT `LLMBackgroundCall` (`game/services/llm/background.py`) and `ImageBackgroundCall` (`game/ui/services/image/background.py`) share NO base class and each define their own `CallStatus` enum, module-level `_in_flight_lock`/counters, and a divergent `start()` (the LLM one has a documented single-lock race fix; the image one uses separate critical sections). A shared `BackgroundCall` base would be a NEW cross-layer abstraction (UI importing from services or a new `game/core` module) with a shared `CallStatus`, in concurrency-sensitive code — not a mechanical 14-line dedup. Out of "byte-identical/trivially-equivalent" scope. |
| 2026-05-20 | **DEFERRED DUP-X-6** (`Fleet.can_use_warp()` wrapper). | Not a logic duplication — the logic lives once in `FleetCapabilityCalculator.can_use_warp()`. The "dup" is the call expression `fleet.capabilities.can_use_warp()` repeated. The audit's proposed wrapper "returns False when capabilities is None" would CHANGE behavior at the live sites (today they call through directly; some call sites use proxy objects `_ChaserProxy`/`self._fleet`/DTO builders that are not `Fleet` instances, so a `Fleet` method wouldn't apply). Wrapper introduction + semantic change, ~15 LOC; risk > value. |
| 2026-05-20 | **DEFERRED order-handler skeleton/template clusters: DUP-X-3, Cluster 11 (`execute_for_issuer`), Cluster 12+21 (`_run_with_issuer`), DUP-X-9 (ship->carried-vehicle converter).** | These touch the strategy order-dispatch path and are NOT byte-identical: they diverge in OrderType, type lookups (FighterWing vs SatelliteConstellation), and — critically — in error/log/event MESSAGE strings and event payloads that tests assert on. Per the orchestrator's determinism-is-sacred guidance ("a dedup that changes a single ... serialized payload is a regression"), parameterizing them safely is a larger, higher-risk refactor than a mechanical shrink. Deferred to a future scoped pass; the cheap+safe `_find_ship` portions (DUP-X-1/DUP-X-7) were taken. |
| 2026-05-20 | **Codex audit (1 round): 0 code regressions, 3 process/coverage findings.** Verified each against live code: #1 (artifacts inconsistent) VERIFIED -> fixed plan.md + checklist; #2 (Cluster 19 satellite path uncharacterized) VERIFIED -> added `TestAggregateSatelliteHangar` (5 tests); #3 (test_dismissable_dialog.py untracked) REJECTED -> expected, orchestrator commits. | Codex independently confirmed Cluster 8/DUP-X-5 behaviour-preserving and no accidental Cluster 2 change. Findings + remediation tracked in `phase_2_checklist.md`. Audit artifact: `AgentCoordination/Scratchpad/Consult/proj465_audit/audit.md.invalid-output-*.txt` (Codex omitted consult/v1 frontmatter so the harness flagged missing-response, but the full audit ran and self-verified 90 tests). |
| 2026-05-20 | **DEFERRED UI parameterization clusters: DUP-X-2 (`facility_has_ability`), Cluster 3 (`_handle_designation`), Cluster 6 (`_open_selection_modal`).** | DUP-X-2 unifies three implementations that "use three different access paths and may diverge on edge cases" (per checklist) and requires giving the UI layer a `component_registry` it doesn't currently hold — a cross-layer wiring change, not a mechanical merge. Cluster 3/6 are higher-LOC UI parameterizations with per-handler callbacks; behavior-preserving but not byte-identical. Deferred to keep this pass within the low-risk mechanical-dedup envelope the orchestrator scoped. |
