# PROJ-109: Decisions Log

> **LOG ALL DECISIONS HERE**
> When you make a design choice or the user specifies a preference, add it to this table.
> Future agents will reference this to understand why things were done a certain way.

| Date | Decision | Rationale |
|------|----------|-----------|
| 2026-02-11 | DEC-001: Project initialized | Starting point for Legacy Cleanup |
| 2026-02-11 | DEC-002: Exclude LEG-UI2-001 (Singleton-to-DI) from PROJ-109 | 18+ call sites for SpriteManager/ShipThemeManager singleton migration. Requires DI container infrastructure. Too complex and cross-cutting for a cleanup project. Warrants its own project. |
| 2026-02-11 | DEC-003: Keep logger convenience functions, remove only the module-level global | `log_debug/log_info/log_warning/log_error` are imported by 109 files. Migrating all to `Logger.instance().log()` would be massive churn with no benefit. Instead: change implementation from `_logger = Logger()` (module-level instantiation) to `Logger.instance()` calls inside each function. This removes the global while preserving the API. |
| 2026-02-11 | DEC-004: FleetOrder.from_dict() - remove only the `coord` type branch | Analysis of `FleetOrder.to_dict()` shows it currently produces: `HexCoord.to_dict()` format, `fleet_ref`, `transfer`, `planet_ref`, `ship_id_list`, `warp_params`, and `raw`. The `coord` type (`{'type': 'coord', 'value': [x,y]}`) is the only truly legacy branch - no current code produces this format. Remove only that branch. The others are active serialization formats. |
| 2026-02-11 | DEC-005: Keep ValidationResult `.message` property | Used by 8+ files for `result.message` access. This is a useful convenience property (returns `errors[0]`), not a backward compat shim. It has clear semantics and reduces boilerplate. Keep it. |
| 2026-02-11 | DEC-006: Phase ordering by risk (safest first) | Phase 1: Dead code deletion (zero callers). Phase 2: Simple removals (1-3 callers). Phase 3: Medium removals (3-10 callers). Phase 4: Complex legacy (deep changes). Phase 5: Foundation cleanup (cross-cutting). This lets us build confidence with easy wins before tackling risky changes. |
| 2026-02-11 | DEC-007: Keep ValidationResult.create() removal and validation_result() removal as separate tasks | `.create()` has zero callers. `validation_result()` has 9 callers in race_validator.py + 1 test. Different effort levels. |
| 2026-02-11 | DEC-008: Exclude LEG-FND-007 (old_ variable naming) | This is a style preference, not a legacy holdover. The `old_` prefix is descriptive for "previous state" tracking in research_service.py. Not actionable for a legacy cleanup project. |
| 2026-02-11 | DEC-009: Exclude LEG-FND-008 (TypeGuard import fallback) | This is a Python version compatibility concern, not a legacy code path. Removing the try/except for typing_extensions would break on Python 3.9. Out of scope unless minimum version is bumped. |
| 2026-02-11 | DEC-010: Exclude LEG-UI2-007 (fallback image creation) | `_create_fallback_image()` is a defensive pattern for missing assets, not a backward compat shim. It serves a real purpose (graceful degradation). Not a legacy cleanup item. |
| 2026-02-11 | DEC-011: Exclude LEG-SIM-006 (safe_evaluate_math_formula) | Despite the "backwards-compatible wrapper" comment, this function provides genuine error handling (catches FormulaException, returns default). It's not a shim - it's an error handling utility. Just remove the misleading comment. |
| 2026-02-11 | DEC-012: BuildQueueScreen legacy mode (LEG-UI1-002) - full removal | The `hex_coord is None` path wraps a single build_context with synthetic `_legacy` ID. All current callers in strategy_screen.py already provide hex_coord, galaxy, and empire. Safe to require these parameters and delete the legacy wrapping. Also delete backward compat test aliases (queue_selector_panel, queue_selector_scrollable, queue_selector_buttons). |
| 2026-02-11 | DEC-013: Rename legacy_components.py to modifier_editor.py | LEG-UI1-007/LEG-UI2-008: The file contains ModifierEditorPanel which is actively used. The "legacy" name is misleading. Simple rename with import update. |
| 2026-02-11 | DEC-014: DesignMetadata legacy mass field (LEG-STR-006) - remove top-level fallback | The `data.get("mass", 0.0)` fallback after `expected_stats.get("mass", ...)` supports a format where mass was stored at top level. Current `to_dict()` stores mass in `expected_stats`. Remove the fallback. |
| 2026-02-11 | DEC-015: LEG-STR-010 (player_empire/enemy_empire) - keep as convenience properties | These properties on GameSession are used by StrategyScreen._focus_on_player_home(), build_queue_portraits, and other places. They are simple array access (`empires[0]`, `empires[1]`), not backward compat. Keep them but remove the misleading "backward compatibility" comment. |
| 2026-02-11 | DEC-016: LEG-STR-013 (classification_config fallback) - keep but remove comment | Hardcoded defaults when JSON loading fails is defensive programming, not backward compat. Keep the behavior, remove the misleading "backward compatibility" comment. |
| 2026-02-11 | DEC-017: LEG-STR-014 (strategy facade empty dict fallback) - keep but fix comment | Returning empty dict when registry unavailable prevents crashes in UI during initialization. This is defensive programming. Remove "legacy behavior" comment, document actual intent. |
