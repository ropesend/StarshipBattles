# PROJ-427: Decisions Log

> **LOG ALL DECISIONS HERE**
> When you make a design choice or the user specifies a preference, add it to this table.
> Future agents will reference this to understand why things were done a certain way.

| Date | Decision | Rationale |
|------|----------|-----------|
| 2026-05-16 | Project initialized from TD-05 source plan | Strategy tech-debt #06/10: split runtime production design lookup from the savegame filesystem and replace the static replay-store ownership. Source: [`TD-05_production_persistence_split.md`](../../../Reviews/results/2026-05-16_strategy-layer-tech-debt-review/Verified%20Problem%20Remediation%20Plans/TD-05_production_persistence_split.md). |
| 2026-05-16 | **No save-schema change in the same phase as I/O removal.** | TD-05 "Weak-LLM Guardrails" explicitly forbid mixing runtime I/O removal with a schema bump. Built counts therefore stay on the repository file via deferred write-back (Phase 4), not on a new `Empire.designs_built_count` field. Any save-format bump (e.g., a hypothetical v4.0.0) is its own separately-approved, single-phase change outside this project. |
| 2026-05-16 | **Keep `DesignLibrary` alive until grep proves all callers migrated.** | The class is the current single-source for both runtime design lookup AND filesystem persistence. Deletion before Phase 6's grep gate (`rg -n "DesignLibrary" game tests docs` returns no production or UI hits) would break live UI callers (`workshop_ship_io.py`, `strategy_build_queue_manager.py`, `transfer_controller.py`, plus any caller discovered by grep). Phase 6 is the only phase that may delete or alias the class. |
| 2026-05-16 | **Explicit "no-disk-read during production tick" integration test.** | The source plan's risk table identifies "Runtime production accidentally repopulates the catalog from disk during the tick" as a primary risk and prescribes an explicit integration test as the mitigation. The test goes under `tests/integration/strategy/production/` and stubs `DesignRepository.scan_designs` / `load_design_data` to raise if invoked during a tick. |
| 2026-05-16 | **`DesignRepository` introduced before `DesignCatalog`.** | Phase 1 is additive: the repository is built around the disk-bound responsibilities lifted out of `DesignLibrary`, with `DesignLoadResult` shape preserved unchanged so callers are untouched. Phase 2 then builds the catalog on top, populating from the repository at session/bootstrap boundaries only. This ordering keeps each phase reviewable in isolation. |
| 2026-05-16 | **Runtime production migration is a single contained phase (Phase 3).** | Spawn, construction-queue validation, and quickstart initial-complex spawn all read disk JSON today; migrating one without the others would leave `save_path` plumbing alive. Phase 3 migrates `ProductionSpawner`, `ProductionEngine` (removes `save_path` threading), `AddToConstructionQueueCommandHandler`, and `quickstart_builder.spawn_initial_complexes` together, with a grep gate at phase close. |
| 2026-05-16 | **`SaveGameService` conversion is one contained phase (Phase 5), not a gradual sweep.** | The source plan's risk table calls out "The repo ends up with mixed static and instance `SaveGameService` usage" as a primary risk. Mitigation: remove `_replay_store`, `set_replay_store`, `get_replay_store` and all call-site usages in one phase, with a closing grep gate. No half-static / half-instance period. |
| 2026-05-16 | **Catalog accessor lives on `GameSession` if PROJ-423 has not landed; absorbs into `SessionRuntimeServices` if PROJ-423 lands later.** | PROJ-423 (TD-02) is a soft predecessor only. If TD-02 has not landed, `GameSession.get_design_catalog(empire_id)` (or `design_catalogs_by_empire[empire_id]`) is the accessor. If TD-02 lands later, the TD-02 cross-plan note ([`TD-02 § Cross-plan coupling with TD-05`](../../../Reviews/results/2026-05-16_strategy-layer-tech-debt-review/Verified%20Problem%20Remediation%20Plans/TD-02_game_session_lifecycle.md)) requires absorbing this into `SessionRuntimeServices` (and `SessionBootstrapState` for the per-empire catalog map). Document the absorption in PROJ-423's decisions log at that time. |
| 2026-05-16 | **`DesignLoadResult` shape is preserved unchanged in Phase 1.** | The repository must be additive: callers still go through `DesignLibrary` until Phase 3/6 migration. Changing the result type in Phase 1 would force caller migration before the catalog exists. Future result-type cleanup is out of scope for PROJ-427. |
| 2026-05-17 | **Project halted at Phase 0 entry; scope-vs-budget blocker.** | Operator-budget assessment: full execution across phases 0-6 with strict TDD requires (a) writing new repository + catalog packages with new test suites, (b) rewriting dozens of existing `tests/unit/strategy/engine/test_production_spawner.py`, `tests/unit/strategy/production_engine/test_spawning.py`, `tests/integration/strategy/production/*`, and quickstart-builder tests that patch `game.strategy.engine.production_spawner.DesignLibrary` at the module boundary (Phase 3 invalidates all those patches), (c) replacing module-global `_replay_store`/`set_replay_store`/`get_replay_store` and updating every test fixture and `app_bootstrap.py` call site (68 hits across `game/` and `tests/`), (d) migrating three UI screens off `DesignLibrary` with cache-invalidation parity to the QA-Obs-3-fixed workshop flow (209 hits across the repo, ~12 live production/UI/test sites under `game/` + `tests/`), (e) absorbing per-empire `DesignCatalog`s and `DesignRepository` into `SessionRuntimeServices` + `SessionBootstrapState` per the TD-02 cross-plan note while keeping the anti-drift `test_init_and_from_dict_use_identical_service_classes` green, and (f) running the full sharded suite as a final gate. This is a multi-hour, multi-pass refactor and cannot be honestly closed in a single execution slot without risking a half-landed change that leaves the test suite or the production tick broken. Halting at the phase-0 entry preserves a clean tree on `proj/PROJ-427/main` for a follow-up resumption rather than committing a partial Phase 3 that breaks `DesignLibrary` patches in dozens of tests. The cross-plan absorption into `SessionRuntimeServices` and the discipline guardrails (no `Empire.designs_built_count`, no v4.0.0 bump, no premature `DesignLibrary` deletion) remain the correct contracts for the resumption. |
| 2026-05-17 | **Split execution: phases 0-2 in this slot, phases 3-6 in a separate later slot.** | Re-scoping the prior 2026-05-17 blocker: phases 0-2 are bounded additive work (write characterization tests, add `DesignRepository`, add `DesignCatalog`, extend `SessionRuntimeServices`/`SessionBootstrap`, extend the anti-drift test). They do not migrate any caller, do not delete `DesignLibrary`, and do not touch the replay store, so the operator-budget concerns about rewriting dozens of `DesignLibrary`-patching tests and replacing 68 `_replay_store` hits do not apply. Phases 3-6 (runtime migration, replay-store conversion, UI migration, deletion gate, full sharded suite as final gate) carry the migration cost and are deferred to a separate execution slot. This split keeps the tree green after each commit on `proj/PROJ-427/main`. |
| 2026-05-17 | **Repository constructor key is `(savegame_path: str \| None, empire_id: int)`.** | Matches `DesignLibrary(savegame_path, empire_id)` so the additive extraction is byte-compatible — `DesignRepository` and `DesignLibrary` resolve to the same `designs/empire_<id>` folder when given the same inputs. This also means the SaveGameService Phase 5 flush can swap one for the other without changing the folder layout on disk. |
| 2026-05-17 | **Repository exposes low-level `save_design_data(design_id, data)`, not the rich `save_design(ship, name, built_designs)` workshop flow.** | `DesignLibrary.save_design` does three things welded together: metadata embedding, overwrite-protection against built designs, and per-turn UI cache invalidation. The repository owns only the disk-write step (Phase 6 keeps the workshop flow on the higher-level callers). Splitting now keeps the repository test-isolated and aligns with the catalog/repository separation in `design.md`. |
| 2026-05-17 | **Catalog + repository absorbed into `SessionRuntimeServices`, not `SessionBootstrapState`.** | PROJ-423 has landed. The wired-services bag is the canonical place for per-session runtime collaborators (per the PROJ-423 anti-drift contract). Per-empire `DesignCatalog`s are runtime state too — but `SessionBootstrapState` is the value-object that `__init__` and `from_dict` both feed into `_apply_bootstrap_state`, not a runtime container. Putting the catalog map directly on `SessionRuntimeServices` means `session.services.design_catalogs_by_empire[empire_id]` is the canonical access surface, consistent with the rest of the bag. The frozen dataclass is re-wrapped via `dataclasses.replace` once empires are known (catalog construction follows `GameInitializer.initialize` / `Empire.from_dict`). |
| 2026-05-17 | **No `GameSession.get_design_catalog(empire_id)` accessor.** | Direct dict access via `session.services.design_catalogs_by_empire[empire_id]` is the canonical pattern, mirroring how callers reach other wired-services members (`session.services.fleet_mutator`, etc.). A `get_design_catalog` method would be a redundant facade. If callers in Phase 3-6 want a defensive accessor, add it then; Phase 2 leaves the surface minimal. |
| 2026-05-17 | **Phase 2 scope narrowed: cache migration + `FacadeSessionState.designs_by_empire` deferred to Phase 6.** | The orchestrator's split-execution scope (phases 0-2 only this run) constrains Phase 2 to additive work that does not migrate existing `DesignLibrary` callers. Migrating `FacadeSessionState.designs_by_empire` from its current `DesignLibrary`-backed shape to a catalog-backed view is a caller migration — it touches the cache contract that workshop / build-queue / transfer-controller UI flows depend on (QA Obs 3). Deferring to Phase 6 (UI caller migration) keeps the Phase 2 commit purely additive and the tree green. Task 2.3 in the checklist is marked DEFERRED with cross-reference. |
| 2026-05-17 | **Session-level `DesignRepository` keyed at `empire_id=0`; per-empire repositories constructed inline.** | `DesignRepository`'s current shape takes `(savegame_path, empire_id)` to match `DesignLibrary` byte-compatibly (decision above). The session-level handle stored on `SessionRuntimeServices.design_repository` is keyed at empire 0 as a canonical filesystem entry-point (for the Phase-5 save-time flush). Per-empire repositories used during `repopulate_from(...)` are constructed on the fly inside bootstrap. A future cleanup could split `DesignRepository` into a folder-rooted root + per-empire view, but that refactor is out of scope for Phase 2 (the current shape keeps the `designs/empire_<id>` layout unchanged on disk). |
| 2026-05-17 | **Phase 6 partial: dependency inversion + one UI caller migrated; `DesignLibrary` deletion deferred.** | Phase 6 scope as written requires migrating three large UI screens (`workshop_ship_io.py` 254 LOC with the QA-Obs-3 cache invalidation contract on the rich `save_design(ship, name, built_designs)` flow; `strategy_build_queue_manager.py` 340 LOC with 4 separate `DesignLibrary(...)` construction sites threaded into build-queue UI subsystems; `design_selector_window.py` 705 LOC with a `design_library`-typed constructor parameter, `search_designs`, and `mark_obsolete`) plus updating ~25 test files. Realistic single-slot scope: complete the **dependency inversion** that moves `DesignLoadResult` to `design_repository.py` (with re-export from `design_library.py` for backwards-compat), and migrate the lightest-weight UI caller (`transfer_controller.discover_pod_designs`) onto the catalog as a proof of the migration pattern. The other three UI screens remain to be migrated in a follow-up slot before `design_library.py` can be deleted; until then the deletion gate intentionally fails. This preserves the tree green after each commit on `proj/PROJ-427/main` and avoids landing a half-migrated UI that breaks the workshop / build-queue / design-selector cache contracts. |
| 2026-05-17 | **Phase 6 remainder spun off to [PROJ-434](../PROJ-434/plan.md).** | Two prior Phase 6 subagents independently deferred the same remaining scope (3 UI screens + 4 `BuildQueueScreen` panel collaborators + ~30 test files + API gaps on `DesignCatalog`/`DesignRepository` + the QA-Obs-3 cache contract through `FacadeSessionState`). PROJ-427 achieved its TD-05 core intent — production off disk, replay-store instance-owned, sharded suite green 20996/20996. The remaining deletion is mechanical cleanup that can ship independently. Spinning the remainder into PROJ-434 lets PROJ-427 close cleanly and unblocks PROJ-428..PROJ-431 from sequencing behind a deletion gate that is a separable concern. `phase_6.status` stays at `partial` on this project's state file; PROJ-434 owns the deletion gate and the final `rg -n "DesignLibrary"` zero-hits assertion. |
