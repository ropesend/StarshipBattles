# PROJ-445: Decisions Log

> **LOG ALL DECISIONS HERE**
> When you make a design choice or the user specifies a preference, add it to this table.
> Future agents will reference this to understand why things were done a certain way.

| Date | Decision | Rationale |
|------|----------|-----------|
| 2026-05-18 | Project initialized | Starting point for Post-refactor residue: Engine + Services layer (Bucket B) |
| 2026-05-18 | Phase 1 ships F-B-001 fix isolated from broader engine cleanup | High-severity production TypeError; no reason to bundle. Keeps PR small and reviewable. |
| 2026-05-18 | Phase 2 — F-B-014 audit found no plain-string CLOSE_WARP_POINT emitters | `IssueCloseWarpPointCommandHandler.execute` at `superweapon_command_handlers.py:160` has always emitted the dict shape `{destination_id, target_hex}`. The legacy plain-string branch in `_parse_close_target` and the special-case `pass` block in `superweapon_order_processor.py` were dead — retired in this phase. The only plain-string reference in the repo was a back-compat test deliberately exercising the legacy path; that test was rewritten as `TestCloseWarpPointTargetShape` (dict-without-target_hex still succeeds; plain-string rejected). |
| 2026-05-18 | Phase 2 — F-B-019 contract wording: affordability/consumption symmetry | Added explicit clause to `IProductionResourceSource.production_consume_resource` Protocol docstring: "MUST return True when `production_has_resources(costs)` returned True for the same `(resource_type, costs[resource_type])` in the same engine tick. Implementers that perform rounding (integer-typed sources) MUST do so symmetrically." Cites PROJ-436 Phase 12 Option C as precedent. The Fleet stocked-fractional fixture for the ratchet test is deferred — Planet is fully covered, Fleet's empty-baseline `False → False` is covered, and the stocked-fractional case requires real ships + `_resource_agg` distribution wiring that is the structural concern of PROJ-444 F-A-010. |
| 2026-05-18 | Phase 2 — DI-2026-05-18-002 option choice: raise on ambiguity | Chose **option (a)**: `CommandRegistry.serializer_codec_for` raises `ValueError` on multi-spec ambiguity with differing codecs, rather than returning a `frozenset` for callers to size-check. Rationale: callers (current and future, including the metadata-driven `Order.to_dict()` dispatch flip) read this method expecting a single codec string. Returning a frozenset would force every call site to assert `|result|==1` defensively; raising at the registry boundary localizes the contract violation to the registration that caused it. The companion class-level ratchet at `test_specs_sharing_order_type_declare_same_codec` already pins the production registry shape; the runtime guard catches overlays/mods that bypass module-load. |
| 2026-05-18 | Phase 2 — partial Tasks 2.2 (private-slot migration half) and 2.4 (Fleet fractional ratchet half) deferred | PROJ-444 Phase 2 is "Not Started" at this writing, so the public `ShipInstance.can_carry_pod` / `load_vehicle` / `unload_vehicle` delegators (F-A-007) and the Fleet rounding fix (F-A-010) haven't shipped. Per the Task 2.2 checklist's three-option matrix, chose option (c) — keep the legacy `_cargo_mgr` access on the new fleet-to-fleet branches so a single migration sweep can swap all call sites (pre-existing + new) when PROJ-444 lands the public delegators. Structural deliverables of both tasks (fleet-to-fleet branch routing, Protocol docstring tightening + Planet contract ratchet) are in place; the deferred work is the engine-side adoption sweep + Fleet-stocked-fractional ratchet case. |
