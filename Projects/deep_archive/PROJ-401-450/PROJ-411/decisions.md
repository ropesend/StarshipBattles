# PROJ-411: Decisions Log

> **LOG ALL DECISIONS HERE**
> When you make a design choice or the user specifies a preference, add it to this table.
> Future agents will reference this to understand why things were done a certain way.

| Date | Decision | Rationale |
|------|----------|-----------|
| 2026-05-10 | Project initialized | Starting point for Optimize Strategy Panel Load Times |
| 2026-05-10 | Project title: "Optimize Strategy Panel Load Times" | User-selected. Action-oriented, 5 words, mirrors triage filename. |
| 2026-05-10 | Scope: bundled 5 panels under one project (rather than 5 separate issues) | User directive in triage: "if there are similar issues they must be coordinated together". Swarm confirmed all 5 share `@fast_panel` opt-out + per-turn-static data they could cache. |
| 2026-05-10 | Phase 1 includes profiling + low-risk shared wins (not measurement-only) | User answer to Phase A question. Reasoning: the shared wins (DesignLibrary cache, lazy Empire icons, `@fast_panel`, `gather_*` caches) are obvious from current code without needing Scalene evidence. Phase 1 lands them; Phase 2 uses the resulting profile to find anything that remains. |
| 2026-05-10 | Acceptance: soft wall-clock target (<100 ms imperceptible target, pragmatic acceptance) + hard count-based regression tests | User: "I would like the delay to be imperceptible, but that may not be realistic. I will accept an honest attempt to make it faster and profile things." `docs/guides/performance_profiling.md` recommends count-based assertions over wall-clock thresholds. We do both: count-based as the regression gates, wall-clock as informational reporting. |
| 2026-05-10 | Smoke scenario: turn 1, 2 empires, 2 systems, 1 planet each, 0 fleets | User: "now things are slow on the 1st turn, so no ships, 1 planet each, 2 systems. Let's get that to be somewhat performant then we can worry about later game issues later." Larger-galaxy perf deferred to a future project. |
| 2026-05-10 | Empire Overview: lazy-load on tab activation (Treasury icons on first Treasury render; portrait/flag on first Population render) | User-chosen approach. Background-thread asset loading rejected (would need Pattern #28 worker — overkill for static assets). Lazy-on-tab matches the existing Planet/Star Registry lazy-icon-on-render pattern (Pattern #11). |
| 2026-05-10 | Land Issue #17 fix before PROJ-411 Phase 1 begins | Risk Assessor flagged that PROJ-410 stale-rows correctness regression (#17) + new DesignLibrary cache could double-mask failures. Sequencing keeps each PR reviewable in isolation. #17 is already `status:in-progress`. |
| 2026-05-10 | `@fast_panel` rollout: all 5 panels in one Phase 1 task | User-chosen. Single coherent visual review; ~3 s per-panel saving reported; easy to revert as a unit if visuals regress. |
| 2026-05-10 | No DISABLE_DESIGN_CACHE rollback flag | User-chosen. Matches CLAUDE.md Rule 3 (root-cause fixes, no compat shims). Rollback by PR revert if needed. |
| 2026-05-10 | Phase 1 size: single phase containing all instrumentation + shared wins | User-chosen. ~7 tasks but coherent: profile_action spans + smoke fixture + DesignLibrary cache + `@fast_panel` rollout + `gather_*` caches + EmpireEconomyService cache + Event Log copy fix + lazy Empire icons. Split alternative (1a measure, 1b fix) rejected as redundant — the wins don't need profile evidence first. |
| 2026-05-10 | DesignLibrary cache home: `FacadeSessionState`, not module-level singleton | Architecture Analyst recommendation. Per-turn lifecycle matches `FacadeSessionState.invalidate_all()` (line 61) called from `StrategySessionFacade.process_turn()` (line 202). Pattern #11's module-level singleton is for immutable assets, not per-turn data. |
| 2026-05-10 | DesignLibrary write-through invalidation: explicit `.pop()` in `save_design()`, no mtime check | Pattern Scout: matches `ShipInstance.invalidate_stats_cache()` style. Mtime check adds disk I/O cost; version counter adds complexity. Explicit invalidation is the established repo idiom. |
| 2026-05-10 | `DesignLibrary` accepts optional `facade_state: FacadeSessionState | None` ctor param (Option A) | Option B (callback injection) was rejected as more flexible but unnecessary for one use case. Option A: UI-side callers pass it; engine-side callers omit it (their work runs inside the turn loop where a cache is irrelevant). Adding one kwarg has zero ripple at sites that don't care. |
| 2026-05-10 | LOC budget: edits in `planet_list_window.py` (737), `empire_panel_window.py` (572), `build_queue_screen.py` (877) capped at ≤15 LOC each | Architecture Analyst guidance. These files are already over the 500-LOC ceiling. Heavier code (cache lookup, instrumentation, lazy state) goes in collaborators (data sources, filters, controllers, facade slice) which have headroom. |
| 2026-05-10 | Wall-clock perf tests: print measured value, fail only on >2× regression | Risk Assessor recommendation. Hardware variance across local/CI/different boxes is high. Count-based assertions are the hard gates; wall-clock numbers are informational. |
| 2026-05-10 | Smoke-scenario fixture lives at `tests/fixtures/perf_smoke_scenario.py` | Test Impact Analyst recommendation. No existing fixture matches "turn 1, 2 empires, 2 systems, 1 planet each, 0 fleets". Reuses `tests/integration/gameplay_loop/conftest.py::two_empire_setup` as a starting point. |
| 2026-05-10 | `TestRowPoolReuseGuard` extended to Planet/Star/Event Log in Phase 3, not Phase 1 | Test Impact Analyst recommendation. Current guard covers only Build Queue's virtual table (PROJ-373 phase 3 lock). Extending it is a regression-gate concern that belongs in Phase 3 after wins land. |
| 2026-05-11 | Start Phase 1 with Issue #17 at `status:awaiting-confirmation` (not yet user-verified) | User accepted the risk on session start. #17 fix landed at commit `a3ced0e1f` with 7 new tests + 360 adjacent green; full sharded suite passing post-merge. Risk Assessor's double-mask concern is mitigated by the fact that #17's fix landed cleanly and is now in `main`. User will smoke-verify #17 separately. |
| 2026-05-11 | Task 1.4 (`@fast_panel` rollout) scope revised | Investigation found that `panel.@fast_panel` in `data/builder_theme.json:72-82` is a **panel** widget selector — it does not directly apply to `UIWindow`. The Performance Analyst's ~3s claim referenced `BuildQueuePanelFactory`'s `UIPanel` children, not the window itself. Approach: profile first (Task 1.10 baseline), then either (a) opt-in matching UIPanel children inside each window if they exist, or (b) add a new `window.@fast_window` theme entry with `shape: rectangle` if the window-level rounded-rectangle is the actual cost. Decision deferred to profile evidence. |
| 2026-05-11 | Tasks 1.6, 1.7 use the same opt-in `facade_state` kwarg pattern as Task 1.5 | The pattern (`if facade_state is not None: check cache; build; store; return`) is repeated three times across `gather_planets`, `gather_stars`, `EmpireEconomyService.get_snapshot`. Considered extracting a `@per_turn_cache(field_name)` decorator helper but rejected: the caches differ in keying (per-empire vs galaxy-wide), the explicit pattern is shorter than the abstraction, and stricter typing is easier with the explicit form. Document the pattern in `docs/02_PATTERNS.md` Pattern #11 in Phase 3 doc work. |
| 2026-05-11 | Task 1.8 scoped down — only Population-tab deferral, not resource-icon laziness | Original Task 1.8 plan had two parts: (a) defer Population-tab portrait/flag loading, (b) lazy per-resource icon loading in Treasury. Only (a) is meaningful for open-time savings on the smoke scenario — Treasury tab is the DEFAULT tab and is built on open regardless, so making the icons within it lazy doesn't reduce open-time. Part (b) is deferred to Phase 2 if profile evidence warrants it. |
| 2026-05-11 | Task 1.9 `list(events)` copy elimination is safe — source is already a per-call fresh list | Confirmed by inspecting `EventSlice.get_all_events` at `event_slice.py:78` — returns `[e.to_dict() for e in events]`, a fresh list-comprehension. No window-level defensive copy needed. `EventLogDataSource.__init__` still does its own defensive copy (line 71) which is unchanged. Window holds a reference. |
| 2026-05-11 | All `facade.facade_state` accesses use `getattr(facade, "facade_state", None)` defensive form | Discovered via `test_empire_panel_ctrl.py` failures: test stubs sometimes use `SimpleNamespace` for the facade attribute, which doesn't carry the `facade_state` property. Safe-attr access keeps tests green without forcing them to provide the full real facade. Applied uniformly across 4 call sites (empire_panel_ctrl, list_windows, planet_list_window, strategy_build_queue_manager × 3). |
| 2026-05-11 | Task 1.10: full-window-init spans added to 4 windows + module-level `load_resource_icons` cache | The 12 original spans only covered narrow data-gathering bits. Real lag was inside pygame_gui widget construction (median 200–1200 ms per window). Added `Panel: PlanetRegistry.window_init`, `Panel: StarRegistry.window_init`, `Panel: EmpireOverview.window_init`, `Panel: BuildQueue.open_for_yard` to capture full open paths. Also added `_RESOURCE_ICON_CACHE` module-level singleton in `empire_treasury_panel.py` mirroring Pattern #11's RaceFlagGallery shape — saves ~40 ms per Empire Overview re-open. |
| 2026-05-11 | cProfile diagnosis of pygame_gui hotspot | 65 s total / 47 s (72%) in pygame_gui theme-id resolution. `str.join` self-time 27.96 s; `ui_appearance_theme.py::build_all_combined_ids` self-time 18.91 s. ~284 widget constructions per window open, each triggering theme-id walks. This is the user-visible lag source. Phase 2 candidates: (a) window reuse (hide instead of kill) — biggest win, (b) reduce widget count per window, (c) memoize `build_all_combined_ids` per `(object_id, class_id, element_ids)` tuple. `@fast_panel` was deferred as it would not address the theme-resolution hotspot. |
| 2026-05-11 | `_show_tab` lazy-build order fix (crash discovered post-session) | Task 1.8's lazy-build originally ran *before* the show/hide visibility loop in `_show_tab`. First Population-tab click crashed with `AttributeError: 'UIScrollingContainer' object has no attribute 'vert_scroll_bar'` because pygame_gui's `UIContainer.add_element` immediately calls `child.hide()` when the parent is hidden, and `UIScrollingContainer.hide()` reads `self.vert_scroll_bar` before `__init__` assigns it. Fix: swap order — show/hide loop first, lazy-build second, so the parent panel is visible when its children are constructed. Regression test `test_show_tab_makes_population_panel_visible_before_lazy_build` added. Full sharded suite green post-fix (20,079 / 4 skipped / 0 failed). |
| 2026-05-11 | F9 real-game profile data validates Track A (window reuse) | Per-window timings from `output/logs/profiling_history.json` capture (session `2026-05-11T17:58`): StarRegistry 5,034 ms, PlanetRegistry 4,410 ms, EmpireOverview 4,192 ms, EventLog 2,534 ms. Re-opens of the same windows are 0–3.5% faster (essentially same cost). BuildQueue (PROJ-376 reuse pattern already implemented) measured 7,088 ms first open → 408 ms re-open (94% reduction). This is the empirical proof Track A is the right next step. |
| 2026-05-11 | Phase 2 scope: Track A only, four windows | User-selected. Track A = window reuse for Planet/Star/Empire/EventLog mirroring PROJ-376 pattern. Track B (reduce first-open cost via widget-count / pygame_gui theme memoization) deferred to potential Phase 3. Rationale: Track A alone makes re-open lag disappear; first opens cost once per session and are dominated by pygame_gui internals that would need more invasive work. |
| 2026-05-11 | Phase 2 PR shape: one PR per window | User-selected. Four separate PRs (Planet → Star → Empire → EventLog) each ~150 LOC, separately reviewable, catches per-window state-cleanup quirks one at a time. Pattern is established by PROJ-376 so each PR is mechanical: add `show()`/`hide()`, extract `open_for_X` from `__init__`, modify registrar to reuse, document state-reset contract. |
| 2026-05-11 | Phase 2 implementation: bundled into one session, separately-committable per window | Original "4 PRs" decision preserved at the commit level — each window's changes are atomic and can be cherry-picked or reverted independently if needed. Session-level work flow: TDD per window, ~5 minutes each. Pattern (`on_close_window_button_pressed → hide`, `show/hide` toggling `is_blocking` + (un)register, `open_for_X` rebind + reset) was uniform across all four. |
| 2026-05-11 | Phase 2 state-reset contract: selection cleared, filters/scroll/search preserved | Each window's `open_for_X` clears its `selected_*` reference (no stale row highlights). Filter state, sort order, scroll position, search-box text are intentionally **preserved across opens** — easier UX for repeat lookups (user re-opens to check the same filter view). User can override by reloading the game session if they want a clean slate. |
| 2026-05-11 | Phase 2 Esc-close still kills (does not hide) | Esc handler in `strategy_event_router.py:119` calls `modals[-1].kill()` — Phase 2 did not change this. Only the X-button close path was changed (override `on_close_window_button_pressed`). Tradeoff: Esc users get the slow re-open path; X-button users get the fast re-open path. Equalizing Esc would require changing the event router contract for all modals — deferred. Documented as a follow-up consideration if user wants it. |
| 2026-05-11 | Empire Overview Phase 2 scope: same-empire reuse only | Cross-empire (hot-seat) reuse would need per-empire widget content invalidation (Treasury snapshot widgets, Population portrait/flag UIImages). That's a deeper refactor that touches `EmpireTreasuryPanel.refresh()` integration + Population panel cleanup. Phase 2 scope narrowed: `open_for_empire` asserts `empire is self.empire`; hot-seat falls back to kill+reconstruct via the registrar. User opens Empire for SAME empire repeatedly → fast. Hot-seat → no win, but no regression. |
| 2026-05-11 | Event Log Phase 2 design: reuse via existing `EventLogDataSource.update_events` API | The data source already had a public `update_events(events)` method that swaps the events list and re-runs the filter. `open_for_events` calls it directly — no new API needed, no widget reconstruction. Hot-seat-safe for free. Title is updated via `set_display_title(...)` (pygame_gui builtin), guarded with `getattr(self, "title_bar", None)` for bypass_init test paths. |
| 2026-05-11 | F9 verification result: Esc-close defeats Phase 2 reuse | User F9 capture showed `window_init` firing twice for all 4 panels with similar durations (Planet 4490→4485, Star 5039→4982, Empire 4163→4175, EventLog 2460→2489). Root cause: user closed each window with Esc, which routes through `strategy_event_router.py:119 → modals[-1].kill()`. My Phase 2 only intercepted the X-button via `on_close_window_button_pressed → hide()`. Esc still kills, slot clears via `on_close_callback`, 2nd open constructs fresh. |
| 2026-05-11 | Task 2.5 added: extend reuse to Esc-close via `request_close()` extension point | Added `StrategyModalWindow.request_close()` defaulting to `self.kill()` (legacy contract preserved). All 4 PROJ-411 reusable windows override `request_close()` to call `self.hide()`. Changed `strategy_event_router.py:119` from `modals[-1].kill()` to `modals[-1].request_close()`. Now Esc and X-button both take the reuse path for these 4 windows; all other modals retain the legacy kill behavior unchanged. 5 new tests in `test_strategy_modal_esc_close.py` (4 parametrized for each reusable window + 1 base-class default-behavior test). |
| 2026-05-11 | F9 verification (Task 2.5): re-open spans now correct, but input to strategy view blocked | User F9 capture showed `window_init` firing only once per panel (perf goal met), `rebuild_data_source` firing 6× for EventLog (reuse confirmed). But the user reported End Turn / zoom / hex-click were all unresponsive after closing a window with Esc. Root cause: pygame_gui's `UIWindow.check_clicked_inside_or_blocking()` consumes left-clicks whenever `hover_point()` returns True for that window. `hover_point()` checks rect collision only — NO visibility check. So a hidden 90%-screen Planet/Star Registry window in the sprite group still ate every click in its rect, blocking the strategy view's controls. |
| 2026-05-11 | Task 2.6 added: visibility-aware `check_clicked_inside_or_blocking` override on `StrategyModalWindow` | Override returns False when `not self.visible`; delegates to pygame_gui's default otherwise. Lives on the base class so all subclasses inherit (including non-reusable ones — harmless since killed modals are removed from the sprite group entirely and the override never reaches that path for them). 2 new tests in `test_strategy_modal_hidden_input.py` covering (a) hidden modal short-circuits to False without delegating, (b) all 4 reusable windows correctly inherit the override via MRO. |
| 2026-05-11 | F9 verification (Task 2.6): End Turn / zoom / hex still blocked after Esc-close | User reported the input-block persisted after Task 2.6. Diagnostic logging in `has_modal_open` showed it correctly returns False after `request_close → hide` — yet pygame_gui still routed clicks to the hidden window. User intuition was the trigger: "whatever was done to prevent interaction behind the active window is persisting after the active window is closed." |
| 2026-05-11 | Task 2.8 added: `hide()`/`show()` must update pygame_gui's `UIWindowStack` | Root cause of the lingering input-block: pygame_gui has its OWN `UIWindowStack` separate from `StrategyWindowManager._modals`. `UIWindow.kill()` calls `window_stack.remove_window(self)`; the original `hide()` did NOT. Hidden windows stayed z-top in pygame_gui's stack, and pygame_gui's hit-test/focus routed events to them, never reaching the strategy view. Fix: consolidated `hide()`/`show()` on `StrategyModalWindow` base. `hide()` now does `is_blocking=False` → `unregister_modal` → `window_stack.remove_window(self)` → `super().hide()`. `show()` mirrors in reverse with dedup-via-remove-then-add. Subclass-level hide/show removed (~80 LOC duplication eliminated). 2 new tests asserting the stack mutation. |
| 2026-05-11 | F9 verification (Task 2.8): input-block STILL present after Esc-close of Planet List | User reported third regression: opens Planet List → closes → cannot End Turn / zoom / hex-click. Tasks 2.5/2.6/2.8 collectively addressed the pygame_gui side (modal-manager unregister, visibility-aware blocking check, window_stack removal), but the StarshipBattles-side gate in `StrategyInputHandler.handle_event` was untouched. |
| 2026-05-11 | Task 2.9 added: `StrategyInputHandler.handle_event` gate must check `.visible`, not `is not None` | Root cause: line 66 of `strategy_input_handler.py` had `if self.scene.ui.window_manager.planet_list_window is not None: handle_event; return`. Under Phase 2 reuse the slot remains populated when the window is hidden. The early-return short-circuited `_handle_button_press` (End Turn), `handle_click` (hex), and `_handle_scroll` (zoom) forever after the first open. Pygame_gui itself was already routing modal clicks correctly (Tasks 2.6/2.8 verified), so the symptom appeared "post-pygame_gui" — events reached the handler then evaporated. Fix: gate on `planet_list and planet_list.visible`. Visible-modal behaviour preserved (original F11/F12 suppression intent); hidden-modal falls through to the normal handler chain. 4 new tests in `test_strategy_input_handler_hidden_planet_list.py` (1 for the preserved branch, 3 for the regression — button-press, hex-click, mouse-wheel zoom). |
| 2026-05-11 | Task 2.10 added: per-empire filter snapshots for Planet/Star registries | User reported filter state leaking across hot-seat empire handoff (Player 1's filters showed up for Player 2). Original Phase 2 decision "filters/scroll/search preserved across opens" was correct for same-empire repeats but wrong on empire switch. Fix (option B per user): each window keeps `_filter_snapshots_by_empire: dict[int, dict]` plus a default snapshot captured at the end of `__init__` (post-Stage-3). On `open_for_galaxy` empire switch: capture outgoing empire's view via existing `capture_*_list_state` helper, restore incoming empire's saved snapshot or the default snapshot via existing `apply_*_list_state` helper. Same-empire re-opens are unchanged — state is preserved in-place. Snapshot path is gated on `_default_filter_snapshot is not None` so bypass-init tests (no Stage-3 widgets) take the legacy path. StarListWindow gained an `empire=` kwarg; `StarListRegistrar` now threads `c.scene.current_empire`. 11 new tests across `test_planet_list_filter_snapshot.py` and `test_star_list_filter_snapshot.py`. |
| 2026-05-12 | User F9 results post Task 2.10 | Profile from latest session showed Phase 2 reuse working perfectly for Planet (1 init), Star (1 init), EventLog (1 init + 6 cheap rebuild_data_source ~10 ms each). EmpireOverview ran 3× window_init at ~4.1 s each — same-empire-only design firing kill+rebuild on hot-seat switches. First-open costs unchanged (Planet 4.5 s, Star 5.0 s, Empire 4.1 s, EventLog 2.7 s). User confirmed Task 2.9 (End Turn / zoom) fixed; noted EventLog filter may still leak (deferred). Next focus: Track B (initial open cost). |
| 2026-05-12 | Task 3.0 added: scoped UI adapter fixes pygame_gui's `build_all_combined_ids` cache key | Phase 1 cProfile pointed at pygame_gui's theme-id resolution (47/65 s = 72 % of open time in `str.join` + `build_all_combined_ids`). Investigation found pygame_gui 0.6.14 already caches results in `unique_theming_ids` but the **cache key** at `ui_appearance_theme.py:1625-1627` is built via chained `str.join(str(x))` where each argument is the separator and each character of the prior result becomes an element — key balloons to ~10 KB. Codex consult evaluated three approaches; recommended scoped UI adapter over monkey-patch (CLAUDE.md, consult_prompt_block, performance_profiling.md all prohibit monkey-patches). Implementation: `StarshipUIAppearanceTheme` subclass with private tuple-keyed `_combined_ids_cache`; `StarshipUIManager` subclass overrides `create_new_theme`. Source-fingerprint guard (`UPSTREAM_HAS_KNOWN_BUG` checks for the `str(element_base_ids).join(` pattern) flips False when upstream is fixed. `pygame_gui` pinned to `==0.6.14` until then. 8 production UIManager sites migrated. 11 new tests. **Projected payoff per cProfile attribution (unverified)**: PlanetRegistry 4.5→~1.3 s, StarRegistry 5.0→~1.5 s, EmpireOverview 4.1→~1.2 s, EventLog 2.7→~0.8 s. Awaiting F9 verification. |
| 2026-05-12 | Task 3.0 F9 verified — actual payoff ~20 %, not projected ~70 % | F9 profile post-patch (`output/logs/profiling_history.json` session `0d6ca814`): PlanetRegistry 4527.9→3720.9 ms (−18 %), StarRegistry 5026.9→3883.8 ms (−23 %), EventLog 2676.9→2147.6 ms (−20 %). EmpireOverview not exercised in this session. Codex's risk #3 played out as predicted: eliminating cache-key self-time exposed the next bottleneck (probably raw widget construction / font rendering / shape generation). cProfile's `str.join` self-time attribution turned out to be opportunistic work happening in parallel with the truly serial path, not the serial path itself. User accepted the 20 % win; further first-open-cost work deferred. Patch and pin stay in place — the 20 % is real and the patch is structurally sound (no monkey-patch, source-fingerprint guard, version-pinned). |
