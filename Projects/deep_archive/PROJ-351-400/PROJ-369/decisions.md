# PROJ-369: Decisions Log

> **LOG ALL DECISIONS HERE**
> When you make a design choice or the user specifies a preference, add it to this table.
> Future agents will reference this to understand why things were done a certain way.

| Date | Decision | Rationale |
|------|----------|-----------|
| 2026-05-05 | Project initialized | Starting point for Strategy: TurnEngine Decomposition (phase-aware ITickPhase migration completion) |
| 2026-05-05 | This project COMPLETES, does not supersede, PROJ-259 + PROJ-365 | PROJ-259 produced `TurnEngineConfig` and battle-engine `ITickPhase`. PROJ-365 produced the per-tick descriptor list. PROJ-369 finishes the strategy half: descriptor-extends the end-of-turn block, removes lazy fallback init, and unifies the execution loop. Citing the parents up front prevents accidental duplication. |
| 2026-05-05 | Use 03c-phase-aware-execution protocol | Five sequential phases with per-phase quality gates fit the work shape. Each phase touches `turn_engine.py` directly and can run in a worktree; `phase_state.json` makes hand-offs explicit. PROJ-367 used 03c successfully for a similar "registry pattern + AST guard" arc. |
| 2026-05-05 | `TurnEngineConfig.create_default()` is the canonical injection factory, not `ApplicationContext` | `ApplicationContext` (game/context.py) is a process-level container for 10 services. Strategy turn engines are session-scoped — one per `GameSession`. `create_default(registries, …)` accepts the session's already-resolved registries and produces a one-shot config. Mixing the two scopes would muddy `ApplicationContext`'s responsibility. |
| 2026-05-05 | Eager construction in `create_default()`, not lazy | The whole point is "tests can tell which default got used." Eager construction makes every default visible at config-creation time. Memory cost: 18 small engine instances per session. Negligible. |
| 2026-05-05 | Lazy properties become trivial passthroughs (kept), not deleted | Public read surface (`engine.foo_engine`) is used by tests and may be used by mods. Trivial `return self._foo_engine` keeps the API stable while removing all fallback init logic. Phase 5 may revisit if any property has zero readers. |
| 2026-05-05 | `_NullBattleResolver` is deleted in Phase 3, not kept as a fallback | Today it exists to mute the "no battle_resolver configured" case. With `TurnEngineConfig.create_default(ai_factory=…)`, the default is always `SimulationBattleResolver`. The only way to end up with `battle_resolver=None` is to explicitly pass it, in which case the caller has opted into "no combat" — `ConflictResolutionEngine` raising loudly at first combat is more useful than a silent warning at construction. |
| 2026-05-05 | End-of-turn descriptors live in the same module as tick descriptors | `turn_phase_registry.py` already houses `TickPhase`, `TickContext`, and `DEFAULT_TICK_PHASE_LIST`. Adding `DEFAULT_END_OF_TURN_PHASE_LIST` here keeps the file under 500 LOC (currently 298) and uses the existing shape unchanged. No new types needed. |
| 2026-05-05 | End-of-turn descriptors reuse `TickContext` with `tick=0` sentinel | Avoids introducing `EndOfTurnContext`. `tick=0` is impossible during the 1..100 tick loop, so it is unambiguous. Phase 1 task list documents the convention with a docstring on `DEFAULT_END_OF_TURN_PHASE_LIST`. |
| 2026-05-05 | `IQualityEngine` / `IAtmosphereEngine` / `IWaterEngine` are minimal | Each has exactly one method (`process_quality_improvement` / `process_atmosphere` / `process_water_modification`), all taking `empires: List`. Mirrors `IPopulationEngine` precisely. Construction (registries kwarg) is NOT in the protocol — that's `TurnEngineConfig.create_default()`'s job. |
| 2026-05-05 | `interfaces/engines.py` may exceed 500 LOC | The 500-LOC rule per docs/03_CONVENTIONS.md applies to production logic files. `engines.py` is interfaces-only (Protocol/ABC declarations). Adding 3 more brings it to ~830 LOC. Acceptable; verify with the user during Phase 2 task review. If the user prefers, split into `engines_core.py` + `engines_terraforming.py`. |
| 2026-05-05 | `tick_phases=` / `end_of_turn_phases=` ctor kwargs accept tuples, not callables | PROJ-365 chose tuples; consistency wins. Test ergonomics for "swap one phase" is `tuple(replacement if p.phase_key == 'X' else p for p in DEFAULT_TICK_PHASE_LIST)`. Future need (e.g., dynamic phase injection from mods) could add a callable kwarg later without breaking the tuple contract. |
| 2026-05-05 | `create_default_turn_engine()` factory deletion deferred to Phase 3 task list | Two callers (test files) only. Either delete with redirect, or keep as a 3-line shim. Decide after Phase 2 lands. |
| 2026-05-05 | Phase 1 chosen as "extract end-of-turn descriptors" first | Lowest-risk preparatory step: it is a mechanical extraction with a clear golden test (mirror `test_default_tick_phase_list.py`). Does not require constructor changes. Lets us validate the descriptor pattern fits end-of-turn before touching DI. |
| 2026-05-05 | PROJ-368 and PROJ-369 may run in parallel | Manifest analysis: PROJ-368 owns `order_processor.py` + `commands/`; PROJ-369 owns `turn_engine.py` + `turn_engine_config.py` + `turn_phase_registry.py`. Zero file overlap. PROJ-369 only consumes `IOrderProcessor` (interface, untouched). Coordinator should still confirm `manifest.md` overlap = ∅ at scheduling time. |
| 2026-05-06 | Codex+Claude joint review: applied r004 edits — see `AgentCoordination/Scratchpad/Discussion/20260506T024652Z_strategy-tech-debt-plans-review/outcome.md`. | E369-1: Phase 3 task list re-greps the test inventory at task start and introduces a shared `turn_engine_factory` fixture in the nearest `tests/conftest.py` for en-bloc migration. E369-2: "17 test files / 17 sites" wording replaced with "≥35 test sites (re-grepped at task start)" across plan.md, phase_3_checklist.md, design.md, and findings/initial_review.md. |
| 2026-05-06 | Phase 3 re-grep: 114 TurnEngine() construction sites across 32 test files (was estimated 35). | Significantly larger blast radius than planned. Per the plan's "100% cleanup, no bandaids" intent, the kwargs are still removed. Migration is bulk via shared `turn_engine_factory` fixture in `tests/conftest.py` plus mechanical sed-style edits for the 71 single-arg `TurnEngine(registries=X)` sites. |
| 2026-05-06 | Phase 3 Q4 (battle_resolver): keep `battle_resolver=` ctor kwarg as escape hatch; ConflictResolutionEngine raises clearly when `battle_resolver=None` and combat triggers. | The `_NullBattleResolver` warn-then-raise-on-call path is replaced by raise-on-call directly via the explicit guard in `ConflictResolutionEngine.resolve_all_conflicts`. No silent warning at construction time. |
| 2026-05-06 | Phase 3 Q2 (`create_default_turn_engine` shim): DELETE. | Only test files depend on it; canonical entry is `TurnEngineConfig.create_default(...) + TurnEngine(...)`. Migration trivially replaces the factory call with the two-line invocation. |
| 2026-05-06 | Phase 3 lazy properties: KEEP as trivial passthroughs (Q1). | Public read surface used by tests (~30 sites read `engine.foo_engine`); deleting them creates a second migration. Trivial `return self._foo_engine` keeps API stable while eliminating fallback init. |
