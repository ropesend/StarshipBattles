# PROJ-379: Decisions Log

> **LOG ALL DECISIONS HERE**
> When you make a design choice or the user specifies a preference, add it to this table.
> Future agents will reference this to understand why things were done a certain way.

| Date | Decision | Rationale |
|------|----------|-----------|
| 2026-05-07 | Project initialized as PROJ-379 from PROJ-377 review MIN-002 carryover. | OpenCode review (req_20260507_044410_7cfefd) flagged the double-seed pattern in `tests/fixtures/saves/_capture_baseline.py` as fragile; the verifier confirmed it; PROJ-377 closeout deferred a fix on the grounds that the docstring documented the trade-off and CI's contract is round-trip identity. PROJ-379 closes that gap with a structural fix rather than continuing to rely on the docstring caveat. |
| 2026-05-07 | **Approach: Option B (hand-built synthetic fixtures).** Rejected Option A (full rng threading through StarGenerator / PlanetGenerator / NameRegistry / image registries / warp generator), Option C (expanded normalization), Option D (static JSON only). User confirmed via AskUserQuestion. | The round-trip test's goal is *serialization* drift, not *generation* drift. Hand-built fixtures are deterministic by construction, light-touch (zero production-code changes), and align with the existing `tests/integration/strategy/test_save_round_trip.py::_build_minimal_planet` and `tests/fixtures/strategy_entities.py::create_test_*` factory pattern. Generation has its own deterministic test at `tests/integration/strategy/test_galaxy_gen.py::test_generate_with_rng_is_deterministic` — PROJ-379 doesn't need to duplicate that. Option A solves a bigger problem (generator-replay) than is in scope; logged as a future opportunity. |
| 2026-05-07 | **Phase 1 includes a field-coverage guard** at `tests/integration/strategy/test_golden_fixture_field_coverage.py` (NEW). User confirmed via AskUserQuestion. | Catches the "developer adds a Planet field but forgets to populate the fixture" drift class — the only realistic regression path the hand-built approach reintroduces vs. generated fixtures. The guard moved from Phase 2 to Phase 1 in the 2026-05-08 Codex peer review (TDD reordering). |
| 2026-05-08 | **Phase 2 guard pattern: serialized-baseline, NOT dataclass-defaults introspection.** The guard calls `planet_to_dict(_minimal_planet())` to compute the per-key default baseline, then collects emitted keys via `set(_serialized_defaults().keys())` (also from the call). | Codex peer review (`arc01_002_codex_to_claude.md`) verified: the original `dataclasses.fields()` introspection has two bugs — (1) `f.default is not f.default_factory` is meaningless because `f.default is MISSING` for default-factory fields, so the first branch records `MISSING`; (2) `planet_type` dataclass default `PlanetType.BARREN` (enum) does not match its serialized form `"BARREN"` (string). The serialized-baseline pattern handles both by deferring to the actual serializer. Also resilient if `planet_to_dict` is later refactored from a single dict literal into programmatic construction (the AST-walk approach would silently drop keys). |
| 2026-05-08 | **Phase 1 follows strict TDD ordering**: failing byte-determinism tests + field-coverage guard land FIRST (in `test_save_round_trip.py` and `test_golden_fixture_field_coverage.py`), then the builder implementation in `_build_galaxy_fixture.py` lands and turns them green, then JSONs regenerate. Tests reference the not-yet-existing module — collection-time `ModuleNotFoundError` is the acceptable TDD-red signal. | Codex peer review flagged that the original Phase 1 ordering (implementation → fixtures → tests in Phase 2) violated the AGENTS.md TDD rule. AGENTS.md states "Write the failing test first, run it to confirm failure, then implement". Reordering also catches a "developer changed the builder shape, forgot to update the test or commit the JSON" regression class via the new committed-fixture-vs-builder-output tests. |
| 2026-05-08 | **Cross-process determinism is asserted by Phase 2 subprocess tests, not assumed from `json.dumps(sort_keys=True)`.** Phase 2 spawns fresh `python` subprocesses with `PYTHONHASHSEED` set to "0", "12345", and "random", compares stdout bytes pairwise. | Codex peer review flagged that Phase 1 in-process tests would pass for a builder that iterates a `set` to populate a fixture list, but the result would differ across processes with random hash seeds. G1 explicitly requires byte-identical output across processes; subprocess testing is the only way to enforce it. The "PYTHONHASHSEED-immune build" rule logged below in this file is the convention; the subprocess tests enforce it. |
| 2026-05-08 | **Phase 1 Task 1.4 makes the `system.planets.append(planet)` step explicit alongside `register_planet`.** | Codex peer review flagged that `GalaxyEntityRegistry.register_planet` (`game/strategy/data/galaxy_entity_registry.py:85-89`) assigns IDs and indexes but does NOT append to `system.planets`. The existing pattern at `tests/integration/strategy/test_save_round_trip.py:60-61` does both. `StarSystem.to_dict` serializes `self.planets` (`game/strategy/data/star_system.py:99-107`); a builder that registers without appending would produce a fixture with zero planets in `to_dict` even though the registry has them. Phase 1 Task 1.4 lists this as an explicit step + a sanity-check assertion (`sum(len(s["system"].get("planets", [])) for s in d["systems"]) == expected`). |
| 2026-05-08 | **PROJ-379 is a real 03c project**: `phase_state.json` + `findings_ledger.md` added at the project root; `python Projects/scripts/phase_dag.py PROJ-379 status` resolves and shows 4 phases all `not_started`. | Codex peer review flagged that the `> **Execution Protocol:** 03c-phase-aware-execution` marker in plan.md was unbacked — `phase_dag.py` failed with `phase_state.json not found`. The 03c protocol at `Projects/protocols/03c_phase_aware_execution.md:59-63` requires this file. The `Projects/scripts/create_project.py` scaffold doesn't create 03c artifacts; recent 03c projects (PROJ-376/377/378) added them as a separate setup step. PROJ-379 follows that pattern. The `phase_state.json` planned_files lists match the post-Codex-review phase boundaries (Phase 1 absorbed the field-coverage guard from Phase 2). |
| 2026-05-07 | **Field-coverage skiplist:** `image_id` and `image_rotation` are exempt from the "non-default value" check. | These two are intentionally set to deterministic placeholders (`_fixture_planet_{id}.png`, `0.0`) by the fixture builder, matching the `_normalize_image_fields` contract from the old capture script. The skiplist is documented inline in the guard test with a pointer to this row. |
| 2026-05-07 | **Reuse `make_galaxy_stub()` from PROJ-378** as the starting point for the hand-built builder, NOT a fresh `Galaxy(radius=N)` real construction. | `make_galaxy_stub` already wires `_state` / `_registry` / `_spatial` for unit tests without paying the ~33 ms heavy `Galaxy.__init__` cost (NameRegistry shuffle, generator init, etc.) that introduces non-determinism. Builder fixtures need only `_state` + `_registry` to exercise `to_dict`/`from_dict`. |
| 2026-05-07 | **Production registration paths preserved:** builder routes hand-built `StarSystem` and `Planet` instances through `Galaxy._registry.add_system()` and `Galaxy._registry.register_planet()` rather than direct `_state.systems[k] = v` writes. | Mirrors the production `Galaxy.from_dict` registration block (`galaxy.py:340-348`). Ensures the round-trip path is symmetric: `from_dict` writes the same shape `to_dict` reads. Side-stepping the registry would diverge the fixture from real save behavior. |
| 2026-05-07 | **Storms NOT included in hand-built fixtures.** The `_strip_storms` workaround from `_capture_baseline.py` becomes unnecessary; storms simply don't appear in the new builder. | PROJ-372 explicitly scoped `Storm.to_dict()/from_dict()` drift OUT of the round-trip test (`test_save_round_trip.py:34-38`). The hand-built builder doesn't generate storms; the existing 5 synthetic round-trip tests in `test_save_round_trip.py` continue to call `_strip_storms()` on the *generated* galaxies they exercise — that path is unchanged. |
| 2026-05-07 | **Phase 3 deletes `_capture_baseline.py` outright** (no alias / deprecation period). | Zero callers outside the file itself; the old fixtures are regenerated by the new builder; the deletion is a clean cut. Grep verification at Phase 3 Task 3.2 confirms no stragglers. |
| 2026-05-07 | **Cross-link PROJ-377 → PROJ-379** in PROJ-377 `decisions.md` at Phase 4. | Closes the audit trail: PROJ-377 review MIN-002 → PROJ-379 resolution. Future agents reading PROJ-377's review report should land on PROJ-379 via the cross-link rather than re-discovering the analysis. |
| 2026-05-07 | **PYTHONHASHSEED-immune build:** builder MUST NOT iterate any `set` to populate a fixture field. Use explicit lists / tuples for warp-point sequences, planet sequences, etc. | Even hand-built fixtures can re-introduce non-determinism if the builder happens to iterate `for sys in {sys_a, sys_b, sys_c}`. The Phase 2 byte-determinism test catches this if it slips, but the convention is logged here as the standing rule. `json.dumps(sort_keys=True)` is the final-stage backstop. |

| 2026-05-08 | **OpenCode review (req_20260508_031844_3fc504) + verifier**: 0 CRIT, 0 MAJ, 0 MIN, 1 INFO — clean. All 9 focus areas PASS. Verifier independently confirmed and ran a mutation test (removed `planet.owner_id = 1` from `_build_decorated_planet`, rebuilt, confirmed field-coverage guard fails with `missing: ['owner_id']`, restored — load-bearing evidence the guard actually catches drift). Sharded delta: +7 from PROJ-378 close baseline (19077 → 19084). MIN-002 cross-link in PROJ-377 `decisions.md` confirmed clean. Reports: `Reviews/results/2026-05-08_031846_code_proj-379-deterministic-golden-save-fixture-closes-_req-req_20260508_031844_3fc504/{report,verifier_report}.md`. | No remediation commits required. PROJ-379 closes clean. |

## Future opportunities (logged, NOT in scope)

- **Option A — full rng threading through `StarGenerator` / `PlanetGenerator` / `PlanetImageRegistry` callers / `StarImageRegistry` callers / `NameRegistry` / `GalaxyWarpGenerator._calculate_warp_distance` and `_should_add_density_edge`.** ~40 module-level `random.*` calls converted to `rng.X()`. Extends the existing PROJ-301/302/303/304 child-rng pattern at `game/strategy/data/galaxy_system_generator.py:158-169`. Useful for any future "replay galaxy from seed" feature; not needed for golden-fixture determinism (which PROJ-379 solves structurally).
- **Storm round-trip drift fix.** Out of scope per PROJ-372. If addressed, the existing 5 synthetic round-trip tests in `test_save_round_trip.py` can drop their `_strip_storms()` calls; the new hand-built fixtures can grow a populated storms section.
- **Migrate the synthetic-capture pattern to other domains** (combat lab `test_history.json`, save/load round-trip tests for `Empire` / `GameSession`). Each is a separate concern.
