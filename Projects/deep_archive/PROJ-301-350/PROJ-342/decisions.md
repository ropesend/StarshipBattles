# PROJ-342: Decisions Log

> **LOG ALL DECISIONS HERE**
> When you make a design choice or the user specifies a preference, add it to this table.
> Future agents will reference this to understand why things were done a certain way.

| Date | Decision | Rationale |
|------|----------|-----------|
| 2026-05-04 | Project initialized | Triggered by user crash on Combat Lab "Run All": `AttributeError: 'ScreenRouter' object has no attribute 'screen'` at `game/ui/screens/test_lab/screen.py:382` |
| 2026-05-04 | **Option C** (full refactor) chosen over Option A (`pygame.display.get_surface()` only) or B (`ScreenRouter.screen` property) | A leaves the broken `self.game` indirection in place; B is a backward-compat shim and violates CLAUDE.md rule 3 ("Root Cause Fixes... no compatibility shims"). C aligns `TestLabScreen` with the modern screen-constructor pattern documented at `docs/03_CONVENTIONS.md §2.4` and used by every other screen. |
| 2026-05-04 | New constructor signature: `TestLabScreen(screen_width, screen_height, battle_scene, scene_callback=None)` | Mirrors `BattleScreen.__init__` (canonical exemplar). Explicit dependencies, no `game` handle. |
| 2026-05-04 | Delete `TestLabUIController.handle_run_visual` and `handle_run_headless` rather than retrofit them with a `battle_scene` parameter | Verified via `git grep` they are not called from production — only their own unit tests. Keeping them just to preserve a `self.game` dependency would re-introduce hidden coupling on the next refactor. |
| 2026-05-05 | **Delete `TestExecutionService` and `TestResultsService`** (Codex finding) | Once the controller methods go, those services have no production callers. Verified via `git grep -nE "TestExecutionService|TestResultsService"`: production hits are exclusively inside the methods being deleted. CLAUDE.md rule 3 mandates removing the old path entirely. |
| 2026-05-05 | Add `_require_display_surface()` helper that raises `RuntimeError` if `pygame.display.get_surface()` returns `None` | Codex pushback on r001: an opaque `AttributeError: 'NoneType' object has no attribute 'fill'` is poor diagnostics. A precondition check at the call site gives a clear, actionable error. Cheap insurance, not a fallback path. |
| 2026-05-05 | Split TDD into two tests: (a) current-crash regression via `__new__`-bypass + `screen.game = SimpleNamespace(battle_scene=...)`, (b) constructor-contract test for the new signature | Codex pushback on r001: a single test using the new constructor would fail with `TypeError` on current code, not the actual `AttributeError`. The split ensures the regression test pins the real crash surface before refactor begins. |
| 2026-05-05 | Absorb `BattleStateViewer` sizing fix and `handle_resize` forwarding into scope | Already touching the dimension-injection story; the viewer accepts dimensions and has `handle_resize` but neither was wired (`screen.py:137-138, 623-628`). 2-3 lines, no scope creep. |
| 2026-05-05 | Update `combat_lab/COMBAT_LAB_DOCUMENTATION.md` (4 sections), `combat_lab/runner.py` docstrings, and `game/simulation/battle_controller.py` docstrings (lines 113-116, 254-260) in the same PR as the code change | CLAUDE.md rule 2 (Documentation First): code and docs must stay in sync. Deleting services that the docs describe creates immediate drift. |
| 2026-05-05 | **Defer** `TestLabScreen` LOC reduction (currently 738 vs `docs/03_CONVENTIONS.md` 500 ceiling), legacy `BattleScreen.{test_mode, test_scenario, test_tick_count, test_completed}` instance vars, and `battle_scene` attribute / `BattleScreen` class naming inconsistency | All three are real debt but each warrants its own focused refactor. Forcing them into this crash-fix project would expand scope unsafely. Recorded under plan.md "Out of scope" as follow-up debt. |
| 2026-05-05 | Use `pygame.display.get_surface()` for batch-mode rendering helpers rather than threading a surface dependency through the constructor | Those helpers (`_render_progress`, `_draw_and_flip`) fire from inside the executor's batch loop, *outside* the normal `IScene.draw(screen)` cycle — they cannot piggyback on the surface parameter. Adding a fourth constructor dependency for "the display" would be DI overkill. `pygame.display.get_surface()` is the canonical pygame accessor used elsewhere in the codebase. |
| 2026-05-05 | Plan reviewed and consensus reached with Codex via inter-agent discussion | See `AgentCoordination/Scratchpad/Discussion/20260505T000631Z_testlab-drop-game-handle/`. r002 is the canonical planning artifact; outcome.md is closed `consensus`. |
| 2026-05-05 | **Architecture Analyst flag (Phase B):** TestLabScreen calling `self.battle_scene.start_battle(controller)` is same-UI-layer peer coupling. Analyst recommended routing this through `scene_callback("start_test_battle", controller=controller)` and having `app.py._handle_test_lab_action` invoke `start_battle`. **Decision: NOT adopted in this PROJ; recorded as follow-up debt.** | Trade-off: (a) `docs/01_ARCHITECTURE.md §"Layer Structure"` says "strict downward-only dependency flow" — this rule governs cross-LAYER dependencies, not same-layer peer references; sibling Pattern Scout finding labels the inter-screen reference "novel but justified" rather than violating. (b) Even routing `start_battle` through callback leaves `_ensure_engine` calling `self.battle_scene._battle_service.create_battle()` and `reset_selection` reading `self.battle_scene.test_*`; full elimination of inter-screen access is out of scope. (c) Routing one call but not the others is partial and inconsistent. (d) The user-driven trigger (Codex discussion) reached `consensus` on r002 without flagging this — adopting the analyst's recommendation now would expand scope unilaterally. The follow-up project would route ALL battle-state access through the router or via DTOs. |
| 2026-05-05 | **Test Impact Analyst dispositions (Phase B):** precise per-file test changes folded into `phase_5_checklist.md`. Net delta: -29 tests in scope (20 service tests deleted, 9 controller tests deleted/updated, ~17 visual run tests updated, 2 new regression tests added) | Concrete plan removes ambiguity from "delete most tests in this file"; verified no hidden TestLabUIController constructors elsewhere. |
| 2026-05-05 | **Risk Assessor pre-existing risks (Phase B):** Risk 3 (stale `_battle_service` reference inside `_ensure_engine`) is pre-existing and not introduced by PROJ-342. Documented as follow-up debt. | Mitigation would require parameterizing `_ensure_engine` or formalizing the controller hand-off contract — both are own-refactor scope. The current PROJ does not regress safety. |
