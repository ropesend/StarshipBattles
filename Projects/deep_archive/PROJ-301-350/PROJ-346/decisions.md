# PROJ-346: Decisions Log

> **LOG ALL DECISIONS HERE**
> When you make a design choice or the user specifies a preference, add it to this table.
> Future agents will reference this to understand why things were done a certain way.

| Date | Decision | Rationale |
|------|----------|-----------|
| 2026-05-04 | Project initialized | Starting point for Closeout Sprint 4 - Vacuous test purges from PROJ-331/338/339/340 review |
| 2026-05-04 | OBSERVATION-A: Phase 1 plan partially pre-empted by commit `0e18b6b9d` ("test(339-review): replace 7 vacuous PROJ-339 panel tests with real pins"), which had already rewritten `test_race_identity_panel.py:349`, `test_race_summary_panel` summary attribute test, parts of `test_empire_treasury_panel.py`, and `test_race_environment_panel.py`. Remaining vacuous tests in this batch (`TestResourceAbbreviations` x3 in `test_empire_treasury_panel.py`, `test_kill_cleans_up_elements` in `test_modifier_impact_grid.py`, `test_refresh_updates_faction_label` / `test_refresh_updates_theme_label` in `test_race_summary_panel.py`, hasattr at race_identity:349) were addressed in this sprint. | The Phase 1 task list reflected a snapshot taken before `0e18b6b9d` landed; targets were re-located by content. |
| 2026-05-04 | OBSERVATION-B: Concurrent-commit contamination — the `test_race_summary_panel.py` edits intended for commit `test(PROJ-346): tighten race_summary_panel assert_called pins to args/results` were merged into another parallel agent's commit `af6ba4073` ("fix(planet-list-window): add virtual_table placeholder ... PROJ-347 T4.1b") because the git index is shared between concurrent agents. The content is in HEAD and tests pass; the commit message is misleading. Adds another data point to PROJ-329A's concurrent-commit audit. | Documented per CLAUDE.md/AGENTS.md root-cause-fix policy; no rewrite attempted on landed history. |
| 2026-05-04 | OBSERVATION-B (continued): Same contamination recurred — `test_hit_effects.py` edits intended for commit `test(PROJ-346 PROJ-331): pin hit_effects observable outputs and fix shield-guard input` were absorbed into `5ace65b24` ("fix(empire-panel-window): keep Stage 1 pure under bypass — defer icon loading (PROJ-347 T4.4)"). Content is in HEAD; both commits' contents are intermingled. | Two contamination events in this sprint indicate the concurrent-agent `git status`/`git add`/`git commit` race window is broader than per-task — happening between `git add` and `git commit -m` even with explicit file naming. Surface to user. |
| 2026-05-04 | UnboundedRegion tautology in `test_load_state_restores_battle` is intentional per the existing inline comment ("MAJ-003 fix") because production hardcodes `boundary=UnboundedRegion()` at battle_controller.py:638. | Per Phase 4 plan instruction: when the production hardcode IS the constraint, restate as "assert hardcode is preserved" with a comment linking the Observation. The existing test comment already does this. Phase 4 strengthening adds an additional behavioural pin (e.g. tick_count restoration) so the test is no longer one-assertion-deep. |
