⚠️ THIS IS A PLANNING-ONLY SESSION ⚠️
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PERMITTED:   Plan documents, questions, analysis, swarm exploration
FORBIDDEN:   Code changes, implementation, production file modifications  
SESSION END: User explicitly approves OR requests plan revisions
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

!EXECUTE_PROTOCOL: Projects\protocols\01_initialize_project.md

I want to start a new refactor/addition project. Below is my description of what I want to accomplish.

1. LOAD the protocol file `protocols/01_initialize_project.md`.
2. ADOPT the 'Project Architect' persona.
   ⚠️ You are NOT the implementer. You are planning ONLY.

3. EXECUTE Phase A: Initial Understanding
   - **MANDATORY:** Create project structure using the helper script:
     ```bash
     python Projects/scripts/create_project.py "Project Title"
     ```
     Do NOT create project files manually. The script ensures proper directory structure.
   - **MANDATORY:** Run full test suite to establish baseline:
     ```bash
     pytest tests/
     ```
     All tests must pass before proceeding. This also initializes testmon.
   - Perform deep code review of relevant areas
   - Ask clarifying questions
   - Make suggestions based on findings

4. EXECUTE Phase B: Deep Dive Swarm Review
   - Launch 6-8 Explore agents with appropriate roles
   - Document findings in the `design.md` file

5. EXECUTE Phase C: Plan Refinement
   - Process swarm findings
   - Ask follow-up questions if needed
   - Create detailed plan with phases, tasks, subtasks in the phase checklist files

   ⛔ STOP: Present the plan and WAIT for explicit "Plan Approved" from the user.
      DO NOT proceed to implementation. This session ENDS after approval.

**Note:** If this project originates from a Code Review, use `review_to_project.py` instead:
```bash
python Reviews/scripts/review_to_project.py <review_folder>
```
This automatically creates the full project structure pre-populated with review findings.

════════════════════════════════════════════════════════════════════════════════
                    PROJECT DESCRIPTION (INPUT FOR ANALYSIS)
                    This is context for YOUR PLANNING work.
                    This is NOT a work order to implement.
════════════════════════════════════════════════════════════════════════════════

<project_description>

[DESCRIBE YOUR REFACTOR/ADDITION HERE]

- What are you trying to accomplish?
- Why is this change needed?
- Any constraints or requirements?
- Any areas of the codebase you know are involved?

</project_description>

────────────────────────────────────────────────────────────────────────────────
REMINDER: Create the plan → Present to user → Wait for approval → END SESSION
          DO NOT IMPLEMENT. Implementation uses "Continue Project" prompt.
────────────────────────────────────────────────────────────────────────────────
