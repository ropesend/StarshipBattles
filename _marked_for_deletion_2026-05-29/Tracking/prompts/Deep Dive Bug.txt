!EXECUTE_PROTOCOL: Tracking/protocols/02b_deep_dive.md

TARGET: BUG-XX

This bug has persisted through multiple fix attempts. Initiate deep investigation.

1. LOAD the protocol file `protocols/02b_deep_dive.md`.
2. ADOPT the 'Lead Debugger' persona.
3. READ the ticket `Tracking/bugs/active/BUG-XX.md` and review ALL previous fix attempts in the Work Log.
4. READ relevant `docs/` files for the affected area:
   - `docs/01_ARCHITECTURE.md`, `docs/02_PATTERNS.md`, `docs/03_CONVENTIONS.md`
   - Any relevant system doc from `docs/systems/`
   The `docs/` directory is the authoritative source for how the codebase should be structured.
   Check if affected code follows documented patterns — a discrepancy may be the root cause.
5. UPDATE status in `Tracking/debug_plan.md` to `[Deep Investigation]`.

6. EXECUTE Phase 1 - Agent Swarm:
   - Launch 4 Explore agents IN PARALLEL
   - Agent 1: Trace code path from entry to bug
   - Agent 2: Map all callers/callees of affected code
   - Agent 3: Find similar patterns that work correctly
   - Agent 4: Review git history of affected files
   - Document findings in `## Investigation Report`

7. EXECUTE Phase 2 - User Interview:
   - Use AskUserQuestion to gather context interactively
   - Document answers in `## User Context`

8. EXECUTE Phase 3 - Diagnostic Logging:
   - Add log_debug() statements at key decision points
   - Document locations in `## Diagnostic Logging`
   - Request user to reproduce and share logs

9. EXECUTE Phase 4 - Hypothesis Development:
   - Track theories in `## Hypothesis Log`
   - Test each hypothesis systematically

10. EXECUTE Phase 5 - Resolution:
   - If root cause found: Fix with TDD, set [Awaiting Confirmation]
   - If not found: Generate Debug Report, set [Needs Human Debug]

CONSTRAINT: Do NOT mark as [Solved]. Do NOT move to bugs/archived/.
